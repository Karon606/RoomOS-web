# 다음 세션 할 일 (2026-09-03 밤 정리)

## 1순위 — 동의서 서명 없이 제출되는 문제 (운영자 지정)

운영자 확인. "발급한 서류 확인하니 쩐티투창의 임의처분동의서에 서명이 안되어 있어.
서명없이 제출된게 문제인듯."

즉 **서명이 빈 채로 제출·발급까지 통과한다.** 잔여 소지품 임의처분 동의서는 서명이 없으면
처분 근거가 없는 법적 서류다.

실측(2026-09-03).
| 호실 | 계약서 서명 | 동의서 서명 |
|---|---|---|
| 413호 쩐 티 투 창 | 42,898자 | **0자(없음)** |
| 503호 아라파트 에야신 | 39,954자 | 39,526자 |
| 411호 쩐 마이린 | 49,838자 | 52,626자 |
| 419호 미로노바 엘레나 | 93,782자 | 30,190자 |
| 501호 지한 모하마드 이스맘 호세인 | 없음 | 없음 (별개 경우일 수 있음) |

- 서명 크기가 전부 3만~9만 자라 **크기 한도 문제가 아니다.**
- 영업장 동의서 기능은 켜져 있다(Property.disposalConsentTemplate.enabled = true).
- 오늘 서명 상한을 1,400,000 -> 900,000 으로 내린 변경은 **저녁 배포**라 쩐 티 투 창
  서명(15:55 KST)보다 나중이다. 원인이 아니다.
- 정상 흐름은 두 서명이 같은 시각이거나 몇 분 차이로 둘 다 남는 모양이다.

- [ ] 서명 없이 제출·발급이 통과하는 경로를 전수로 찾는다(원격 서명·대면 서명 양쪽).
- [ ] 제출 게이트를 세운다. 동의서가 켜진 영업장에서 서명이 비면 제출이 막혀야 한다.
- [ ] 발급 게이트도 볼 것. 동의서 서명 없이 계약서가 발급된 상태다.
- [ ] 실패가 침묵하는 자리가 있는지 확인(오늘 반복해 나온 클래스).
- [ ] 이미 빠진 건(쩐 티 투 창)의 재서명 요청 경로가 있는지, 없으면 만들지 판단.
- [ ] 감지망 + 역주입.
- 주의. 법적 서류라 loop.md §4 대상이다. 설계 보고 후 운영자 승인 먼저.
- 패널 소집 필요(2026-09-03 밤 시도했으나 API 529 로 중단됨. 다시 돌릴 것).

## 2순위 — 발급본 표기가 박제되지 않는 문제

운영자 원칙 선언. "발행한 계약서는 손대면 안되지."

`ContractFile.issuedSnapshot` 을 열어보니 최상위 키가 `v, facts, issuedAt, signature,
bodySource, shareLinkId` 이고 `facts` 의 이름 관련 키는 `tenant.name`(원본 한글) 하나뿐이다.
**"이 종이를 어느 표기로 인쇄했는가"가 어디에도 안 남는다.**

그래서 발급본 표시가 이렇게 갈린다.
- 태그(ContractFile.nameStyle)가 있으면 그 태그로 렌더.
- 태그가 null 이면 resolveDocNameStyle 이 **지금 상태**로 재해석(사람 단위 -> 외국인이면 영문).

후자가 원칙 위반이다. **사람 단위 설정(Tenant.docNameStyle)을 나중에 바꾸면 이미 서명받은
계약서의 표시가 통째로 바뀐다.**

실측.
| 호실 | 태그 | 사람 단위 | 화면 |
|---|---|---|---|
| 413호 쩐 티 투 창 | ko | en | 한글 (종이는 영문 — 운영자 육안 확인) |
| 503호 아라파트 에야신 | en | null | 영문 |
| 419호 미로노바 엘레나 | null | null | 영문(재해석) |
| 411호 쩐 마이린 | null | null | 영문(재해석) |
| 514호 딘 티 디엠 뀐 · 501호 지한(2건) · 502호 응우옌 | null | null | 영문(재해석) |

- [ ] 발급 시점 표기를 박제할 자리를 정한다(스냅샷 필드 / 태그 필수화 등). 스키마 변경 여부 판단.
- [ ] **쩐 티 투 창의 ko 태그가 어떻게 박혔는지.** 그 계약의 contractFieldOverrides 는 null 이고
      사람 단위는 en 인데 태그는 ko 다. 해석 결과와 저장값이 갈리는 자리가 남아 있으면
      **앞으로 발급되는 건에도 계속 오기가 박힌다.** 이것부터 확인할 것.
      (서명 15:55 KST / 표기 버그 수정 358adb49 배포 15:49 KST 로 아슬아슬하게 걸친 시각이다.)
- [ ] 감지망 — 발급본 표기가 이후 재해석되지 않는다는 축.
- 패널 소집 필요(2026-09-03 밤 시도했으나 API 529 로 중단됨).

## 보류 — 계약서 nameStyle 백필

`scripts/backfill-contract-name-style.ts` 는 커밋돼 있고 **기본이 예행**이다(--apply 로만 적용).
예행 결과는 발급본 외국인 7건(null -> en 6, ko -> en 1)이었다.

**적용하지 않는다.** 운영자 원칙("발행한 계약서는 손대면 안되지")에 비추면 태그가 null 인 6건은
발급 시점 표기를 증언하는 데이터가 없어서 추측으로 고치는 일이 된다. 2순위 작업에서 박제 구조를
세운 뒤에 다시 판단한다.

쩐 티 투 창의 ko 1건은 성격이 다르다. 운영자가 종이를 직접 보고 영문이라고 확인해 준
**증거 있는 오기**다. 태그는 종이가 아니라 종이에 대한 기록이므로 기록을 사실에 맞추는 것은
원칙 위반이 아니라고 보는데, 이 판단을 운영자에게 한 번 더 확인받고 진행할 것.

## 미확인 실기 (운영자 확인 대기)

오늘 배포한 것 중 아직 눈으로 안 본 것들.
- [ ] 홈 알림 > 끈 알림 펼치기 > "알림 N건 다시 켜기" 버튼. 8건이 한 번에 살아나는지,
      토스트의 적용취소로 되돌아가는지. 숫자가 8이 아니라 7로 보일 수 있는데 정상이다
      (그 사이 발행한 건이 빠진 것 — 종전 과대 표시를 정정했다).
- [ ] 입주자 상세 > 수납 정보(퇴실자) > "퇴실 정산" 회색 라벨 아래 보증금·이용료 정산 카드가
      한 덩이로 묶여 보이는지.
- [ ] 입주자 등록 > 신분증·계약서 사진 올리기. 큰 사진(계약서 전면)도 통과하는지.
      버튼에 스피너가 돌고 폭이 안 튀는지.
- [ ] C-2 ~ C-5 (운영자가 "나중에 확인할게"라고 한 항목들).

## 사이드 조사 요청 (운영자 질문, 급하지 않음)

현금영수증 발행 API. 국세청 직접 API 는 없고 팝빌·바로빌 같은 등록 사업자(ASP)의 API 를
쓰는 것이 실무 경로다. 요청이 오면 후보 ASP·대략 단가·우리 데이터에 더 채워야 할 필드
(현금영수증 식별번호, 자진발급 여부 등)·환불 시 발급 취소 연동까지 정리해 문서로 남긴다.
