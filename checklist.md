# 퇴실 자동 청소 예정일 입력 (2026-08-20, 운영자 승인)

목표 한 줄. **퇴실 처리에서 실제 청소 예정일을 입력받는다. 기본값은 저장한 날이 아니다.**

성립 조건은 '칸이 생겼다'가 아니라 **'어느 경로로 퇴실하든 같은 규칙을 지난다'** 다.
호출부가 둘이라 한 곳만 고치면 다른 경로로 퇴실한 건이 여전히 저장한 날로 박힌다.

## 0단계 준비·패널
- [x] 정본 정독(AGENTS·loop·Work_log 2026-08-20 (7)(8)·08-05/06 청소·knowledge·brand-guide)
- [x] 워크트리 환경(.env.local·node_modules 심링크, 본선 스키마 4칸 포함 확인 후 prisma generate 불요)
- [x] 기준선 실측(tsc 0 · verify:fast exit 0 · 캘린더 회귀 214 · eslint 491 · verify:db 기지 예외만)
- [x] 실데이터 조사(RoomCleaning 21건 · 살아 있는 PLANNED 4건 · 자동 생성분은 2건이고 둘 다 소프트삭제됨)
- [x] 전문가 패널 4인 회수 — 고시원 도메인 · 정보구조 UX · 웹디자이너 · 백엔드 **전원 회신**
- [x] 설계 확정(이견·미채택 근거는 context-notes)

## 1항목 규칙 정본 (c5c8c780)
- [x] `lib/checkoutCleaning.ts` 신설 — `defaultCheckoutCleaningYmd` · `resolveCheckoutCleaningYmd`
- [x] 기본값 규칙 = 퇴실 다음 날, 이미 지났으면 오늘
- [x] 입력 세 갈래 — undefined(안 보냄) · null·빈칸(미정) · 정상값(고른 날)
- [x] 날짜 산술은 UTC 자정 덧셈, 대소는 문자열 사전순
- [x] 회귀 20케이스 · verify:fast 편입

## 2항목 없는 날짜 차단 (479244c1)
- [x] 왕복 검사 — `2026-02-31`(3/3 로 굴러감) · 평년 `2026-02-29`(3/1) 차단
- [x] 0벌림 없는 날짜·자리 바뀜·13월 축 추가, 윤년 2/29 는 통과
- [x] 회귀 20 에서 26

## 3항목 서버 배선 (2b3afb42)
- [x] `ensureCheckoutCleaning` 이 규칙 정본을 지난다 — `opts` 를 **필수 인자**로(컴파일 강제)
- [x] 호출부 둘 다 `moveOutYmd`·`cleaningYmd` 를 넘긴다
- [x] 미정이면 `scheduledDate: null` 로 만든다(청소가 필요하다는 사실은 남는다)
- [x] `checkoutTenant`·`checkoutWithDepositRefund`·`applyStatusTransition` 에 `cleaningDate` 통과
- [x] 조용하던 세 갈래에 `notice` — 이미 열린 예정이 있어 안 만듦 · 예외로 실패
- [x] 퇴실을 막지 않는 원칙 유지(`ok: false` 로 절대 안 내려감)
- [x] 열린 건의 날짜는 **안 덮는다**(무통보 덮어쓰기 금지)
- [x] `'use server'` 파일에 동기 함수 export 안 함(2026-08-05 전면 장애 클래스)
- [x] 소스 가드 3축 · 역주입 6종 전부 발화 · 회귀 26 에서 40

## 4항목 DatePicker emptyLabel (94f48916)
- [x] 빈 값에 이름을 붙이는 선택 프롭 — 안 주면 종전 경로와 완전히 동일
- [x] 실측 대비 라이트 2.48 에서 10.71, 다크 3.83 에서 17.35

## 5항목 퇴실 미니폼 둘 (9d01bb7e)
- [x] `components/cleaning/CheckoutCleaningDateField.tsx` 신설 — 두 모달이 **같은 컴포넌트**
- [x] 자리는 퇴실일 바로 아래, 돈 블록 앞(두 화면 동일)
- [x] 따라 움직이는 규칙을 효과 없이 파생으로(`picked === null` 이 곧 '아직 안 건드림')
- [x] 세그먼트 안 세움 — 비우는 길은 달력 안 '초기화'
- [x] 호실 없는 계약에는 칸을 안 그리고 값도 안 보냄
- [x] 홈 모달 `dirty` 에 반영(그 모달의 기존 idiom 인 '기본값과 달라졌는가')
- [x] 퇴실일 DatePicker 두 곳에 focus-visible 링(§09)
- [x] 홈 알림 경로에 `notice` 토스트(그 파일이 이미 쓰는 `pushToast` 정본)

## 6항목 목록 표기 (5fb3b66c)
- [x] 날짜 없는 예정을 '—' 대신 '예정일 미정'으로

## 7항목 기록
- [x] Work_log 2026-08-20 (10)
- [x] knowledge/open-issues 해결분 갱신 + 새 미해결 적립
- [x] knowledge/domain-cleaning 예정일 규칙 등재
- [x] checklist · context-notes

## 게이트
- [x] tsc 0
- [x] `npm run verify:fast` exit 0 (커밋 훅 6회 전부 통과)
- [x] `npm run verify:db` 기지 예외(서류 표시값 소재지 3건)에서만 정지
- [x] 캘린더 드리프트 행 41 · 막대 72 위반 0 · 청소 고아 0
- [x] 프로덕션 빌드 exit 0
- [x] eslint 491 유지(신규 0)
- [x] 320/360/390 x 라이트·다크 x 두 폼 x 세 상태 **36조합 넘침 0 실측**
- [x] 배포 전 웹디자이너 패스 — **차단 1건 반영 후 통과**(704e175b)

## 8항목 디자이너 패스 반영 (704e175b)
- [x] **차단**: 달력 '오늘' 버튼이 min·maxDate 를 안 봤다 — 정본 한 곳에서 봉합(호출부 열 곳 공유)
- [x] 보조줄이 실제 버튼 이름('초기화')을 부르게
- [x] 청소일이 퇴실일보다 이른 채 남는 상태에 인라인 한 줄(§27.2), 값은 안 고침
- [x] 그 줄의 색을 `--tc-d` 에서 `--danger-fg` 로 — 다크 대비 1.98 을 실측으로 잡음
- [x] 재실측 36조합 넘침 0 · 하네스가 전환 도중 색을 재던 것 발견해 재측정

## 안 한 것(의도)
- [ ] 기존 데이터 정정 — 대상 0건이라 할 일이 없다(자동 생성분 2건은 이미 소프트삭제됨)
- [ ] 열린 예정이 있을 때 날짜 갱신 — 무통보 덮어쓰기라 승인 대상으로 올림
- [ ] 캘린더 요약 줄 '날짜 미정 N건' — 조회 한 번이 더 필요한 신규 기능이라 범위 밖
- [ ] 홈 모달 환불 세그먼트 raw button 교체 — 돈 컨트롤이라 승인 대상
- [ ] `updateTenant`·`batchUpdateTenants` 퇴실 경로의 청소 미생성 — 선재 결함, 기록만
