# 호실 관리 캘린더 재편 0단계 — 체크리스트

운영자 확정 2026-08-21. 스키마 변경 0 · DB 쓰기 0. 워크트리 작업, 푸시 없음(메인이 머지).

## 1. 탭 라벨 개명 (b2a5fe1e)
- [x] '청소' → '작업'
- [x] '입퇴실 캘린더' → '작업 일정'
- [x] `ViewTabId` URL 값('cleaning'·'moves') 불변 — 북마크·홈 딥링크 보호
- [x] 탭 줄 폭 실측 320/360/390/1280 라이트·다크 — 넘침 0
- [x] 스크롤러 aria-label 도 '작업 일정 트랙'으로 동기화

## 2. 청소 하드코딩 제거 (1dfdecb8)
- [x] 트랙 레일 문구를 `moveWorkRailLabel`(kindLabel + 상태) 정본으로
- [x] 범례 '청소 예정·완료' → '작업 예정·완료' (네 칸 고정 유지)
- [x] 요약 줄 머리 '청소' → '작업'
- [x] `workKindLabel` → `cleaningKindLabel` 개명 + 비공개화(청소 공급자 안으로)
- [x] `knowledge/domain-cleaning.md` 문구 정본 항목 갱신

## 3. 축 필터 신설 (3e3634e5)
- [x] `SegmentedControl` 3칸(전체/입퇴실/작업) — §23 1차 필터 정본, 라디오·단일
- [x] 상태는 `MoveCalendar` 내부 `useState`(memo 보존)
- [x] 클라이언트 필터 — 서버 왕복 0
- [x] `moveRowSurvives` 술어를 lib 에 세워 조립·화면 공유
- [x] `packWorkLanes` export — 작업 걷은 행 재팩
- [x] 자리는 카드 머리(범례 옆) — 운영자 오더대로
- [x] 다중 선택 만들지 않음

## 4. 빈 상태 두 갈래 (3e3634e5 · dd03776d)
- [x] ① 데이터 없음 — '이 기간에 일정이 없습니다'
- [x] ② 필터가 가림 — §27.6 '다른 보기에 N건 ›', 자동 해제 없음
- [x] §17 CTA 최대 1 준수
- [x] 탈출 CTA 무조건 노출(건수 0 인 창에서 갇히지 않게)

## 5. 표식 유지·라벨 접기 (2fd2861f)
- [x] 라벨 강하 시에도 표식은 레일에 기립
- [x] 행 아래 줄 §11 두 건 + '외 N건' 접기
- [x] `containerType: inline-size` + `100cqw` 로 줄 폭 봉합

## 6. 자 교정 (99babaf4 · dd03776d)
- [x] `estWidth` 한글 0.851em / 그 외 0.564em + 글자 크기 인자
- [x] 레일 +4 패딩 반영
- [x] 옛 +8 눈대중 여유 제거 — sticky 104 → 80 복귀

## 7. 회귀
- [x] `test-move-calendar` 214 → 236 (라벨 7축 + 축 필터 15축, 기존 214 무변동)
- [x] `check-move-calendar-drift` 위반 0(행 41 · 막대 72)
- [x] `test-checkout-cleaning` 32 유지

## 게이트
- [x] tsc 0
- [x] `npm run verify:fast` exit 0
- [x] `npm run verify:db` 기지 예외(서류 표시값 소재지 3건)에서만 정지
- [x] 프로덕션 빌드 exit 0
- [x] eslint 491 유지(신규 0)
- [x] 320/360/390 라이트·다크 넘침 0 실측
- [x] 배포 전 웹디자이너 패스

## 안 한 것(의도 · 1단계 이후)
- [ ] 작업 종류 테이블 · 작업 기록 테이블 — 스키마라 범위 밖
- [ ] 환경설정 카드 · 지출 연결 — 범위 밖
- [ ] '작업' 탭(청소 뷰) 본문 어휘 — 종목 테이블이 서기 전에는 청소가 맞다
- [ ] '작업 일정 (N)' 접미가 작업을 안 세는 것 — 홈 타일과 앵커된 값이라 운영자 판단
- [ ] 호실 열 클릭 · 막대 오터치 — 오류신고 트랙(별도 승인 절차)
