# 계약서 서명 예방 4건 (2026-09-04, 운영자 오더)

어제 두 사람이 각각 한쪽만 서명하고 끝났다. 506호 팜 까오 끄엉은 동의서만,
413호 쩐 티 투 창은 계약서만. 둘 다 제출을 안 했고, 계약서만 서명한 쪽에는
"원격 서명 완료" 알림이 떠서 운영자가 그것을 믿고 발급했다.

**두 사람의 기존 건은 손대지 않는다.** 나중에 서류가 필요하다는 연락이 오면
그때 서명을 다시 받는다(운영자 결정). 예방만 한다.

## 1. 둘 다 서명돼야 '서명 완료'
- [x] 계약서와 동의서 둘 다 있어야 완료 알림. 동의서를 쓰는 영업장에서만 그렇고,
      안 쓰는 곳은 계약서 하나로 완료다
- [x] 홈 종 알림과 푸시 둘 다 같은 판정을 쓴다

## 2. 서명 하나를 마치면 남은 것을 확실히 알린다
- [x] **외국인이 한글을 못 읽어도 눈치챌 수 있어야 한다**(운영자 명시).
      텍스트에만 기대지 않는 시각 신호가 필요하다
- [x] 506호처럼 "제출 버튼이 안 나오니 끝났나 보다" 하고 나가는 것을 막는다

## 3. 표기가 한 종이 안에서 갈리지 않게
- [x] 외국인이면 상단 성함도 영문, 서명 옆도 영문(어제 PDF 는 상단 한글·서명 영문)
- [x] 서명 요청 **전에** 한글·영문을 고를 수 있다
- [x] **고른 것이 입주자에게 전달될 때도 그대로** 표시된다

## 4. 앞으로 계약서를 손댈 때 항상 볼 체크리스트
- [x] 위 셋을 감지망으로 세운다
- [x] 다른 체크 항목도 조사해 추가한다(패널에 위임)
- [x] knowledge 노트로 적립

## 게이트
- [x] tsc 0 · verify:fast · eslint 신규 0 · 빌드 · 역주입 · 웹디자이너 패스 · push

## 5. 제3 서류 준비 — D1 판정 일반화 (2026-09-06, a7005f1f)
- [x] 판정을 슬롯 배열 위로 옮긴다. **동작 불변** — 진리표 35건 그대로 통과
- [x] 꺼진 서류의 기존 서명이 판정에서 사라지지 않는다(partial 이 none 으로 안 내려앉는다)
- [x] 서류 이름을 코드가 아니라 슬롯에서 읽는다. 셋 이상이면 건수로 말한다
- [x] 서명 화면의 서류 이름을 한 자리로. 표시 문자열은 지금 것 그대로
- [x] 그물 둘 보수 — catch 검사를 거리에서 구조로, 첫 호출에서 전수로

## 6. 제3 서류 D2 — **완료** (운영자 승인 2026-09-06, 배포 2026-09-07)
- [x] Property.signDocuments · LeaseTerm.documentSignatures · ContractShareLink.docSignedAt
- [x] 제목 + 문단 + 서명 구조의 서류 에디터 (환경설정 계약서·서류 탭)
- [x] 사용 중지(발급본 보존 · 신규 생성만 차단) — 삭제 경로 자체가 없다
- loop.md §4 에 걸려 스키마 승인을 먼저 받았다. 승인 후 DDL·시공 완료.

## 7. 제3 서류 D2 — 패널 설계 확정 (2026-09-06)

### D2-1 스키마와 리졸버 (동작 0)
- [x] `lib/signDocuments.ts` 신설 — 리졸버·key 발급·병합·슬롯 조립 두 벌
- [x] `scripts/test-sign-documents.ts` 진리표
- [x] **운영 DB DDL 3줄** — properties.signDocuments ·
      lease_terms.documentSignatures · contract_share_links.docSignedAt, 전부 nullable (승인·적용 완료)
- [x] schema.prisma 세 줄 + prisma generate

### D2-2 축 통일 (커스텀 동작 0, 판정 축만 교정) — **완료**(승인 2026-09-06)
- [x] 홈 알림·패널 배지의 stage 입력을 링크 축에서 계약 축으로
- [x] 표시 자리의 `hasContractSignature:` 리터럴 조립 전폐
- [x] 실DB 전후 대조 — 노출 중인 건의 판정 변화 0건 증명 (check-sign-axis-shift, verify:db)
- [x] 백필 안 한다. 저장값은 두 축 다 참이고 거짓말은 읽는 자리에 있었다
- 실측 15/37건이 두 축에서 갈렸고, 그 12건의 원인은 재서명으로 확인됐다(사고 아님).

### D2-2b 기존 화면 결함 셋 — **완료**(승인 2026-09-06)
- [x] 영문 안내가 `twoDocs` 에 묶여 1장 영업장 외국인에게 안 뜨던 것 — 서류 개수와 분리
- [x] `.last` 강조가 `twoDocs` 에 묶여 1장 영업장에서 안 켜지던 것 — 슬롯 정본이 판정
- [x] `askedRef` 세션 1회를 진행 단계별 1회로 (서류 셋이면 2번째 뒤 침묵 = 9/3 사고 재현)

### D2-3 스냅샷·서명·발급 축 (화면 무변동) — 완료(3커밋)
- [x] ContractData.signDocuments · /sign N개 cage · target 일반화
- [x] 동결·printedFacts·issuedSnapshot·폐기/복원/지우기/이어받기 배선
- [x] 서류 없는 계약의 발급 HTML 이 문자 단위로 종전과 같음을 대조(test-print-companion)
- [x] 잠금·폐기 대상·서명본 진입·재발급 가드가 커스텀 서명을 센다(필수 칸으로 타입 강제)
- [x] 옛 링크 스냅샷(signDocuments 칸 없음) 방어 — 정규화 한 자리

### D2-4 설정 에디터 (스위치 온) — 시공 완료, 디자이너 패스 진행 중
- [x] '추가 서류' 카드 별도 신설 (동의서 카드와 통합하지 않는다)
- [x] 제목 + 본문 textarea + 사용 중지 · 접힌 '사용 중지된 서류(N)' 에 다시 사용
- [x] 삭제 버튼 없음 — 중지가 요구를 전부 덮는다. 서버 병합에 삭제 경로 자체가 없다
- [x] 전용 출구 둘(saveSignDocument·setSignDocumentRetired) — 병합 정본만 지난다
- [x] 웹디자이너 패스 — 차단 2건 반영(접힘 토글 정본 문법, 전부 중지된 카드의 거짓 빈 문구)
- [ ] 운영자 실기 확인

## 8. 서명 화면 안내 언어 (2026-09-06 운영자 요청, D2 뒤에 착수)

> "외국인이라고 모두 영어를 잘하는건 아니야. 요청할 때 한글로 안내할지 아니면 다른 언어로
> 안내할지 선택할 수 있는 옵션이 있어야 해. 가능하다면 국적에 따라서 (베트남어, 영어,
> 미얀마어, 한글, 일본어, 대만어, 중국어, 카자흐스탄, 방글라데시, 러시아 등) 안내문의
> 표시 언어가 달라지면 더 좋고"

지금 서명 화면의 병기는 **한국어 + 영어 고정**이다. 2026-09-06 에 영문 줄을 1장 영업장까지
넓혔는데, 그 작업 자체가 "외국인은 영어를 읽는다"는 전제 위에 서 있었다.

- [x] 실측 완료 — 베트남 5·방글라데시 2·러시아권 3·일본 1·중국 1 (+영어). 8언어로 배포
- [x] 발급 피커(국적 기본값 + 운영자 변경, 한국어 포함 — 김명화님 케이스)
- [x] 언어를 스냅샷에 박제, 다른 언어 재발급 시 옛 링크 자동 닫기
- [x] lib/signGuideText 사전(65키 × 8언어, tsc 전량 강제) + 그물 둘
- [x] 계약서 본문 제외(참고용 번역본은 12번 별건)
- [ ] **운영자 번역 크로스체크** — 대비표에서 어색한 문구를 키 단위로 지시
- [ ] 실기 확인 (아래 목록)

## 9. 매월 납부일을 입주자 등록 항목으로 (2026-09-06 운영자 오더, 기록만)

> "입주자 등록에 매월 납부일 항목을 신설하고 (계약한 날, 입주한 날, 납부하는 날이 다를 수
> 있으므로) 디폴트는 입주한 날이 납부하는 날로 지정되도록 하되 (당연히 수정도 되고)
> 입주한 날 기준으로 금액 책정이 되도록 하고 입주일보다 납부일이 멀어지거나 가까울 경우
> 일할 계산해서 첫달, 즉 입주달에 금액을 포함해서 계산하도록 해줘. 만약 예약단계에서
> 계약서 작성을 하게되면 납부일이 필수항목이 되어 안 넘어가도록 막도록 하고"

**왜 나왔나.** 정금순님(422호) 계약서의 매월 납부일이 비어 있었다. 계약에는 `5` 가 들어
있는데 종이만 비었다. 예약 상태에서는 납부일을 일부러 비우는 규칙
(`DUE_PENDING_STATUSES`, app/(app)/tenants/actions.ts:40, 2026-07-30 운영자 지적)이 있고,
계약서를 예약 단계에서 쓰면 그 빈 값이 스냅샷에 얼어붙는다. 규칙이 틀린 게 아니라
"납부일이 없는 시점"과 "계약서를 쓰는 시점"이 겹쳐 있는 것이 원인이다.

- [x] 입주자 등록 폼에 '매월 납부일' 칸 신설. 기본값은 **입주한 날**, 수정 가능 — B 배포
      (TenantClient, 입주일 바꾸면 파생 패턴일 때만 따라감 = shouldFollowMoveIn 가드)
- [x] 세 날짜가 서로 다를 수 있음을 전제로 한다 (계약일 · 입주일 · 납부일)
- [x] 금액 책정 기준은 **입주한 날** — C 배포(lib/billing.ts firstMonthGap, 귀속월=입주달)
- [x] 납부일이 입주일보다 멀거나 가까우면 그 차이를 **일할 계산해 첫 달(입주달) 금액에 포함**
      — 해석 1(운영자 확정 2026-09-06): P>D 만월+앞구간 일할 가산, P<D 다음 납부일까지 일할만.
      기한은 입주일(firstMonthDueYmd). 실DB 전후 대조 노출 0건(check-first-month-shift).
- [x] 예약 단계에서 계약서를 쓰면 납부일이 **필수**가 되어 넘어가지 못하게 막는다 — D 배포.
      막다른 거절이 아니라 채움 창(DueDayFillDialog)이 서고, 저장하면 하던 일을 이어서 한다.
      값은 표시값이 아니라 **원천(lease.dueDay)** 저장(setDueDayForContract) + 미서명 활성 링크 닫힘.
- [x] 기존 `DUE_PENDING_STATUSES` 규칙 — 저장 시 강제 비움 두 곳은 **보존으로 바꿨다**(설계 D 2-5).
      문이 원천을 채우는 것이 정식 경로가 되면서, 거주 전 저장·호실 일정 적용취소가 그 값을
      걷으면 서명받은 종이의 납부일이 원천에서 사라지기 때문이다. 등록 시 안 넣는 것(오염 경로
      차단)은 그대로다. 감사 규칙 1-b 는 계약서 흔적(링크·파일) 없는 건만 잡게 예외를 달았다.
- [x] 막는 자리 — 네 문 전부. 서명 요청(issueContractShareLink, 화면 둘 다 배선)·이어받기
      (renewContractShareLink)·발급 API(409, SNAPSHOT 예외)·계약서 화면(진입 시 모달 + 발급·
      미리보기 문 앞 선채움 + 인라인 셀 원천 저장 분기). 그물 check-due-day-gate(역주입 4건 적발).
- 주의: 금액 계산은 loop.md §4(결제 로직)에 걸린다 — 운영자 확인 완료("추천대로 진행해줘", 2026-09-06)

## 10. 발급 PDF 를 벡터로 — 실측 결과 **저장은 이미 벡터다** (2026-09-07 확정)

정금순님 발급본(834KB)을 내려받아 분석했다. 텍스트 그리기 연산자 3,503회 · 폰트 199개 —
글자가 전부 실문자다(이미지 오브젝트 7개는 도장·로고·서명뿐). **파일은 고칠 것이 없다.**

이미지처럼 보인 것은 **앱 안 뷰어**다. 아이폰 iframe 결함(확대 잘림·2페이지 소실, 신고
56a0657b) 때문에 뷰어가 각 장을 PNG(약 2.5배)로 그려 보여준다 — 화면에서 확대하면 픽셀이
보이는 게 그래서다. 출력 품질은 이미 지켜지고 있다. 아이폰 인쇄는 **원본 PDF 를 공유 시트로
넘겨 벡터로 인쇄**되고(useDocPrint), 밖으로 보낸 파일도 벡터다.

- [x] (선택) 데스크톱 인쇄 — **넘어간다**(운영자 결정 2026-09-07: "이미 되고 있으니 넘어가자").
      아래는 그때의 판단 근거로 남긴다. 데스크톱 인쇄만 화면 그림(래스터)을 인쇄한다. 원본 PDF 인쇄로 바꿀지는
      운영자 판단 — 뷰어 인쇄 경로는 신고 이력이 많은 자리라 필요할 때만 손댄다

## 11. 입실계약서가 2장으로 늘어난 건 — **확정** (2026-09-07)

실측: 정금순님 발급본이 물리 3장(계약서 2 + 동의서 1)이다. 원인은 추정대로 분량이다 —
본문 4절 23항목 1,970자 + 조기 퇴실 요금 절(단기 정책 켜진 영업장의 일반 계약 전부에 붙음)
+ 환불 규정이 88% 축소로 감당되는 선을 넘었다.

처리 방향(운영자 결정 대기).
- (권장) **2장 허용을 정상으로 둔다.** 축소 하한 88% 는 가독성 바닥이라 더 줄이면 안 되고,
  여러 장 출력은 꼬리말(쪽번호·영업장명)이 이미 붙는다. 내용이 많은 계약서가 2장인 것은
  결함이 아니다.
- 또는 계약서·서류 탭에서 조항을 손질해 분량을 줄인다(운영자 몫).
- 축소 하한을 더 내리는 안은 반대 — 88% 미만은 글씨가 읽기 어렵다.

## 11-옛. 입실계약서가 2장으로 늘어난 건 (2026-09-06 조사 시점 기록) — **위 11번으로 대체됨**

> 아래는 추정 단계의 기록이다. 2026-09-07 실측으로 확정됐으니 위 11번을 보라.
> 남겨 두는 것은 그때의 추정과 실측이 어디서 갈렸는지가 다음 판단의 재료이기 때문이다.

발급은 88% 까지 축소해 한 장에 맞추고, 그래도 안 되면 100% 로 되돌려 여러 장으로 낸다.
지금 실리는 분량은 본문 4절 23항목 1,970자 + 조기 퇴실 요금 절 2항목 106자 + 환불 규정
139자 = 약 2,215자 / 26항목이다.

최근에 늘어난 것 둘.
- **조기 퇴실 요금 절**(2026-08-29 신설). 붙는 조건이 '단기 정책 켜짐 + 단기 아닌 계약'인데
  제기역점은 단기 정책이 켜져 있어 **일반 계약 전부에 붙는다.**
- 비상연락처에 이름 추가(2026-08-31)로 그 행이 길어졌다.

- [x] **실측 완료**(2026-09-07) — 정금순님 발급본이 물리 3장(계약서 2 + 동의서 1). 추정이 맞았다.
- [x] 처리 방향 **확정**(운영자 결정 2026-09-07: "현재 기준 유지하자"). 2장 출력을 정상으로
      둔다. 축소 하한 88% 가 가독성 바닥이라 더 줄이지 않고, 여러 장 출력에는 쪽번호·영업장명이
      이미 붙는다. 분량을 줄이려면 계약서 본문에서 조항을 덜어내는 길이 남아 있다(운영자 몫).

## 12. 계약서 참고용 번역본 — **착수 확정** (2026-09-07 운영자 오더)

> "번역시 발생하는 미묘한 차이로 분쟁소지가 걱정되고 거꾸로 내가 이해를 못해서 문제가 생길 수
> 있을텐데. 그래서 만약 서명은 한글로 된 계약서로 받되 내용 참고를 위한 번역본을 보여주는 건
> 어떻게 생각해?"

방향은 운영자 안이 맞다. 한국어 정본에만 서명받고 번역본은 참고용으로만.

### 운영자 확정 요구 (2026-09-07)

> "번역본은 참고용이며 한국어 원본이 우선한다라는 조항이 들어가도록 해줘. 조항도 있고
> 번역본에도 해당 문구가 있어야 하며 계약서와 동일한 포맷으로 하면 오히려 헷갈릴 수 있으므로
> 다른 템플릿으로 표시되도록. 그리고 번역본에는 서명을 안하지만 서명 시점에 해당 번역본도
> 박제해야 돼. 영업장마다 운영하는게 다르므로 번역본을 운영할지 여부는 환경설정에서 정하도록
> 하고 번역본도 직접 입력할 수 있도록 하되 현재 항목별로 하나씩 있는 구조를 그대로 살리면서
> 어떻게 다양한 언어의 번역본을 입력할 수 있게 할지는 여러 패널의 도움이 필요해."

- [x] 우선 조항을 **계약서 본문에** 신설(번역본은 참고용, 해석이 다르면 한국어 원본 우선)
- [x] **번역본에도** 같은 뜻의 문구 표시
- [x] 번역본은 **계약서와 다른 템플릿**으로 조판(같은 포맷이면 어느 것이 정본인지 헷갈린다)
- [x] 번역본에는 **서명하지 않는다**(서명은 한국어 정본에만)
- [x] **서명 시점에 번역본도 박제**
- [x] **환경설정에 운영 여부 스위치**(영업장마다 다름). 끈 영업장은 이 기능 전과 문자 단위 무변동
- [x] **번역본 직접 입력** — 항목별 구조를 살리면서 여러 언어를 넣는 방법은 패널 설계에 맡김
      (본문이 4절 23항목 + 제목 + 서약문이라 8언어면 230문자열이다. 입력 부담이 성패)

### 1단계 시공 완료 (2026-09-08) — 판정 정본 · 우선 조항 · 박제 · 편집 카드
요구 일곱 가지는 1~3단계 + 대면 서명(2026-09-08) + 7언어 적재·번체 공개(2026-09-09)로 전부 섰다. 1단계에서 실제로 선 것은 아래다.
- [x] `lib/contractTranslation.ts` 신설 — 파서·해석(원문 폴백 + fallbackCount)·고아 계수·병합 정본.
      열쇠는 한국어 문장 자체. ko 는 번역 대상이 아니다
- [x] 우선 조항 한국어 문안 고정 + 번역본 쪽 8언어 고지 사전(Record 전량 선언, tsc 가 누락을 잡는다)
- [x] 박제 — 링크 발급 시 `templateSnapshot.translation` 에 **그 링크 언어 하나의 해석 완료본**,
      서명 제출 시 `signedContractSnapshot` 으로 승계, `printedFacts` 에 translation 축
- [x] 환경설정 '참고용 번역본' 카드(추가 서류 카드 아래) — 운영 토글 즉시 저장 + 언어별 사전 폼 저장
- [x] 그물 둘 — `test-contract-translation`(75건) · `check-contract-translation-axis`(ⓐ~ⓖ).
      **package.json 등록은 운영자 몫**(이번 작업은 수정 금지였다)
- [x] 2단계 — 서명 화면(ContractView) 번역본 카드. **우선 조항이 종이에 실제로 붙는 자리도 거기다**
- [ ] 발급 PDF 부착 — 별도 결정 사항
- [x] 3단계 — 줄 맞춰 붙여넣기(2026-09-08 배포)
- [ ] 4단계 — AI 초안 번역 (운영자 결정 대기. 7언어 초기 번역은 번역가 패널 + 적재 스크립트로 이미 채움)

### 2단계 시공 완료 (2026-09-08) — 서명 화면 카드 · 우선 조항 호출부 · 피커 캡션 · 전문 열람
- [x] 서명 화면 번역본 카드 — 종이(`paper-cage`) **위**, 기본 펼침 접힘 카드, 원격에서만.
      표시 원천은 `templateSnapshot.translation` 박제 하나다(지금 사전을 다시 해석하지 않는다).
      칸이 없으면 카드를 안 그린다. **읽음 확인 요소 0개**(체크박스·버튼·서명 없음, 법률 판정)
- [x] 조판을 계약서와 갈랐다 — 종이가 아니라 앱 카드 문법(`--cream`·본문 14px·pale-neutral 배지
      `참고용 · Reference only`·그 언어의 자기 이름). 헤더 밴드·정보표·서명란·워드마크·워터마크 없음.
      항목 번호는 계약서와 같은 번호, 원문으로 남은 줄에는 회색 `원문` 표식
- [x] 우선 조항 렌더 호출부 — 화면(ContractView)과 인쇄(contractPrintHtml) **양쪽에 같은 순서로 마지막**.
      `ResolvedBody.translation` → `ContractData`/`PrintContractData` 로 형제 절과 같은 길
- [x] 발급 피커 언어별 번역 진행 캡션(`번역 12/26`·`비공개 · 번역 n/총`·`번역본 없음`). 막지는 않는다
- [x] 발급 상세 시트에서 박제 전문 열람 — 새 화면 없이 Modal 재사용, 본문은 서명 화면 카드와 같은 컴포넌트
- [x] 그물 이어 붙임 — `test-contract-translation` 75 → **114건**, `check-contract-translation-axis` ⓐ~ⓖ → **ⓐ~ⓞ**.
      역주입 5건 전부 발화. **package.json 은 이번에도 수정 금지라 새 스크립트를 안 만들었다**(기존 둘에 이어 붙였다)
- [x] 무회귀 — 번역본 없는 계약서의 인쇄 HTML 이 HEAD 와 **바이트 동일**(8가지 입력 형태 실측)

### 검수용 번역본 PDF 내보내기 — 시공 완료 (2026-09-17 운영자 오더·승인)

> "난 한자를 못읽기 때문에 알수가 없어... 모든 번역본에 대해서는 pdf로 다운로드 받을 수 있게 해줘."
> "띄우는 것은 좋은데 다운로드 받을 수 있는 버튼을 알려줘야지 창을 끄니까 다시 방법이 없어"

미리보기 창은 끄면 사라져 **번역이 맞는지 읽을 줄 아는 사람에게 물어볼 길이 없었다.** 그 길을 낸다.
입주자용이 아니고 계약서도 아니며 앱에 보관되지 않는다. 정본은 knowledge/contract-translation-print.md.

- [x] 글꼴 넷 저장소 반입 — `public/fonts-i18n/`(NotoSansJP·SC·TC·Bengali Regular + `OFL.txt`).
      Google Fonts 원본 그대로, CDN 폴백 없음. **`public/fonts/` 에 안 섞는다** — 거기 넣으면 계약서·
      확인서 두 라우트가 쓰지도 않는 9MB 를 진다. 기존 두 글롭은 한 글자도 안 건드렸다
      (좁히면 `getPretendardBase64()` 가 CDN 폴백으로 내려앉아 신고 0aed3bdd 가 되살아난다)
- [x] 언어별 글꼴 지도 실측 — Pretendard 는 **한자 0자·벵골 0자**, 라틴확장추가 256/256 전량,
      키릴 254/256. `Record<TranslationLang, string | null>` **전량 선언**이라 언어가 늘면 tsc 가 막는다
- [x] `lib/contractTranslationPrintHtml.ts` 신설 — A4 1단, 계약서와 **다른 서식**(운영자 확정).
      머리 표식 · 고지 박스(정본 그대로) · 검수 안내 한/영 · 진행 한 줄 · 회색 `원문` 표식 ·
      **모든 장** 꼬리말. 계약번호·로고·도장·서명란·워드마크·영업장명·이름·금액 **전부 없음**
- [x] `app/api/contract-translation/pdf/route.ts` 신설 — 인증 3단은 기존 라우트 글자 그대로,
      `requireEdit()` throw 만 잡아 403. `propertyId` 는 **쿠키에서만**. 클라는 `{ lang, dict }` 둘만
      보내고 나머지는 서버가 DB 에서 읽어 **미리보기 useMemo 와 같은 인자**로 정본을 부른다
      (저장 전 draft 기준 — 저장본을 읽으면 이 기능의 쓰임이 뒤집힌다)
- [x] **글꼴 실패에 부분 성공 없음** — 503 `FONT_UNAVAILABLE`. 운영자가 두부와 정상 글자를
      구별하지 못하므로, 네모 종이를 보내면 검수자가 멀쩡한 번역을 틀렸다고 답한다
- [x] **아무것도 저장하지 않는다** — prisma 쓰기 0 · `uploadToDrive` 0. 되돌릴 상태가 없다는 것이
      적용취소가 필요 없는 근거다
- [x] 버튼 두 자리 — 카드 도구 줄 **넷째**(앞 셋과 같은 '저장하지 않는 도구') · 미리보기 모달
      `footer`. 둘 다 정본 `SendDocButton`(아이폰 공유 시트·0바이트 가드가 그 안에 있다),
      `label` prop 안 넘김, 인쇄 버튼 신설 안 함
- [x] 감지망 — `check-print-selfcontained.ts` 확장(축 1-b 폰트 두 벌·1-c 파일 실재 20KB·
      `FONT_FILE_RE` 에 Noto 넷) + `check-translation-print.mjs` 신설(축 4 저장 0 · 축 5 표식 ·
      축 6 순서대로 전부). `verify:fast` 등록. **역주입 10건 전부 발화**(import 만 남기고 호출
      지우는 수법 포함)
- [x] 실측 — 7언어 실제 사전으로 PDF 생성, **두부 0자**(글자 종수 전수 대조).
      `setContent` 200~673ms(zh 가 최대, HTML 7.9MB). 이 라우트만 상한 25초, 계약서는 안 건드림
- [x] 가이드 등재 — `docs/brand-guide-v2.0.md` §26.1

착수하게 되면 지킬 세 가지(2026-09-06 정리, 위 요구와 함께 본다).
- [x] 번역본에 "참고용이며 법적 효력이 없고, 해석이 다르면 한국어 원본이 우선한다" 우선 조항을
      그 언어로 명시 (국제계약 표준 관행)
- [x] **보여준 번역본도 박제** — 입주자가 "번역이 달랐다"고 주장할 수 있으므로, 서명 당시
      화면에 뜬 번역 문안이 스냅샷에 얼어야 한다 (안내 언어의 signLang 과 같은 규칙)
- [x] 본문이 영업장 편집 템플릿이라 손번역 유지가 안 됨 (결정: 열쇠=한국어 문장, 고친 줄만 고아·재번역. 2026-09-09 서약문·4항 갱신으로 실증) — 번역 생성·검수 흐름을 먼저 정할 것
      (기본 템플릿만 전문 번역 / AI 번역 + 고지 / 조항 단위 사전) — 운영자와 결정

## 표시값 배지 → '직접 입력 N칸' 목록 (2026-09-09, 계약서·실거주 확인서)

배지 `표시값 수정` 이 뜻을 안 알려주고 누를 수 있는 것으로 오해됐다. 손으로 고친 칸은 입실자
정보를 바꿔도 안 따라오므로(임대료를 올렸는데 계약서만 옛 금액으로 나가는 사고) 신호는 필요하다.

- [x] 문구 — `표시값 수정` → **`직접 입력 N칸 ›`**(버튼), `개별 수정본` → **`본문 수정본`**(배지 유지)
- [x] 누름 — 목록 모달(Modal size xs, 제목 `직접 입력한 표시값`). 행마다 칸 이름 · 지금 값 · 자동값(tnum)
- [x] 되돌리기 — 행마다 `적용취소`(btn-subtle sm + rotate-ccw 14px). **새 서버 액션 없이**
      기존 저장 액션에 그 키만 null 을 실어 보낸다(필드 단위 적용취소). 확인창 없음(§27.4), 결과 토스트
- [x] 되돌린 행은 사라지고, **마지막 행이 사라지면 모달이 닫히고 버튼도 내려간다**(목록 = 서버 상태의 거울)
- [x] 잠금(서명 완료) — 행에 컨트롤 자리가 아예 없고 하단 한 문장이 이유를 맡는다.
      문장은 칸 토스트와 같은 정본(`fieldLockMessage(multiVersion,'here')`)
- [x] 자동값 출처 — 계약서는 서버가 내려준다(`fieldAuto`·`overriddenFieldKeys`), 확인서는 이미 있던 `autoFields`.
      **`nameStyle` 만 예외** — 정규화가 이 키를 자동값과 같아도 안 걷어내므로 종이의 표기를 실제로
      바꿀 때만 행으로 센다(안 그러면 N 과 행 수가 어긋난다)
- [x] 조판 — `.toolbar-badge` 를 warning → **info**, r-pill 999 → r-sm 6, ring 제거(§04·§07·§11).
      누를 수 있는 쪽은 배지가 아니라 버튼(`.toolbar-btn-info`/`.rc-btn-info`, `.toolbar-btn-warn` 문법)
- [x] 두 화면 공용 — 모달 한 컴포넌트(`components/doc/FieldOverrideListModal`), 토스트 문장도 공용.
      툴바 되돌리기 라벨도 `자동값 복원`·`자동값으로` → **`전체 적용취소`** 로 통일(§16)
- [x] 링크 스냅샷 무회귀 — 화면 전용 두 칸은 `contractShare` 가 저장 전에 벗긴다.
      안 벗기면 표시값을 고친 계약의 `templateSnapshot` 바이트가 달라지고,
      **운영자가 덮어 감춘 자동값이 입주자가 여는 공개 JSON 에 실린다**
- [x] 그물 — `scripts/test-field-override-list.ts` (진리표 + 배선, 81건). 역주입 12건 전부 발화.
      **package.json 수정 금지라 verify:fast 에 못 엮었다 — 상위에서 이어 붙일 것**

### 웹디자이너 패스 반영 (2026-09-09)
- [x] 차단1 확인서 버튼이 형제보다 작음 — `btnClass('secondary','md','rc-btn-info')` 로 형제와 같은
      정본(md 44px)을 타게 하고, `.rc-btn-info` 는 색만 얹는다(치수 선언 제거)
- [x] 차단2 계약서 툴바에서 버튼이 주 CTA 뒤에 섬 — 배지·버튼을 `toolbar-link` 뒤,
      `toolbar-spacer` 앞으로 옮겼다(확인서와 같은 자리). 조건부 렌더 조건은 그대로
- [x] 권고3 §06 금액 — `toLocaleString()+'원'` 을 `fmtWon` 정본으로
- [x] 권고4 진행 중 다른 행이 조용히 무시됨 — `disabled={undoingKey !== null}` 로 전 행 잠금.
      **라벨 고정 + `aria-busy` 쪽을 골랐다**(§10 너비 고정)
- [x] 권고5 값 짝 — `지금 …` / `자동값 …` 두 값에 다 이름을 붙여 줄바꿈돼도 안 사라진다
- [x] 그물 갱신 — 확인서 치수 검사를 'btnClass 정본을 타는가 + 치수를 스스로 안 정하는가' 로 바꾸고,
      툴바 자리·§06·§27.2·§10·값 이름표 검사를 더했다(89 → 100건). 역주입 7건 신규 전부 발화

## 퇴실·투어 완료·입실 처리 적용취소 (2026-09-10, 운영자 승인 설계 시공)

같은 클래스 전수에서 나온 세 자리. 서버 전이표(`lib/leaseTransitions`)는 셋 다 이미 허용하는데
상태 전환 위젯이 그 버튼을 안 그렸다. 위젯이 되돌림을 모르는 게 아니라 세 자리만 빠져 있었다.

### 1단계 투어 완료 되돌리기
- [x] `TenantStatusTransitions` TOUR_DONE 분기에 `undoTourDone`(투어 완료 적용취소 → WAITING_TOUR).
      파괴적인 '입실 취소' **앞**에 둔다(되돌리기는 취소가 아니고, 위험한 것이 줄 끝이다)
- [x] 투어 완료 직후 토스트에 `적용취소` 액션(액션이 붙으면 pushToast 가 6초를 고른다)
- [x] 함께 되돌아갈 것 없음 — `roomVacantForStatus` 가 투어 단계에 null 이라 공실 무변화

### 2단계 입실 처리 되돌리기
- [x] ACTIVE 분기에 `undoMoveIn`(입실 적용취소 → RESERVED)
- [x] 입실 처리 직후 토스트에 `적용취소` 액션
- [x] 되돌림이 하는 일은 **열린 거주 구간 삭제가 전부**임을 확인 — 기존 전환 경로가 이미 한다
      (`syncRoomStayOnSave` 의 자격 게이트: RESERVED 는 점유가 아니라 열린 구간을 지운다)

### 3단계 퇴실 되돌리기
- [x] `lib/checkoutRevert.ts` 순수 술어 둘 — `checkoutRevertTarget`(예정일이 오늘 이후면 퇴실 예정,
      없거나 지났으면 거주중) · `checkoutRevertBlock`(보증금 반환 기록 → 차단, 이용료 환불 스냅샷 →
      기존 `RENT_REFUND_LOCKED`). **화면은 라벨만 받고 서버가 정한다**
- [x] `applyStatusTransition` CHECKED_OUT 출발 분기 — 술어로 목적지·차단 판정, `moveOutDate` null,
      거주중 목적지면 기존 정리 블록(퇴실 예정일·일할·스냅샷)을 CHECKED_OUT 출발까지 넓힘
- [x] 이력은 **새 전환 행**. 퇴실 예정으로 갈 때는 퇴실 완료 행의 사유를 실어 다음 퇴실이 이어받게 한다
      (`inheritableCheckoutReason` 이 CHECKED_OUT 행에서 null 로 멈춘다). `changedAt` 소급 없음
- [x] 확인창 §14 주의 등급 영향 고지형 — 걷는 것(예정 청소)과 안 되돌리는 것(끝난 청소·발급 서류·
      호실에 적용된 예약가)을 함께 말한다. 확인 라벨 `퇴실 적용취소`
- [x] 진입점은 수납 정보 '퇴실 정산' 우산 아래 `btn-subtle sm` — TenantBody 위젯은 못 쓴다
      (`getTenantDetail` 이 CHECKED_OUT 을 안 싣고, 싣는 순간 primaryTenantLease 폴백이 죽은 계약을 집는다)
- [x] 토스트 없음 — 퇴실 제출이 보증금 반환·이용료 환불을 먼저 적어 6초 되감기가 가짜 적용취소가 된다
- [x] 수정 폼 뒷문 — `updateTenant` 가 퇴실일을 지우는 경로에 **같은 차단 술어**를 걸었다

### 그물
- [x] `scripts/test-checkout-revert.ts` 진리표 34건 — 목적지(없음·지남·오늘·남음) × 차단(2×2) ×
      전이 허용 × 사유 승계 × 확인창 주어 조사
- [x] `scripts/check-checkout-side-effects.mjs` 축 ⓡ 신설(새 파일 안 만들고 기존 그물에 축 추가)
- [x] 역주입 14건 전부 발화(술어 우회·차단 제거·moveOutDate 미삭제·정리 블록 미확장·사유 미승계 2종·
      위젯 버튼 제거·토스트 액션 제거·진입점 제거·서버 질의 우회·뒷문 게이트 제거·진리표 2종)
- [ ] **`test-checkout-revert.ts` 를 verify:fast 에 못 엮었다 — package.json 수정 금지. 상위에서 이어 붙일 것**
      (`test-field-override-list.ts` 때와 같은 자리다)

### 2차 반영 (2026-09-10, 운영자 승인 + 웹디자이너 패스)
- [x] 이중 점유 차단 — `checkoutRevertBlock` 세 번째 갈래 `roomOccupied`. 판정은 **점유 정본 재사용**
      (`OCCUPYING_STATUSES` 로 방을 잡는 상태를 고르고 `primaryRoomLease` 로 대표가 있는지 본다).
      자기 자신 제외도 술어 안에서 한다 — 두 호출부가 각자 빼면 한쪽이 잊는다
- [x] 우선순위 보증금 반환 → 환불 스냅샷 → 이중 점유(돈이 먼저)
- [x] 두 경로(전환 액션·수정 폼 뒷문)가 같은 술어에 `roomLeases` 를 **조회해서** 넘긴다.
      수정 폼은 저장 뒤 설 방(`newRoomId`)을 본다 — 옮겨 갈 방이 찼는지가 물음이다
- [x] 확인창 문구 교정 — '걷습니다'(주석 은어)와 '예약가'(화면 이름 아님) 제거.
      `잡힌 퇴실 청소 예정은 지웁니다. 끝난 청소·발급한 서류·호실에 적용된 예약 이용료는 되돌리지 않습니다.`
- [x] 되돌림 버튼 셋에 rotate-ccw(§16) — `TransitionDef.undo` 플래그, 변형은 줄 형제 따라 secondary
- [x] `투어 완료` 성공 토스트 겹말("투어 완료 완료") 해소 — `successToast: '투어 완료 처리'`(그 키만)
- [x] `확정 해제` → **`확정 적용취소`**(§16 라벨 단일). 확인창 제목·확인 라벨도 같은 동사로 맞췄다
- [x] RotateCcw 는 `components/doc/FieldOverrideListModal` 에서 그대로 import(ui 로 승격 안 함 — 별건)
- [x] 진리표 47건(이중 점유 축 신설: 없음·거주·예약·퇴실 예정·비거주·퇴실 완료·취소·본인만·중복 id)
- [x] 그물 축 ⓡ 확장 — 점유 정본 사용·손수 나열 금지·두 경로의 `roomLeases` 조회 인자·아이콘·
      라벨·겹말·확인창 금지어. 역주입 20건 전부 발화

## 점검 수정이 뒤 점검에 안 딸려간다 (2026-09-11 김치, 운영자 승인 설계 시공)
- [x] ① 순수 함수 `lib/stockLedger.planCheckPropagation` — 이월 연쇄·위치별 정지·보정 전체 정지·
      행 생성·음수 거부·총량 재계산. 진리표 `scripts/test-check-propagation.ts` 62건(김치 실제 값 포함)
- [x] ② 표식 `carried` 쓰기 — `applyLocationCheck`(점검 위치 false·나머지 true), createStockCheck
      carryOver true·입력 false, updateStockCheck 동일. 삭제 스냅샷에도 carried 동봉
- [x] ② 서버 액션 — `previewStockCheckPropagation`(쓰기 없음), `updateStockCheck(propagate)` 게이트,
      수정+전파 한 트랜잭션, `undoUpdateStockCheck` 한 스냅샷
- [x] ③ 화면 — CheckEditForm 저장 전 §14 3지선다(함께 조정/이 기록만/취소), §16 토스트 6초 적용취소
- [x] ④ 허브 자기 마커 봉합 두 겹(화면 '채우기 전' + 서버 isHubChecked) · 감지
      `check-restock-hub-drift` 절 추가 · 정리 `scripts/fix-hub-restock-marker.ts`(예행)
- [x] ④ 백필 `scripts/backfill-check-propagation.ts`(예행) — 같은 정본·같은 적용 함수, `--revert` 동봉
- [x] 감지망 `check-stock-ledger-parity` — 소스 가드 12축 + 이월 행 대조. 역주입 7건 전부 발화
- [x] 표식 후속(운영자 오더) — 덮어쓴 행 true·정지 행 false·새 행 true, 값 같아도 표식 다르면 계획에
      행으로. 되돌리기 스냅샷에 표식 이전 값. 진리표 77건(표식만 케이스 신설)
- [x] 웹디자이너 패스 4건 — 확인창 본문 4단화, §29 화살표 표기(stockShiftAsk 2자리 포함),
      물음형 제목 + fmtDateKor, 주석 이모지 2곳 제거
- [x] 백필 둘 운영자 적용 완료 — verify:db 전 구간 통과(허브 마커 0건, 이월 행 대조 2건)
- [ ] **운영자 실행 대기** — `backfill-check-propagation.ts 83f2089a-… --apply` 한 번 더.
      9/11 의 값은 이미 옳고 표식만 2행(5층 하단 이월 · 5층 상단 실측)이 비어 있다.
      안 돌려도 장부는 정확하고 그 두 행만 구식(null)으로 남아 다음 수정 때 휴리스틱을 탄다
- [x] 체인 등록 — `test-check-propagation.ts` 를 verify:fast 에(운영자 반영, test-stock-ledger 뒤).
      신규 두 백필·정리 스크립트는 일회성이라 미등록

## 보관 위치 계층화 — 트리 (2026-09-14, 운영자 승인 설계 시공)

운영자 요청. "4층 주방에 김치냉장고가 있고 상단·하단이 있는데 각각 개별로 놔버리니 한번에
체크를 못 해. 레이어가 하나 밖에 없어서." 9/11 김치 점검에서 냉장고 상단·하단 분류를 깜빡해
9/10 내역을 나중에 고쳐야 했던 사고가 근거다.

결정(운영자, 전부 권장안). 스키마 `parentId` 자기참조 + 유니크 교체 · 모든 노드가 자기 재고 보유 ·
깊이 상한 4 · 표기는 공백 결합(`4층 김치냉장고 상단`, 오늘 이름과 글자 동일) · 다중 위치 저장은
체인(서버 무변경)으로 시작 · 같은 저장에 허브 실측이 있으면 비허브 부족 게이트 건너뜀 · 옮길 때
앞부분 떼기 제안 기본 켬 · 접힘 비영속·기본 펼침 · 하위 있는 삭제 거부 · 가이드 §22 트리 그룹 헤더 등재.

**선행(완료).** `carried` 승계 결함을 닫고 백필했다(6d4eb246·71c5d1f8). verify:db 초록.

- [x] 0. `scripts/migrate-location-tree.mjs` 예행 · 6축 참조 지문 JSON → 검증: 14행, 건수 실측 일치
- [x] 1. `--apply`(비파괴 DDL) + `schema.prisma` + generate → 검증: 지문 동일 · tsc 0 · overview 스냅샷 diff 0
- [x] 2. `lib/locationTree.ts`(순수) + `scripts/test-location-tree.ts` → 검증: DFS·pathName·순환 거부·깊이 4 거부·루트 중복 거부·떼기 제안, 역주입
- [x] 3. 서버 읽기: `getStorageLocations` 가 parentId·depth·pathName 을 DFS 로, 표시 자리 전부 pathName → 검증: 트리 없는 상태에서 화면 문자열 diff 0, check-query-fanout
- [x] 4. 서버 쓰기: create(parentId) · move(parentId, stripPrefix) · reorder(형제 전체성) · delete 하위 거부 · undo → 검증: 타 영업장 부모 거부, 자기 서브트리 이동 거부, 형제 중복 거부, 적용취소 원복
- [x] 5. 위치 관리 모달 트리화(DFS·들여쓰기·RowActionBtn 메뉴·옮기기 Modal) → 검증: 트리 없는 오늘 데이터
      직렬화 대조(14칸 전부 depth 1 · DFS 순서 == sortOrder 순서 · pathName == name 14/14). **웹디자이너 패스는 독립 패널 몫**
      - 순서 저장을 **형제 집합 단위**(`reorderStorageLocations(parentId, 그 형제 id 만)`)로 바꿨다 —
        종전처럼 루트 전체 DFS 목록을 넘기면 자식이 하나 생기는 순간 전체성 검사에 걸려 "목록이 최신이 아닙니다" 만 뜬다
      - 옮기기 적용취소 순서는 **rename → move → reorder**(검수 지적 반영). move 를 먼저 하면 떼어낸 이름이 옛 형제와 겹쳐 멈춘다
- [x] 6. 점검 패널: 트리 드롭다운·서브트리 그룹·체인 저장·허브 마지막+allowHubClamp·(품목,위치) 재시도 → 검증:
      역주입 12건 전부 발화, 임시저장 축(getLocationDrafts·savedAt·3상태 Badge) 서브트리 전체로 확장, verify:db 초록
      - 저장 memo 가 **서브트리 루트의 pathName** 이다(백필 매칭 키) — 그물이 그 자리를 본다
      - HUB_SHORT 도 '멈춤'이다. 팝업이 그 한 건을 처리하면 **나머지를 이어 달린다**(버리면 아래 칸 실측이 사라진다)
- [x] 7. `scripts/check-location-tree-drift.ts`(verify:db): 순환·깊이·형제 이름 중복·형제 sortOrder 중복·남의 영업장 부모
      → 검증: 다섯 축 전부 역주입에서 발화(6건), 실DB 위반 0건. verify:db 등록 완료
- [x] 8. `scripts/check-location-name-axis.mjs`(verify:fast): 표시 자리가 pathName 을 쓰는가 → 검증: 한 자리 되돌리면 빨강.
      쓰기 다섯 축은 `scripts/check-location-actions-wiring.mjs`(verify:fast)가 본다 — 서버 액션 진리표는 DB 가 있어야 돌아 소스 가드로 갈음했다
- [x] 9. 문서: `knowledge/domain-inventory.md` '보관 위치 트리' 절 신설 · `knowledge/glossary.md` 에 표기 경로·체인 저장 ·
      가이드 §22 '위치 트리' 등재(세 표면의 문법) · context-notes 결정 근거. **Work_log·INDEX 는 이번 시공 범위 밖**
- [x] 10. 커밋 단위: DDL+스키마 / 헬퍼+진리표 / 읽기 / 쓰기 / 관리 모달 / 점검 패널 / 감지망 / 문서. 푸시는 묶어서 — 8커밋 0a571eed 로 푸시, 실데이터 트리 구성(09-15), 운영자 실기 확인 "다 잘 보인다"
- [x] 11. 독립 검수 반영(서버 몫, 2026-09-14). 화면 몫은 다른 패널이 검수 중이라 손대지 않았다
      - [x] A. **영업장 전체 pathName 유일** — `conflictingPathName`(lib/locationPaths, 순수)을 생성·이름 바꾸기·이동 셋이 부른다.
            이동·이름 바꾸기는 **서브트리 자손의 새 경로까지** 본다. 그물은 드리프트 ⓕ축 + 배선 ⓕ축
            → 검증: 실DB 행으로 과도기 4단계 판정 확인(루트 `4층 김치냉장고` 생성 통과 · 그 아래 `상단` 신설 거부 ·
            평면 루트를 떼기로 이관 통과 · 이관 후 `상단` 신설 거부), 역주입 3곳 전부 발화
      - [x] B. **허브 3순위를 DFS 로, 쌍둥이 둘 다** — `overview.resolveHubSync` 는 DFS 순 `locations` 를 그대로 받고,
            `ledgerShift.resolveItemHubLocationId` 는 두 findFirst 를 영업장 위치 **한 번 읽기**로 합쳤다(쿼리 −1)
            → 검증: 전 품목 허브 판정 직렬화 **바이트 동일**(개요 36행 + 리졸버 36 + 부분집합 계약 72), 한쪽만 되돌리면 빨강
      - [x] C. 삭제 거부 문구를 pathName 으로(거부할 때만 색인을 읽어 성공 경로 왕복 불변)
      - [x] D. **색인 순수 테스트** — 색인을 `lib/locationPaths.ts`(prisma 미의존)로 내리고 `scripts/test-location-paths.ts`
            신설(37단언, verify:fast 에 `test-location-tree` 뒤) → 검증: 검수가 설계한 우회(`pathName` 을 `name` 으로)를
            역주입하면 **화면 축 그물은 초록인 채** 진리표만 4건 빨강
      - [x] E. 이동 적용취소 주석을 실제 순서(rename → move → reorder)로 정정


## 위치별 기본 보기 (2026-09-15)

재고 관리(소모품·부식)의 기본 보기를 **위치별**로 바꾼다. 설계 패널 "요청 1" 그대로 — 확대 없음.
서버·DB·저장 규칙 무변경, 화면 진입 기본값과 인라인 패널의 마감 동작만 바뀐다.

- [x] 1. 상태·복원 — `deepLinked`(`?focus=`·`?q=`) 로 초기값을 가르고, 복원은 `=== 'item'` 만,
      저장 키를 `stayeum-inventory-view2` 로 승격 → 검증: tsc 0, 새 그물 5축 초록, 역주입 3건 빨강
- [x] 2. 세그먼트 옵션 순서를 `위치별`·`아이템별` 로(§23 — 기본값이 첫 칸) → 검증: 새 그물 옵션 축
- [x] 3. 인라인 저장 후 동작 — 성공 토스트 `N건 저장됨` + 입력 비움, `onClose()` 는 `else` 아래
      한 곳. 모달 모드는 종전대로 닫힘 → 검증: `check-draft-lifecycle` ① 초록, 역주입 빨강
- [x] 4. `loading.tsx` 본문을 패널 골격으로(제목 h-5 · 2열 h-11 · 안내 w-2/3), 카드 5장 제거
      → 검증: 새 그물 스켈레톤 축
- [x] 5. `scripts/check-inventory-view-default.mjs` 신설 + verify:fast 등록(소스 가드 5축, 주석 걷고 봄)
      → 검증: 역주입 4종 전부 발화, 백업 원복 후 cmp 동일
- [x] 6. 문서 — `knowledge/domain-inventory.md` 체인 저장 7항 + 감지망 · `context-notes.md` 키 승격 근거


## 점검 위치 전체 (2026-09-15)

위치별 점검 선택기 맨 위에 **전체** 한 행을 둔다. 설계 패널 "요청 2" 그대로 — 확대 없음.
서버 저장 규칙·체인 세 축·허브 게이트 무변경, 범위와 그 범위가 갈라 놓는 두 축(memo·들여쓰기)만 바뀐다.

- [x] 1. 센티널 `ALL_LOCATIONS = '__all__'`(같은 파일 카테고리 세그먼트와 같은 문법)
      → 검증: tsc 0
- [x] 2. `LocationTreePicker` 에 `allRow` prop — 노드 map **앞**에 전체 행(44px·루트와 같은 10px
      들여쓰기·선택 시 체크와 선택색·우측 `N품목` mono) + `my-1 border-t` 구분선. 트리거는 `전체 위치`.
      카운트는 **링크된 품목의 중복 없는 수**(쌍 합계 아님) → 검증: 자리 단언 역주입 빨강
- [x] 3. 범위 — `isAll` 이면 `scope = locs`(목록이 DFS 라 숲이 곧 트리 순서), `selectedLoc = null`
      → 검증: `check-draft-lifecycle` 범위 축 초록
- [x] 4. 들여쓰기 기준식에 `isAll ? 1 :` — 안 넣으면 모든 행이 0단으로 무너진다
      → 검증: 새 그물 4' 단언, 원복 역주입 빨강
- [x] 5. memo 를 두 갈래로 — 전체는 **접두 패턴 밖** `전체 위치 점검`(괄호 안에 넣으면 '전체' 라는
      위치가 생기는 순간 백필이 오인한다). 그물 3' 단언도 두 갈래로 갱신(의도된 변경, 같은 커밋)
      → 검증: `위치별 점검 (전체)` 역주입에 그물 둘 다 빨강
- [x] 6. `getLocationDraftsFor(locationIds)` 신설 — 위치 수와 무관하게 쿼리 2개. 패널의
      `Promise.all(ids.map(getLocationDrafts))` 를 이 한 호출로(전체면 36 → 2).
      `getLocationDrafts` 는 임시저장 비우기 스냅샷이 여전히 부르므로 남긴다
      → 검증: `check-query-fanout` 초록, fan-out 복귀 역주입 빨강
- [x] 7. 진행 표시 — 체인 한 건마다 `done` 을 올리고, 멈춘 자리가 빈 문자열이면 중립 톤
      `저장 중 · N/M`(끝나면 지운다), 주 버튼도 `저장 중… N/M`. 실패·정리 실패는 종전 문구가 덮는다
      → 검증: tsc 0, 실기 확인 3번
- [x] 8. 문구 — 빈 범위 `배정된 품목이 없습니다.`(전체) · `최근 점검 …`(전체는 '이 위치' 생략) ·
      이중 차감 물음 목록은 5건 + `외 N건`(§14 고정 영역이 버튼을 밀어내지 않게)
- [x] 9. 감지망 — `check-location-name-axis` 3' 두 갈래 + 4' 들여쓰기 기준·선택기 행 자리,
      `check-draft-lifecycle` ⑦ 드래프트 fan-out 금지 → 검증: 역주입 5종 전부 발화, 백업 cp 원복 cmp 동일
- [x] 10. 문서 — `knowledge/domain-inventory.md` 체인 저장 8항 · `knowledge/glossary.md` 체인 저장 ·
      `context-notes.md` 결정 근거. **Work_log·INDEX 는 이번 시공 범위 밖**
- [ ] 11. 운영자 실기 확인 5건(아래 보고서) — 45쌍 저장 시간은 실기에서 잰다


## 패널 안 옮기기 (2026-09-15)

위치별 점검 패널의 품목 행에서 바로 위치 이동을 연다. 설계 패널 "요청 3 (c)안" 그대로 — 확대 없음.
저장 경로·체인·허브 게이트·거부 규칙 무변경. 바뀌는 것은 진입 자리 하나, 서버 인자 하나, 그리고
'옮긴 순간 두 쌍의 미저장 입력은 무효' 라는 규칙 하나다.

- [x] 1. 서버 `transferLocationStock(sourceRemainingQty?)` — 있으면 출발지 행을 `장부 − N` 대신
      그 값으로(실측), 그 행만 `carried: false`. 도착지 행은 표식 없음(null). memo 에 `· A 실측 R`.
      0 이상 유한수 검증, 거부 규칙·`closeItemLocation` 무변경
      → 검증: tsc 0, `check-stock-ledger-parity` 소스 가드 초록, 역주입 3종 빨강
- [x] 2. 모달 `TransferStockModal` — `initialFromId` · `lockItem`(품목 셀렉트 고정) · `z={260}` ·
      `onDone(result)`. 4단계 아래 `옮긴 뒤 {출발지} 남은 양 (선택)`(§12 라벨 12/500 · 입력 44px ·
      placeholder 빈칸 · 캡션 `안 적으면 장부에서 뺀 값으로 둡니다`). 미리보기 박스에 실측값과
      장부 차이 한 줄. 맞바꿈이면 숨김. 일반 진입(헤더 `위치 이동`)은 종전대로
      → 검증: tsc 0, 목업 대조
- [x] 3. 패널 행 — 비허브 행 하단 줄 좌측 `다른 곳으로`(우측 `옮김 없음` 과 같은 한 벌,
      `justify-between` 한 줄). 허브 행에는 없음(§27.1)
      → 검증: `check-draft-lifecycle` ⑧ 허브 가지 단언 초록
- [x] 4. 완료 처리 `handleTransferDone` — 두 쌍의 `beforeQtys`·`afterQtys` 키 삭제 + 그 쌍의 서버
      드래프트 삭제(반환값 읽음), 삭제 전 스냅샷, `onDone()` 갱신, 토스트 **하나**(success·액션 6초)
      + §16 적용취소(`deleteStockCheck` → 입력 복원 → 있었던 드래프트 재저장 → `onDone()` → info)
      → 검증: ⑧ 전 축 초록, 키 미삭제 역주입 빨강
- [x] 5. 감지망 — `check-draft-lifecycle` ⑧ 신설, `check-stock-ledger-parity` 소스 가드에 이동
      점검 표식 절 신설 → 검증: 역주입 4종 전부 발화, 백업 cp 원복 cmp 동일(git 미사용)
- [x] 6. 문서 — `knowledge/domain-inventory.md` '패널 안 옮기기' 절 · `knowledge/glossary.md` 2항 ·
      `context-notes.md` 결정 근거. **Work_log·INDEX 는 이번 시공 범위 밖**
- [ ] 7. 운영자 실기 확인 4건(아래 보고서)
- [ ] 8. 독립 디자이너 검수(시공 담당 몫 아님)


## 위치 트리 드래그 (2026-09-16)

보관 위치 관리 모달의 손잡이가 **부모를 넘나든다**. 설계 패널 "보관 위치 트리 — 부모를 넘나드는
드래그" + 운영자 확정 그대로. 기존 `옮기기` 모달 경로·`moveStorageLocation` 은 손대지 않는다.

- [x] 0. 가이드 §22 — '형제 안 순서 전용' 삭제 + 드래그 고스트 등재문(세 구간·고스트·자리표·
      표시선·막힘·settle·자동 스크롤 48px/420px·드래그는 보조 입구)
      → 검증: `docs/brand-guide-v2.0.md` §22 두 항목
- [x] 1. `lib/locationTree.preserveName(옛 pathName, 새 부모 pathName)` + `stripPrefixSuggestion`
      얇은 래퍼 → 검증: `test-location-tree` 71 통과(떼기 네 사례 값 불변 + 붙이기 네 사례)
- [x] 2. `actions.ts placeStorageLocation(id, parentId, index, preserve)` — 거부 규칙 다섯은
      이동과 글자까지 같은 호출, 이름 제안만 `preserveName`, 쓰기는 `$transaction` 하나
      (자기 parentId·name + 새 형제 전체 sortOrder), undo 는 `LocationMoveUndo`
      → 검증: tsc 0, 배선 그물 ⓖ 초록
- [x] 3. `check-location-actions-wiring` ⓖ 신설 — place 다섯 규칙·트랜잭션·형제 전체성·undo·권한·
      영업장 + 드롭 핸들러가 2연타 대신 place + 포인터 이동에 `setLocs` 없음 + 캡션 축 +
      드래그와 모달이 같은 `reasonOf` → 검증: 역주입 4종 전부 빨강
- [x] 4. 드래그 엔진 교체 — `locHitOf`(세 구간 히트) · ref 고스트 · 자리표 · 표시선 · 가장자리
      자동 스크롤 · 고스트 아래 이름 제안 한 줄. `locReflow`·`locOwnerAt` 삭제
      → 검증: tsc 0, eslint 496 델타 0
- [x] 5. `reasonOf` 모듈 레벨 공용화 + 형제 이름·전체 이름 두 줄 추가
      → 검증: 드래그·모달 한 벌(그물 ⓖ')
- [x] 6. 이름 제안 기본값·라벨 방향별(모달·드래그) — 최상위면 끔, 아니면 켬 / 떼기·붙이기
- [x] 7. 캡션 교체 + `check-location-name-axis` 에 표시선 깊이 산식 한 줄
      → 검증: 옛 문구 사본 0, 그물 초록
- [x] 8. 문서 — `knowledge/domain-inventory.md` '부모를 넘나드는 드래그' 절 · `glossary.md` 2항 ·
      이 파일 · `context-notes.md`. **Work_log·INDEX 는 이번 시공 범위 밖**
- [ ] 9. 운영자 실기 확인 6건(보고서)
- [ ] 10. 독립 디자이너 검수(시공 담당 몫 아님)


## 서류 시트 실거주·사업자등록증 (2026-09-16)

설계 패널 "서류 시트 — 실거주 확인서 다시 작성 문 · 전화번호 정본 · 사업자등록증 행" 그대로.
운영자 결정 A(실거주 상시 문)·C(등록증 한 행, 미등록이면 없음)·D(09-08 발급본 보존)·
B(1차 셋만 정본, 계약서 경로 셋은 드라이런만). 확대 없음.

- [x] 1. 진리표 먼저 — `test-doc-bundle` 에 케이스 ①~⑧(무회귀·등록증 행·다호실 1행·미등록·
      형식·실거주 상시·중립 그룹 제외), 218~222 의 "residence 도 undefined" 단언을 **의도된
      규칙 변경으로 뒤집고 주석**. `test-tenant-phone` 신설 18케이스
      → 검증: 둘 다 처음에 빨강 확인, 지금 81 + 18 통과
- [x] 2. `lib/tenantContact.ts` 신설 `pickTenantPhone(contacts, kinds=['PHONE','LANDLINE'])` —
      주 연락처 → 국내 → 본국 폴백, 비상 전면 제외. 호출부 셋(실거주 발급 · 서류 시트 문자 ·
      입주자 문자)만 옮기고 계약서 경로 셋은 안 건드림
      → 검증: `check-tenant-phone-axis` 신설·verify:fast 등록, 역주입 2종 빨강
- [x] 3. 드라이런(읽기 전용, 커밋 안 함) — 서류 축 0명 / 문자 축 6명. 결과는 보고서에
- [x] 4. A — `lib/docBundle` 이 실거주 발급본에 `canWriteNew = true`('other' 제외),
      `DOC_WRITE_NEW_LABEL` 상수, 229~231 주석 교체. 시트 라벨을 상수로
      → 검증: `check-doc-write-gate` ⓔ 신설, ⓑ `canWriteNew = paidThisMonth.has(` 생존 확인
- [x] 5. C — `DocBundleDocType` 'bizcert' · `DocBundleInput.bizCert` · `TenantDocBundle.propertyName` ·
      `DocBundleGroup.kind` 'property'(계약 다음, 중립 앞, 키 `property:bizcert`, 다호실도 1행).
      조회부가 `bizCertDriveFileId·bizCertMimeType·name` 을 넘기고, 시트 보조줄 `{영업장명} · {형식}`,
      메일 `personName` 분기, `/api/doc-file`·`/doc/[fileId]` 네 번째 소유 가지 **양쪽 함께**
      → 검증: tsc 0, `check-doc-file-label` DOC_WORDS 에 '사업자등록증' 추가 후 0건
- [x] 6. 공유 갈래 사후 토스트 회수(운영자 지적) — `lib/useDocShare` 는 `shared && text && !copied`
      일 때만 한 줄, 시트에 인라인 사전 안내. 형제 3화면은 shareText 미전달이라 무변경
      → 검증: `test-doc-share-queue` 38 통과, 형제 호출부 전수 확인
- [x] 7. 문서 — `knowledge/doc-issue-history.md` 표 12행 + '다시 만드는 문의 기준' · '영업장 서류
      동봉' 두 절, `property-public-facts.md` 등록증 절, `doc-vocabulary.md` 행 액션,
      `glossary.md` 3항, `context-notes.md` 결정 근거. **Work_log·INDEX 는 이번 시공 범위 밖**
- [ ] 8. 운영자 실기 확인 3건(아래 보고서)
- [ ] 9. 독립 디자이너 검수(시공 담당 몫 아님)

### 검수 반영 (2026-09-16, 커밋 903e6528 이후)

디자이너 6건 · 코드 검수 7건. 파일 범위 동일, 재고 파일 무접촉.

- [x] D1. 그룹 key 충돌 — `key={g.kind === 'lease' ? g.leaseTermId : g.kind}`(property·other 가 둘 다 null)
- [x] D2. 환경설정 문구 어미 반복·없는 이름 — `상담 도구의 문자·메일 첨부와 입주자 정보 > 서류에서 보낼 수 있습니다.`
- [x] D3. 보조줄 링크 둘에 `min-w-[44px]`(다시 작성 41px)
- [x] D4. `lib/docMime` LABEL·EXT 에 `image/heif` 등재(HEIC 와 같은 라벨)
- [x] D5. 등록증 큐 항목 `dateStr` 을 `''` 로(em dash 가 파일명 접미로 새는 길)
- [x] D6. 빈 상태·머리 판정 — EmptyState 는 계약·중립 그룹 기준, 머리는 property 그룹이 있으면 참
- [x] C1. 미납 안내·공지 문자 발신자 둘도 정본으로(호출부 셋 → 넷, 드라이런 결함 넷이 거기서도 산다)
- [x] C2. 그물 ⓒ 를 이름 무관 **꼴** 여덟 가지로. 새로 잡힌 rooms 9 · export 2 · TenantClient 1 등
      실측으로 ALLOW 개수 갱신(7파일)
- [x] C3. 그물 ⓑ 를 '호출 존재' 에서 **'결과 사용'** 으로 → 검수 우회(호출만 남김) 역주입 빨강 확인
- [x] C4. 정본에 `id` 2차 정렬키 + 본국 폴백 안에서도 주 연락처 우선. 단언 둘 추가(21케이스)
- [x] C5. `test-doc-bundle` 에 "문이 서는 docType ⊆ DOC_WRITE_NEW_LABEL 키" 단언(83케이스)
- [x] C6. `check-doc-write-gate` ⓔ 완화 — 대입은 `(true|!!cert)`, 라벨은 JSX 텍스트 노드만
- [x] C7. 시트 기기 경로 파일명이 표기를 안 따르는 것(커밋 전부터) → `knowledge/open-issues.md` 별건

## 아이템별 점검 폼 통일 · 토스트 이중 · 모달 스켈레톤 (2026-09-15, 운영자 결정 (c)안)

설계 패널 A~C. 파일 범위 — `InventoryClient.tsx`·`inventory/actions.ts`·`lib/stockCheckMerge.ts`·
점검 스크립트 넷·`knowledge/domain-inventory.md`·`glossary.md`. **서류 시트 파일 무접촉.**

- [x] A-1. 상태 — `beforeQtys`(허브 포함 전 행)·`afterQtys`(비허브만), 초기값 `{}`.
      `hubTouched`·`hubAutoAfter` 바인딩 제거, 허브 실측 여부 = `beforeQtys[hub] !== ''`
- [x] A-2. 드래프트 — 문서에서 `hubTouched` 폐기(읽기 한 줄만 호환), cross-mode 병합의 허브 특례 제거,
      `rowDrafts[locId]` 복원 + `Badge tone="inspect"` 3상태
- [x] A-3. 행 문법 — 위치 패널 정본 복제(참고줄 공통·허브 honey 상자 제거·`저장된 값`·`옮김 없음`·
      허브 `차감 후 N`), `qtyInputCls` 공용 상수로 승격, 점검일 44px, 안내문 화살표 삭제,
      칩 → `최근 점검` 캡션, 요약줄 `이번 옮김`, 단순 모드 죽은 코드(프리필·`이전`·`모두 이전 수량으로 확인`) 제거
- [x] A-4. 저장 (c) — `lib/stockCheckMerge.applyLocationChecks(base, patches, allowHubClamp)` 신설,
      `createStockCheck` 에 `locationPatches` 갈래(같은 `const base` 한 줄 재사용), HUB_SHORT 는
      `patchIndex` 를 싣고 한 건도 저장 안 함, 멱등창은 전 패치 일치 시에만
      → 검증: `test-stock-check-merge` 62 통과, `check-stock-ledger-parity` 소스 가드 초록
- [x] B. 토스트 — 상세 모달의 `점검을 저장했습니다` 삭제, CheckForm 이 두 갈래 모두
      `재고 점검 저장됨 [적용취소]`(같은 날 안내는 detail), `다음 품목` 은 보기 화면 맨 위 영속 한 줄.
      HubShort 갈래는 `HubShortDialog` 의 §16 토스트가 그대로 주인(이중 통지 금지 — 보고서 참조)
- [x] C. 스켈레톤 — `DetailModalSkeleton({ mode })` 신설, 래퍼 `px-5 sm:px-6 py-4 space-y-3 delayed-fallback`,
      맨몸 `SkeletonRows rows={5}` 제거
- [x] 감지망 — `check-draft-lifecycle` ⑨(대칭 22축)·`check-inventory-view-default` ⑥(골격 6축)·
      `test-stock-check-merge` 7~10장. 역주입 4종 전부 빨강
- [x] 6단계 감사 — `scripts/audit-item-check-hub-carried.ts` 예행 **0건**(적용은 별도 승인 대기)
- [x] 문서 — `knowledge/domain-inventory.md` 한 절, `glossary.md` 2항, `context-notes.md` 결정 근거
- [ ] 운영자 실기 확인 5건(아래 보고서)
- [ ] 독립 디자이너 검수(시공 담당 몫 아님)

### 드래그 재검수 권고 5건 (2026-09-16, 차단 0 · 위 작업과 별건)

파일 — `InventoryClient.tsx`·`docs/brand-guide-v2.0.md` 둘뿐.

- [x] R1. 고스트 제안 줄 라벨을 상수 `전체 이름` 으로. 삼항이 죽어 있었다 — `preserveName` 이
      pathName 을 보존하므로 이름이 바뀌는 드롭은 미리보기 조건(`nextPath !== pathName`)에 안 걸리고,
      뜨는 경우(접두 불일치·최상위)는 언제나 `nextName === name` 이다. §22 등재문도 실제 화면 예시로
- [x] R2. 목록 위 캡션 글자 고정(`placing` 삼항 제거 → `min-h` 불필요). `저장 중…`·reason·note 는
      footer 의 **기본 창고 캡션 상자 한 곳**이 맡는다(평시 문장 / 드래그 중 `reason ?? note` /
      저장 중 `저장 중…`). 전용 빈 줄 23px 이 사라졌다
- [x] R3. `min-h-[2lh]` 제거(Firefox 111~119 미지원·선례 0). 남긴 자리 없음 — 저장소 전체 `lh` 0
- [x] R4. 깊이 상한 행의 note 를 **몸통 띠**(`top+12 <= y < bottom-12`)에서만. 위·아래 12px 은 유효
      실선이 서는 자리라 '못 넣는다' 가 거짓이 된다. §22 등재문 '그 구간에서는' → '몸통 자리에서는'
- [x] R5. 고스트 `min-h-[54px]` → 세션 실측 `rects[first].height` 를 `LocDragView.rowHeight` 로 실어 씀

### 독립 디자이너 검수 9건 (2026-09-16, ee33917d 대상)

- [x] D1. 드래그 footer 한 상자 붕괴 — 평시 문장을 **자리만 남기고**(invisible) 그 위에 겹쳐 쓴다.
      어느 폭에서도 평시·드래그·저장 중 세 상태 높이가 같다(px·`lh` 어느 쪽도 안 박는다).
      §22 등재문 "이유 줄은 늘 마운트, 캡션은 min-h-[2lh]" 문장도 실제 구현으로 정정
- [x] D2. §12 입력 높이 혼용 — `qtyInputCls`(두 화면 공용)·단순 모드·잔량·메모 전부
      `px-3 py-2.5 … min-h-[var(--input-h-touch)]` 한 벌로(옮기기 모달과 같은 문법)
- [x] D3. 골격 여백을 모드별로 — check `px-5 py-4`, view `px-5 sm:px-6 pt-3`
- [x] D4. 골격 세로를 실제에 맞춤 — 행 골격을 다섯 단(제목·참고줄·라벨·입력 44·옮김 없음)으로,
      빠져 있던 **안내문 2줄 블록** 추가. 행 개수 2 고정은 유지
- [x] D5. 허브 행 표기를 §22 정본 `기본 창고` Badge(pale-amber)로
- [x] D6. `차감 후 N` 캡션을 **위치 패널 허브 행에도** 신설(빼는 쪽이 아니라 맞추는 쪽)
- [ ] D7. 비허브 행 `다른 곳으로` — **이번엔 뺐다**(아래 사유)
- [x] D8. 복원 루프에도 빈 값 필터 — 배지 시딩 규칙을 저장 경로와 맞춤
- [x] D9. §16 적용취소 결과 토스트 — 세 갈래가 쓰는 공용 옵션 한 곳에 `pushToast('info', …)`

### 독립 장부·코드 검수 9건 + 가드 우회 봉합 (2026-09-16)

- [x] L1. 허브 실측 제출의 부족 게이트 — `allowHubClamp: hubMeasured`(패널 술어와 글자 맞춤) +
      이동 뒤 재시도에서 **허브 패치도 함께 제외**(그 값은 이동 전의 사실)
- [x] L2. 20초 멱등창 — 날짜·메모·보정 여부·`carried === false` 를 함께 보고, 삼킨 경우
      `deduped: true` 로 돌려줘 **적용취소를 안 붙인다**
- [x] L3. `applyLocationChecks` 가 순서(허브 맨 뒤, 안정 정렬)와 중복 거부를 직접 지킨다.
      진리표에 '허브가 먼저 온 경우'·'같은 위치 두 번' 축
- [x] L4. `patchIndex` 소비 — 서버가 `stuckLocationId` 를 함께 주고 팝업이 그 칸 이름을 띄운다
- [x] L5. 감사 스크립트가 **그 점검 시점의 허브**(허브 후보 집합 ∩ 그 점검 행)로 찾도록. 재예행 결과 아래
- [x] L6. parity 소스 가드를 `matchAll` 로 — base 조립문이 2줄 이상이면 위반
- [x] L7. 옛 드래프트 호환을 정본 한 함수(`hubDraftToBefore`)로 올리고 **읽는 세 자리** 전부에
- [x] L8. (D8 과 같은 자리 — 한 번에)
- [x] L9. `sameDayNotice` 술어 — **`locationPatch` 인 것이 의도**임을 이름과 주석으로 명시(아래 사유)
- [x] 가드 우회 봉합 — ⑨ 에 '안 적은 행은 패치에 안 실린다' 세 축(조립부 `continue` · 허브는
      `hubMeasured` 일 때만 · 조립부에 `prevMap` 없음) + 진리표 '실측 행 수 == 패치 수'

### 재검수 반영 — 차단 1 + 권고 6 + 우회 봉합 (2026-09-16, a8789ed7 대상)

- [x] A. `HubShortDialog` 의 적용취소 둘이 `deduped` 를 본다. `retry` 타입에 `deduped?: boolean`,
      '옮기고 채움 완료' 는 **이동만** 되돌리고(보충은 내 것이 아니다), '부족한 채로 저장' 은
      적용취소를 안 붙인다. 그물 축도 팝업 두 자리를 본다
- [x] B. ④ 죽은 분기 — 지우지 않고 그물을 **"두 조건이 배타다"** 로 바꿈.
      `allowHubClamp` 는 `saveArgs.allowHubClamp || !!o.allowHubClamp`(undefined 덮어쓰기 제거)
- [x] C. footer 겹친 `<p>` 에 `overflow-hidden` 두 겹 — 사용자 이름이 긴 이유 문구가 버튼 위로 흐르지 않게
- [x] D. 폼 `차감 후` 술어를 패널과 같게(`restockSum > 0 && beforeStr === '' && prevQty !== undefined`).
      참고줄 표시 조건에서도 `rowIsHub && restockSum > 0` 항 제거
- [x] E. `check-inventory-view-default` ⑥ 가 주석이 아니라 **마크업**을 겨눈다(골격 함수 본문에서
      안내문 두 줄 + 점검일 라벨 순서, 입력 44px 개수)
- [x] F. 진리표 13번의 주석을 정정 — 봉합의 근거가 아니라 **서버 쪽 결함**(이월 행을 실측으로 찍는 변경)을 잡는 축
- [x] G. 멱등창에 `patches.length === lastMeasured` — 진부분집합 삼킴 차단
- [x] 우회 봉합(구조) — `entered` 집합을 `onChange`·드래프트 복원 둘에서만 채우고, 복원은
      **서버 문서의 키**(`draftedIds`)에서만 가져온다. 조립부·`hubMeasured`·과잉 입고 신호 셋 다 그 집합을 술어로
- [x] 별건 2건은 `knowledge/open-issues.md` 에만(고치지 않음)

### HEIC 업로드 정규화 (2026-09-16)

아이폰 사진(HEIC)이 그대로 저장돼 첨부·발급이 깨지던 입구 넷을 **올리기 전에 브라우저에서**
정규화한다. 운영자 결정: 등록증은 PDF 한 장으로 저장, 같은 부류(도장·로고·스캔 계약서)를
함께 고침, 업로드 카드에 PDF 권장 문구.

- [x] 1. `lib/uploadImage.ts` 신설 — `fileToUploadPdf`(종이) · `fileToUploadImage`(그림).
      디코드는 `ocrImage` 정본 문법(EXIF 회전 픽셀 박기 · 비트맵·캔버스 즉시 반납),
      **디코드 실패는 던진다**(조용한 원본 통과 금지). 이미지→PDF 는 pdf-lib dynamic import,
      1장 고정, 용지 비율 = 사진 비율
- [x] 2. 사업자등록증 배선 — `handleBizCertSelect` 가 변환 File 을 싣고, `accept` 는 그대로,
      서버 `BIZ_CERT_MIME_OK` 를 `application/pdf` + JPEG·PNG 안전망으로 축소(HEIC 거절).
      4MB 가 **변환 후 기준**임을 주석·안내 문구에 명시. 변환했으면 토스트 detail 로 사실을 말함
- [x] 3. 같은 부류 — 도장·계약서용 로고는 PNG 로 정규화(알파 보존), 스캔 계약서는
      **mime 화이트리스트 신설**(종전에는 크기만 봤다) + `finalizeContractScan` 이 Drive 판정
      mime 도 함께 검사
- [x] 4. 발급 방어 — `residenceCertOverlay` 도장 임베드에 형제(`rentReceiptPdf`)와 같은 try/catch
      (서류는 나가고 사실은 서버 로그에). `google-drive.sniffImageMime` 을 `docMime` 위임으로
      묶어 쌍둥이 제거(옛 논리는 mp4 까지 image/heic 이라 답했다)
- [x] 5. 감지망·진리표 — `scripts/test-upload-image.ts`(31축) verify:fast 등재,
      `check-upload-hygiene.mjs` 에 업로드 입구 축 ⓖⓗⓘ 신설, `test-doc-mime.ts` 에 두 sniff 가
      같은 바이트에 같은 답을 한다는 단언 10건
- [x] 6. 기존 데이터 — `scripts/audit-heic-uploads.ts`(읽기 전용) 예행 **0건**.
      서버 변환이 불가하므로 백필은 재업로드 요청뿐이라는 사실을 `knowledge/open-issues.md` 에
- [x] 7. 문서 — `doc-file-format` 의 "이미지를 PDF 로 싸지 않는다" 를 **내보내기** 규칙으로
      좁히고 업로드 정규화를 예외 절로, `property-public-facts` 등록증 절 갱신
- [ ] 8. 운영자 실기 확인 — 아이폰에서 HEIC 등록증 업로드(1번), 도장, 스캔본, 실거주 확인서 발급

### 퇴실 낱말 zht·zh (2026-09-16)

번체 `退住` → `遷出`, 간체 `退住`·`退房` → `搬离`. 운영자 확정(대만 정형화계약 원문 실측으로
退住 유지 근거가 무너졌고, 간체는 迁出 의 户口 색 때문에 搬离 로 갈렸다).

- [x] 1. DB 사전 실값을 줄 전문으로 뜸(설정 > 계약서 > 번역이 쓰는 `parseContractTranslations`
      경로). zht 17줄 · zh 17줄 — 두 언어 다 살아있는 13줄 + 옛 '입실료' 열쇠 고아 4줄
- [x] 2. 문안표를 **세션 scratchpad** 에만 두고(저장소 금지) 모든 줄에 `ko` 열쇠를 담음.
      `forbid` 는 줄마다 하나씩(zht 退住 17 · zh 退住 13 · 退房 4)
- [x] 3. 예행 34줄(zht 17 · zh 17) 무차단 → 적용 34줄, 줄 수 일치. 적용 뒤 재실측
      zht 遷出 17 · zh 搬离 17 · 退住/退房/退租 0. `退費`·`退款` 은 손 안 댐
- [x] 4. 코드 사전 4자리 — `lib/contractTranslation.ts` 268행 `clause.zht`·`clause.zh`,
      269행 `none.zht`·`none.zh`. `deduct` 무변경, `{{청소비}}` 보존,
      `入住時`·`入住时`·`使用費`·`使用费` 무접촉
- [x] 5. 진리표 **14단계 신설**(13단계와 섞지 않음 — 13은 법 프레임 축, 14는 실무어 축).
      단언 8 + 실측 줄. 13단계 `FORBIDDEN.zht` 에 `退租` 추가
- [x] 6. 역주입 둘 다 빨강 — ⓐ 되돌리기(zht clause 遷出→退住) 1실패, ⓑ 문장 통째 삭제
      (none.zht·none.zh) 2실패(급소 단언이 잡음). 원복은 백업 파일 복사로만
- [ ] 7. 운영자 실기 확인 — 계약서 화면에서 번체·간체 미리보기에 새 낱말, 발급 직전 드리프트
      경고 없음. **적용 시점에 열려 있던 계약서 화면은 새로고침 필요**

독립 검수 반영 (2026-09-16)

- [x] 8. 14단계가 제 기각 사유를 보게 함 — 단언 둘과 실측 줄 정규식에 `退宿`(기숙사)·`迁出`
      (호적) 추가. 주석이 이름까지 댄 낱말을 그물이 안 보고 있었다
- [x] 9. 13단계 `FORBIDDEN.zh` 에도 `退租` — 간체·번체 표기가 같은 글자라 zht 에만 두면 비대칭
- [x] 10. **자형 순도 단언 신설** — 간체 전용 13자(`费 洁 对 价 结 离 时 与 无 内 后 务 为`)가
      zht 문안에, 번체 전용 짝이 zh 문안에 있으면 빨강. 낱말만 보면 'zht 자리에 간체 본문 +
      `遷出`·`使用費` 두 글자만 번체' 우회가 여덟 단언을 다 통과한다. 대상은 코드 사전의
      zh·zht 문안 전부(권장 청소비 셋 + 환불 + 서명 안내 `SIGN_KEYS`, 언어당 69자리)
- [x] 11. **DB 사전 낱말 그물 신설** — `scripts/check-contract-dict-terms.ts`(verify:db 등재).
      영업장이 설정 화면에서 사전을 직접 고칠 수 있는데 그 축을 보는 그물이 없었다. 금지 낱말
      (6언어 29개)과 자형 순도를 같이 보고, **고아 줄은 갈라서 보고**(종이에 안 나가므로 참고).
      읽기 전용. 실측 — 살아있는 336줄 · 고아 84줄 · 위반 0 · 고아 9(ja 옛 `入室`·`退室`)
- [x] 12. 기록 정정 — 박제 안전 근거가 '만료' 가 아니라 '닫혔고(closedAt) 한국어(signLang ko)',
      "45건 중 살아있는 것 0" 은 `ContractShareLink` 전체가 45건이고 중국어가 0건(ko 4 · en 1 ·
      ja 1 · 미지정 39), 되돌림 안전망 한 줄, glossary 의 `搬离`·`退租` 근거 긴장 해소
- [x] 13. 역주입 셋 다 빨강 — 자형 우회 판(zht 에 간체 본문) · `退宿`/`迁出` 주입 · DB 그물 우회.
      원복은 백업 파일 복사로만

### 연락처 대체·예약 확정 게이트 (2026-09-16)

본인 전화번호가 없는 사람에게 문자가 안 가던 것을 비상 연락처로 돌리고, 대신 그 상태로는
예약을 확정하지 못하게 막는다. **종이(계약서·실거주 확인서)는 대체하지 않는다.**
운영자 확정: 본인 전화(국내·유선·해외 모두 인정)가 있어야 확정 · 메신저 아이디만이면 거부 ·
단체 공지는 대체 대상을 기본 선택에서 빼되 누가 빠졌는지 보여 주고 문구를 복사하게 한다 ·
저장 후 토스트는 안 띄운다(폼 안 안내가 지속 사실을 진다).

- [x] 1. 정본 추가 + 진리표 먼저 — `lib/tenantContact.ts` 의 `pickTenantPhone`(55~67)은
      **한 글자도 안 고침**. 밑에 `PickedPhone`·`TenantPhoneContactWithOwner`·
      `pickTenantPhoneWithFallback`·`trimPickedPhone`·`reservationConfirmPhoneDenial`·
      `RESERVATION_PHONE_DENIAL_PREFIX` 추가. `scripts/test-tenant-phone.ts` 26 → **44건**
      (대체 9 · 게이트 8 + 주인 표기 네 갈래). 빨강 확인 4종(kinds 무시 2실패 · 정렬 제거 2실패 ·
      주인 표기 갈래 소실 3실패 · 소급 통과 제거 2실패)
- [x] 2. 게이트 배선 셋 — `leaseSaveDenial`(등록·계약 추가, 새 입주자는 폼 연락처로 판정) ·
      `updateTenant`(연락처 저장 **뒤**, `alreadyConfirmed: !!currentLease.reservationConfirmedAt`) ·
      `applyStatusTransition`(`!!lease.reservationConfirmedAt`, 계약의 주인으로 조회).
      셋 다 정본 한 함수를 부른다(`roomRequiredDenial` 전례). 폼 경로는 인라인 `setError` +
      연락처 칸 포커스(§30 목표선은 정본 ViewportOffsetGuard 가 진다), 전환 경로는 §15 error
      토스트 + 액션 `입주자 정보`
- [x] 3. 폼 안 안내 — `TenantClient.tsx` 연락처 섹션 맨 아래, **지금 입력값**으로 판정
      (`contactPreview` → 정본 두 함수). 틴트만(`--warning-bg`) · 좌측 팁 없음 · 12px
      `--warm-mid` · r-md 8 · 패딩 10/14. 확정 토글을 켠 채 막히면 확정 캡션 자리가
      11px `--danger-fg` 로 바뀜
- [x] 4. 문자 넷 대체 + 받는 사람 고지 — 컨텍스트 타입 넷을 `phone: PickedPhone | null` 로
      (타입 변경이 읽는 자리 10곳을 전부 드러냄). 고지 줄은 `TenantSmsModal`·`UnpaidSmsModal`·
      `TenantDocBundleSheet`·`TenantDocSmsComposeSheet` 넷, 11px `--warm-mid`, 위 5px, 안 붉음
- [x] 5. 단체 공지 — 기본 선택 세 자리를 `defaultPick`(본인 번호만)으로, 행 꼬리표 `· 비상`,
      빠진 사람 패널(접었다 펴기 + `문구 복사`)을 고르는 화면·쓰는 화면 양쪽에. 복사 성공은
      §15 토스트 한 장(액션 없음), 실패도 말한다
- [x] 6. 실거주 확인서 발급 화면 안내 한 줄 — 전화 칸이 빈 경우에만
- [x] 7. 실태 스크립트 `scripts/check-tenant-own-phone.ts`(.ts — 정본 import, 조회 한 번,
      번호 마스킹). 실측 127명 중 본인 전화 123 · 비상만 2 · 연락처 없음 2 · 메신저만 0 ·
      **게이트가 지금 막는 사람 0명**(넷 다 거주도 예약도 아님) · 대체로 새로 문자 갈 사람 2명 ·
      실거주 확인서 연락처가 빌 사람 4명
- [x] 8. 감지망 — `check-tenant-phone-axis.mjs` 에 ⓓ(종이 넷에 대체 이름 금지)·ⓔ(확정 세 자리가
      정본을 부르고 `reservationConfirmedAt` 을 읽는가) 신설, ⓐ 확장(대체 갈래의
      `isEmergency`·`kinds`), ⓑ 를 두 함수 이름으로 일반화. **ALLOW 7 무변경**
- [x] 9. 문서 — `knowledge/tenant-own-phone-fallback.md` 신설 + INDEX 등재
- [ ] 10. 운영자 실기 확인 6건 — 아래 보고의 실기 목록

### 자재 설치·폐기 두 축 (2026-09-16)

비용은 5개인데 설치된 것은 3개로 보이는 축 분리 + 시공 중 파손·분실·교체 폐기 기록 문.
운영자 승인 — (a)안(지출 행에 두 칸) / 자리 수는 안 넣음 / 나눠 배정 조작 개선은 다음 회차 /
교체 한 동작은 폐기 문법의 일부라 포함 / combineAssets 이력 고아 함께 수정.

- [x] 1. 순수 집계 분리 — `app/(app)/inventory/assets/aggregate.ts` 신설(`'use server'` 아님).
      `aggregateAssets`·`serializeSpecKey`·`buildAssetDetail`·`fmtQty`·`SpecKey` 이동. **동작 변화 0**
      (집계 스냅샷 392,706바이트 전부 동일로 실측)
- [x] 2. 스키마 2칸 — `Expense.disposedAt`(date)·`disposalReason`(text), nullable·인덱스 없음.
      예행(BEGIN→DDL→검사→ROLLBACK) 뒤 **운영자 승인**받고 적용. `db push` 안 씀.
      적용 전후 지출 591행 지문 `05108f5b…` **바이트 동일**
- [x] 3. 읽기 축 — `RawAsset`·`AssetItem` 에 폐기 5칸, `getDurableItems` select 2칸.
      **`durableExpenseWhere`·버킷 분류 무접촉**(폐기 행은 그 방 버킷에 남는다)
- [x] 4. 쓰기 축 — `MoveData` 확장, `disposeAsset`(게이트 넷 = 정본 `disposalDenial`)·
      `undoDisposeAsset`·`undoDisposalRow` 신설. `mergeUnassignedGroup` 양쪽 `disposedAt: null`,
      `revertAssignmentLog` 조회도 같은 클래스로 봉합. `combineAssets`·`setAssetQtyUnit` 이
      `disposedIds` 를 함께 받음. **교체 한 동작** = 배정 액션의 `replace`, 한 트랜잭션·토큰 하나
- [x] 5. 화면 — `valueSub` 조건부(`누적 N개`, 폐기 0이면 undefined) · 상세 '설치·폐기' 블록
      (§12 자동 합산 읽기전용 `--sand-s`·tnum) + 캡션 + `[폐기·분실 기록]` + 폐기 목록(`적용취소` 단일) ·
      폐기 Modal(`xs`, 옮기기 문법 복제, 사유 select) · 선택 알약바 `폐기·분실`(수령 대기 섞이면 숨김) ·
      `buysOf` 가 `disposed` 를 거름
- [x] 6. 그물 — `test-asset-disposal.ts`(verify:fast, **61건**) ·
      `check-asset-disposal-wiring.mjs`(verify:fast, 급소 = mergeUnassignedGroup 의 `disposedAt: null`) ·
      `check-asset-overinstall.ts`(verify:db, **명부이지 위반 아님**, 실측 24건) ·
      `knowledge/domain-inventory.md` 절 + INDEX 한 줄
- [x] 7. combineAssets 이력 고아(별도 커밋 단위) — `AssetAssignmentLog` 의 라벨·규격 3종을 대상 값으로
      함께 옮기고 옛값을 `affected.assetLogs` 에 실어 `undoItemNameMerge` 가 온전히 원복
- [x] 8. 검증 — tsc 0 · verify:fast 0 · verify:db 0 · next build 0 · eslint 496(기준선 496, 델타 0)
- [x] 9. 금전 불변 실측 — 정적 지문 바이트 동일 + **폐기 시뮬 4건**(502호 2개·504호 1개·미배정 2개·
      전량 폐기) 전부 총지출·방별·월·영수증·RoomWork 지문 동일, 카드 안 사라짐
- [x] 10. 역주입 5종 전부 빨강(②·⑤ 는 진리표가 못 보고 배선 그물이 잡는다), 백업 cp 원복 cmp 동일
- [ ] 11. 운영자 실기 확인 5건 — 아래 보고의 실기 목록

### 독립 검수 반영 — 디자이너 15 + 장부·금전 13 (2026-09-16)

- [x] D1 목적지 변경이 교체 사유를 비운다(`AssetsClient.tsx:1683`) + 같은 클래스(살아 있는 수량 0)도 이유줄
- [x] D2 등록증 효과 재실행이 `certThumb` 을 먼저 내린다(`SettingsForm.tsx:2095`)
- [x] D3 폐기 미리보기가 자리를 말한다 — `이 방에` → `${place}에`(`:1852`)
- [x] D4 optgroup em dash → `·`(`:1745`) + 그물에 줄 단위 em dash 단언
- [x] R1 등록증 로딩에 `Skeleton delayed-fallback`, `PDF` 글자는 실패 갈래만(`SettingsForm.tsx:2233`)
- [x] R2 오류 폴백 중복 제거 — `첫 장을 여는 데 실패했습니다.`(`:2108`)
- [x] R3 여러 품목 폐기 모달 `width="sm"`(`AssetsClient.tsx:1786`)
- [x] R4 폐기 목록 적용취소 히트 30 → 44px(`:1560`)
- [x] R5 배정 변경 이력 어휘를 §16 정본으로 — `되돌리기`→`적용취소`·`지우기`→`삭제`·안내줄·토스트·확인창.
      **그물 확인 결과**: `check-undo-verb.mjs` 는 (ㄱ) `GUARDED` 파일 명단에 이 파일이 없고
      (ㄴ) 일반 스캔은 `label: '적용취소'` + 토스트 `되돌렸습니다` 동거만 **info 로** 찍을 뿐 exit 1 이 아니며
      (ㄷ) **버튼 라벨 글자(`>되돌리기<`)는 아예 안 본다**. 그래서 투명했다 → 내 그물에 전수 단언 신설
- [x] R6 §12 다섯째 항 — `자동 계산` 캡션 추가(`:1523`)
- [x] R7 조사 정본 신설(`lib/statusReasons.ts:78~86` `eunNeunOf`·`iGaOf`·`withEunNeun`) + 손박음 6자리 교체
- [x] R8 전량 폐기 카드 `valueDanger`(`:812`) — 0개 카드의 옮기기가 죽던 것은 D1 과 같은 클래스라 함께 해소
- [x] R9 폐기 목록 제목(`:1546`)
- [x] R10 다크 검은 구멍 3칸 — 영업장 로고(`:921`)·계약서용 로고(`:948`)·도장(`:2266`)
- [x] R11 `폐기` 표식을 §11 메타 칩으로(`:1584`·`:813`)
- [x] L1 **분할 복제 목록을 한 자리로**(`actions.ts:654` `cloneExpenseScalars`) + 세 자리 전부 경유 +
      **스키마에서 읽어 대조하는 단언**. 실측 재현·확인 아래 보고
- [x] L2 `setCommonAsset` 이 폐기 행을 데려간다(`:445`)
- [x] L3 교체도 `disposalDenial` 정본 통과 + `already` 에 `isCommonAsset: false`(`:886~895`)
- [x] L4 적용취소 복원이 `propertyId` 로 잠긴다(`:1046`)
- [x] L5 수량 미기록 축 — `liveUnits`(`aggregate.ts:88`) + 진리표 76건(미기록 15건 신설)
- [x] L6 교체분은 배정 이력에서 되돌리기 차단(`:925`)
- [x] L7 선입선출 tie-break 에 `id`(`:707`·`:760`)
- [x] L8 묶음 잔존 판정이 폐기도 센다(`:1287`)
- [x] L9 `undoDisposalRow` 짝 조건에 `excludeFromInventory`·`receivedAt`·`qtyValue not null`(`:1178`)
- [x] L10 여러 품목 폐기가 **한 트랜잭션**(`disposeAssets`, `:1116`)
- [x] L11 `combineAssets` 이 all-null 규격 키를 그 라벨 규격이 1종일 때만 옮긴다(`:500`)
- [x] L12 `disposedIds` 를 서버가 정체성으로 직접 찾는다(`withDisposedSiblings`, `:37`)
- [x] L13 `check-asset-overinstall.ts` 에 `.catch` — 명부는 체인을 안 끊는다
- [x] 가드 우회 봉합 — 그물이 인자의 **정체**를 본다(`live` 의 정의 대조)
- [ ] 운영자 실기 확인 — 아래 보고의 목록

## 오류신고 시공 — 계측·단가 기준·영수증 결제수단 (2026-09-17)

### 0 계측이 눈을 뜬다 (신고 bf0a6fff 의 증거 공백)
- [x] 정본 `lib/viewportProbe.ts` 에 `probeAfterEntrance`(열었을 때 한 번)와 `probeReport`
      (두 스냅샷 나란히)를 더한다. 재는 문법은 기존 `viewportProbe()` 그대로 — 새 계측 문법 없음
- [x] **시점은 시간이 아니라 순서로 정한다** — `getAnimations().playState==='running'` 의 `finished`
      를 `allSettled` 로 받고, 같은 프레임의 `animationend`(useSettleEntrance)가 신고창 제 클래스를
      걷을 때까지 rAF 한 번 더. 도는 모션이 없으면 rAF 두 번. 2026-09-08 벽시계 금지 결정 준수
- [x] `ErrorReportButton` 이 열릴 때마다 최신 한 장만 ref 에 보관(DB·localStorage 안 씀), 제출 때
      `probeReport(열었을 때, 제출할 때)` 로 합쳐 싣는다. 개인정보는 한 글자도 안 는다(같은 probe)
- [x] 그물 — `test-viewport-probe.ts`(verify:fast, 7건) + `check-kbd-canonical.mjs` 에 배선·벽시계
      금지 단언 6줄
- [x] **헤드리스 실측으로 두 스냅샷이 갈리는 조건을 만들었다**(아래 보고). 열었을 때 `등장모션잔존=2`,
      제출할 때 `0`, 블록에 `제출할 때 (달라진 줄: modal)`

### 1 단가 기준 날조를 없앤다 (신고 73c18e13 앞부분)
- [x] **제3 경로를 재현으로 확정** — 영수증 인식 행(`FinanceClient.tsx:1814`)이 기준을 아예 안
      실어서 `basisOf`(`:560`)의 `?? 'spec'` 으로 떨어졌다. 09-15 두 행이 같은 영수증·같은
      `orderId`·사업자번호까지 실려 있어 OCR 경로임이 장부로 확인됨
- [x] 정본 `lib/unitBasis.ts` 신설 — `storedUnitBasis`(기록만) · `inferUnitBasis`(규칙 넷) ·
      `resolveUnitBasis`(기록 우선). 판정은 `lib/units` 의 `isLengthUnit`·`isVolumeSizeLabel` 에서 온다
- [x] 날조 제거 — `actions.ts:307` 이 기록 없으면 `null`. 단가 역산(`:312`)은 숫자를 내야 하므로
      정본 규칙을 쓴다
- [x] 다섯 자리 + 하나를 같은 정본으로 — 피커 휴리스틱 · 리셋 셋 · `basisOf` · 영수증 인식 행 ·
      수정 프리필 단가 복원 · `lib/setHint` 의 과거 단가 역산
- [x] 영수증 인식 행이 **직전 구매의 기록도 본다**(운영자 요구 "이전에 어떻게 저장했는지에 따라서").
      `unitFills` 를 `lastFills` 로 넓혀 같은 조회에서 기준·추적단위를 함께 받는다
- [x] 백필 27건 적용 — 예행·적용·멱등·`--revert` 원복·재적용까지 실측(아래 보고). 금전 지문 불변
- [x] 그물 둘 — `test-unit-basis.ts`(진리표 30건) + `check-unit-basis-wiring.mjs`(verify:fast) ·
      `check-unit-basis-drift.ts`(verify:db, 장부 축)
- [ ] **SpecWizard 는 안 건드렸다** — 롤 + m 을 고르면 길이 규칙과 어긋난 규격당을 제안한다.
      배선 그물의 '알려진 공백' 목록에 사유와 함께 올려 뒀다. 호출부 넷이 걸려 별건

### 2 영수증 결제수단을 살린다 (신고 73c18e13 뒷부분)
- [x] 홈 경로 복구 — `pendingReceipt.ts` 의 `GeminiResult` 타입과 재조립 두 군데에 `cardName`·
      `cardLast4`·`vendorBizNo`. 사업자번호도 같이 빠져 있었다(같은 클래스)
- [x] 브랜드 매칭 양방향 + 계정 쪽 표기 셋(브랜드·별칭·표시명). 부분 일치는 두 글자부터
- [x] 못 맞히면 **확인형 고지 한 줄** — "영수증에는 현대카드로 결제한 것으로 보입니다". 카드를
      고르면 사라진다. 자동 `payMethod` 기입은 안 한다(정산 상태가 따라 움직인다)
- [x] 그물 — `check-receipt-card-relay.mjs`(verify:fast). **목록을 손으로 세지 않고**
      `ReceiptOcrResult` 를 읽어 대조한다(2026-09-16 cloneExpenseScalars 와 같은 클래스)
- [ ] **별칭 학습은 못 했다** — 저장할 칸이 없다. `ItemNameAlias` 재사용은 품목명 자동완성으로
      그대로 샌다(`finance/actions.ts:1795~1808` 이 모든 `preferredLabel` 을 제안 목록에 넣는다).
      새 칸·새 테이블은 loop.md §4(스키마 변경) 대상이라 운영자 승인 필요 — 보고에 적었다
- [ ] 곁가지(고치지 않음) — `lib/paymentMethods.ts:4` 에 `체크카드` 가 없는데 지출 옵션
      (`settings/actions.ts:599`)에는 있다. 수납 축과 지출 축이 다른 정본을 쓴다

### 검증
- [x] tsc 0 · verify:fast 0 · verify:db 0 · next build 0 · eslint 496(기준선 496, 델타 0)
- [x] 역주입 9종 전부 빨강, 백업 cp 원복 후 cmp 6파일 전부 동일
- [ ] 운영자 실기 확인 — 보고의 목록
