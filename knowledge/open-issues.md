# 미해결 · 후속 작업

## 진행 예정
- **[완료 3b853b2] 메뉴 아이콘 세트 v2** — 운영자 의뢰 디자인(Stayeum Icons 17종)을 Sidebar·BottomNav 에 적용. 약점 4개(지출 화살표·결산↔실거주·호실↔도면·체크리스트) 해소, 시세↔방문 구분, 미사용 IcoLab 제거. 비주얼만 교체(라우트·매핑 불변).
- **②-b 과납=기타수익**: 카테고리명 '기타 임대수입' 유지 여부 운영자 확인 후 오류신고(4ff2d332) done.

## 운영 규칙 (신규)
- **오류신고**: 세션 시작 시 `node --env-file=.env.local scripts/check-error-reports.mjs` 로 open 건 확인 → 운영자에게 보고·진행 여부 확인. 처리 후 `done <id>`. (AGENTS.md 규칙) 자취 캡처는 현재 '이동·JS에러'까지, 클릭·서버액션은 후속 확장.

## 시각 확인 권장 (실기기)
- **#1b 계약서 PDF** — 실제 발급 1회로 한 장에 잘 들어가는지·여백(상하좌우 14mm) 대칭 확인. (로컬에서 puppeteer 렌더 불가). 완전 WYSIWYG(화면=PDF 동일 CSS)은 후속 과제. [[domain-contracts]]

## 데이터 점검 권장 (코드 아닌 운영)
- 재고: 과거 '위치 미지정 수령'분이 빠졌을 수 있어, 라면·물티슈·키친타월(롤) 등 **창고 실측 1회** 권장. [[domain-inventory]]
- 위치별 점검 과거 중복(2026-06-13 8쌍)은 이후 절대값 재점검에 덮여 현재 잔량 무영향 → 정리 보류.

## 백로그
- lint 부채(no-explicit-any 다수, 주로 catch 블록).
- 분할/병합 엔진 공용 위젯화(지출 방별 분배).

## 디자인 가이드 v2.0 미해소 2건 — 해소 완료 (운영자 결정 2026-07-11)
- InventoryCard radius: 14px(r-xl) 정규화 확정, 코드 반영(7149469).
- 테이블 내부 z 리터럴: v2.0 §08에 지역 예외 조항 명문화, 코드 불변.

## 2026-07-13 오류신고 파생 백로그
- 날짜 표기 KST 정합(fmtDate) — 전 페이지 타임스탬프가 KST 기준으로 정정됨(자정 전후 하루 차 보이는 건 올바른 값). 배포 시점 운영자 공유 필요.
- /inventory React #418(hydration text mismatch) — 재현 후 근원 확정 필요(단위 신고와 무관 판정).
- StockCheck @db.Date 절단 — KST 저녁 수령 건 자동 점검 날짜가 전날로 저장(§4 인접, 저장 시 KST 절단 필요).
- 기존 결함 기록: updateExpense propertyId 미스코프, captureItemSpecOptions 중복 호출(무해).
- 검색 1.5차: 요청·서류 그룹 + 최근 검색 고도화. v2 후보: 수납 월·금액 검색, pg_trgm.

## 2026-07-14 오류신고·§4 파생 백로그
- savePayment FIFO 스탬프 유실: forcedTargetMonth가 이미 완납월이면 portion=0이라 현금영수증 체크가 무기록(적대검증 v-3). 현재는 스트립 InfoHint 고지 + 원터치 토글로 사후 보정. 근본 수정(이월 첫 record 스탬프)은 결제 인접이라 별도 §4 건.
- ~~updatePayment propertyId 미검증~~ — 2026-07-22 해소(ffbf182): update·delete·restorePayment 스코프 검증.
- ~~import replace 경로 하드삭제~~ — 2026-07-22 해소(ffbf182): 정본 deleteExpense 재사용.
- ~~receivedAt 재설정 시 자동 점검 미재생성~~ — 2026-07-22 해소(ffbf182): confirmReceipt 정본 경로로 재생성 + 날짜 동기화.

## 재고 품목 정렬 프리셋 (2026-07-18 메모 → 2026-07-22 완료 9c44449: 수동 기본·가나다·최근 추가, 표시 전용)
순서 편집 모드(수동 드래그)를 만들고 **사용해 본 뒤 필요하면** 진행. 후보: 재고수량순·재고추가순·가나다순 등 여러 정렬 기준 선택.
- '재고 적은 순'은 당장은 불필요 판정 — 품목별로 기준 수량이 달라 절대량 비교가 의미 없음(운영자).
- 진행 시 참고: 정렬 기준은 표시 전용이어야 하며 수동 sortOrder(2026-07-18 도입)와 공존 설계 필요.

## 서류 발급 이력 관리 체계화 (2026-07-22 운영자 지시, 큰 건수 없을 때 진행)
실거주확인서·납부확인서·계약서 발급 이력이 현재 시간순 평면 목록 — 건수가 쌓이면 뒤죽박죽이라 찾기 불편해질 예정. 효율적 관리 방법을 설계해 정리할 것(운영자: "조만간 한번 정리해서 효율적으로 관리하는 방법을 찾아보자").
- 후보 아이디어(설계 시 검토): 입주자별 그룹핑, 기간(월별) 접기, 상태(거주중/퇴실) 필터 확장, 같은 사람 재발급분 최신본 대표 표시 등. 기존 정본(SegmentedControl·SearchBar) 안에서.
- 진행 시 오류신고 5단계 프로세스(패널 설계·승인)로.

## 재고 화면 메뉴 구조(IA) 재설계 (2026-07-22 운영자 지적, 보류 — 반드시 전문가 패널과 진행)
운영자 지적 3건: ① 소모품·부식에만 선택·순서 편집·정렬·입력점검·관리설정·품목추가가 있고 비품·자재로 넘어가면 사라짐(두 뷰 헤더 불일치) ② '순서 편집'이 품목 순서인지 방·그룹 순서인지 스코프 불명확 ③ 방·공용부 그룹 순서 변경 경로가 위치 관리 안에 숨어 발견 불가.
- 운영자 명시 지시: 생각보다 큰 건이라 **혼자 하지 말고 전문가(패널) 도움 필수**.
- 방 순서 커스텀은 방번호순이 앱 전역 규칙(수납·호실관리·그리드)이라 비품만 바꾸면 정합 파괴 — 유지+명시 / 비품 전용 / 전역 설정 3안 중 운영자 결정 필요.
- 착수 시 UX·디자인 패널 설계 → 계획 보고 → 승인 → 작업(오류신고 5단계 문법). 사전 조사 보고가 이 노트 아래 붙어 있으면 그것부터 읽을 것.

## 홈 예상 지출 tier 3단 필터 누락 (2026-08-12 용어 통일 조사에서 확인, 산식 축이라 미수정)
`dashboard/page.tsx` 의 `tierImmovable`·`tierVariable` 은 활성 고정 규칙을 **전부** 더한다 — `projectedRecurringExpense` 와 달리 `!isPending`·`!recordedExpenseId` 필터가 없다. 그래서 고정 실기록액이 추정보다 크면 세 항의 합이 `expectedExpense` 를 넘고, `tierSavable = Math.max(0, ...)` 이 0 으로 클램프되어 '수시' 칸이 사라진다.
- 6·7·8월 실측에서는 미발동(세 항 합 == 예상 지출).
- 이번 작업은 표시 어휘만 손대라는 지시였고 이건 산식이라 손대지 않았다. 고치려면 tier 필터를 `projectedRecurringExpense` 와 맞추거나(기록분은 실액으로) 클램프를 없애고 음수를 노출해야 하며, 둘 다 카드 숫자가 움직인다 — loop.md §4 대상.

## 주단위 시작 계약의 월 계약 전환 흐름 부재 (2026-08-12 운영자 확정 백로그)
520호 김민정 건의 실체 — 원래는 월 단위 계약으로 들어오는데 사정상 **주단위로 계약하다가 눌러앉게 되어 정식 월 계약을 뒤에 만들고, 이미 낸 금액을 거꾸로 맞춰 나간** 예외 케이스다(운영자 원문). 그래서 이 계약만 보증금 수납이 '보증금 3만 + 청소비 2만'으로 갈라져 있고, 보유 보증금의 수납 기준 청소비 몫에 홀로 잡혔다. 데이터는 사실 그대로라 정정 대상이 아니다.
- **요구**: 이런 상황(주단위·단기 선입금 후 월 계약 정식화)을 앱이 구조적으로 받는 전환 흐름을 마련할 것 — 기존 수납·보증금·청소비를 새 계약 조건에 재귀속하는 절차가 수작업 역산이 되지 않게. 운영자 "이 상황도 대처할 방법을 마련하는 게 맞아. 그치만 예외상황인 것은 확실해"(빈도 낮음, 우선순위는 운영자 신호 대기).
- 관련: 7월 김민정 사건(이중 계상·청소비 누락처럼 보이던 것들)이 전부 이 수작업 역산의 파생이었다. 재무 탭 청소비 몫은 2026-08-12 계약 기준 축으로 변경 승인됨.

## MonthSelector 자체 폭 안정화 (2026-08-20 실측 확인, 공용 컴포넌트라 별건)
월 셀렉터 알약 폭이 **보고 있는 달에 따라 다르다** — 상대월 배지('지난달'~'11개월 전')·[오늘] 버튼·보더 두께(1px 대 1.5px)가 전부 조건부라 알약 테두리 안 내용이 상태마다 바뀐다. 헤더는 `flex-wrap` 이고 flex 줄바꿈은 shrink **이전에** 자연폭으로 판정되므로, 셀렉터가 넓어지면 헤더가 접힌다.
- **실측(1px 스윕, 320~1280)**: 과거 달 680px 미만에서 두 줄 · 이번 달 600px 미만 · 미래 달 640px 미만. 즉 **600~679px 구간에서 달을 바꾸면 헤더가 102 ↔ 46 으로 56px 뛴다.**
- **이 화면만의 문제가 아니다.** 같은 헤더 문법 + MonthSelector 를 쓰는 화면이 아홉이고(홈·수납·지출·입주자·재고·자산·요청·카드정산·월세영수증), 거기서도 ◀ 를 눌러 이번 달에서 지난 달로 가면 같은 구간에서 같은 반사가 **지금도** 난다. 입퇴실 캘린더의 스크롤 동기화(2026-08-20)는 트리거를 하나 더한 것이지 클래스를 만든 것이 아니다.
- **처방은 이미 형제에 있다.** `ViewTabs.suffixWidest`(2026-08-20 신설)가 같은 종류의 결함을 유령 span 으로 자리를 예약해 닫았다 — 가장 넓은 상태를 보이지 않게 같은 격자 칸에 겹쳐 폭을 잡는 방식이라 ch·px 매직넘버가 없다. 셀렉터도 같은 처방이 맞지만 **배지 문구 폭이 가변**('지난달' 대 '11개월 전')이라 가장 넓은 경우로 상시 예약해야 하고, 공용 컴포넌트라 아홉 화면에 번진다.
- **채택하지 않은 대안**: ① 768px 미만에서 셀렉터를 항상 자기 줄에 세우기 — 393~767 전체가 40px 상시 증가하고 여기엔 실사용 폰 폭(375·390·430)이 전부 든다 ② 680px 전용 브레이크포인트 — 저장소 최초의 미등재 브레이크포인트가 된다(§20 은 768 하나만 등재).
- 착수 시 디자이너 패스로. 관련: [[month-scope]].

## ~~자동 생성 퇴실 청소의 예정일이 '저장을 누른 날'이다~~ (2026-08-20 해결)
운영자 승인("실제 청소 예정일을 입력 받는 쪽으로")으로 닫혔다. 규칙 정본은 `lib/checkoutCleaning`
이고 기본값은 **퇴실 다음 날, 이미 지났으면 오늘**이다. 퇴실 미니폼 둘에서 고칠 수 있고 비우면
날짜 없는 예정이 된다. 근거와 갈린 결정은 [[domain-cleaning]] 과 Work_log 2026-08-20 (10).
- 남은 물음 하나. **업체가 보통 며칠 뒤에 오는가**는 앱이 모른다. 통상 이틀이면 기본값이 +2 가
  맞다(지연이 예정일 다음 날부터라 +1 이면 퇴실 이틀 뒤 아침에 빨개진다). 운영자 확인 대상.

## 이미 열린 퇴실 청소가 있으면 운영자가 고른 날짜가 안 들어간다 (2026-08-20, 승인 대상)
`ensureCheckoutCleaning` 은 그 방에 열린 CHECKOUT PLANNED 가 있으면 아무것도 안 만들고 돌아간다
(한 방에 퇴실 청소는 하나라는 규칙). 이제 미니폼이 날짜를 받으므로 그 경우 **고른 날짜가 어디에도
안 남는다.** 지금은 그 사실을 `notice` 로 알리고 끝낸다.
- 패널 둘(UX·웹디자이너)은 **그 행의 예정일을 입력값으로 갱신**하라고 했다. 방은 하나, 퇴실
  청소도 하나, 운영자가 말하는 답도 하나라는 논리다.
- 안 한 이유. 그 행은 운영자가 '날짜 변경'으로 손수 정한 날일 수 있고, 퇴실 저장이 말없이 덮으면
  무를 길이 없는 덮어쓰기가 된다([[rollback-always-required]] 원칙). 지금 문제가 '앱이 혼자 날짜를
  정한다'인데 그 답으로 날짜를 하나 더 혼자 정할 수는 없다는 판단이다.
- 갱신을 원하면 운영자 승인 뒤에 넣는다.

## 퇴실 경로가 넷인데 청소를 만드는 것은 둘뿐이다 (2026-08-20, 선재 결함)
`status: 'CHECKED_OUT'` 을 쓰는 자리가 넷이다. `checkoutTenant`(홈 알림)와 `applyStatusTransition`
(상태 전환 위젯)은 `ensureCheckoutCleaning` 을 부르지만, **`updateTenant`(입주자 편집 폼의 상태
select)와 `batchUpdateTenants`(일괄 편집)는 안 부른다.** 그 두 경로로 퇴실하면 청소 예정이 아예
안 생긴다. `app/api/import/route.ts` 도 같은 상태인데 이관용이라 의도적 제외인지 불명이다.
- 예정일 입력이 이 결함을 만든 것은 아니다(종전에도 안 만들었다). 다만 이제 '퇴실하면 청소가
  선다'가 화면의 약속이 됐으므로 갈림이 더 눈에 띈다.
- 고치는 것은 곧 규칙 변경이라 승인 대상이다. 함께 `status: 'CHECKED_OUT'` 소스 감지망을 놓아야
  다섯째 경로가 또 조용히 생기지 않는다(형제 문법: `check-tenant-lease-take.mjs`).

## 홈 퇴실 경로에 `roomStillOccupied` 가드가 없다 (2026-08-20, 선재 결함)
`applyStatusTransition` 은 그 방을 아직 쓰는 다른 계약이 있으면 공실 처리와 청소 생성을 통째로
건너뛴다. 그런데 `checkoutTenant`(홈 알림 경로)에는 그 검사가 없어 **무조건** `isVacant: true` 로
바꾸고 청소도 만든다. 방 하나에 계약 둘인 구성(룸메이트, 거주 + 창고)에서 홈으로 퇴실하면 사람이
사는 방이 공실이 되고 '청소 필요'가 붙는다. 공실 표시 쪽이 더 무거운 축이라 별건으로 다뤄야 한다.

## 되돌린 청소가 자동 생성분으로 오인될 수 있다 (2026-08-20, 스키마 필요)
손으로 만든 청소도 `leaseTermId` 를 얻는 길이 있다. `completeCleaning` 에서 '받은 청소비로 부담'을
켜면 비어 있던 `leaseTermId` 를 퇴실 계약으로 자동 귀속시키고, `reopenCleaning` 은 되돌릴 때 그
칸을 안 내린다. 그러면 `leaseTermId 있음 + CHECKOUT + PLANNED` 인, 자동 생성분이 **아닌** 행이
생기고 퇴실을 되돌리면 `clearAutoCheckoutCleaning` 이 그것을 소프트삭제한다. 그 함수가 명시적으로
피하려던 사고("사람이 넣은 일정을 코드가 없앤 것")다.
- 근본 봉합은 생성 출처 칸(`autoCreated` 또는 `origin`)이라 스키마 변경이고 loop.md 4번 대상이다.
- 발생 확률은 낮다. 실데이터에서 그 모양인 행은 확인되지 않았다(자동 생성분 2건은 둘 다 삭제됨).

## 공실 집계 제외 방의 퇴실도 청소를 만든다 (2026-08-20, 문구 아닌 규칙)
창고·사무실처럼 `isVacancyExcluded` 인 방에 걸린 계약이 퇴실하면 CHECKOUT 청소가 생긴다. 캘린더는
그 방에 행 자체를 안 세우므로 안 그려지는데 '청소 필요 N실'에는 세어져 유령이 된다. 창고를
비웠다고 회전 청소를 하지는 않으므로 자동 생성 대상에서 빼는 쪽이 맞지만 규칙 변경이다.

## 월 밴드 '변동 없음' 이 청소가 있는 달에도 뜬다 (2026-08-20, 문구 한 낱말)
`MoveCalendarRow` 의 월 밴드는 `eventCount === 0` 이면 '변동 없음'을 옅게 적는다. 그 수는 입퇴실만
세므로(청소는 설계상 `events` 에 안 들어간다) 청소만 있는 달은 밴드가 '변동 없음'이라 적으면서
바로 아래 트랙에 청소 띠가 선다.
- 지금은 탭 이름이 '입퇴실'이라 문맥이 잡아 주고 있고, 라벨을 늘리면 2026-08-20 배포 전 디자이너
  패스가 방금 고정한 탭·밴드 폭 산술에 다시 손대야 해서 이번에는 안 건드렸다.
- 실데이터에서는 아직 안 밟힌다(창 안 네 달 중 청소만 있는 달이 없다).


## 가이드 §12 의 오류 메시지 색(`--tc-d`)이 다크에서 대비 미달이다 (2026-08-20 실측)
§12 는 "에러: 보더 --tc + 11px --tc-d 메시지" 라고 적는데, `--tc-d`(#7C2D26)는 다크 블록에서
재정의되지 않아 다크 배경 위에서 **대비가 1.98:1** 이다(§28 본문 4.5:1 미달, 프로덕션 CSS 로 실측).
- 저장소 코드의 다수파는 이미 `--danger-fg` 를 쓴다. 그쪽은 라이트 #A03C2E · 다크 #E08A75 로
  갈려 라이트 6.14 · 다크 7.05 다.
- 즉 **가이드 문장이 라이트 전용**이다. §12 를 글자대로 따르는 새 코드가 다크에서 안 읽히는
  오류 메시지를 만든다. 가이드 문안 정정 대상이고, `--tc-d` 를 쓰는 자리 전수 확인이 따라온다.

## DatePicker 트리거에 접근 가능한 이름이 없다 (2026-08-20, 배포 전 패스 지적)
`DatePicker` 는 `aria-label` 프롭이 없고 호출부의 `<label>` 에도 `htmlFor` 가 없다. 그래서 소리로
읽히는 이름이 값 글자(날짜)뿐이다. 퇴실 미니폼에 청소 예정일이 서면서 **같은 생김새의 이름 없는
날짜 버튼 둘이 처음으로 세로로 붙었고**, 소리로는 어느 쪽이 퇴실일인지 구분할 단서가 없다.
- 고치려면 `DatePicker` 에 프롭을 하나 늘려야 하고 호출부가 약 60곳이라 별건이다.
- 다음에 `DatePicker` 를 손볼 때 함께 가져갈 것. WCAG 4.1.2.

## '작업 일정' 탭 접미가 작업을 안 센다 (2026-08-21 0단계, 운영자 판단 대기)
탭 이름이 '입퇴실 캘린더'에서 '작업 일정'으로 넓어졌는데 접미 N 은 그대로 **그 달의 입퇴실
건수**다(`app/(app)/room-manage/RoomManageClient.tsx:571` `moveEventCount`). 이름과 값이
어긋난다. 그 수는 홈 '이달 입퇴실 N건'과 같은 한 벌(`events`)이라 여기만 넓히면 두 화면이
갈리고, 넓히려면 홈 타일도 함께 결정해야 한다 — 그래서 0단계에서 안 건드렸다.
- 선택지: ① 접미를 입퇴실 + 작업 합으로(홈 타일도 함께) ② 접미를 입퇴실로 유지하고 이름
  옆에 뜻을 명시 ③ 접미를 떼고 축 필터가 건수를 말하게.
- 관련: [[domain-cleaning]] · Work_log 2026-08-21.

## 캘린더에 작업 조작 진입점이 없다 (2026-08-21 0단계, 기록만)
작업 레일은 설계상 `pointer-events: none` 이다(Android Blink 터치 래치 신고 d8554128 재발
방지). 그래서 캘린더에서 청소를 보는 것은 되지만 날짜를 바꾸거나 완료 처리할 길이 없다 —
요약 줄 칩이 방 모달로 보내는 것이 유일한 우회다. '날짜 변경' 버튼 자체는
`components/cleaning/CleaningRowBody.tsx:336` 에 상시 있고 게이트는 `canEdit` 하나뿐이다.
- 레일에 손닿을 것을 되돌리는 것은 위 신고 표면을 다시 여는 일이라 별도 설계가 필요하다.
