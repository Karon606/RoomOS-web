# 발급 서류의 세 자리와 작성 문

발급본이 사는 모델은 셋이고 서류 종류는 넷이다. `ContractFile`(계약서, 판본·폐기·구버전 개념
보유) · `RentReceiptFile`(kind 로 납부 확인서와 보증금 영수증) · `ResidenceCertFile`(실거주 확인서).

## 화면 셋의 소관이 다르다

한 종이를 네 자리에서 관리하면 확인 문구와 적용취소 동선이 갈라진다. 그래서 자리마다 물음이 하나다.

| 화면 | 답하는 물음 | 능력 |
|---|---|---|
| 서류 시트(`TenantDocBundleSheet`) | 지금 무엇을 보낼까, 지금 사실로 새로 만들까 | 종류별 **최신 한 건** 선택·발송 · 다시 만드는 문(납부=조건부·실거주=상시) · 영업장 서류(사업자등록증) 동봉 |
| 계약서 파일 패널(`ContractFilesPanel`) | 이 계약의 계약서는 | 판본·폐기·구버전 관리 |
| 전역 발급 이력(`/rent-receipts`) | 전 입주자에서 찾기 | 검색·상태 필터·삭제·다시 작성 |
| 발급 이력 목록(`TenantDocHistory`, 2026-09-03) | 이 사람에게 언제 무엇이 나갔나 | **열람뿐** |

넷째를 만들 때 관리 화면이면 중복이라는 것이 패널 판정이었다. 없던 능력은 정확히 하나
"이 사람의 과거 발급본 열람"이었고 그것만 채웠다. **계약서는 그 목록에 없다** — 바로 위 패널이
제 개념으로 다루고 있어 같은 파일이 한 화면에 두 번 서면 두 표현이 갈라진다(운영자 결정).

배치는 '계약서 파일' Section 의 **다음 형제**다. 안에 넣으면 계약서 아닌 서류가 그 제목 아래 서고,
Section h3(12px)보다 카드 헤더(14px)가 커 위계가 뒤집히고, Section 이 자식 간 간격을 안 줘 두
카드가 0px 로 맞붙는다. 접힘·지연 조회 문법은 `PaymentHistoryAll` 정본 그대로다.

## '이번 달 확인서 작성' 문 (2026-09-03)

서류 시트가 `발급본 있으면 [보기], 없으면 [작성]` 배타라, 지난달 발급본이 있으면 새로 만들 길이
없었다. 이 시트가 발급의 유일한 입구가 된 뒤로(2026-08-29) 그 길이 곧 막힌 길이었다.

문은 **둘의 곱**으로만 연다. 최신 발급본이 이번 달 것이 아니고(`canWriteNew` 는 stale 일 때만
서고), 그 계약에 이번 달 실입금이 있을 때다.

- **납부 게이트인 이유.** 납부 전에 확인서를 만들면 허위 서류다. 다만 발급 능력이 사라지는 것은
  아니다 — 기록 없는 달의 수동 발급은 발급 화면의 월 스테퍼와 전역 발급 이력의 '다시 작성'이
  그대로 감당한다. 이 문은 매월 반복되는 정상 흐름의 지름길이다.
- **버튼이 아니라 링크인 이유.** 행 주석이 "행마다 버튼이 하나라 한 줄을 유지한다"고 못박고
  있고, [보기]와 다투면 320px 에서 제목이 먼저 잘린다. 같은 시트의 '판본 바꾸기'가 쓰는 보조줄
  링크 문법을 쓴다. 간격은 **감싸는 div 가 준다** — 앵커에 `-my-2` 와 `mt-0.5` 를 함께 얹으면
  둘이 같은 마진을 다퉈 위쪽 음수 마진이 죽는다(Tailwind v4 실컴파일 확인).
- **닫혀 있을 때 침묵하지 않는다.** 조건부로 나타나는 문은 부재 이유를 말해야 한다. stale 조각과
  한 문단 두 문장으로 합친다("이번 달 발급본이 아닙니다. 납부가 확인되면 새로 작성할 수 있습니다").
- **단기는 제외.** 발급 화면이 대상월을 무시하고 입주월로 고정해(회계 오더 2026-07-27) 링크
  라벨 '이번 달'과 도착 화면이 어긋난다. 종이는 옳게 나오지만 화면이 거짓말을 한다.
- 판정 정본은 `lib/docBundle` 이 세우고 화면은 읽기만 한다. 실입금 술어는 `lib/rentPaid` 하나다
  (귀속월 기준·보증금 제외·양도인 제외·조정 전표 제외·금액 0 초과).

## stale 판정은 귀속월이다 (2026-09-03)

`RentReceiptFile.targetMonth`('YYYY-MM'). 발행일과 **다른 축**이다.

8월에 9월분을 선납받아 미리 발급하면, 발행일 기준으로는 9월에 그 종이가 stale 로 뜨고 9월 납부
기록도 있어 문까지 열려 **같은 달 확인서가 두 장 나간다.** 귀속월로 판정하면 닫힌다.

- 화면이 `anchorMonth` 를 실어 보내고 서버가 rent 일 때만 형식 검사 후 적는다. `fields.targetMonth`
  는 '2026년 9월분' 표시 문자열이고 운영자가 고칠 수 있어 판정의 근거가 못 된다.
- 기존 15건은 NULL 이고 백필하지 않는다. 옛 발급본의 귀속월은 표시 문자열에만 있고 그것은
  사실의 근거가 아니다. NULL 이면 발행일 폴백이라 화면이 1비트도 안 바뀐다.

## 곁다리로 잡은 것

`nameStyle` 이 15건 전부 비어 있었다. 2026-08-30 커밋이 서버에 `body.nameStyle` 을 받는 코드를
넣었는데 **화면이 그 값을 안 보내고 있었다.** 그 커밋이 고치려던 증상(영문 발급본의 파일명이
한글로 조립됨)이 그대로 살아 있었다. 귀속월을 배선하면서 같이 고쳤다.
**서버가 받는 코드만으로는 아무것도 증명하지 못한다** — 실데이터에서 그 칼럼이 차 있는지 세야 한다.

## 다시 만드는 문의 기준 (2026-09-16)

문이 서는 서류와 안 서는 서류를 가르는 축은 **그 종이가 증명하는 것이 지금인가 그때인가**다.

| 서류 | 문 | 왜 |
|---|---|---|
| 입실료 납부 확인서 | **조건부** | 귀속월이 있어 낡음을 판정할 수 있다. 낡았고 이번 달 실입금이 있을 때만 연다 |
| 실거주 확인서 | **상시** | 지금 여기 사는가를 증명하는데 낡음을 판정할 축이 없다 |
| 보증금 영수증 | 없음 | 받았다는 **사건**의 증빙. 지난 사건을 지금 사실로 다시 만들 수 없다 |
| 계약서 | 없음 | 서명이 걸린 서류. 다시 만드는 일은 판본·폐기 개념이고 계약서 파일 칸의 소관이다 |
| 사업자등록증 | 없음 | 우리가 그린 종이가 아니라 받아 둔 원본. 등록하는 자리는 환경설정 하나뿐이다 |

- **실거주에 낡음 배지를 안 세운다.** 어제 뽑은 종이도 오늘 이사를 갔으면 거짓이고, 석 달 전
  종이도 여태 살고 있으면 참이다. 판정할 근거가 없는데 '오래됐습니다'를 적으면 없는 사실을
  지어내는 것이다. 그래서 배지 대신 **상시 보조줄 링크** 하나이고, 스키마는 한 칸도 안 늘었다.
- **닫힌 문의 부재 이유를 말하는 문단은 납부에만 있다.** 실거주는 닫히는 상태가 없다
  (`canWriteNew` 가 `false` 로 서는 갈래가 없고, 미발급이면 행에 이미 [작성]이 선다).
- **중립 그룹('그 밖의 보관본')에는 문이 없다.** 어느 계약의 종이인지 앱이 말할 수 없으면 어느
  계약으로 다시 쓸지도 말할 수 없다. 거기에 문을 두면 작성 화면이 계약 없이 열려 추론이
  엉뚱한 계약을 고른다 — 서류 지목(`lib/documentLease`)이 이미 정리한 클래스와 같은 뿌리다.
- 라벨은 `DOC_WRITE_NEW_LABEL`(lib/docBundle) 한 벌이다. 납부는 '이번 달 확인서 작성',
  실거주는 '다시 작성'. **문장이 서류마다 다르므로** 화면에 리터럴로 적으면 반드시 갈린다 —
  실거주 링크에 '이번 달'이 붙으면 그 자리에서 곧바로 거짓이 된다.

## 영업장 서류 동봉 — 사업자등록증 (2026-09-16)

시트에 **계약 축이 아닌 그룹**이 하나 생겼다. 등록증은 영업장에 걸린 종이라 계약 축으로
눕히면 방을 둘 쓰는 사람의 시트에 같은 파일이 두 번 서고, "어느 계약의 등록증이냐"는 물음
자체가 성립하지 않는다. 그룹 하나·행 하나·`leaseTermId: null` 이고 키는 `property:bizcert` 다.

- **미등록이면 그룹 자체가 없다.** 빈 행을 세우고 '작성'으로 보내지 않는다 — 우리가 만드는
  종이가 아니라 받아 둔 원본이고, 등록하는 자리는 환경설정 하나뿐이다([[property-public-facts]]).
- **자리는 계약 그룹 다음, '그 밖의 보관본' 앞.** 계약이 이 화면의 주인공이고, 중립 그룹은
  말할 수 없는 것들의 자리라 그 뒤에 두면 영업장 서류가 함께 묻힌다.
- **발급일을 안 적는다.** 종이에 찍힌 날은 발급기관의 날짜이고 우리는 그것을 읽지 않는다.
  업로드 시각을 그 자리에 넣으면 종이의 날짜인 척하는 값이 된다. 보조줄은 `{영업장명} · {형식}`
  이고 형식은 `docMimeLabel` 정본이 말한다(사진으로 올린 등록증을 PDF 라 부르지 않는다).
- **이름 자리에 영업장 이름이 선다** — `{영업장명}_사업자등록증`. 상담 도구가 이미 쓰던 이름
  그대로다(같은 파일이 두 입구에서 다른 이름으로 나가면 받는 쪽이 다른 서류로 읽는다).
  그 이름의 정본은 `DOC_TYPE_FILE_LABEL.bizcert` 이고 상담 도구도 `docFileLabel` 로 갈아탔다.
- **소유 검증 가지가 둘 다 늘었다** — `/api/doc-file` 과 `/doc/[fileId]` 에 `property.findFirst({
  id: propertyId, bizCertDriveFileId })`. 한쪽만 늘리면 파일은 내려오는데 보기 화면이 404 가
  나거나 그 반대다. 권한 면이 넓어지는 것이 아니라 **'내 영업장 내 파일' 한 칸**이 는 것이다
  (where 에 `id: propertyId` 가 함께 걸려 남의 등록증 Drive ID 로는 안 열린다).
- 사진·PDF 큐는 그대로다. 이미지로 올린 등록증은 원본 한 장이 최종 형태이고, PDF 는 사진
  모드에서 장수만큼 PNG 가 된다([[doc-file-format]]).

## 감지망

- `scripts/test-doc-bundle.ts` 81 케이스 — 문 판정(stale·납부·종류·단기·실거주 상시)과 선납
  귀속월, 영업장 서류 그룹(무회귀·한 행·미등록·형식).
- `scripts/test-tenant-phone.ts` 18 케이스 — 서류·문자가 고르는 번호(주 연락처·본국 폴백·비상 제외).
- `scripts/check-tenant-phone-axis.mjs` — 번호 고르기 손사본 금지, 아직 안 옮긴 자리는 수까지 적은 예외.
- `scripts/test-doc-history.ts` 14 케이스 — 이력 정렬(안정 정렬 포함)과 보조 문구.
- `scripts/check-doc-write-gate.mjs` — 실입금 술어 사본 금지, 문 판정이 정본에만(납부·실거주 둘 다),
  stale 이 귀속월, 보조 문구·문 라벨 상수 하나. 두 기능이 갈라지는 지점이 판정 사본이다.
- `scripts/check-doc-file-label.mjs` — 파일 이름에 서류명 리터럴 금지(`사업자등록증` 포함).
  **다만 `fileName` 이 안 적힌 조립 줄은 이 그물의 사각이다** — 상담 도구의 등록증 이름이 그
  사각에 있어 그물이 아니라 손으로 정본(`docFileLabel`)에 붙였다.

## 관련 노트

[[doc-name-style]] 서류 성명 표기. [[doc-file-format]] 저장 서류의 형식과 판본.
[[doc-vocabulary]] 서류 동사 정본. [[doc-variables-hub]] 서류 변수 허브.
