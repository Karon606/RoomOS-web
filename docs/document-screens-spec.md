# 서류 화면 정본 규칙 (운영자 오더 2026-08-04)

계약서 · 실거주 확인서 · 입실료 납부 확인서 · 보증금 영수증 네 종의 화면을 하나의 규칙으로 통일한다.
특정 서류를 지칭하지 않는 한 모든 서류에 적용된다.

운영자 원문 — "지금 이 세 종류 모두 달라. 완전 개판이야. 기능과 디자인 모두 최대한 통일은 되어야지."

착수 전이다. 이 문서는 **합의된 규칙**이고, 설계·구현은 아래 미결이 닫힌 뒤에 시작한다.

## 규칙 (운영자 지시 원문 요지)

**1. 진입점.** 입주자 관리에서 특정 입주자를 열면 가장 하단에 계약서·실거주 확인서·입실료 납부 확인서 버튼이 있다. 그 입주자에게 발급할 수 있는 모든 서류는 여기서 닿을 수 있다. '관련 서류' 메뉴를 통해서도 갈 수 있다.

**2. 돌아가기.** 좌측 상단 버튼으로 돌아간다. 목적지는 **어디서 왔느냐**로 정해진다. 입주자 관리에서 왔으면 그 입주자 상세 팝업으로, '관련 서류'의 실거주 확인서 메뉴에서 왔으면 실거주 확인서 목록으로.

**3. 편집 방식은 실거주 확인서를 기준으로.** 발급될 페이지가 그대로 보이고, 고칠 항목을 직접 눌러 텍스트를 수정한다. 납부 확인서의 "필드를 채우고 미리보기를 눌러야 결과가 보이는" 방식은 직관적이지 않다. 계약서도 미리보기 페이지에서 직접 수정할 수 있어야 하며, **수정 범위는 입주자 정보와 계약서 내용 양쪽**이다. 미리보기에서 입주자 정보를 고치면 입주자 상세정보에도 실시간 반영된다.

**4. 계약서 진입과 스캔본.** 서명 요청·서명본 발급은 입주자 상세 하단의 '계약서' 버튼으로 들어간 미리보기 상태에서 한다. 다만 스캔본이 있으므로 입주자 상세 팝업의 계약서 파일 부분은 남긴다. 스캔본이 있으면 '계약서 스캔본 보기', 없으면 '계약서 스캔본 올리기'. **여러 장이면 보기를 눌렀을 때 생성 날짜·시각과 작은 썸네일을 보여주고 무엇을 열지 고르게 한다.**

**5. 상태 배지.** 계약서의 현재 상태를 배지로 표시한다. 서명 전, 서명 완료(날짜·시각 포함), 링크 활성화 상태.

**6. 미리보기 화면 상단 액션.** 자동값으로 입력 · 저장 및 보내기 · 발급 · 인쇄.
- **저장 및 보내기**는 2단계다. 먼저 PDF 냐 JPG 냐를 묻고, 고르면 그다음 '저장'만 할지 '문자메시지 보내기'(고른 형식이 그대로 첨부)를 할지 묻는다.
- 실거주 확인서는 작성일 입력 필드가 상단에 그대로 남는다.
- 계약서는 서명 전이면 [서명 요청], 서명 후면 [재서명 요청]. 재서명 요청은 기존 서명이 지워지므로 확인 팝업을 띄우고, 요청 버튼을 눌러 문자로 링크가 나가면 실제로 기존 서명이 삭제된다. 되돌리기가 있어야 한다.
- **서명 전에는 발급 버튼이 아예 없다.** 서명 후에만 생긴다.

**인쇄.** 세 서류 모두 화면에 입력된 내용 그대로 출력된다. 계약서는 서명 전이면 서명 전 상태 그대로 나가되 '여기에 서명하세요' 같은 안내는 빠진다 — **출력해서 직접 서명받고 스캔하는 흐름**이 있기 때문이다. 화면과 출력물의 포맷이 틀어지지 않아야 한다.

**확대·축소.** 모든 서류의 미리보기 화면에서 손가락 터치로 확대·축소가 가능해야 한다.

## 운영자 확정 답변 (2026-08-04)

| # | 물음 | 답 |
|---|---|---|
| 1 | 발급된 서류를 다시 열면 | **열람 화면.** 발급한 후의 상태가 보여야 한다. 미리보기(작성)와 열람은 계속 분리 |
| 2 | 서명 후 수정 시 서명의 효력 | **서명을 삭제하고 다시 받는다.** 규칙 3의 '동의받고 수정' 경로는 폐기. 단 기존 링크가(닫혔더라도) 재활성화되어 새 링크를 다시 안 보내도 된다 |
| 3 | 인쇄 출처 | **미리보기 화면과 폰트·틀까지 완전히 동일하게 나오는 쪽.** 그게 서버 PDF 면 서버 PDF |
| 4 | 납부확인서 양식 정본 | **지금 발급본을 양식 원본으로 삼아 좌표를 새로 뜬다** |
| 5 | 입주자 서명 화면 | **지금 그대로 둔다** |
| 6 | '저장 및 보내기'가 사진 저장을 흡수하나 | **흡수한다.** 2026-08-01 결정은 없는 것으로 하고 뒤집는다 |
| 7 | 서명 되돌리기 범위 | **이미 발급한 계약서에 한해서만.** 되돌리면 PDF 에 저장된 서명이 되돌려진다 (실현 가능성 검토 필요) |
| 8 | 하단 버튼 수·대상 | 수는 중요하지 않다. 서류·상황에 따라 다르게 보이므로 더욱 그렇다. **공간이 좁으면 2줄로 넘어가도 된다.** 퇴실자에게도 띄운다 — 과거 내역을 위해 영수증·계약서를 봐야 한다. 단 실거주 확인서는 **날짜가 퇴실일까지로 표시**되고, 필요하면 수동 수정 가능 |
| 9 | 스캔본 여러 장 | 보기를 누르면 생성 날짜·시각과 작은 썸네일을 보여주고 고르게 한다 |
| 10 | 확대 안 되는 화면 | **계약서, 아이폰.** 작은 글씨를 읽기 어렵다. **예전에 같은 이유로 고쳤는데 이번 계약서 수정에서 재발했다** |

## 이 규칙이 뒤집는 기존 결정

- **2026-08-01 '계약서 출력' 하단 버튼 제거** — 중복 접점이라는 이유였다. 규칙 1·4가 되돌린다
- **2026-08-01 보내기·사진 저장 분리 유지** — "목적지가 다르면 별개 동사"였다. 답 6이 뒤집는다
- **2026-08-04 인쇄의 자리를 서류 뷰어 한 곳으로** — 규칙 6이 미리보기에도 둔다. 답 1로 두 화면이 각자 자기가 보여주는 것을 인쇄하는 것으로 정리된다

## 2차 확정 (2026-08-04)

| # | 물음 | 답 |
|---|---|---|
| A | 확대냐 읽기 모드냐 | **확대가 메인.** 글의 위치를 계속 따라가며 읽는 것도 중요하다. 읽기 모드도 가능하게는 해 둔다 |
| B | 배율까지 같게 하나 | **같게 한다.** 축소는 레이아웃을 원래 폭으로 잡은 뒤 통째로 줄이는 방식이라 줄바꿈·2단 분할·페이지 배분이 안 바뀌고 글자 크기만 작아진다(`app/api/contract/generate/route.ts:183-200` 확인). 확대가 제대로 되면 작아져도 읽히므로 A 와 충돌하지 않는다 |
| C | 납부확인서 양식 원본의 뜻 | **배경 박제가 아니다.** 선·박스·라벨 같은 고정 요소의 좌표만 상수로 뽑고 로고·영업장명·사업자정보·도장은 지금처럼 런타임에 채운다. 다른 영업장에서도 써야 한다 |
| D | 서명 이미지를 발급본마다 보관하나 | **보관한다.** 미발급 상태에서 재서명은 되돌리기가 없어도 된다 — 아직 미완이고, 서명을 받으려는 상황이면 다시 받으면 된다 |

## 확정된 원인 규명

**계약서 확대가 안 되는 원인 (재발, 신고 원문 "예전에도 고쳤는데 또 재발")**
1. `ContractView` 가 `resize` 마다 `window.innerWidth` 로 종이 배율을 다시 잡는다. iOS 는 핀치 줌 때 resize 를 쏘고 innerWidth 가 줄어든 것처럼 보고하므로, **브라우저가 키운 만큼 종이가 작아져 글자 크기가 그대로다.** 이 상쇄 코드(2026-05-10)가 핀치 허용(2026-06-26)보다 먼저 들어와 있었다 — 그 수정은 애초에 반쪽이었다
2. 그동안 실제로 쓰이던 확대 경로는 [미리보기·인쇄] 가 여는 **iOS 기본 PDF 뷰어**였고, 2026-08-04 커밋 323b2f8 이 그 버튼을 걷으면서 가림막이 사라졌다

**서명 되돌리기 구현안** — `ContractFile` 에 서명 두 칸을 더해 발급 시점 서명을 발급본과 함께 기록한다. 발급된 PDF 는 원래 안 지워지므로 "이미 발급한 계약서에 한해서만"이 조건문이 아니라 데이터로 성립한다. 새 테이블 없이 컬럼 추가다(loop.md 4번).

## 지금 있는 결함 (규칙과 별개로 고쳐야 함)

- `reopenContractShareLink` 가 `submittedAt` 을 안 본다. 제출 완료된 링크에 부르면 성공을 돌려주는데 **입주자는 여전히 '이미 제출되었습니다' 화면만 본다.** 답 2 의 재활성화는 이 함수를 고쳐 쓰는 게 아니라 새로 짜는 일에 가깝다
- **서명본 발급이 실시간 DB 로 만들어진다.** 화면은 서명 시점 내용을 보여주는데 나가는 PDF 는 현재 내용이다. 드리프트 경고 팝업이 유일한 방어이고 그마저 통과된다
- `issueContractShareLink` 의 활성 링크 재사용 조건에 `signedAt` 이 없다. 서명 후 미제출 링크가 스냅샷 갱신 없이 재발송된다
- 실거주 확인서는 인쇄하면 백지가 나온다 (`@media print` 가 화면 전체를 display:none)
- 드리프트 비교에 **성명·생년월일·연락처가 빠져 있다.** 금액만 본다 — 서명한 사람과 계약서의 사람이 달라져도 경고가 없다

## 3차 확정 (2026-08-04)

**미리보기의 인쇄** — 발급본 PDF 를 이미지로 구워서 인쇄한다. 발급본과 바이트가 같은 것이 보장된다.
지금 뷰어가 쓰는 배율(2배, 약 144dpi)은 화면용이라 종이에서 8.7pt 조항이 눌린다.
**인쇄 시점에는 배율을 3.5~4(약 252~288dpi)로 따로 잡는다.**

**미리보기 밖 경로** — 임대료 인상 예약·퇴실 정산·크론은 서명의 존재를 모른다.
거기서 값이 바뀌어도 **자동으로 서명을 지우지 않는다.** 통제 불능이 된다.
대신 드리프트가 감지되면 배지를 '서명과 내용 불일치'로 띄우고 재서명을 권한다.
그러려면 드리프트 비교에 **성명·생년월일·연락처를 반드시 추가**해야 한다(지금은 금액만 본다).

## 용어 정본 (안)

| 지금 | 정본 | 근거 |
|---|---|---|
| 자동값으로 | **자동값 복원** | 이 버튼은 손으로 고친 값을 버리고 시스템이 계산한 처음 값으로 되돌린다. '입력'이 아니라 '복원'이 실제 동작이다. §29 2~6자 안(5자). 앱의 다른 되돌리기인 '적용취소'와 구분된다 — 그쪽은 방금 한 동작 하나를 무르는 것이고 이쪽은 폼 전체를 처음 상태로 되돌린다 |
| 시스템 계약서 | **앱 발급본** | 사전의 '발급'(공식본을 만들어 보관·이력에 남김)을 그대로 쓴다. '시스템'은 사용자 어휘가 아니다 |
| 스캔 계약서 | **스캔본** | 사전이 이미 '스캔본 올리기'를 예외 라벨로 등재해 뒀다. 새 낱말을 만들지 않는다 |
| 돌아가기 | **‹ 목적지이름** (예 `‹ 입실자 정보`, `‹ 계약서`) | 규칙 2가 목적지를 다섯 갈래로 정했다. '돌아가기'는 어디로 가는지 안 알려준다. 지금 네 화면이 이미 목적지 이름을 쓰고 있고 §29 의 이동 링크 문법과도 맞는다. 대신 히트 영역을 44px 로 통일한다 |

'앱 발급본 / 스캔본'은 계약서 파일 목록의 출처 배지와 규칙 4의 버튼 라벨에 함께 쓴다.
버튼은 규칙 4 그대로 **계약서 스캔본 보기 / 계약서 스캔본 올리기**.

## 착수 단계 (되돌리기 쉬운 순서)

**0. 지금 깨진 것 복구** — 규칙과 무관하게 이미 결함이고, 이것만으로 계약서 글씨를 읽을 수 있게 된다.
계약서 확대 상쇄 봉합(resize 재계산이 핀치 줌을 되돌리지 않게) · 확대를 서류 넷으로 확대 ·
실거주 확인서 인쇄 백지 수정 · 확대 감지망 신설.
접점이 좁고 각 파일 단위로 되돌릴 수 있다.

**1. 화면 골격·액션 통일(§30)** — 운영자가 "개판"이라 한 것의 본체.
상단은 돌아가기 + 상태 배지 + 서류별 보조(실거주 작성일)만. 실행 버튼은 하단 액션바로.
액션은 자동값 복원 · 저장 및 보내기 · 인쇄 · [발급 또는 서명 요청 또는 재서명 요청].
'저장 및 보내기'는 2단계 다이얼로그로 사진 저장을 흡수한다.
인쇄는 발급본 PDF 를 구워서. 돌아가기 목적지는 `from` 열거 키로.
입주자 상세 하단에 계약서 버튼 신설.

**2. 편집 방식 통일** — 납부확인서를 좌표맵으로(고정 요소만 좌표, 브랜드는 런타임).
계약서 미리보기에서 입주자 정보·계약 내용 직접 수정 + 실시간 반영.

**3. 서명 생애주기 (loop.md 4번 승인 필요)** — `ContractFile` 에 서명 두 칸 추가.
재서명 요청 = 서명 삭제 + 링크 재활성화(reopen 을 새로 짠다) + 되돌리기.
드리프트 비교에 인적사항 추가 + 불일치 배지.
서명본 발급이 실시간 DB 로 나가던 것 봉합.
재서명 요청이 `LeaseTerm` 의 서명 네 칸을 null 로 만들면 **본문 편집 잠금은 파생값이라 자동으로 풀린다.**
잠금 해제를 위한 별도 코드가 필요 없다 — 잠금을 컬럼으로 저장하지 않은 이유가 이것이다(2026-08-04).

**4. 나머지** — 스캔본 여러 장 썸네일 선택 · 읽기 모드 · 퇴실자 실거주확인서 날짜 처리.

**5. 계약서 조판 소스 통합 (마지막, 위험 최고)** — 화면과 출력의 CSS 두 벌을 한 벌로 합쳐
배율까지 같게 만든다. 코드는 되돌려도 **잘못 발급된 계약서는 안 되돌아간다.**
앞 단계가 다 안정된 뒤에 단독으로.
