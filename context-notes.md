# 고정 지출 카드 실기 신고 2건 — 결정과 근거 (2026-08-19)

## 왜 두 곳을 같이 고쳤나
행 마크업과 핸들러 4종이 사실상 복붙이고(SettingsForm 1382~1425 대 FinanceClient 4581~4632),
서버 액션은 getRecurringExpenses·updateRecurringExpense·deleteRecurringExpense 한 벌을 공유한다.
운영자는 같은 목록을 환경설정과 지출 관리 두 경로로 본다. 한쪽만 고치면 반대편에서 같은 신고가 온다.

## [수정] 무반응의 정체
버튼 배선·disabled·z 겹침은 전부 정상이었다. 문제는 **응답이 화면 밖에서 일어난다**는 것이다.
편집 폼이 목록 위에 렌더되는데, 목록 아래쪽 행에서 열면 900px 넘는 블록이 스크롤 위치보다 위에
삽입된다. 크롬의 스크롤 앵커링이 보던 행을 못박아 화면 픽셀이 하나도 안 바뀐다.

헤드리스 실측(320px, 마지막 행에서 [수정] 클릭).
- 시공 전: scrollTop 1139 에서 2073 으로 뛰고 보던 행 top 은 300.3 그대로. 폼 top −1022 / bottom −104,
  뷰포트 상단 56. 즉 폼 전체가 화면 위쪽 밖. formVisible false.
- 시공 후: 폼 top 56 = 뷰포트 상단 56. formVisible true.

앵커링이 없는 엔진(사파리)에서는 반대로 화면이 폼 높이만큼 통째로 어긋난다. 어느 쪽이든
"수정 폼이 열렸다"로는 안 읽힌다.

## 왜 제자리 편집(1순위)이 아니라 스크롤 이동(차선)인가
UX 패널 1순위는 PaymentRecordList:141 문법대로 목록 안 그 자리에서 폼을 여는 것이었다.
채택하지 않았다. 폼이 150줄이라 map 콜백 안으로 옮기는 것은 환경설정 화면에 큰 구조 변경이고,
이번 신고가 요구하는 것은 "열린 것을 보이게 하라"이지 "편집 위치를 옮겨라"가 아니다.
대신 패널이 제시한 차선 셋을 **전부** 넣었다. 스크롤 이동 · 폼 제목에 대상 이름 · 편집 중 행 표식.
셋 중 하나만 하면 또 무반응으로 읽힌다는 것이 패널 판정이었다.

## 스크롤 문법은 신설이 아니다
FinanceClient:340 이 재고 프리셋 패널을 같은 이유로(신고 57e65e23) 이미 봉합해 둔 자리다.
lib/useFocusSection:26 도 rAF + scrollIntoView({ block:'start' }) 다. 그 문법을 그대로 옮겼다.
§25 의 "scrollIntoView 금지"는 ViewTabs 가로 넘침 한정이라 여기에 걸리지 않는다.

## [비활성] 딜레이
서버 왕복 + router.refresh 가 끝날 때까지 아무 표시가 없었다. 같은 파일의 형제 핸들러
(SettingsForm 161·179·204·223)는 전부 trackSave 로 감싸 §17 상단 진행 바를 띄우는데
고정지출 핸들러만 빠져 있었다. 지출 관리 쪽은 recMgmtPending 이 폼·묶기 버튼에서만 읽혀
목록 화면에서는 무표시였고, 결과값도 버려 실패해도 조용했다(§27.2 위반).

동반 발견. 환경설정 토글이 서버에 `!r.isActive` 를 보내면서 로컬은 `!x.isActive` 로 다시 뒤집었다.
연타하면 서버 한 번 대 화면 두 번으로 갈린다. 보낼 값을 먼저 확정해 같은 값을 쓰게 했고,
행별 busyId 잠금으로 연타 자체를 막았다.

## 품명이 눌리던 기계적 이유
제목만 truncate(overflow hidden)라 flex 최소폭이 0 으로 풀리고, 배지는 min-content 까지만 줄어
자리를 지킨다. 액션 블록은 shrink-0 이다. 그래서 폭 경쟁에서 품명이 항상 먼저 0 이 된다.

320px 실측(라이트·다크 동일값).

| 품명 | 시공 전 배정폭 / 필요폭 | 시공 후 |
|---|---|---|
| 전기요금 | 18.2 / 48px | 47.8 / 48px |
| 일반전화이용료 | 22.4 / 84px | 83.6 / 84px |
| 오성미네랄-정수기 관리 | 54.5 / 129px | 128.6 / 129px |
| 임대관리비 (묶음 3 · 자동이체) | 0 / 60px | 59.7 / 60px |
| 건물 임대료 (비활성) | 25.6 / 63px | 63 / 63px |

묶기 해제까지 4버튼인 행은 시공 전 320px 에서 행이 3px 넘쳐 삭제 버튼이 잘렸다. 시공 후 0.

## 배치 결정
- 640px 미만은 액션 줄을 아래로 내린다(§20). 형제 다수파가 `sm:` 이라 브레이크포인트를 맞췄다.
  640px 에서 제목에 남는 폭은 환경설정 370px · 모달 274px 로 최장 품명 148px 을 넉넉히 넘는다.
- 배지 줄에 flex-wrap. 이러면 배지가 다음 줄로 내려가지 제목을 깎지 않는다. 그래서 truncate 를
  걷고 break-keep 으로 바꿨다. 실데이터는 전부 한 줄에 들어가고, 더 긴 이름은 공백 단위로 접힌다.
- 행 액션은 RowActionBtn 정본. 히트영역 32px 에서 44px 로 올라가고(§09·§10) 색 토큰은 동일해
  보이는 색은 안 바뀐다. 두 줄로 접힐 때 정본이 먹는 -my-2 히트영역이 겹치지 않도록 gap-y-4(16px)
  를 줬다. gap-y-1.5 였다면 위아래 히트영역이 10px 겹쳐 엉뚱한 버튼이 눌린다.

## 손대지 않고 남긴 것 (운영자 판단 필요)
1. **배지 개수.** §11 상한은 2개인데 이 행은 최대 4개(묶음 N · 자동이체 · 비활성 · YYYY-MM부터)다.
   자동이체·활성시작을 메타로 내리면 상한을 지키지만, 정보 위치가 바뀌는 변경이라 손대지 않았다.
2. **비활성 행 opacity-50.** 라이트 1.83:1 · 다크 2.31:1 로 §28 의 4.5:1 미달이다. 다만 형제
   (활성화 예정 카드 FinanceClient:3172 opacity-50, 그 표 3187 opacity-60)가 같은 방식이라
   이 행만 고치면 형제와 어긋난다. 클래스 전수 안건으로 분리한다.
3. **행 배경 bg-[var(--canvas)].** 다크에서 --canvas 는 #000000 이라 #1A130E 카드 안에 순흑이
   들어간다. 저장소 전반의 중첩 행 관행이라 여기만 바꾸면 오히려 갈린다.
4. **납부일 표기 불일치.** 환경설정은 30일 이상을 '말일'로 접고 지출 관리 모달은 '매월 31일'로
   그대로 적는다. 같은 항목이 두 화면에서 다르게 읽힌다. 표기 규칙이라 도메인 판단 대상이다.
5. **행 radius·패딩.** 이 행만 rounded-sm px-3 py-2.5 로 §07 카드 체계(r-xl) 밖이다. 신고와
   무관한 시각 변경이라 남겼다.

## 검증 방식 메모
워크트리에 node_modules·.env.local 을 심볼릭 링크로 걸고, 실제 globals.css 를 Tailwind v4 로
컴파일해 실측 하네스를 만들었다. 폰트는 public/fonts/PretendardVariable.woff2 를 @font-face 로
직접 물려 실제 자폭으로 쟀다. 하네스는 .tmp-harness 아래에 두고 작업 후 지웠다.
