# 컨텍스트 노트 — 긴급 신고 3건 시공 (2026-08-26)

419호 계약서가 깨진 pdf 로 도착한 사고에서 출발했다. 신고는 셋인데 뿌리는 하나로 이어져 있다.
결정과 근거를 적는다.

## 조사가 신고보다 넓었다

신고는 "메일 첨부가 깨졌다"였지만, 저장 서류 바이트를 소비하는 자리를 전수로 보니 여섯 자리가
전부 "서류는 PDF다"를 전제하고 있었다. 그중 보기 화면은 **스캔본이 열리지도 않던 상태**다
(pdfjs 가 JPEG 바이트에 던진다). 운영자는 그걸 신고하지 않았는데, 스캔본을 화면에서 열어 볼
일이 없어서였을 것이다. 메일 한 곳만 고쳤으면 나머지 다섯이 그대로 남았다.

## 판정을 이름이 아니라 바이트로 한 이유

업로드 파일명은 사용자 입력이다. 확장자가 없을 수도 있고 거짓일 수도 있다. Drive API 로
mime 을 물어보는 방법도 있었지만 파일마다 왕복이 하나 늘고, 발송·보기·공유는 어차피 바이트를
손에 쥐는 자리라 스니핑이 왕복 0으로 같은 답을 낸다. ContractFile 에 mimeType 칼럼을 두는
안은 기각했다 — 실바이트와 어긋날 수 있는 사본이고, 스니핑이 있으면 필요가 없다. 그래서
C1·C2 는 스키마 변경이 0이다.

이름 추정(`guessDocMimeByName`)을 남긴 것은 메일 초안 화면 때문이다. 그 자리는 아직 바이트를
안 받았는데 첨부 목록을 그려야 한다. 추정이 틀려도 발송 순간 스니핑이 다시 판정하므로 파일은
옳게 나간다 — 화면 표기만 잠깐 어긋날 수 있고, 그 대가로 초안 조회가 파일 수만큼 빨라진다.

## 화이트리스트 밖을 octet-stream 으로 떨어뜨린 이유

/api/doc-file 은 인증을 지나지만 **파일 내용 자체는 신뢰 대상이 아니다**(업로드된 것이다).
스니핑이 `text/html` 이나 `image/svg+xml` 을 만들어 주면 그 헤더로 바이트가 브라우저에
인라인 렌더되고, 그 안의 스크립트가 우리 오리진에서 돈다. 그래서 판정 함수가 애초에 그
mime 을 만들지 못하게 했다 — 호출부가 조심하는 것이 아니라 만들 수 없게.

## 대표 판정을 안 건드린 이유

"제출용만 남아서 보낼 게 없다"의 손쉬운 해법은 파생 판본을 대표로 승격시키는 것이다.
그러면 근거가 폐기된 표시본이 대표 계약서 자리에 앉는다 — 정본 주석이 그걸 막으려고 쓰인
규칙이다. 그래서 **승격이 아니라 선택**으로 풀었다. 대표 판정은 한 글자도 안 바뀌었고,
행의 기본 선택도 여전히 대표본이라 아무것도 안 만지면 종전과 같은 종이가 나간다.

## 판본 선택을 행 확장으로 한 이유

판본마다 행을 하나씩 세우는 안은 기각했다. 행 수가 계약서 부수만큼 늘어 기본 체크·10건 상한·
그룹 머리 문법이 전부 흔들리고, 무엇보다 키의 뜻이 "계약과 종류"에서 "파일"로 바뀐다.
행은 1개로 두고 `versions` 를 얹어, 판본이 둘 이상이거나 대표가 공석일 때만 판본 줄이 선다.
1부짜리 계약(현재 거의 전건)은 픽셀이 종전과 같다.

키 접미(`#contractFileId`)를 서버가 **재조회한 행의 versions 안에서만** 되바꾸는 것이 핵심이다.
클라이언트가 Drive 파일 ID 를 못 고른다는 종전 방어가 그대로 유지된다.

## 증거와 지위를 가른 이유

목적 번복을 issuePurpose 직접 갱신으로 하면 코드베이스 전체에 박힌 "발급 시점 증거"라는
전제가 거짓이 된다(스키마 주석·발급 상세 시트·계약 실무 검토). 법무 관점에서도 "그때 무엇으로
발급했나"와 "지금 무엇으로 취급하나"는 다른 사실이고, 책임 이전 우려는 증거가 남아야 다툴 수
있다. purposeOverride 를 따로 둔 안은 원 규약이 지키려던 것을 그대로 지키면서 번복만 연다.

강등 거부(마지막 실계약을 파생으로 못 내림)는 규약이 막던 방향의 봉인이자, 발급 게이트가
이미 금지하는 상태("실계약 없이 파생만")를 화면이 만들지 못하게 하는 정합이기도 하다.
적용취소는 예외로 뒀다 — 직전 값 복원이라 새 위험을 만들지 않는다.

## 소비처 전수 이관이 이 기능의 진짜 작업량이었다

`effectiveIssuePurpose` 를 만드는 것은 열 줄이고, 그것을 **읽어야 하는 네 자리**(입주자 파일
목록·계약서함·새 버전 게이트·서류 보내기)를 전부 찾아 이관하는 것이 본체다. 한 곳이라도
issuePurpose 를 직접 읽으면 "패널은 실계약이라는데 목록은 아니라는" 상태가 돌아온다 —
정본 주석이 그 증상을 이미 기록해 둔 클래스다.

## 감지망을 만든 축

번복 기능의 유일한 실패 모드는 현재 값과 이력이 갈리는 것이다(분쟁에서 "언제 누가 무엇으로
바꿨나"를 못 답한다). check-contract-purpose-log 가 세 축으로 그걸 잡는다. 지금은 대상 0건이라
아무것도 안 걸리지만, 두 번째 쓰기 경로가 생기는 날 발화한다.

## 419 정정은 코드가 아니라 운영 조치다

C5 로 복구 경로가 생겼으니 배포 후 운영자가 스캔본을 되살리면 된다. 그 JPEG 이 실제 서명
원본이라 제출용 승격보다 증거 보존상 낫다. Drive 휴지통 30일 시한이 걸려 있어 화면이 남은
기간을 적게 했다 — 시한을 아는 것은 코드인데 사람만 행동할 수 있는 자리라서다.
