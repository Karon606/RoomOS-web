# 호실 관리 캘린더 재편 0단계 — 컨텍스트 노트

## 왜 이렇게 갈랐나

**탭 라벨만 바꾸고 URL 값은 그대로 뒀다.** `ViewTabId` 의 `'moves'`·`'cleaning'` 은 북마크와 홈
딥링크(`?tab=moves&month=`)가 물고 있다. 사람이 읽는 이름과 기계가 읽는 값은 수명이 다르다.

**`workKindLabel` 이 결함이었다.** 규칙이 "명사가 없으면 '청소'를 잇는다"인데 이름이 'work…'
이고 export 돼 있었다. 종목이 늘면 도배 사유가 그 함수를 지나 '도배 청소'가 되고, 소리로도
"404호 도배 청소 완료"가 된다. `cleaningKindLabel` 로 개명하고 청소 공급자(`fetchMoveWorks`)
안에 가뒀다. 다음 종목은 자기 규칙으로 자기 `kindLabel` 을 내고, 조립·화면은 그 값을 그대로
쓴다. 화면이 쓰는 문구 조립(`moveWorkRailLabel`)은 `lib/moveCalendar` 에 뒀다 — 컴포넌트 안에
두면 "종목이 늘어도 낱말이 안 갈린다"를 회귀가 한 글자도 못 본다.

**범례는 네 칸으로 고정한다.** 표면이 가르는 것은 **종류가 아니라 상태**다(예정·완료). 종류는
옆 글자가 진다. 그래서 종목이 열이 되어도 스와치는 안 늘어난다 — 이것이 현행 규약의 미덕이라
'청소 예정·완료'를 '작업 예정·완료'로 넓히는 것으로 족했다.

## 축 필터

**상태를 `RoomManageClient` 에 두지 않았다.** 그 컴포넌트는 `useSearchParams` 를 구독해 스크롤
중 180ms 마다 재렌더된다. 새 prop 참조가 불안정하면 `memo(MoveCalendar)` 가 깨져 트랙 600여
노드가 그 주기로 다시 그려진다. 필터는 `MoveCalendar` 내부 `useState` 다. 대가는 새로고침·탭
왕복에 값이 안 남는 것인데, 필터는 잠깐 좁혀 보는 손이지 이 화면의 정체성이 아니다.

**행 생존 판정을 lib 술어로 뽑았다**(`moveRowSurvives`). 조립(`assemble`)의 `if (bars.length
=== 0 && rowWorks.length === 0) continue` 와 화면 필터가 **같은 것을 부른다**. 화면이 규칙을
다시 적으면 행 생성 규칙이 바뀔 때 화면만 옛 규칙에 남는다.

**'작업' 축에서도 거주 막대는 지우지 않는다**(운영자 확정). 막대를 지우면 `gaps`(N일 공실)·
`overlaps`(겹침 밴드)·`tail`(다음 입주 예정)이 통째로 근거를 잃는다. 그 셋은 서버가 막대에서
계산한 값이라, 막대 없이 남기면 화면이 거짓을 그리고 지우면 화면이 다시 계산해야 한다 — 둘 다
두 번째 진실이다. 그래서 '작업' 축은 **행만 거른다**(작업 있는 행). 필터가 만든 행 객체는
남기는 쪽 참조를 그대로 쓰고 원본은 어느 축에서도 변조하지 않는다(회귀로 못 박음).

**거르는 것은 카드 안 트랙뿐이다.** 요약 줄('다가오는 14일')은 "다음에 뭐가 있나"라는 다른
질문에 답하고, 충돌 Status Row 는 사고 알림이라 필터 뒤에 숨으면 안 된다. 월 밴드 '변동 없음'
은 입퇴실 사건의 말이라 그대로다.

**빈 상태 탈출 CTA 는 무조건 세운다.** 필터가 트랙을 비우면 카드가 안 서고, 카드 머리에 사는
축 컨트롤도 함께 사라진다 — 그 버튼이 컨트롤로 돌아가는 유일한 문이다. 건수를 0 으로 셀 수
있는 창(관통 거주만 있어 `eventCount` 가 0)에서 문이 없으면 갇힌다.

**필터가 트랙을 비웠다 되살리면 스크롤러가 새 DOM 이다.** 착지 ref 는 옛 값 그대로라 착지가
다시 안 걸리고 창 첫날에 서 있게 되며, scroll 리스너도 옛 노드에 붙어 있어 URL·탭 접미·
'오늘로'가 침묵한다. 두 효과의 deps 에 `rows.length` 를 넣어 받았고, 리마운트만인 경우에는
서버 착지 달이 아니라 **마지막으로 보던 달**로 돌아간다.

## 자 교정과 sticky

`estWidth` 가 글자 크기와 무관하게 한글 11px 로 치고 있었다. 실측(Pretendard Variable 헤드리스)
한글 전각은 0.864em — 10.5px 레일 라벨에서 33% 과대였다. 자가 넉넉한 쪽으로만 틀리니 깨져
보이진 않았지만, 설 수 있던 라벨이 행 아래로 떨어졌다.

**옛 `+8` 을 남기면 교정이 무의미해진다.** 그것은 과대한 자 위에 또 얹은 눈대중이었다. 자가
맞아진 뒤에도 8px 을 더하면 한 칸이 통째로 더 필요해진다. 표준 연쇄(퇴실 청소 D · 도배 D+1 ·
장판 D+4)에서 '도배 예정'이 정확히 그 한 칸 때문에 2칸에서 3칸이 되어 양옆이 막히고, 행마다
아래 줄이 하나씩 생겨 **sticky 가 80 에서 104 로** 뛴다. 기하에 실재하는 항은 라벨 안쪽 패딩
4px 뿐이라 그것만 남겼다. 새 자는 측정 대비 여전히 8~13% 넉넉하다 — 공백을 비전각 0.564em 으로
세는 덕이라, 안전 방향으로 틀린다.

**표식과 라벨을 갈랐다.** 라벨 자리가 없어도 표식은 레일에 남는다 — '언제'가 이 레일의 존재
이유라, 표식까지 내리면 그 날짜가 트랙에서 통째로 사라진다. 말은 행 아래 줄이 지고 소리는
표식 aria 가 진다. 아래 줄은 §11 병렬 패턴대로 두 건까지 펴고 '외 N건'으로 접는다.

**행 아래 줄 폭은 `100cqw` 로 잰다.** `max-w-full` 은 트랙 폭(수천 px)이라 truncate 가 화면
바깥에서야 들었다. 스크롤러에 `containerType: inline-size` 를 세워 '보이는 창'을 자로 삼았다.
스크롤러는 이미 독립 포맷팅 컨텍스트라 컨테인먼트가 레이아웃을 더 바꾸지 않고, sticky 는
스크롤포트 기준이라 영향이 없다(실측 확인 — sticky 80 불변).

## 성능 제약(앞선 패널이 못 박은 것, 전부 지킴)

레일은 동적 팩 유지(종목 고정 배정 금지) · `pointer-events: none` 유지 · 인터랙티브 트랙 36
불변 · 작업 배열은 `data` 안 · `bars`·`events`·`firstChangeDay`·`covered` 무접촉(감지망 소스
축 4·5 통과).

## 범위 밖 발견 (수정 안 함)

1. `components/room-manage/MoveCalendar.tsx:948` — 행 좌측 호실 열이 `<div>` 다. onClick·
   role·tabIndex 가 없어 눌러도 아무 일이 없다. 오류신고 '왼쪽 호실 눌러도 이동 안 됨'의
   직접 원인으로 보인다. 같은 파일 안에서 방으로 가는 길은 요약 줄 청소 칩(`onOpenRoom`)뿐이다.
2. `components/room-manage/MoveCalendar.tsx:1092` — 막대 버튼이 `onClick` 하나다. 가로
   스크롤 중 손가락이 스치면 그대로 발화한다(터치 슬롭·이동거리 판정 없음). 오류신고
   '막대 오터치 빈발 · 스치듯 터치 무시'의 직접 원인으로 보인다. 같은 트랙에서 레일은
   `pointer-events: none` 이라 무관하다.
3. '날짜 변경' 버튼은 `components/cleaning/CleaningRowBody.tsx:336` 에 상시 렌더되고 게이트는
   `canEdit` 하나뿐이다(`RoomCleaningPanel.tsx:27`). 즉 목록·패널에서는 보인다. 신고가
   캘린더 화면에서 나온 것이라면 원인은 **캘린더에 청소 조작 진입점이 아예 없는 것**이다
   (레일 비인터랙티브, 요약 줄 칩은 방 모달로만 간다).
4. '작업' 탭(옛 청소 뷰) 본문 어휘는 전부 '청소'다('+ 청소 예정 등록'·'이번 달 청소비').
   종목 테이블이 서기 전에는 그것이 사실이라 안 건드렸다. 1단계에서 함께 넓힐 자리다.
5. '작업 일정 (25)' 접미가 작업을 하나도 안 센다 — 그 25 는 그 달의 입퇴실 건수이고 홈
   '이달 입퇴실 N건' 과 앵커돼 있다. 이름과 값이 어긋나지만 홈 타일이 함께 움직이는 값이라
   운영자 판단 항목으로 올린다.
6. `check-move-calendar-drift` 의 `WORK_IDENTS` 에 새 술어 이름(`moveRowSurvives`)은 안 든다.
   의도한 것이다 — 그 축이 지키는 것은 "작업이 거주 계산에 섞이지 않는가"이고 술어는 두
   배열의 **길이만** 본다. 다만 다음 사람이 술어에 작업 필드를 더하면 그물이 못 본다.
