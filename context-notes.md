# 컨텍스트 노트 — 사업자등록증 보내기 (운영자 오더 2026-08-18)

갈리는 선택마다 근거와 되돌리는 법을 적는다.

## 배경 사실

- 오더는 두 조각이다. ① 상담 도구에서 사업자등록증을 문자·메일 첨부로 보낼 수 있게,
  ② 그 파일을 올리는 곳은 환경설정 안에.
- 상담 도구는 2026-08-17 에 선 코너다(신고 ce05bb74). 지금까지는 전부 '탭 = 복사'였고
  모달 부제가 그렇게 약속하고 있다.
- 서류 보내기 1단계도 같은 날 들어왔다(신고 44501308). 그쪽 정본이
  lib/shareFile · lib/docShareQueue · lib/useDocShare · components/ui/SendDocButton 이다.
- 환경설정 파일 업로드 정본은 도장(stampDriveFileId)이다. 세션 발급 → Drive 직접 PUT →
  마무리. 로고 2종·계약서 스캔본이 같은 축을 쓴다.

## 패널 검토 요지 (UX · 웹디자이너 · 도메인)

### UX
- 줄의 자리가 첫 쟁점이었다. 복사 목록 안에 넣자는 안이 있었으나 **모달 부제**
  ("탭하면 클립보드에 복사됩니다")가 그 목록의 약속이라, 복사 아닌 줄을 섞으면 부제가
  거짓말이 된다. 구분선 아래 '동작 줄' 구역(단기 요금 계산이 이미 있는 곳)으로 확정.
- 그 구역 안에서 사업자등록증을 먼저 세웠다. 위 정보 블록과 같은 서류에서 나온 값이라
  거리가 가깝다. 단기 요금 계산은 성격이 다른 도구라 뒤로 간다.
- 되돌리는 법: 두 줄의 JSX 순서를 바꾸면 된다. 복사 목록 안으로 옮기려면 부제도 함께 고쳐야 한다.

### 웹디자이너
- 행 문법은 ROW_CLS 재사용(py-3 · min-h-44 · hover --cream-soft · active --sand · focus ring
  inset). 2행 스택은 단기 요금 계산 줄과 같은 타입 스케일(14/500 제목 + 12 설명).
- **후행 아이콘은 chevron 이다.** 저장소에 share/send 아이콘이 아예 없고, 보내기 계열
  진입점(SendDocButton · EntityModal 서류 보내기 · DocMultiShareBar · 서명 요청)은 전부
  아이콘 없는 텍스트다. 없는 아이콘을 새로 들이면 가이드에 없는 도상이 하나 생긴다.
  copy 아이콘은 절대 안 된다 — 바로 위 줄들이 그 아이콘으로 '복사'를 말하고 있다.
  동작 구분은 라벨이 진다(그래서 '사업자등록증 보내기').
  되돌리는 법: chevron path 를 다른 도상으로 바꾸면 되지만, 그때는 §29 등재가 먼저다.
- 환경설정 카드에서 **도장 카드를 그대로 베끼지 않은 곳이 둘**이다.
  ① 업로드 label 을 btnClass('primary','sm') 로 썼다. 도장 카드는 클래스를 손으로 적어
     --coral 이 굳어 있는데, §10 은 raw button 을 금지하고 btnClass 가 그 우회로다.
     손사본을 하나 더 만들면 토큰을 바꿀 때 그 자리만 뒤처진다.
  ② 미리보기 바탕을 --cream-soft 로 썼다. 도장·로고 카드의 --canvas 는 다크에서 #000 이라
     --cream(#1A130E) 카드 위에 검은 구멍이 뚫린다(§28 토큰 쌍).
  결과로 같은 탭에 persimmon 업로드 버튼과 coral 업로드 버튼이 나란히 선다. 이건 새 카드가
  튀는 것이 아니라 **도장 카드가 정본에서 벗어나 있는 것**이라, 기존 3종 정리를 범위 밖
  항목으로 올려 두었다(승인 대기). 지금 함께 고치지 않은 이유는 오더 밖 화면 변경이라서다.
  되돌리는 법: 새 카드의 label 을 도장 카드와 같은 손클래스로 바꾸면 시각은 즉시 맞는다.

### 도메인
- 원천은 새로 만들지 않고 Property 에 붙였다. 영업장당 한 건이고, 사업자 정보
  (businessInfo)와 같은 축이다.
- **mime 을 컬럼으로 둔 것**이 갈린 선택이다. 도장은 이미지 하나뿐이라 필요 없었다.
  대안 둘을 기각했다. ① 열 때마다 바이트를 받아 sniff — 환경설정을 열 때마다 최대 4MB 를
  서버가 읽는다. ② 매번 Drive 메타 조회 — 화면 로드마다 외부 왕복이 하나 는다.
  되돌리는 법: 컬럼을 안 읽고 /api/biz-cert 의 되짚기(%PDF 매직 + sniffImageMime)에만
  기대면 컬럼 없이도 동작한다. 화면의 이미지·PDF 분기만 못 한다.
- **mime 을 클라이언트 값으로 저장하지 않는다.** 그 값이 곧 전송 Content-Type 이 된다.
  ownedDriveFileMime 이 Drive 에 되물어 받은 값을 쓰고, 같은 호출이 소유 검증도 겸한다
  (finalizeStamp 계열이 driveFileId 를 무검증으로 받던 클래스, 2026-08-03 E페이즈).
- **공개 권한을 붙이지 않는다.** 상호·대표자·소재지가 한 장에 모인 서류다. 도장과 같은
  판정이고, 화면이 공개 URL 을 쓰지 않으므로 인증 프록시로 충분하다.
- **역할 게이트를 따로 걸지 않았다.** 등록증에 적힌 값(상호·사업자번호·대표자·소재지)은
  상담 도구의 복사 줄과 계약서 헤더가 이미 게이트 없이 보여 주는 값이다. 계좌만
  canReadScope(role,'money') 로 끊는 현행과 어긋나지 않는다.
  되돌리는 법: consultInfo 에서 bizCertMimeType 을 역할로 비우고, route 에서도 같은 판정을 건다.

## 확정한 수치와 그 이유

- **4MB 상한.** 도장·로고는 5MB 다. 이 파일만 낮춘 이유는 서버리스 함수가 바이트를 통째로
  실어 응답하는 유일한 경로이기 때문이다(그 경로의 실질 한도 4.5MB). 5MB 로 두면 경계
  부근 파일이 업로드는 되고 전송에서만 터진다 — 상담 중에 터지는 자리다.
  되돌리는 법: MAX_BIZ_CERT_BYTES 만 고치면 되지만, 그 전에 응답을 스트리밍으로 바꿔야 한다.
- **코너 열 때 프리페치.** 공유 시트는 탭 직후에만 열 수 있다. 탭하고 나서 내려받으면
  허용 시간이 지나 전송이 다운로드로 새어 나간다(신고 5c99b5c8 클래스). 서류 보내기 시트가
  체크하는 순간부터 준비를 거는 것과 같은 이유다. 대가는 코너를 열 때마다 작은 요청 하나.
  되돌리는 법: load() 안의 프리페치 세 줄을 지우면 탭 시점 fetch 로 돌아간다.
- **캐시를 열 때마다 버린다.** 환경설정에서 교체한 직후 옛 파일이 나가면 안 된다.
  값 재조회가 이미 같은 규칙이다.

## 시공 중 발견 — try/finally 안의 setState

setSending(false) 를 finally 로 묶었더니 eslint 총계가 497 → 498 이 됐는데, 새 경고의 정체가
**기존 eslint-disable 이 쓰이지 않음**이었다. 즉 react-hooks 컴파일러 규칙이 이 컴포넌트
분석을 통째로 포기해, useEffect 의 set-state-in-effect 검사까지 조용히 꺼진 것이다.

이분 탐색으로 원인을 `} finally { setSending(false) }` 한 줄로 좁혔다(그 줄만 빼면 규칙이
다시 발화). 지시를 지우면 총계는 497 로 맞지만 그 컴포넌트의 훅 검사가 영구히 꺼진 채 남는다.
catch 가 전부 삼키므로 finally 없이 마지막 줄에 두면 결과가 같고 규칙도 살아난다 — 그렇게 했다.

같은 모양(try/finally + setState)이 SettingsForm 의 업로드 핸들러들에 널려 있다. 거기는
disable 지시가 없어 아무도 모른다. 훅 검사가 꺼진 컴포넌트가 더 있는지는 확인하지 않았다
(범위 밖 관찰).

## 열어 둔 것

- 이메일 첨부는 공유 시트가 알아서 한다(메일 앱이 시트의 대상 중 하나다). 앱이 직접 메일을
  보내는 2단계(Resend)는 서류 보내기 쪽 대기 항목 그대로다.
- 기존 업로드 카드 3종 토큰 정리는 승인 대기(checklist 범위 밖 항목).
