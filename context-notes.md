# 환경설정 IA 2단계 — 결정과 근거 (2026-08-19)

## 왜 저장부터 손댔나
재편은 화면 일이지만 위험은 전부 저장에 있다. `updatePropertySettings` 는 기본정보 폼 하나를
FormData 통짜로 받아 Property 의 모든 칼럼을 한 번에 덮는 함수였다. 이 구조에서 필드를 다른
탭으로 옮기면, 그 필드가 안 실린 폼으로 저장하는 순간 해당 칼럼이 null 이 된다.

1단계에서 슬러그 하나가 이 함정에 빠졌고(영업장명만 고쳐 저장해도 소개 페이지 주소가 지워졌다)
`formData.has('publicSlug')` 가드로 막았다. 2단계는 그 함정이 열 개로 늘어나는 일이다.
그래서 순서를 뒤집었다 — 화면을 옮기기 전에 저장을 필드 단위로 쪼갰다.

규칙은 하나다. **폼이 그 필드를 실어 보냈을 때만 그 칼럼을 쓴다.**
어느 탭에서 저장하든 그 탭이 담당하지 않는 칼럼은 패치에 등장하지 않으므로 덮일 수가 없다.

## 왜 순수 함수로 뽑았나 (lib/propertySettingsPatch)
`actions.ts` 안에 두면 두 가지가 막힌다. ① 회귀 테스트가 prisma·인증을 통째로 끌고 와야 한다.
② 감지망이 볼 자리가 서버 액션 본문 안이라 "저장 해석"과 "권한·리밸리데이트"가 한 덩어리로 섞인다.
빼내고 나니 액션은 세 줄이 됐고(권한 확인 · 패치 · revalidate), 테스트는 DB 없이 38항을 돈다.

## 체크박스 hidden '0' 짝 — 필드 단위 저장의 필수 부품
꺼진 체크박스는 FormData 에 아예 안 실린다. 필드 단위 저장에서 `has` 는 "이 탭이 이 필드를
담당한다"는 뜻인데, 체크박스만은 그 부재가 "껐다"와 "소관 아님" 둘 다를 뜻하게 된다.
둘을 구분 못 하면 안전한 쪽(안 씀)으로 기울 수밖에 없고, 그러면 **체크를 푸는 저장이 영영
반영되지 않는다** — 운영자는 껐다가 새로고침하면 되살아나는 토글을 보게 된다.

hidden '0' 을 같은 이름으로 앞에 세우면 has 는 항상 참이고 값은 `getAll().includes('1')` 로 갈린다.
이 짝이 빠지는 것이 조용한 회귀라 감지망 축 ⓔ 로 못박았다.

## 감지망에서 나온 뜻밖의 발견 — 주석 제거기가 눈을 감고 있었다
`check-settings-slug-guard` 의 `strip()` 은 슬래시별표를 무조건 블록 주석 시작으로 봤다.
SettingsForm 에 `accept="image/*"` 가 있다. 거기서 가짜 주석이 열리고 다음 별표슬래시까지가
통째로 공백이 됐는데, 그 구간이 하필 **기본정보 폼 전체**(영업장명·주소·보증금·전용면적)였다.

즉 축 ⓒ(폼에 슬러그 입력이 되살아났는가)는 1단계에 심긴 이후 줄곧 무력이었다. 슬러그 입력을
그 자리에 되살려도 통과했을 것이다. 역주입을 돌리다 미발화 3건이 나와서 알았다 —
그물을 넓히지 않았으면 못 봤다. 여는 자리를 중괄호 뒤(JSX 주석)와 줄 첫머리로 좁혀 봉합했다.

교훈: 소스를 정규식으로 읽는 그물은 **역주입으로 발화를 확인하기 전까지 통과를 믿으면 안 된다.**
"위반 0건"은 위반이 없다는 뜻일 수도, 보지 못했다는 뜻일 수도 있다.

## 탭 키를 개명한 이유
`room` 이 분류 관리(결제 수단 포함), `finance` 가 요금·정책(방타입 없음)을 가리키게 되면
다음 사람이 코드를 거꾸로 읽는다. 앱 안에서 그 두 키를 가리키는 링크는 0이었고(딥링크는
`?tab=website` 둘뿐), 모르는 값은 종전대로 기본정보로 착지한다. 별칭 표를 두지 않은 것은
지킬 링크가 없는데 코드만 늘기 때문이다. 대가는 옛 북마크가 기본정보로 떨어지는 것뿐이다.

## 탭 차례를 이렇게 정한 근거
안에서 밖으로 걸어 나가는 차례다. 우리가 누구인지(기본정보) · 얼마를 받는지(요금·정책) ·
무엇으로 부르는지(분류 관리) · 무엇을 써 주는지(계약서·서류) · 손님에게 어떻게 보이는지(웹사이트) ·
누가 함께 쓰는지(멤버 관리) · 그리고 안쪽 살림 둘(데이터·도구 · 화면).
앞의 넷이 영업장 자체를 정하는 칸이고 뒤의 넷은 도구다.

## 카드 배치에서 갈린 판단
- **요금·정책**: 장기 기본값 → 단기 예외 → 매달 나가는 돈. 받는 돈에서 나가는 돈으로 읽힌다.
  고정 지출이 '요금'은 아니지만, 운영자가 돈의 기준을 손보러 오는 자리는 하나여야 한다(승인 지도).
- **분류 관리**: 방 축 넷 → 돈 축 셋 → 요청 → 품목. 앞의 여덟은 OptionSection 정본 하나라
  같은 손놀림이고, 품목 세부스펙만 지출 저장에서 저절로 쌓이는 사전이라 추가 칸이 없다.
  카드 아홉을 소제목으로 묶고 싶었지만 새 인터랙션 패턴은 승인 대상이라(loop.md 4) 평평하게 뒀다.
- **계약서·서류**: 옮겨 온 셋을 카드 하나에 담고 안에서 border-t 소제목으로 확인서 축과
  계약서 동반 축을 갈랐다. 기본정보 폼이 쓰던 그 소제목 문법 그대로다. 저장 버튼은 형제 카드
  (사업자 정보)처럼 제목 줄 오른쪽. 폼 자체가 카드다.
- **저장 피드백 통일**: 새 카드가 ContractTab 안에 있지만 저장 손잡이는 부모의 `handleSubmit` 을
  그대로 받아 쓴다. 같은 행동이 탭마다 다른 토스트를 내면 안 된다.

## 이사 안내를 상자 하나로 합친 이유
기본정보에서 나간 갈래가 넷이다(웹사이트·요금·정책·계약서·서류·분류 관리). 상자를 갈래마다
세우면 카드 아래쪽 절반이 안내가 되고, 안내가 화면의 주인이 된다. 한 상자 안에 네 줄로 모았다.
1단계 슬러그 줄도 그 상자로 흡수했다 — 어차피 같은 시기에 함께 걷는다.

## 실측에서 확인한 것
- 좁은 폭 30조합(5탭 × 320/360/390 × 라이트·다크): 신규 넘침 0. 탭 줄은 46px 한 행 유지.
  라벨이 길어져 트랙 스크롤 폭이 635 → 662 로 늘었지만 §25 가로 스크롤 안이라 문서 넘침은 0.
- 계약서 탭 320px 의 11px 넘침은 **재편 전과 동일**(1bd40ad5 로 되돌려 같은 하네스로 측정 —
  같은 4요소, 같은 right=331.2). 계약서 본문 카드의 섹션 삭제 버튼이고, 이번 diff 가 닿지 않은
  마크업이다. 원인은 섹션 제목 input 의 `min-width:auto`(플렉스 아이템 기본값)가 축소를 막는 것.
  고치면 한 토큰(`min-w-0`)이지만 "카드 내부 무수정 이동" 원칙 밖이라 손대지 않고 보고만 한다.
- 저장 무회귀는 두 겹으로 실증했다. ① 단위 회귀 38항(내가 가정한 폼) ② 실제 DOM 이 만드는
  FormData 를 그대로 정본에 먹인 실측(화면이 진짜 보내는 폼). 둘 다 탭 밖 칼럼 침범 0.
  ②에서 체크박스를 전부 풀고 다시 뽑아 해제 저장이 false 로 남는 것까지 확인했다.

## 다음 사람이 알아야 할 것
- 환경설정에 입력 칸을 새로 만들면 세 곳을 함께 고친다.
  ① `lib/propertySettingsPatch` 에 `formData.has` 가드 ② `check-settings-slug-guard` 의 GUARDED
  ③ 체크박스라면 hidden '0' 짝. ②를 빼먹으면 감지망 축 ⓓ 가 커밋을 막는다(그게 목적이다).
- 이사 안내 상자 넷은 2026-09 중순~하순에 걷는다. 코드에 TODO 로 날짜가 박혀 있다.
# 계약서 '폐기하고 다시 작성' — 결정과 근거 (신고 63cd1049, 2026-08-19)

## 사건의 정체
운영자가 한 일은 "발급본 한 부 삭제"인데, 하려던 일은 "이 버전을 폐기"였다.
앱에는 후자라는 개념이 아예 없었다. 있는 것은 둘뿐이다.
- 파일 한 부 삭제(ContractFile.deletedAt) — 잠금과 무접점
- 서명 지우기(lease 서명 4칸 + 격리본 null) — 증거 파괴

실데이터 재구성(읽기 전용, 입주자 e1fe94c6 · 501호).
```
08-19 05:56  링크 f0c0b642 발급 (스냅샷 nameStyle 'ko' = 한글 이름)
      06:01  동의서 서명 · 06:10 계약서 서명 · 06:10 제출
      06:13  발급 #20260819-014 (bodySource SNAPSHOT, 박제 성명 '지한 모하마드 이스맘 호세인')
      06:17  #014 삭제 (deletedAt)          ← 운영자가 '폐기'로 여긴 조작
      06:19  발급 #20260819-015 (성명 동일)  ← "원래대로 돌아왔어"
```
lease 는 지금도 서명 4칸이 전부 살아 있다. 그래서 잠금은 한 번도 풀린 적이 없다.

## 잠금 사슬 (파일:행)
1. `app/contract/[tenantId]/actions.ts:60` isSignatureLocked — 서명 4칸 OR
2. `:201` saveContractFieldOverride 거부 → `:63` FIELD_LOCK_MSG
   성명 표기(nameStyle)가 contractFieldOverrides 의 한 키라(`lib/contractFieldOverrides.ts:34`)
   이름 표기 전환이 이 잠금에 걸린다
3. `ContractView.tsx:324` bodyLocked → `:528` 성명 표기 select 가 cell-locked 버튼으로 → `:328` 같은 문구 토스트
4. `app/(app)/tenants/actions.ts:3717` deleteContractFile — deletedAt 만 세운다(잠금 무접점)
5. `app/(app)/contracts/actions.ts:139` files 쿼리 deletedAt: null → `lib/contractIssue.ts:32` 미발급 판정 부활
6. `ContractsClient.tsx:264` 대기 행 [발급] → signatureLive 라 `?share=` 서명본 화면
7. `app/api/contract/generate/route.ts:169` documentName(tenant, leaseFields?.nameStyle) — 오버라이드가
   없으니 기본 'ko' → 새 발급본도 한글 이름

## 왜 "지웠는데도 안 되는가"
문구는 "서명란의 X 버튼으로 저장된 서명을 지우면 다시 고칠 수 있습니다"(단수)인데
실제 조건은 네 칸 OR 이다. 이 계약은 계약서 서명과 동의서 서명이 둘 다 있어 X 하나로는 안 풀린다.
네 칸을 한 번에 지우는 문(clearContractSignature 'all')은 **드리프트 경고 다이얼로그의
'재서명 받기' 갈래에만** 있다(ContractView.tsx:758). 이 건은 발급본과 현재 값이 같아 드리프트가
0 이라 그 대화상자 자체가 안 뜬다 — 진입로가 구조적으로 닫혀 있었다.

## 채택 설계 — '폐기'를 정식 동사로 세운다
원칙: **증거는 하나도 지우지 않는다. 라이브 잠금만 푼다.**

한 트랜잭션에서
1. lease 의 서명 4칸 + 격리본 + 표시값/본문 오버라이드 사본을 `contractVersionArchive` 에 append
2. lease 의 서명 4칸·격리본을 비운다 → 라이브 렌더가 현재 값으로 풀린다
3. 이 계약의 발급본 전부에 `voidedAt` 도장 — 파일도 Drive 원본도 issuedSnapshot 도 안 건드린다
4. 살아 있는 서명 링크를 전부 닫는다(closedAt 만) → 발급 대기·종 알림이 함께 해소된다

되돌리기(사용자 원칙 rollback-always-required): 아카이브 마지막 항목을 그대로 복원한다.
그 사이 새 서명이 들어왔으면 거부한다 — 되살리면 새 서명이 덮인다.

### 왜 새 테이블이 아니라 컬럼 두 개인가
버전의 정본은 이미 ContractFile 이다(2026-08-11 운영자 승인 4건, Work_log 2980행 —
"버전 = 발급본"). 그 축을 새 테이블로 갈아엎으면 이번 신고와 무관한 화면 셋이 따라 움직인다.
필요한 것은 (a) 발급되지 않은 서명까지 담을 이력 한 칸 (b) 발급본에 찍을 폐기 도장 하나뿐이다.

### 왜 clearContractSignature 도 같은 정본을 타는가
코드 주석이 이미 그 판단을 적어 두었다 — "전량 삭제냐 부분 삭제냐는 조작의 이름일 뿐이고,
결과가 '서명 0' 이면 남은 근거도 같기 때문이다"(actions.ts:90). 결과가 서명 0 이 되는 갈래는
전부 아카이브를 지나가게 했다. 그러지 않으면 증거 파괴 경로가 하나 남는다.

### 왜 재서명이 필요한가 (계약 실무)
이름 표기가 잘못된 계약서는 정정본에 다시 서명받는 것이 정석이다. 서명은 A 에 했는데 B 를
발급하지 않는다는 2026-08-03 원칙과 같은 자리다. 폐기 확인창이 그 사실을 먼저 말한다.

## 감지망을 함께 고친 이유 (박제 무결성)
폐기·재서명을 하면 **기존 축 셋이 거짓 위반을 낸다.** 기능만 넣고 축을 두면 다음 verify:db 가
빨갛게 된다.
- check-sign-date-integrity 축 1 — 폐기된 종이의 계약일은 그때 그 서명일이 맞다 → 폐기본 제외
- check-contract-override-lock G1·G5 — 비교 대상이 '가장 최근 서명 링크'라, 폐기 후 이름을 바꾸면
  이미 무효인 링크와 대조해 위반이 뜬다 → **'지금 서명을 만든 링크'로 한정**
  (같은 규칙을 check-sign-date-integrity 축 3 이 2026-08-11 502호 재서명 때 이미 채택했다)
- 같은 규칙을 checkContractShareDrift 에도 — 폐기 후 대면 재서명한 발급에서 옛 링크와 비교해
  허위 드리프트 경고가 뜨고, 그 경고의 '재서명 받기' 를 누르면 방금 받은 서명이 폐기된다
- 새 축 G7 — 폐기 이력에 서명 증거가 하나도 없으면 위반. 폐기가 '조용한 삭제'로 퇴화하는 것을 막는다

SNAPSHOT_BASELINE 6 → 8 은 이 작업과 무관한 선행 상향이다. 오늘 운영자가 발급한 두 부(#014·#015)
때문에 실측이 8 이고, 스크립트 자신이 "8 로 올려라"를 출력하고 있었다(기준선을 안 올리면
그 사이 사라진 박제를 축 B 가 못 본다).

## 실증 결과 (읽기 전용, 함수 수준)
501호 실데이터를 그대로 읽어 폐기 → 표기 전환 → 재발급을 메모리에서만 돌렸다. DB 무변경.
```
지금       잠김 true · 본문 출처 SNAPSHOT · 성명 '지한 모하마드 이스맘 호세인'
폐기 직후  잠김 false · 본문 출처 LIVE · 옛 링크가 지금 서명 출처 false
           이력에 계약서 서명·동의서 서명·시각 2개·격리본·발급본 2·닫은 링크 1 전부 담김
표기 전환  저장될 오버라이드 { nameStyle: 'en' } · 성명 'Jihan Md Ismam Hossain'
재발급     인쇄 사실 사영에서 바뀐 축은 tenant.name 하나뿐(부수 이동 0)
되돌리기   폐기 직전과 동일 true
```

## 왜 폐기 버튼을 danger 가 아니라 warn 톤으로 두었나
라이트 테마에서 `--danger-fg` 는 `--coral` 과 같은 값(#A03C2E)이다. 그 색으로 두면 같은 줄의
solid [발급] 과 같은 계열이 되어 두 번째 주 버튼처럼 읽힌다(§22 는 한 줄에 solid 하나).
폐기는 되돌릴 수 있고 증거를 남기는 조작이라 '주의' 톤이 성격에도 맞는다. 형제 [공통 템플릿으로]
와 나란히 서는 경우는 개별 수정본이 있는 계약뿐이고, 320px 실측에서 라벨이 서로 구분된다.

## 범위 판단
- 버전 조회 화면은 최소로 둔다(발급 이력에 [폐기됨] 배지). 운영자 오더의 우선순위가
  '폐기·재작성 흐름 완성'이고, 조회 화면은 2026-08-11 3단계 백로그에 이미 있다.
- 폐기본의 [보내기]·[보기] 는 막지 않았다. 폐기본을 실수로 보내는 위험은 배지로만 경고한다 —
  버튼을 걷는 것은 §30 액션 집합 변경이라 별건 판단이 필요하다(범위 밖 관찰에 적는다).

## 게이트 결과
- tsc 0 · eslint 신규 0(손댄 파일 기준선 30 errors/15 warnings 와 동일, 새 파일 2개 0건)
- verify:fast 통과(pre-commit 훅이 커밋마다 돌렸다) · test-contract-void 31 케이스 · 역주입 2종 발화
- 계약 축 3종 통과 — 계약일 정합 발급본 25건, 본문 잠금 서명본 15건 대조(건너뛴 링크 0건),
  발급본 박제 8건(기준선 8)
- 프로덕션 빌드 성공
- **verify:db 는 체인 두 곳에서 실패하는데 둘 다 기준선에서 동일하다.** verify-recurring-estimate
  (전기요금 예약금액 — 2026-08-19 (5) 에서 KST 날짜 경계 버그로 원인 확정, 전역 수정 승인 대기)와
  check-document-override-lock(소재지 3건 — 같은 날 기록된 잔여). 체인이 끊긴 뒤의 축 22개는
  하나씩 따로 돌려 전부 통과를 확인했다.
- 새 UI 넘침 0 — 320/360/390 × 라이트/다크 6조합, 헤드리스 실측(문서 넘침 0, 각 블록 right 가
  뷰포트 안). 계약서 화면은 배경이 고정된 라이트 전용 화면이고 목록 두 곳만 다크를 탄다.
