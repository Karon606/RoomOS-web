# 컨텍스트 노트 — 오류신고 16f691e1 시공 (2026-08-24)

작업 일정 트랙(/room-manage?tab=moves)의 증상 둘. 결정과 그 근거를 적는다.
**표시·인터랙션 층만이다** — `lib/moveCalendar` 조립은 한 글자도 안 건드렸다.

## 실측 하네스를 세우고 시작했다

0단계 배포(e245aa06)가 남긴 불변 증거표를 지켜야 하는 작업이라, "안 바뀌었다"를 말로 하면
안 되는 자리였다. 실데이터를 `buildMoveRange` 로 조립해 `renderToStaticMarkup` 으로 트랙을
찍고, 빌드된 Tailwind CSS + 로컬 Pretendard 를 얹어 헤드리스 크롬에서 쟀다.
축은 320/360/390/1280 × 라이트·다크 × `pointer: fine`·`coarse` = **16조합**이다.

`pointer` 축이 새로 붙었다. 헤드리스 기본은 `fine` 이라 그대로 재면 `--mc-lane` 이 36px 로
잡혀 **터치 기기의 44px 레인을 한 번도 안 보게 된다.** CDP `Emulation.setEmulatedMedia` 로
갈아 끼웠다. 이 축이 없었으면 아래 '작업만 있는 행' 결함을 못 봤다.

하네스가 0단계 값을 **그대로 재현했다**(노드 609 · 트랙 1040 · sticky 80 · 행 19 · 막대 36 ·
표식 14 · 공실 캡션 20). 재현이 곧 하네스의 신뢰 근거다.

## 증상 ② — 가드는 막대가 아니라 **표면**에 건다

브리프는 "막대 버튼에 제스처 판정이 없다"였고, 자연스러운 처방은 막대마다 pointer 핸들러를
다는 것이다. 그렇게 안 했다. **스침이 click 을 낳는 것은 막대의 성질이 아니라 끌리는 표면의
성질이다.** 스크롤러 한 곳에 캡처로 걸면 그 위에 무엇이 서든 함께 덮이고, 실제로 이번에
새로 세운 호실 열 버튼이 **코드 0줄로** 같이 지켜졌다.

성능도 같은 방향이다. 이 파일은 스크롤 중 재렌더를 피하려고 축 필터까지 URL 밖에 둔 자리다
(`axis` useState 주석). 막대 36개에 핸들러 셋씩 배는 것은 그 방향의 정반대다. 지금은 트랙
전체에 핸들러가 통틀어 셋이고 참조도 고정이다.

삼키는 문법은 새로 만들지 않았다 — `lib/useLongPress:27` 이 이미 조상의 `onClickCapture`
에서 자식 click 을 `stopPropagation` 으로 끊는다. 같은 길이다.

### 축이 둘인 이유 (하나로는 못 막는다)

- **이동(빗변 16px)** — 브라우저가 스크롤을 가져가면 click 이 애초에 안 나므로 이 축이 혼자
  일하는 구간이 좁아 보인다. 그런데 **스크롤이 끝까지 간 트랙**에서는(이 스크롤러는
  `overscroll-behavior-x: contain` 이다) 아무리 쓸어도 스크롤이 시작되지 않아 브라우저의
  클릭 억제가 안 걸린다. 거기서는 이동 축이 유일한 방어선이다.
- **흐름(그 사이 트랙이 스크롤됐나)** — 엔진 슬롭(약 8px) 미만으로 스쳐 트랙이 한두 픽셀만
  흐른 손짓은 16px 에 안 걸리는데 이 축에는 걸린다. 관성으로 흐르는 트랙을 손가락으로 짚어
  멈추는 동작도 여기서 잡힌다(그때 이동은 0 이다).

성능 패널이 "이동 축만으로는 안드로이드에서 잡을 것이 없을 수 있다"고 낸 지적이 흐름 축을
낳았고, UX 패널이 낸 '스크롤 끝 구간'이 이동 축을 살렸다. **두 지적이 서로의 사각이었다.**

### 임계는 16px 빗변 — 디자이너 이견을 기각했다

디자이너는 10px 을 냈다(`RoomCard` 를 "§23 등재 공통 제스처"로 인용). 기각한 근거 셋.

1. **그 인용이 사실이 아니다.** 가이드를 grep 하면 10px·500ms 제스처 규격이 **없다**.
   `RoomCard.tsx:47` 주석이 "v2.0 §23 공통 제스처(500ms, 10px)"라고 적어 두었을 뿐이다.
2. **실패의 방향이 반대다.** `RoomCard`·`useLongPress`·`ErrorReportButton` 의 임계는
   **롱프레스를 취소**하는 자라, 과민해도 롱프레스만 취소되고 탭은 산다. 여기서 과민하면
   정직한 탭이 삼켜지고 사용자에게는 "눌렀는데 아무 일도 안 남"으로 보인다 —
   **이번 신고의 반대편 증상이 정확히 그것이다.**
3. **같은 일을 하는 전례가 따로 있다.** `PhotoStrip:141`·`PhotoViewer:134` 가 끌리는 표면
   위의 탭 대 끌기를 `Math.hypot(...) > 16` 으로 가른다. 16 은 새 숫자가 아니다.

축별 abs 가 아니라 빗변인 것도 같은 전례를 따랐다. 축별 16 은 축당 16 을 허용해 실제
22.6px 이동이 탭이 되는데, 이 트랙에서 모호한 손짓이 정확히 대각선이다(가로로 끌리는 트랙
위에서 시작한 세로 페이지 스크롤).

**실측 경계**(헤드리스 크롬): 가로 16px 까지 열리고 **17px 부터 억제**. 세로 15px 까지
열리고 16px 부터 억제. 대각선 11/11(빗변 15.6) 열림 · 12/12(빗변 17.0) 억제.

### 시간 임계는 없다. 대신 `detail === 0` 이 있다

운영자 지시대로 시간을 안 본다(저속 탭 오탐). 그런데 시간을 빼면 **래치를 비울 자리**가
문제가 된다. 손가락이 버튼 밖에서 떨어지면 click 이 그 버튼에 안 오고, 그러면 다음
pointerdown 까지 래치가 남는다. `ErrorReportButton` 은 이것을 60ms 타이머로 푸는데 그것이
곧 시간 축이다.

`detail === 0` 으로 풀었다. 키보드 Enter·Space 와 보조기기 활성화가 만든 click 은 클릭
횟수를 안 싣는다(UI Events 규약). 그 길에는 '끌림'이 애초에 없으므로 어느 축도 관여하면
안 된다. **화살표로 트랙을 스크롤한 뒤 Enter 를 치는 동선이 실재하므로** 이 한 줄이 없으면
키보드 사용자가 아무것도 못 연다. 실제 크롬 탐침으로 확인했다(흐름 눈금을 올린 뒤 Enter·
Space 둘 다 통과).

## 증상 ① — 목적지는 방 모달, 시드는 없다

운영자 확정대로 방 모달이다. 계약 시드를 안 싣는 이유가 둘 더 있다. ① 이 칸은 모든 레인을
가로질러(`gridRow: '1 / -1'`) 서 있어 여러 막대 중 어느 계약을 고를지에 자의적이지 않은
답이 없다. ② 계약이 하나도 없는 행(청소만 있는 공실 방)에도 이 칸은 선다.

열기 경로는 `MoveCalendar` 안이다. 위에서 prop 으로 내리면 `RoomManageClient` 가
`useSearchParams` 를 구독하므로 인라인 함수가 매 렌더 새 식별자가 되고, `memo` 가 죽어
스크롤이 URL 을 적는 180ms 마다 트랙 수백 칸이 다시 그려진다(0단계에서 확인된 함정).
같은 김에 요약 줄의 인라인 화살표도 이 `openRoom` 으로 수렴시켰다 — 같은 호출이 한 파일에
둘 있을 이유가 없다.

### `aria-label` 에 호실번호를 다시 싣는다

브리프는 "보이는 글자가 이미 말하는 것을 중복하지 말라"였는데 **기각했다.** `aria-label` 은
접근명을 더하는 게 아니라 **대체**한다. 번호를 빼면 화면에는 '508'이 보이는데 접근명은
'상세 열기'가 되어, 음성 제어 사용자가 "508 누르기"라고 부를 이름이 사라진다
(WCAG 2.5.3 Label in Name). UX 패널 판정이고 옳다.

### 작업만 있는 행 — 20px 터치 타겟

`pointer: coarse` 축을 넣은 덕에 걸린 결함이다. 거주 레인이 없는 행(청소만 있는 공실 방)은
`--mc-work`(20px) 레일만 있어 행이 20px 이고, 호실 칸이 버튼이 된 순간 **그 20px 이 곧 터치
타겟**이 된다(§09 44px). `globals.css` 의 "이 레일은 비인터랙티브라 44px 규격 적용 대상이
아니다"라는 근거가 그 행에 한해 더는 참이 아니게 됐다 — 주석도 함께 정정했다.

**히트영역 확장(`.mc-bar::after` 문법)은 여기서 못 쓴다.** 호실 칸은 위아래 이웃 칸과 맞닿아
있고 셋 다 sticky·z-20 이라, 위로 뻗으면 이웃 행의 탭을 빼앗고 아래로 뻗으면 DOM 순서가
늦은 이웃에게 먹힌다. `RowActionBtn` 의 히트·크롬 분리도 음의 마진이라 같은 벽에 부딪힌다.

처방은 손가락 기기에서만 레일 자를 나눠 합이 44px 이 되게 하는 것이다(`--mc-work-touch`).
`--mc-lane` 36/44 와 같은 문법이고, `fine` 에는 선언 자체가 안 걸려 **데스크톱 기하가 한
픽셀도 안 움직인다** — 그것이 0단계 불변 증거표를 지키는 방식이다.

실데이터에는 이런 행이 **0건**이라 합성 행 넷을 심어 확인했다(레일 1·2·3층 + 5자 호실번호).
coarse 에서 44/44/60px, 44px 미달 0. fine 에서는 20/40/60px 그대로.

## 배포 전 디자이너 패스가 죽은 규칙을 하나 찾아냈다

`.mc-row:hover .mc-room { background: var(--cream-soft) }` 가 **한 번도 적용된 적이 없다.**
호실 칸 셋이 전부 인라인 `style={{ background: 'var(--cream)' }}` 를 들고 있었고, 인라인은
특이도와 무관하게 스타일시트를 이긴다. 그 규칙 바로 위 주석이 "sticky 호실 열은 자기 배경을
들고 있어 함께 바꿔 주지 않으면 그 칸만 안 따라온다"고 막으려던 증상이 화면에서 그대로
일어나고 있었다 — 행에 마우스를 올리면 왼쪽 66px 만 크림으로 남는다.

헤드리스로 확인했다. 시공 전 hover: 행 rgb(245,237,224) / 호실 칸 rgb(251,246,239).
시공 후: 둘 다 rgb(245,237,224). rest 픽셀은 전후 동일하다.

**선재 결함이라 원래는 안 건드릴 자리다.** 고친 이유는 이번에 그 칸이 인터랙티브가 됐기
때문이다 — 행 안에서 유일하게 자기를 안 밝히는 컨트롤이 되면 그것은 이번 변경이 만든
이질감이다. 되돌리기 쉽게 별도 커밋으로 뺐다.

## 포커스 링 색 — 라이트가 안 바뀌는 것이 판정의 핵심이었다

`--coral`(#a03c2e)은 다크 카드 표면(#1A130E) 위에서 **2.78:1** 이라 WCAG 1.4.11(비텍스트
3:1) 미달이다. `--tc-text` 는 라이트에서 `var(--coral)` **그대로**라 라이트 픽셀이 한 개도
안 바뀌고, 다크에서만 #C9614C 로 밝아져 4.63:1 이 된다(§19 페어).

`.mc-bar:focus-visible` 도 함께 바꿨다. 호실 열만 바꾸면 **한 행 안에서 포커스 링 색이 둘로
갈린다** — 한쪽만 고치는 것이 안 고치는 것보다 나쁜 자리다.

오프셋은 안쪽 -3px 이다. ① 이 칸은 스크롤러 `left:0` 에 sticky 로 붙어 있어 바깥 링의 왼쪽
변이 스크롤포트 밖에 그려져 사라진다(§09 를 형식으로만 만족하게 된다). ② 왼쪽 3px 은
`.attn` 코랄 팁 자리라, -1·-2 면 충돌 행에서만 코랄 위 코랄이 된다. **값의 출처가 그 3px**
이라 임의 수치가 아니다.

## 남긴 것 (고치지 않고 기록만)

- **데스크톱 마우스에서 작업만 있는 행은 20px** 이다. §09 의 44px 은 터치 규격이라 `fine` 에는
  안 걸리지만, WCAG 2.5.8(AA 24×24)은 미달이다. 실데이터에 그런 행이 0건이라 지금 닿는
  경로가 없고, `fine` 에 최소 높이를 주면 0단계 불변 증거표가 데이터와 무관하게 움직인다.
  가이드에 '데스크톱 최소 타겟'이 없는 것이 근본이라 운영자 안건이다.
- **`check-datepicker-shell` 위반 3건**(`DepositStatusPanel:363·439·514)이 이 워크트리를
  따기 전 main(68f1638c)에 **이미 있었다.** 보증금 트랙이 인라인 미니폼을 세우며 껍데기를
  안 넘긴 자리다. stash 로 확인했고 이번 변경분과 무접점이다. 다른 트랙의 선재 회귀라
  여기서 안 고쳤다. 다만 이 때문에 `verify:fast` 가 그 지점에서 멎으므로, 새 감지망을
  그보다 **앞으로** 옮겨 배선했다(안 그러면 이번 그물이 영영 안 돈다).
- `RoomCard.tsx:47` 주석이 10px·500ms 를 "v2.0 §23 공통 제스처"라고 적는데 가이드에 그
  규격이 없다. 주석이 없는 조항을 인용하는 상태다.
