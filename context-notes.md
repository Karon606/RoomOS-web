# 퇴실 정산 세 신고 맥락 (2026-09-02)

## 왜 정본 섹션에 갈래가 없었나
RentSettlementSection 은 previewCheckoutRefund 를 'legal' 고정으로 부르고 shortStay 응답을 버린다. 위젯(CheckoutProrationWidget)만 shortStay 견적을 받아 자동 선택했다. 두 화면이 같은 서버 함수를 쓰면서 갈래 판단이 위젯 한 곳에만 있었던 게 클래스. 그래서 갈래 판단을 lib/checkoutSettlement.ts 한 벌로 뽑아 두 화면이 같이 쓴다.

## 단기 요금이 기본인 이유
8/29 운영자 확정. 1개월을 못 채운 중도 퇴실은 처음부터 단기로 계약했을 때와 같은 금액을 받는 게 원칙이고, 위젯이 이미 그렇게 동작한다. 정본 섹션도 같은 기본값을 써야 화면마다 답이 달라지지 않는다.

## '환불 안 함'을 세그먼트에 넣은 이유
운영자가 506호에서 원한 게 "환불 없이 퇴실". 금액 칸에 0을 치는 것과 달리 의사가 로그·확인창에 남는다. 라벨 네 개는 '위약금 / 면제 / 단기 / 환불 없음'으로 줄였고(아래 절), 위젯 라벨도 같이 맞춘다(두 화면 라벨이 한 글자도 다르면 안 된다는 UX/UI 오더).

## 견적이 실수납보다 크면
차액은 청구하지 않는다(설계 리드 기본). 환불액은 max(0, prepaid − base) 로 0 에서 멈추고, 캡션에 "차액 N원은 청구하지 않습니다"만 적는다. 위젯의 적용 금액(청구액)은 base 그대로 두어 종전 동작 유지.

## 확인창을 세 호출처 공용 헬퍼로 뽑은 이유
TenantClient 만 '전액 환불' 확인이 있고 나머지 둘은 없었다. 환불 0 또는 계산값과 다른 금액이면 "이용료 환불 N원 · 보증금 반환 M원으로 처리할까요?" 한 문장으로 두 금액을 묶어 묻는다(UX/UI). 한 벌이어야 다음에 또 한 화면만 빠지지 않는다.

## 위젯 환불액 입력 (신고 3)
편집 칸을 환불액으로 뒤집고 적용 금액은 읽기전용 파생(§12 자동 합산 읽기전용). 저장 형식은 그대로 checkoutProratedAmount = prepaid − 환불액. prepaid 가 0 이면 환불이 성립하지 않으니 종전 적용 금액 칸을 그대로 둔다.

## 신고 1 사유 승계
판정은 lib/checkoutReason.ts 한 벌이다. inheritableCheckoutReason(logsNewestFirst) 는 최신부터 거슬러 CHECKOUT_PENDING 으로 들어온 행의 사유를 돌려주고, 그 전에 예정 구간 밖 전이(거주 복귀 등)를 만나면 null 이다 — 연장했다가 다시 나가는 사람에게 옛 사유가 붙으면 안 된다. 시스템 라벨('퇴실 한 달 전 자동 전환' 등)은 사유가 아니라 건너뛴다. 서버(checkoutTenant)·프리즘 미니폼·입주자 수정 폼·감사 규칙 7·정적 축 ⓞ 가 이 판정을 같이 쓴다. 프리필과 같은 값이면 캡션으로 출처를 밝히고, 그 상태는 Modal dirty 로 치지 않는다.

수정 폼은 getTenants 의 statusLogs 로 판정하는데 그 조회가 toStatus 필터라 연장 복귀 행(예정에서 거주로)이 빠져 있었다. 그러면 판정이 옛 예정 행까지 내려가 잘못 잇는다. 조회 where 에 그 전이를 OR 로 더했고, 종료 사유 표시(endReasonText)는 toStatus ACTIVE 행을 빼서 종전 화면이 안 바뀐다.

## 라벨을 '단기'·'환불 없음'으로 줄인 이유
웹디자이너 실측(2026-09-02). 320px 폼 폭 224px 에 글자 확대 1.25배까지 보면 '단기 요금 / 환불 안 함'은 한 줄에 안 선다. 짧아진 뜻은 세그먼트 위 전제문(settlementPremise)과 갈래 캡션(settlementPickCaption)이 채운다. 전제문이 있어야 '환불 없음' 옆의 '면제'가 이용료 면제(전액 환불)로 거꾸로 읽히지 않는다. 캡션은 두 화면이 단기 갈래를 다른 근거로 설명하고 있어서 정본화했고, check-rent-settlement-branch 축 ⓑ 가 두 화면의 호출을 검사한다.

## 확인창을 caution 하나로 통일한 이유
전액 환불과 나머지의 등급이 갈리면 위험도가 뒤집힌다(계산값과 조금 다른 갈래가 더 세게 보이면 안 됨). 취소는 늘 무변경이라 기본 라벨. 사람이 안 만진 계산값 0 도 caution 으로 묻는 건 운영자 판단 항목으로 남겼다.

## 위젯 환불액 칸을 두 상태로 둔 이유
refundInput(선납 있을 때)과 amountInput(선납 0 이면 종전 적용 금액 칸)이 따로다. 하나로 합치면 선납 0 인 계약에서 환불 칸이 서는데 환불이 성립하지 않는다. 초과 입력은 지우지 않고 danger 테두리와 적용 버튼 비활성으로 막는다 — 값을 지우면 왜 막혔는지 안 보인다.

## 506호 데이터
앱의 '적용취소'(RentRefundUndoRow)로 운영자가 직접 되돌린다. 스크립트로 데이터를 만지지 않는다(데이터 땜빵 금지). 413호는 운영자가 수동 환불액을 원하므로 undo 대상이 아니고 금액은 미정.

## 감사 규칙을 하나로 합친 이유
계획은 둘(단기 자격인데 위약금 갈래 확정 / 위약금 갈래 환불이 단기 환불보다 큼)이었는데, 환불 스냅샷에 갈래가 안 남아 첫째는 판정할 수 없고 둘째가 첫째를 포함한다(위약금 갈래인데 환불이 단기보다 작으면 손해가 없어 잡을 이유도 없다). 그래서 규칙 6 하나로 세웠다. 갈래를 스냅샷에 남기려면 finalizeRentRefund 인자와 세 호출처를 건드려야 해서 이번 범위 밖. 운영자가 일부러 면제를 고른 건 무시(dismiss)로 닫으면 재적재 안 된다.

## 예행(dryRun) 옵션을 넣은 이유
runIntegrityAudit 는 크론에서만 돌고 결과를 오류신고로 바로 적재해서 새 규칙을 확인할 길이 없었다. dryRun 이면 위반만 돌려주고 안 쓴다. scripts/inspect-integrity-audit.ts 가 그 길이고, 규칙 6 은 실데이터에서 506호 문정현(79,800)과 413호 정은숙(112,200) 두 건을 잡았다. 413호는 신고 3 의 그 방이고 오늘 12:30 에 위약금 갈래로 확정된 것 — 운영자가 수동 환불액을 정하기로 한 건이라 적용취소 후 재확정 대상.
