# 컨텍스트 노트 — 서류 메일 재설계 (2026-08-25)

즉시 발송 사고("헉, 이거 바로 발송되는거였네")에서 출발한 재설계. 운영자 요구 여섯 가지
(주소 선택·문구 작성·템플릿·내용 확인·첨부 확인·최종 발송)에 환경설정 문안 커스터마이즈
(텍스트 + HTML)가 추가됐다. 설계는 Fable 5 패널, 시공은 산출 그대로(임의 확대 금지).

## 왜 "한 프레임, 두 모드"인가

모드 없는 HTML 편집기 하나면 HTML 모르는 운영자가 문구 한 줄 고치려다 마크업을 깨뜨린다.
문서 전체 HTML 대체는 첨부 상자의 사실성·plain text 병행·미리보기=발송 단일 코드 보증을
한꺼번에 깨뜨린다. 그래서 프레임(헤더·첨부 상자·푸터)은 고정하고 본문 영역만 갈아 끼운다.

## 프리필은 치환 완료 문장이다

확인 화면의 제목·본문 칸에는 변수가 아니라 **치환이 끝난 최종 문장**이 선다. 운영자가
보는 글자가 곧 나가는 글자여야 한다는 신고 원문("어떤 내용이 발송되는지 확인")의 직역이다.
수정본을 템플릿에 도로 얹어 렌더하므로 재치환이 일어나지만 무해하다 — 이미 값이라 바뀔
변수 표기가 없고, 운영자가 일부러 {이름} 을 타이핑하면 오히려 의도대로 채워진다.

## 클라이언트는 HTML 을 못 보낸다 (예외 하나)

발송 draft 는 제목·본문 텍스트·답장 주소뿐이다. HTML 본문은 서버가 DB 문안에서만 읽는다.
예외는 환경설정 카드의 저장 전 미리보기 — 폼의 HTML 을 renderDocMailSample 로 보내지만,
새니타이즈를 지나 sandbox iframe 으로 돌아올 뿐 메일로 나가는 길이 아니다. 컴포즈의
미리보기 재렌더는 본문 텍스트가 바뀔 때만 디바운스로 부른다(제목은 HTML 본문에 안 들어간다).

## sanitize-html 를 lib/docMail 에 두고 카드가 임포트하지 않는 이유

lib/docMail 은 서버 전용이다(머리 주석 명시). 클라이언트가 임포트하면 sanitize-html 이
번들에 딸려 온다. 그래서 카드의 placeholder 에 쓸 내장 기본 문안도 상수 임포트가 아니라
getDocMailSettings 가 내려준다(사본 금지, 정본은 lib/docMail 하나). DocMailTemplate 타입만
`import type` 으로 가져온다(빌드에서 지워진다).

## 저장 범위·감지망

replyToEmail 은 기본정보 폼의 새 필드라 propertySettingsPatch 의 "실려 온 필드만 쓴다"
그물, test-settings-save-scope(45), check-settings-slug-guard 의 GUARDED 에 다 등재했다.
docMailTemplate 은 폼 통짜 저장이 아니라 전용 액션(updateDocMailTemplate)만 쓴다 —
Json 비우기는 Prisma.DbNull(계약서 스냅샷 비우기와 같은 정본, { set: null } 은 값으로 저장됨).

## 복원(undo)이 로컬인 이유

'기본 문안으로 복원'은 폼 값 되채움이고 서버 쓰기는 저장 버튼 하나가 한다. 그래서 복원의
적용취소는 '저장 안 누르기'로 항상 성립한다(§16). 저장된 커스텀을 실제로 지우는 길은
복원 후 저장 = 전 칸 빈 값 = 서버가 칼럼 null 로 정규화.

## 발송 이력에 본문을 안 남기는 이유

문자 이력(renderedBody)과 달리 메일 본문은 길고 HTML 이라 저장 가치 대비 개인정보 부피가
크다. 분쟁("보냈다/안 받았다")의 근거는 언제·누구에게·무슨 서류·몇 건·어떤 파일명·서버
발송 id(resendId)로 충분하다. 기록 실패는 발송 결과를 뒤집지 않는다(메일은 이미 나갔다).

## 마이그레이션 두 파일로 간 이유

설계는 한 파일(migrate_doc_mail.sql)이었지만 발송 이력이 7번째 커밋(별도 승인 항목)으로
붙으면서 mail_logs 는 migrate_mail_log.sql 로 분리했다 — 커밋 단독 되돌림 단위와 맞춘다.
둘 다 additive 라 역순 DROP 으로 풀린다. 프로덕션 적용은 prisma db execute(둘 다 완료).

## 빌드 ENOTEMPTY 1회

커밋 4 게이트에서 .next/build rmdir ENOTEMPTY — iCloud 동기화가 만든 잠금이다. .next 정리
후 재빌드 0. 코드 원인 아님(iCloud 사본 `* [0-9].*` 제거 규칙과 같은 계열).

## 디자이너 패스는 직접 수행했다 (패널 중단 고지)

패스 서브에이전트가 세션 한도(22:50 리셋)로 중단됐고 운영자가 계속을 지시했다. 전례
(2026-08-24, 패널 회신 부재 시 직접 수행)대로 가이드 §10 §12 §13 §14 §16 §26 §27 §28 §29 를
직접 대조했다. 잡은 것 셋 — --cream-soft 표면 위 --warm-muted 소형 글자 2곳(다크 4.46:1,
형제 시트 TenantDocBundleSheet 에 이미 기록된 결함 클래스라 같은 클래스 전수 확인), 문안
카드 설명의 문장 끝 콜론 1곳. 확인창 z(--z-confirm 340 > 컴포즈 280), 메일 프레임의 §26
언어(먹1색·라운드 없음·흑백 안전), 320px 넘침 없음, 글자 알약 0(check-pill-text)은 통과.

