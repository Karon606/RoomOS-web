# 컨텍스트 노트 — 오류신고 ce05bb74 (상담 도구)

운영자 부재 상태에서 패널 권장안으로 확정한 건이다. 각 항목에 되돌리는 법을 함께 적는다.

## 배경 사실
- 신고 요지: 카톡·네이버톡 상담 중 웹사이트 링크·계좌번호·사업자등록번호·영업장 위치를 자주
  알려주는데 기억이 안 나고 타이핑이 귀찮다. 복붙하기 좋은 것을 모은 공간을 원한다.
  운영자가 스스로 "그 안에 단기 계산도 함께 넣고 적절한 명칭을" 을 제안했다.
- 홈의 '단기 요금 계산' 버튼은 헤더가 아니라 본문 첫 줄(`flex justify-end` + MonthSelector)에
  서 있었다. 운영자의 "여기 있는 게 좀 이상하다" 는 정확한 관찰이었다 — 월 필터 줄에 월과
  무관한 도구가 서 있었다.

## 패널 논의 요지
- **UX**: 라우트 독립 진입(전역 헤더)이 최단이라 주장. 다만 헤더 세 번째 아이콘은 360px 에서
  영업장 이름 truncate 를 악화시킨다는 비용을 스스로 지적. 명칭은 '상담 도구'(운영자가 쓴 낱말,
  §29 2~6자, 퍼널 용어 '문의' 와 충돌 없음). 행 전체 탭은 iOS 롱프레스 선택을 가로채 복사
  실패 폴백이 죽는다고 반대.
- **웹디자이너**: 그릇은 Modal sm 풀블리드. 행은 2행 스택(라벨 위·값 아래) — 1행 좌우 배치는
  320px 에서 계좌번호가 안 들어간다. 말줄임 금지(보이는 값 = 복사되는 값). hover 에 `--canvas`
  금지(다크에서 `#000000` 이라 모달 표면보다 어둡다). 홈 줄에 `flex-wrap` 필수 — 과거월
  MonthSelector 혼자 291px 로 콘텐츠 폭 288px 을 이미 넘고 있었다. 복사에 §16 적용취소는
  적용 안 됨(가짜 undo 금지).
- **도메인**: 계좌 원천은 `Property.bankAccount` 하나. `FinancialAccount` 는 지출·카드정산 원장이라
  카드 끝 4자리·개인 계좌가 섞여 있고 수납 계좌 표식이 없다. 주소는 `Property.address`
  (`businessInfo.address` 는 사업자등록증 표기라 소재지와 다를 수 있다). 공개 URL 은 정본 함수가
  없어 리터럴이 흩어져 있으니 lib 로 뽑아야 한다. 서버 액션은 `requireRouteAccess` 가 못 막으니
  계좌에 `canReadScope` 를 직접 걸어야 한다.

## 확정한 선택과 되돌리는 법

1. **자리 = 홈 월 선택 줄 + 전체 메뉴 도구 타일** (전역 헤더 기각).
   - 근거: 헤더 세 번째 아이콘은 전 라우트에서 영업장 이름 표시폭을 깎는 전역 비용인데,
     전체 메뉴 '도구' 그룹이 이미 라우트 독립 2탭 경로(하단 '전체' → 타일)를 제공한다.
   - 되돌리려면: `components/layout/Header.tsx` 우측 액션군에 44px 버튼을 더하고
     `ConsultToolsModal` 을 거기 마운트한다. 그때 320px 헤더 재측정 필수.

2. **명칭 = '상담 도구'**.
   - 근거: 코너에 복사 항목과 계산기가 함께 든다. '영업장 정보'(디자이너 안)는 계산기를 못 덮고,
     '자주 쓰는 정보' 는 7자로 §29 상한(2~6자) 초과다. '도구' 는 사이드바 그룹 라벨로 이미 쓴다.
   - 되돌리려면: `ConsultToolsModal` 의 `title`, `DashboardClient` 버튼 라벨, `Sidebar` 타일 라벨
     세 곳의 문자열만 바꾼다. 320px 예산은 한글 6자까지 여유가 있다(현 버튼 실측 85.03px).

3. **단기 요금 계산 진입을 코너 안으로 이관**. 전체 메뉴 타일은 그대로 남겼다.
   - 이것은 2026-07-06 지시("문의 전화 시 홈에서 바로")를 덮어쓰는 결정이다. 홈에서 한 탭이
     늘지만 진입 경로 자체는 2개 → 3개로 는다(홈 상담 도구 · 전체 메뉴 도구 타일 · 코너 안).
   - 되돌리려면: `DashboardClient` 월 선택 줄에 `StayQuoteModal` 버튼을 되살린다. 그때 라벨 합이
     320px 을 넘으므로 `flex-wrap` 은 유지해야 한다.

4. **계좌는 `Property.bankAccount` 원문 통째로**. 파싱하지 않는다.
   - 상위 지시서는 "계좌(FinancialAccount)" 였으나 도메인 검토로 뒤집었다. 근거는
     `knowledge/property-public-facts.md`.
   - 되돌리려면(원치 않을 값이지만): 액션에서 `financialAccounts` 를 `type: 'BANK_ACCOUNT'` 로
     걸러 내리고 행을 복수로 그린다. 어느 계좌가 수납용인지 가릴 컬럼이 먼저 필요하다.

5. **안내문 묶음은 4줄, 계좌·사업자등록번호 제외**.
   - 근거: 첫 줄이 카톡 대화 목록 미리보기가 된다(영업장명). URL 은 단독 줄이라야 자동 링크가
     붙는다. 위치를 물은 사람에게 계좌가 함께 가면 입금 요구로 읽힌다.
   - 되돌리려면: `buildIntroText` 배열에 줄을 더한다. 계좌를 넣는 안이면 '계좌 포함' 토글을
     따로 두는 편이 낫다는 UX 의견이 있었다(묶음을 둘로 나누지 말 것).

6. **행 전체 탭 = 복사** (별도 복사 버튼 기각).
   - 상위 지시서가 "탭 한 번에 복사" 로 못박았고 디자이너 안과도 맞는다. UX 가 지적한
     iOS 롱프레스 폴백 문제는 값 span 에 `select-text` 를 걸어 살렸다.
   - 되돌리려면: 값과 우측 44px `Btn` 을 분리하고 행의 `button` 을 `div` 로 낮춘다.

7. **복사에 적용취소 없음**. §16 은 "적용하는" 기능이 대상이고 클립보드는 되돌릴 수단이
   없다(가짜 undo 금지). 액션 토스트는 지속이 6000ms 라 6행을 훑으면 하단이 18초 막힌다.

## 실측에서 나온 봉합 2건
- **안내문 URL 줄 넘침**: `break-keep` 만으로는 끊을 자리 없는 URL 이 320px 에서 34px 삐져나갔다.
  `[overflow-wrap:anywhere]` 를 함께 걸어 한글 줄은 그대로 두고 URL 만 접는다.
- **홈 월 선택 줄 기존 결함**: 과거월 MonthSelector 실측 267.33px + 버튼 85.03px + gap 8 = 360.36 이
  320px 콘텐츠 폭 288px 을 넘었다. `flex-wrap` 추가로 접는다. 이번 달(181.14px)에는 안 접혀
  세로 높이 44px 그대로다 — 평소 화면의 밀도 변화 0.

## 범위 밖 (보고만)
- `getUnpaidSmsContext`·`getPersonalSmsContext` 가 역할을 안 보고 `bankAccount` 를 반환한다.
  제한 스태프가 직접 호출하면 계좌를 받는다. 같은 클래스지만 이번 신고 밖이라 손대지 않았다.
- `CardSettlementClient` 의 "계좌번호 복사됨." 마침표가 다른 세 복사 지점과 다르다(§29 정합).
- `check-contract-issued-snapshot.ts` 의 SNAPSHOT_BASELINE 이 실측(6)보다 낮다는 안내가
  verify:db 에 뜬다(위반 0, 이번 작업과 무관).
- 공개 URL 도메인 리터럴이 `next.config.ts` 리다이렉트와 환경설정 안내 문구에 아직 남아 있다.

---

## 봉투 단위 오염 — 왜 저장 자리마다 정화가 필요한가 (2026-08-10)
- **정화는 입구에 한 번이 아니라 출구마다다.** 8/4 는 OCR 파서(`cleanUnit`)와 등록 다품목 한 곳만 막았다. 단위는 폼에서 손으로도 들어오고 수정 저장으로도 들어온다 — 입구가 여러 개라 막을 곳은 DB 로 나가는 자리다. 네 자리를 같은 표현(`cleanUnit(x) ?? null`)으로 맞춰 놨으니 새 저장 경로가 생기면 형태로 대조할 수 있다.
- **detail 문자열도 저장이다.** 목록에 보이는 '20—' 은 표시 계층이 만든 게 아니라 저장된 문자열이다. 값 칸만 정화하고 문자열을 놔두면 화면에서는 고쳐진 게 없다.
- **빈 단위는 '없음' 이 아니라 '어긋남' 이다.** 재고 매칭은 `qtyUnit` 이 null 이거나 같을 때만 같은 품목으로 본다. 그래서 빈 단위 자체는 안전하지만, 카드가 '매' 를 갖고 있고 새 구매가 '개' 로 들어오면 그 구매가 통째로 잔량에서 빠진다. OCR 채움이 프리셋·이력에서 값을 가져오는 이유이고, 모르면 **비워 두는** 이유이기도 하다(추측한 '개' 가 가장 위험하다).
- **채움 순서는 프리셋 경로를 따랐다.** 라벨 일치 프리셋 → 크기 뗀 부모 프리셋 → 직전 구매 이력. 이력이 이기게 하는 안도 있었지만(그 영업장 관행이 정본), 승인된 순서를 그대로 구현했다. 프리셋 표가 봉투류·기본 식자재만 덮고 값도 실데이터와 같은 '매' 라 현재 데이터에서 두 안의 결과가 갈리지 않는다.
- **부모 단위는 전원일치일 때만.** 크기 형제(10L·20L·50L·100L)의 단위가 하나라도 다르면 부모를 등록하지 않는다. `resolveUnitHint` 가 이미 "혼재면 맨숫자가 정직하다" 를 선언했고, 표시가 아니라 **입력 기본값**을 만드는 자리라 더 보수적이어야 한다.
- **남은 상류.** OCR 프롬프트의 품목 사전이 단위 없는 품목을 `규격단위 — · 수량단위 —` 로 적는다. AI 에게 줄표를 단위 자리에 보여 주는 셈이라 "—" 를 되돌려 받는 경로일 수 있다. 승인 범위 밖이라 안 건드렸다.

## 홈·수납 금액 정합 — 왜 이렇게 갈렸고 무엇을 정본으로 삼았나 (2026-08-11)
- **행 붕괴가 뿌리였다.** 방마다 대표 계약 하나만 행으로 만들면서, 한 방에 계약이 둘일 때 나머지 하나의 청구가 화면에서 통째로 사라졌다. 게다가 대표 선택이 정렬 없는 조회 순서라 어느 쪽이 사라질지도 비결정적이었다. 방이 아니라 계약이 청구의 단위다. 418호(거주 + 비거주)가 이미 2행 선례였는데 그것이 예외로 다뤄지고 있었다.
- **캡 비대칭은 설계가 아니라 누락이었다.** 홈의 `leaseByIdForCap` 이 `activeLeases` 로만 만들어져서 퇴실 계약은 map 에 없었고, 그래서 `leaseRentMap`(= `lease.rentAmount`) fallback 으로 떨어졌다. `lib/leaseStatus` 주석이 `Math.min(paid, rent)` 을 "사용 가능"이라고 옵션처럼 적어 둔 것이 그대로 굳었다. 의도된 회계 정책이 아니라 fallback 이 정책 행세를 하던 상태다.
- **회계 패널과 한 곳에서 갈렸고, 그 이유를 남긴다.** 패널은 퇴실 항에도 락인 캡을 걸자고 했다(B안). 오늘 데이터에서 A와 B는 값이 같고, B의 취지(초과분이 조용히 수익이 되는 것을 막는다)는 옳다. 그러나 예상 축(`checkedOutRecognized`)은 무캡인데 실수납 축만 캡을 걸면 같은 이름의 항이 두 화면에서 달라진다 — 이번 오더의 제1 원칙과 정면으로 부딪힌다. 예상 축까지 캡을 거는 것은 홈 예상 수입을 움직이므로 승인 범위 밖이다. 그래서 값은 A(무캡, 두 축 동일 함수)로 가고 B의 취지는 감지망으로 옮겼다.
- **캡션은 스스로 더한다.** 서버가 계산한 홈 실수납을 캡션에 그대로 박는 안도 있었다. 그러면 홈과 원 단위로 같아지지만, 등식의 항들이 그 값에 안 맞을 때 화면에 안 맞는 등식이 뜬다. 헤드라인 수납액은 '이 목록의 진행률'로 유지하라는 운영자 결정이 있으므로, 캡션의 첫 항은 그 헤드라인이어야 한다. 동일성 보장은 감지망이 진다 — 예상 축 캡션이 2026-08-07부터 쓰던 방식 그대로다.
- **미래월은 0%도 거짓이다.** 같은 진행바 문법이 현재월에서 '다 밀렸다'를 뜻하기 때문이다. 아직 도래하지 않은 질문에는 숫자로 답하지 않는다. 선납 보조줄 값은 행에서 되계산하지 않고 `getPaidRevenue` 를 부른다 — 미래월 행은 서버가 `totalPaid` 를 0으로 잠가서 화면이 `balance` 를 수납액으로 재해석해야 하는데, 그건 화면이 자기 식을 만드는 그 패턴이다.
- **대조 스크립트는 정본을 부르지 않았다.** 홈 경로와 수납 캡션 경로를 각각 손으로 재현하고 `lib/billing` 만 공유했다. 새 정본 함수를 부르면 자기 자신과의 비교가 된다. 재현 충실도는 두 번 점검했다 — 처음 판에서 실수납 캡에 `isAfterMoveOutMonth` 가 빠져 있었고, 그대로 뒀으면 '차 0'이 거짓이 될 뻔했다.
- **남은 판단거리.** ① 422호 파트쿨리나 6월 record 의 락인이 262,500 으로 잘못 굳어 242,500 허수 미수가 서 있다(단기 사용료 전액을 5월에 인식했는데 6월에 20,000 이 또 잡히는 구조, 실제로는 청소비여야 한다). 근원 수정 + 백필 + 감지 세트가 필요한 별건이다. ② `getCheckedOutRecognizedRevenue` 와 홈 payments 조회가 `isBillingAdjust` 를 제외하지 않는다(정본 `unpaidForLease`·리포트·영수증은 전부 제외). 오늘은 전표 actualAmount 가 전부 0이라 무영향. ③ 미래월 행의 `totalPaid` 0 잠금은 '총납부액 0원 · 잔액 +N' 모순을 만든다. ④ `getCheckedOutLeasesWithRevenue` 는 이번 변경으로 소비처가 사라졌다.

## 부가수익 분류 규칙 (2026-08-11 운영자 정본) — 결정과 근거

**왜 구조로 가르고 사유 텍스트를 안 보나.** 미환불 사유(`WITHHOLD_REASONS`)는 운영자가 자유롭게
고르는 값이다. 사유가 계정을 정하면 같은 거래가 선택에 따라 다른 계정으로 간다. 청소비 몫은
`lease.cleaningFee` 로 계약에 적혀 있어 객관적으로 갈리므로 그쪽을 근거로 삼았다.
판정식은 `cleaningFeeDeductible` — 퇴실 폼이 최대 환불액을 여는 그 식이다. 서버가 다른 식을 쓰면
화면과 서버가 또 갈린다(2026-08-10 에 이미 겪은 자리).
`depositComposition` 은 쓰지 않았다. `cleaningCredit` 이 `cleaningFeeInDeposit` 이 꺼지면 항상 0이라
별도형 영업장에서 분리가 통째로 사라진다. 그 함수는 '입실 시점에 보증금이 무엇으로 채워졌나'를
재는 것이지 퇴실 공제액이 아니다.

**왜 2행인가.** 성질이 다른 두 거래(용역 대가 / 손해배상성 잡수입)를 한 라벨로 뭉치면 최소한
한쪽은 오분류이고, 세무 소명에서 "이 5만원 중 얼마가 무엇이냐"에 원장으로 답할 수 없다(회계 패널).
현재 데이터에 혼합 케이스는 0건이지만 경로는 열려 있다 — 미반환 5만에 계약 청소비 2만인 계약에서
사유를 '원상복구비'로 고르면 바로 발생한다.

**2행이 만든 새 실패 모드.** `getDepositRefundForLease` 가 `findFirst` 로 한 행만 잡아 적용취소가
반쪽이 되면 부가수익 한 행이 유령 매출로 남는다(§16 가짜 undo). 배열로 바꾸고, 감지망에
'미반환분 부가수익 합 == withheldAmount' 축을 넣어 반쪽 상태를 잡는다.

**결제수단이 판별자인 이유.** 퇴실 정산이 만든 청소비는 새로 들어온 돈이 아니라 이미 받아 둔
보증금의 성격이 바뀐 것이다. 생성부가 이미 `payMethod='보유 보증금'` 을 찍고 있었고 적용취소 조회도
그 규약을 쓰고 있어, 새 필드를 만들지 않고 그 축을 정본으로 승격했다.
Prisma `NOT`/`not` 은 SQL 3치 논리 때문에 `payMethod` 가 NULL 인 행까지 떨어뜨린다(생성 SQL 실측
확인 — `NOT "payMethod" = $1`). 수기 청소비는 결제수단이 빌 수 있고 그건 분명히 입실 수령분이라
`OR: [{ payMethod: null }, { payMethod: { not: X } }]` 로 명시했다.

**502호를 왜 안 고쳤나.** 사실관계가 507·509 와 완전히 같은 클래스인데(회계 패널도 동일 클래스로
판정) 운영자가 지목한 2건에 없었다. 데이터 수정은 지시 범위를 넘지 않는다. 대신 `verify:data` 가
매번 이름으로 보여 주므로 잊히지 않는다.

**감지망을 게이트에 다 넣지 않은 이유.** 존량 오분류를 게이트에 넣으면 502 승인 전까지 `verify:db`
체인 전체가 첫 스크립트에서 끊겨 나머지 18종이 아예 안 돈다. `check-spec-missing` 때 정한 판정 기준
그대로다 — 개발자가 지금 고칠 수 있으면 게이트, 운영자가 판단해야 하면 입력 대기.
생성 경로(앞으로 생길 건)는 소스 가드로 게이트에 남겨 규칙 자체는 퇴행하지 않게 했다.

## 정산 데이터 정정 2건 (2026-08-12 운영자 확정) — 판단과 근거

**남태우는 카테고리 한 칸만 달랐다.** 읽기 전용 대조 결과 502호 행(7c913657)은 507·509 정정본
(c35a4fba·3a9b03fc)과 `payMethod='보유 보증금'`·tenantId·leaseTermId·financialAccountId=null·
detail '<이름> 퇴실 · 청소비'·계약 보증금 50,000/청소비 20,000/미반환 20,000 까지 전부 같았다.
그래서 category 만 바꿨다. 어제 만든 dedup(`CLEANING_FEE_RECEIVED_WHERE`)이 걸려 있는 축
(청소비 잔고·either/or 판정)은 payMethod 로 판별하므로, 카테고리가 '청소비' 가 되어도 이 행은
입실 수령분으로 세지지 않는다 — 실증: 청소비 잔고 realized 는 남태우 재분류만으로는 차 0이다.

**입주자 연결이 안 되던 진짜 이유는 '못 고르는 것'이 아니라 '조용히 끊기는 것'이었다.**
선택지가 `roomStatus`(그 달 수납 행)에서 파생돼 CHECKED_OUT 이 아예 없었고, `select` 는 모르는
`defaultValue` 를 만나면 첫 항목을 고른다. 첫 항목이 '연결 안 함' 이라 **카테고리만 고쳐 저장해도
연결이 사라진다.** 즉 운영자가 본 것은 표시 문제가 아니라 데이터 손실 직전 상태였다.
지출에는 같은 클래스가 없다 — `Expense` 에는 입주자·계약 연결 칸 자체가 없고(roomId 만 있다),
앱 전체에서 `leaseTermId`·`tenantId` 를 사용자가 고르는 자리는 이 select 하나뿐이다(전수 확인).
`financialAccountId` 는 hidden 으로 원값을 실어 보내고 있어 같은 함정이 없다.

**선택지에서 CANCELLED 를 뺀 이유.** 26건 전부 수납 record 0건이고 24건은 호실도 없다(문의 단계
취소). 넣으면 목록만 길어진다. 이미 그런 계약에 묶인 수익이 생겨도 화면이 기존 연결을 별도 항목으로
세우므로 끊기지 않는다 — 두 겹으로 막았기 때문에 목록 범위를 좁게 잡을 수 있었다.

**미반환분 분류 축을 게이트로 올린 근거.** 비차단이었던 유일한 이유는 존량 3건이 운영자 확인
대기였다는 것이다(G-4 영구 실패 게이트 회피). 마지막 한 건까지 승인·정정돼 대기 입력이 사라졌으니
`check-spec-missing` 기준("개발자가 지금 고칠 수 있으면 게이트")이 그대로 적용된다. 앞으로 울리는
경우는 생성 경로 퇴행이나 손편집뿐이고 둘 다 푸시 시점에 고칠 수 있다. 체인 **마지막**에 둔 것은
여기서 끊겨도 앞의 19종이 이미 다 돈 뒤가 되게 하기 위함이다.

**422 는 락인 오염이라 카테고리 정정으로 안 끝난다.** 실물은 6월 record 하나
(exp 262,500 / act 20,000 / payDate 2026-05-11 / memo '2026년 5월분 채우고 남은 금액')였고,
운영자 산식의 500원 부가수익도 실재했다(2026-05-11 '기타 · 현금납부 차액', 연결 없음).
262,500 + 20,000 + 500 = 283,000 으로 산식과 원 단위까지 맞는다.
6월 record 는 **귀속월도 성격도** 틀렸다 — 단기는 입주월 1회 청구라 6월엔 청구가 없고, 그 20,000 은
청소비다. 그래서 락인만 내리지 않고 재분류했다: 원 record 소프트삭제 + 정본 경로
(`saveCleaningFeePayment`)가 만드는 것과 같은 모양의 청소비 부가수익(받은 날 2026-05-11).
락인을 20,000 으로 낮추는 안은 기각했다 — 허수 미납은 사라지지만 청소비가 계속 6월 이용료로 선다.

**detail 에 '[이관]' 을 안 쓴 이유.** `backfill-cleaning-fee-income.mjs --revert` 가
`category='청소비' AND detail LIKE '%[이관]%'` 로 부가수익을 지우는데, 그 스크립트의 record 복원 조건은
`isDeposit: true` 다. 422 record 는 `isDeposit: false` 라 부가수익만 지워지고 record 는 삭제된 채 남아
20,000 이 증발한다. 표식을 memo 로 옮겨 그 왕복을 피했다.

**근본 경로는 이미 두 겹으로 막혀 있었고, 세 번째 구멍이 살아 있었다.**
`billForLeaseMonth` 의 단기 입주월 단일 청구(2026-07-20)와 `savePayment` 의 `shortAbsorb`(같은 날)가
그 사건의 재발을 막는다. 그런데 `rewriteLockedExpectedForRentAmount` 는 `base` 에 `isShortTerm`·
`moveInDate` 를 안 실어서 엔진의 단기 규칙이 통째로 꺼진 채 돌고 있었다(둘 다 있을 때만 발동하는
옵셔널 필드다). 아직 단기인 계약에서 이용료가 바뀌면 **입주월 밖 달에 양수 락인을 새로 찍는다** —
코드가 422 를 다시 만드는 길이다. 형제 둘(`ForDiscountChange`·`ForRentSchedule`)은 단기를 아예
건너뛰므로 무관하고, 유일한 호출부(updateTenant 단기→월 전환)는 그 시점에 이미 `isShortTerm=false`
가 커밋된 뒤라 이번 변경으로 동작이 바뀌지 않는다(회귀 0).

## 서류 묶음 발송 1단계 — 결정과 근거 (2026-08-17, 신고 44501308)

**행을 계약 축으로 세운 이유.** 사람 단위로 열되 행은 계약이다. 한 사람이 방을 둘 쓰면 납부 확인서는
계약마다 다른 종이라, 사람 하나로 눕히는 순간 어느 계약의 종이인지 화면이 말할 수 없다. 서류 지목
(lib/documentLease, 2026-08-13)이 이미 정리한 클래스와 같은 뿌리다. 그래서 모든 행이 leaseTermId 를
명시하고, 말할 수 없는 파일은 중립 그룹으로 뺀다 — 없는 계약에 갖다 붙이는 것이 가장 나쁜 답이다.

**계약서가 딸린 계약에 없는 근거.** 합본 계약서(2026-08-13)가 추가 호실을 이미 싣는다. 규칙은
계약서 파일 칸(ContractFilesPanel.extraLeases)과 문자 그대로 같다 — `parentLeaseTermId === null` 인
계약만 제 계약서를 갖는다. 서버도 딸린 계약의 단독 발급을 막고 있어 세 자리가 한 규칙이다.

**설계보다 한 겹 넓힌 곳 둘, 그 이유.** 설계는 "보증금 있는 계약만 / 거주 계약만"이라고 했다.
그대로 두면 이미 발급된 종이가 발송 화면에서 사라진다(보증금이 0 이 된 계약, 비거주 계약에 예전
발급된 실거주 확인서). 후보는 '만들 수 있는가'의 문제이고 발급본은 '있다'는 사실이라 성격이 다르다.
그래서 **미발급 칸은 설계대로 안 세우고, 발급본이 있으면 세운다.** 실데이터에서는 둘 다 0건이라
지금 화면은 설계와 픽셀이 같다(전수 스윕 48명 위반 0).

**끝난 계약의 발급본을 중립 그룹에 담는 이유.** 발급 대상 상태(CONTRACT_ISSUE_STATUSES)만 그룹으로
여니, 퇴실한 사람의 계약서가 통째로 사라졌다. 실측 13명이 그 모양이다(대개 스캔 계약서 한 장).
'계약 표시 없음'으로 뭉뚱그리면 거짓말이라 표기를 둘로 갈랐다 — 연결이 끊긴 파일은 '계약 표시 없음',
끝난 계약의 발급본은 '지난 계약'.

**'오래된 발급본' 문구를 납부 확인서에만 붙인 이유.** 그 서류만 '그 달의 사실'을 증명한다. 계약서와
보증금 영수증, 실거주 확인서에는 오래됐다는 말 자체가 성립하지 않는다. 막지는 않는다 — 지난달
확인서를 보내야 하는 상황이 실제로 있다.

**행 규칙을 조회에서 뗀 이유(lib/docBundle).** 실데이터 대조 스크립트가 규칙을 다시 적으면 그 사본이
검증하는 것은 사본 자신이다. 규칙을 순수 함수로 두고 조회는 DB 행을 먹이기만 하게 해, 실데이터
스윕과 회귀 케이스와 화면이 **같은 함수**를 통과한다.

**준비 큐를 형식마다 나눈 이유(회귀 방지).** 큐의 캐시 키는 driveFileId 뿐이라 형식을 안 가린다.
형식이 고정인 종전 세 화면에서는 드러날 수 없던 사실인데, 이 시트가 사진/PDF 전환을 처음 들여오면서
"PDF 를 골랐는데 PNG 가 첨부되는" 길이 열렸다. 큐를 키로 나누는 대신 훅이 형식마다 큐를 하나씩 든다 —
큐의 공개 API 를 안 건드려 세 화면의 동작이 그대로다. 캐시가 형식을 안 가린다는 사실 자체를
케이스로 못 박아 다음 사람이 다시 밟지 않게 했다.

**다페이지 확장의 무회귀 계약.** 사진 변환이 1페이지 전용이라 계약서를 사진으로 다건 전송하면 뒷장이
유실됐다. 큐가 항목당 Blob 배열을 들도록 넓히되 **한 장짜리에는 장 번호를 안 붙인다** — 영수증·확인서
다건과 계약서 PDF 다건의 파일명이 종전 그대로여야 한다. 이 축을 테스트 첫 절에 둔 이유다.

**하단 알약의 320px 한계(실측).** §22 SelectionPillBar 는 카운트·문구·버튼·닫기가 전부 nowrap 이라
문구가 길면 알약이 화면 밖으로 나간다. 실측 — '용량이 커서 나눠 보내주세요' 320px 71px·360px 31px
이탈, '변환 중 10/10' 도 320px 5px 이탈. **닫기 버튼이 화면 밖에 놓여 선택 모드를 나갈 수 없다**(기능
결함). 문구만 줄어들 수 있게 하니 세 폭 모두 맞는다. 360·390 은 글자가 다 들어가 픽셀 무변동이다.
그래서 장수 초과 문구는 알약에 '사진 N장'만 두고, 처방('몇 건을 빼거나 PDF 로')은 폭이 있는 시트
본문에 한 줄로 뒀다 — 설계의 "바가 개수 안내"를 그대로 따르는 배치이기도 하다.

**층(z) 문법.** 시트는 프리즘 셸의 **패널 안에서** 그린다. 그러면 셸이 만든 층 안에서 그 위에 올라간다 —
발급 상세 시트(IssuedContractSheet)가 이미 쓰는 문법이고, 토큰 밖 z 값을 새로 만들 필요가 없다.
알약도 시트 본문 안에서 그려 같은 이유로 시트 패널 위에 선다(aboveModal).
