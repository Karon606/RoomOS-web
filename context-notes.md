# 입퇴실 캘린더 청소 1단계 — 결정과 근거 (2026-08-20)

승인된 범위는 "청소 예정·완료를 캘린더에서 보이게 한다, 스키마는 1칸만"이다. 아래는 그 안에서
갈린 결정들과, 승인문의 글자와 다르게 간 자리의 근거다.

## 패널 회신 현황

설계 패널 4인 전원 회신(호실 운영 도메인 · 정보구조 UX · 웹디자이너 · 프런트엔드 성능).
회신은 각자 최종 출력으로 받았다(SendMessage 미사용 — 전달 실패 전례를 피하려 처음부터 그렇게
지시). 성능 패널 회신은 메인을 거쳐 들어왔다.

**배포 전 디자이너 패스는 서브에이전트가 끝내 회신하지 않았다.** 기다린 뒤 그 관점을 직접
채웠다 — 아래 '자체 디자이너 패스' 절이 그것이고, 남의 판정으로 적지 않는다.

## 채택한 것

### 작업은 `bars`·`events`·`firstChangeDay` 어디에도 안 들어간다 (승인문 2번)

이 트랙의 세 배열은 서로 다른 질문에 답한다. `bars` 는 공백(gaps)과 충돌의 씨앗이고,
`events` 는 홈 '이달 입퇴실 N건'과 호실 관리 탭 접미 N 이 **함께** 딛는 한 벌이며,
`firstChangeDay` 는 월 창의 행 정렬 키다. 청소를 어디 하나에 섞으면 세 곳이 동시에 거짓이
된다. 최악은 충돌 쪽이다 — 거주와 청소가 겹쳤다는 빨간 밴드가 서고, 운영자가 [겹침 확인]을
누르는 순간 그 거짓이 `LeaseOverlapAck` 행으로 굳는다. 되돌릴 수는 있지만 그때는 이미
"이 방에 두 사람이 있었다"는 기록이 생긴 뒤다.

끼운 자리를 `packLanes` **직후 · `covered` 루프 직전**으로 잡은 것은 성능 패널 권고다.
충돌 루프가 이미 끝나 판정 대상(holding)이 안 변하고, 작업 날짜가 공백 계산에 닿을 길이
코드 순서상 애초에 없다.

### 행 생성 규칙에 '공실 집계 제외'를 더했다 (승인문 5번의 보강)

승인문은 "공실 작업은 행을 만들고 거주 중 작업은 안 만든다" 둘뿐이었다. 실데이터를 훑다가
601호가 걸렸다 — 옥탑 창고(`nonResidentVacant=false`)이고, 8/13 청소가 있는데 그날은
NON_RESIDENT 구간(8/14~)이 시작되기 **전날**이라 캘린더 눈으로는 '공실'이다. 규칙을 글자
그대로 적용하면 창고에 행이 하나 서고, 그 행은 "이 방이 곧 입주 가능하다"는 거짓 신호가 된다.

`lib/vacancy` 의 `isVacancyExcluded` 가 정확히 그 오탐을 막으려고 있는 정본이라(주석에
"415호·사무실·옥탑 601~604 오탐 방지"가 적혀 있다) 같은 술어를 조회가 부르고 조립이 읽는다.
도메인 패널도 독립적으로 같은 지적을 했다. 특정 호실번호를 코드에 적지 않는다(멀티테넌트).

### 지연은 예정일 **다음 날**부터 (도메인 패널)

예정일 당일은 아직 지연이 아니다. 아침에 여는 화면이 오늘 할 일을 이미 늦었다고 말하면
그 표시가 아무것도 뜻하지 않게 된다. 실데이터에 마침 한 건이 있다 — 422호 8/19 예정,
오늘 8/20 이라 '지남'.

### 예정 담당자는 **다른 칸** (승인문 6번)

완료 시점의 `performer` 와 한 칸을 쓰면 안 된다. `reopenCleaning` 이 "수행자 구분은 완료
시점의 사실"이라는 규칙으로 그 칸을 내리도록 이미 설계돼 있어서, 완료를 되돌리는 순간
'누가 하기로 했는가'라는 계획까지 함께 사라진다.

입력은 **선택**이고 기본값이 없다(도메인 패널 동의). 완료 폼은 '업체'를 기본으로 두지만
그것은 일어난 일을 적는 자리다. 계획 단계에서 기본값을 박으면 아직 안 정한 것이 '업체로
정함'으로 저장되고, 그 거짓이 그대로 캘린더 요약 줄에 선다. `null` 이 곧 '아직 안 정함'이라
기존 21행은 백필이 0 이다.

이름 칸(`performerName`)은 계획 단계에 안 둔다. 정하는 것은 누가 하느냐지 어느 업체냐가
아니고, 업체 이름은 완료할 때 실제로 맡긴 곳을 적는다.

### 조회는 형제로, 방 목록은 조회가 넘긴다

`fetchMoveWorks` 를 `lib/moveCalendarData` 안에 둔 이유는 `fetchMoveLeases` 와 같다 —
감지망이 화면과 같은 조회를 지나야 한다. 조립에는 `acks` 와 같은 문법으로 인자를 넘겨
순수 함수 규약을 유지했다.

`fetchMoveLeases` 에 `workRoomIds` 를 더한 것은 판정 때문이다. 창 안에 입퇴실이 하나도
없는 방(관통 거주)은 `changed` 에 안 걸려 그 방의 계약이 조립에 **아예 안 닿는다**. 그러면
사람이 사는 방에 '공실 작업' 행이 하나 선다. 행 목록은 안 늘리고 점유 계약만 함께 읽는다.

### `works` 는 `data` 안에만 (성능 패널)

`RoomManageClient` 가 `useSearchParams` 를 구독하고 트랙 스크롤이 180ms 마다
`replaceState(null, …)` 로 라우터를 동기화하므로 부모가 그때마다 리렌더된다. 청소 배열을
두 번째 prop 이나 인라인 콜백으로 넘겼다면 `memo(MoveCalendar)` 가 매번 깨져 트랙
900여 노드가 180ms 마다 다시 그려진다. 전부 서버가 만든 `data` 안에 실었다.

## 승인문의 글자와 다르게 간 자리

### 1. 레인이 아니라 **얇은 레일**(20px) — 승인문 1번의 "별도 레인"

승인문의 근거는 "하루짜리 작업 하나가 그 방 행 높이를 통째로 한 단 늘려 행 높이가 데이터에
따라 들쭉날쭉해진다"였다. 레인 풀 분리는 그 근거를 반만 푼다 — 풀을 갈라도 층 하나가
36px(터치 44px)이면 청소가 있는 행마다 그만큼 붙는다. 실측으로 트랙 세로가 **720 → 1188px
(+65%)** 이었다.

레일(20px)이면 **720 → 980px(+36%)** 이다. 디자이너 1차 회신은 "레인 높이를 낮추지 마라"
였는데, 그 근거 셋(§09 터치 44px · §05 최소 글자 · 레인 높이 관행)은 전부 **글자를 이고
있고 손이 닿는** 막대를 전제한다. 이 레일은 `pointer-events: none` 이고 표식 자체에는 글자가
없다(라벨은 그 옆 트랙 칸에 선다). 세 근거 중 적용되는 것이 없다. 20px 은 §05 최소
글자(10.5px · 행간 1.4 ≈ 15px)를 담는 가장 작은 4px 배수다.

### 2. 라벨은 띠 **옆**에 세운다 — 승인문 4번의 "라벨을 못 세우는 자리는 트랙에 안 그린다"

승인문의 글자를 최대로 좁게 읽으면(라벨이 띠 **안에** 안 들어가면 트랙에서 내린다) 모든
청소가 하루짜리이므로 트랙 레일이 **영구히 빈다.** 그러면 승인문 1번의 "별도 레인에 그린다"가
성립하지 않는다. UX 패널이 같은 모순을 독립적으로 지적했다.

그래서 이렇게 갈랐다. 라벨을 **트랙 어디에도** 못 세우는 자리(양옆이 다 막힌 자리)만 트랙에서
내리고 행 아래 한 줄로 떨군다. 띠 옆에 세우는 것은 이 파일이 이미 쓰는 문법이다(막대 라벨의
`place()` 가 `side: 'right' | 'left'` 로 같은 일을 한다). 이 규칙이면 §03 판정에도 안 걸린다 —
표면 옆에 항상 글자가 있으므로 "표면 하나가 상태를 혼자 말하는 자리"가 아니다.

실데이터에서는 창 안 14건 전부가 트랙에 섰고 행 아래로 떨어진 것이 0건이다. 떨어지는 경로는
살아 있지만 오늘 데이터로는 안 밟힌다.

### 3. 트랙 라벨은 `청소 예정`, 사유는 다른 자리에 — 승인문 4번의 "종류는 글자로"

승인문의 뜻은 "색으로 종류를 가르지 마라"(viz 팔레트 안이 실측 기각된 맥락)다. 그것은 지켰다 —
색이 가르는 것은 상태뿐이고 종류는 어디서도 색이 아니다.

다만 트랙 라벨에 사유까지 넣으면 `퇴실 청소 예정`(4칸) · `공사·도배 후 청소 완료`(7칸)이 되어
대부분의 청소가 자리를 못 잡고 트랙 밖으로 떨어진다. '언제'를 보여주려던 레일이 그러면 텅 빈다.
사유는 행 아래 줄·요약 줄·aria 문장이 **온전히** 말하므로 종류가 글자로 사라지지는 않는다.

### 4. 지연은 표면이 아니라 **글자**가 진다 — 승인문 4번의 "지연만 --tc 계열"

`--tc` 계열은 지켰다(`--overdue-fg` 는 라이트에서 `#A03C2E` 로 `--tc` 와 같은 값이다).
`--tc-text` 를 안 쓴 것은 실측 때문이다 — 다크에서 `--inspect-bg` 위 3.51:1 로 AA 미달이다.

표면으로 안 가른 이유는 §03 이다. 표면 셋이 상태 셋을 혼자 말하기 시작하면 밴드 티어 판정에
걸리고, 작업용 밴드는 v2.0 미등재라 그 순간 승인 대상이 된다. §18 이 이미 "표면이 중립일 때
글자가 상태를 진다"를 쓰고 있어 새 문법도 아니다.

### 4-1. 가이드가 이 자리를 이미 갈라 놓았다 (§03 본문 인용)

§03 의 밴드 표면 5종 문단은 판정 기준을 이렇게 적는다.

> 판정 기준은 하나다 — 표면 **하나가** 상태를 혼자 말하는 자리인가. 배지·행 틴트는 옆에 선
> fg 글자가 뜻을 지므로 이 티어를 쓰지 않는다.

작업 띠는 **옆에 선 글자가 뜻을 지는 자리**다(그것이 안 되는 자리는 트랙에서 내린다). 그래서
밴드 티어가 아니라 §04 틴트 토큰(`--inspect-bg`·`--neutral-bg`)을 그 등재된 역할 그대로 쓴다.
**미등재 밴드를 신설한 것이 아니다.**

지연 글자도 같은 문단이 이미 답을 준다.

> §18 "연체 카드: 호실번호·금액 --tc" 는 **표면이 중립일 때 글자가 상태를 지는 문법**이고,
> 밴드 티어는 표면이 이미 짙은 테라코타라 같은 붉음을 글자에 또 얹으면 … AA 를 못 넘는다.

작업 라벨이 앉는 자리는 밴드 위가 아니라 트랙 바탕(중립 `--cream`)이다. 그래서 글자가 상태를
지는 쪽이 맞고, 실측도 그것을 뒷받침한다(라이트 6.14:1 · 다크 7.05:1).

### 5. 완료 표면에 1px 링을 더했다

디자이너 실측이 결정적이었다 — `--neutral-bg` 를 트랙 바탕(`--cream`) 위에 합성하면 ΔE76 이
**3.50** 이라 띠가 있는지조차 안 보인다. 링(`--neutral-ring`)은 ΔE76 10.10 으로 보인다.
새 색이 아니라 같은 티어의 `-ring` 짝이라 등재 어휘 안이다. 예정 쪽도 대칭으로 붙였다.

승인문의 "예정과 완료는 표면 농도로 가른다"는 유지된다(라이트 ΔE76 3.52 · 다크 6.44). 다만
그 농도차만으로는 라이트에서 약하므로 **상태는 옆 글자가 확정한다**.

### 6. 요약 줄 청소 칩에 배지를 안 썼다

`StatusBadge` 톤 일곱(paid·await·unpaid·overdue·exit·movein·info)이 전부 입퇴실·돈의
어휘다. 청소 상태를 얹으려면 미등재 톤을 새로 만들어야 하고 그것은 승인 대상이다.
'overdue'(연체) 톤을 빌려 쓰는 안도 봤지만 그 낱말은 돈의 것이라 기각했다.

## 채택 안 한 것

- **UX: 트랙 표식을 아예 비인터랙티브 + 진입을 방 모달로** — 절반만 채택했다. 비인터랙티브는
  채택(성능 패널도 같은 권고, Android Blink 터치 래치 봉합 자리라 표면을 더 안 내준다).
  다만 행 아래 줄도 비인터랙티브로 뒀다 — 44px 버튼으로 만들면 그 줄이 22px 이 아니라 44px 이
  되어 세로 이득이 사라진다. 진입은 요약 줄 칩(44px)과 방 모달 두 곳이다.
- **UX: '변동 없음' 월 밴드 문구를 좁히자** — 안 건드렸다. 그 탭의 이름이 '입퇴실'이라 문맥이
  이미 잡혀 있고, 라벨을 늘리면 배포 전 디자이너 패스가 방금 고정한 탭 폭 산술에 다시 손대야
  한다. 범위 밖 발견으로 적어 둔다.
- **디자이너 1차: 레인 높이를 낮추지 마라** — 위 '다르게 간 자리 1' 참조.
- **도메인: 행 생성 판정을 사유(reason)로 하자** — 승인문이 점유 여부로 못박았고, 실데이터
  에서 두 판정의 결과가 같다(사유로 갈라야 할 세 방이 이미 전부 행을 갖고 있다). 다만 도메인의
  진짜 걱정("거주 중 청소라고 **분류**하면 표준 운영이 오분류된다")은 aria 문장에서 받았다 —
  분류하지 않고 "그날 이 방에 사람이 있습니다"라는 관찰만 덧붙인다.

## 실측이 잡은 결함 (시공 중 자체 수정)

헤드리스 계측에서 aria 문장을 훑다가 걸렸다. 창 안 청소 14건 중 **5건**이 "404호 공사·도배 후
완료"로 읽혔다 — 무엇이 완료됐는지가 문장에서 통째로 빠진다. 원인은 어휘의 출처다.
`CLEANING_REASON_LABEL` 은 청소 목록용이라 그 화면의 열이 이미 '청소'를 말하고 있어서
'공사·도배 후'·'입실 중 요청'·'기타'처럼 명사가 없는 낱말이 섞여 있다. 캘린더에는 그 열이 없다.

사유 어휘를 다시 적지 않고(사본이 곧 두 번째 진실) 정본 라벨을 그대로 쓰되 '청소'가 없을 때만
한 낱말을 잇는 규칙 하나를 뒀다.

## 미적용 마이그레이션 — 머지 전 선행 조건

`prisma/migrate_cleaning_planned_performer.sql` 은 **아직 DB 에 안 갔다**(승인문: 적용은 메인 몫).
적용 전에는 그 칸을 읽거나 쓰는 경로가 `column room_cleanings.plannedPerformer does not exist`
로 실패한다 — `fetchMoveWorks`(캘린더 입퇴실 탭), `loadCleaningRows`(호실 관리 청소 뷰 ·
방 상세 청소 위젯), `createCleaning`(청소 예정 등록) 넷이다. 퇴실 자동 생성
(`ensureCheckoutCleaning`)과 청소비 잔고는 이 칸을 안 봐서 영향이 없다.
**머지·배포보다 DDL 적용이 먼저다.**

감지망(`check-move-calendar-drift`)은 이 칸을 안 읽는다(작업 조회를 안 부른다). 그래서
`verify:db` 는 지금도 기지 예외에서만 멈춘다.

## 자체 디자이너 패스 (서브에이전트 회신 없음, 직접 수행)

**차단 1건 — 지연 낱말이 앱 정본과 갈렸다.** '지남'은 이 화면이 새로 지어낸 말이었다. 같은
사실을 부르는 말이 이미 있다 — `dashboard/alerts.ts` 의 "퇴실 예정일 경과 N일 — 퇴실 처리
필요"이고, §18 도 진행형 알림의 어휘를 "경과 N일 · 처리 필요"로 등재해 두었다. '예정일 경과'로
바꿨다(c6e1177f). '경과' 한 낱말로 줄이지 않은 이유는 뜻이 갈리기 때문이다 — 그 말만으로는
'진행 경과'로도 읽힌다. 라벨이 세 칸에서 다섯 칸으로 길어졌지만 실측 영향 0.

**§03 판정** — 위 '4-1' 절 참조. 밴드 티어가 아니라 §04 틴트를 등재된 역할대로 쓴 것이라
미등재 밴드 신설이 아니다.

**§29(AI 티)** — 이번 diff 의 사용자 노출 문자열에 em dash·화살표·이모지·느낌표 0건.
새 문자열은 '담당 (선택)' · '미정' · '청소' · '담당 미정' · '청소 예정/예정일 경과/완료' ·
'예정된 입퇴실은 없습니다.' 뿐이다.

**형제 문법 대조** — `CleaningPlanForm` 의 담당 세그먼트는 **같은 폼 바로 위 '사유' 줄과 같은
문법**(raw 버튼 알약, 선택 시 코랄 채움)이다. 공용 `SegmentedControl` 로 바꾸면 한 폼 안에서
두 문법이 되므로 형제를 따랐다. 320px 산술로 한 줄에 든다(52+52+52+59+gap 18 = 233px < 폼 폭).

**열어 둔 판단 하나** — 트랙 작업 라벨의 글자색을 `--ink-2` 로 뒀다(승인문 표기 그대로). 다만
이 라벨은 띠 **위**가 아니라 트랙 바탕에 앉으므로, 형제인 막대 밖 라벨(`--ink-s`)과 견주면
한 단계 진하다. 승인문이 `--ink-2` 를 명시했고 대비도 라이트 13.34:1 로 여유가 커서 그대로
뒀지만, 운영자 눈에 무겁게 읽히면 `--ink-s` 로 한 단계 내리는 것이 형제 문법에 더 맞다.

