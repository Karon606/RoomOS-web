# 퇴실 자동 청소 예정일 입력 — 결정과 근거 (2026-08-20)

운영자 승인은 "실제 청소 예정일을 입력 받는 쪽으로" 한 줄이다. 아래는 그 안에서 갈린 결정과,
승인문의 글자와 다르게 간 자리의 근거다.

## 패널 회신 현황

설계 패널 4인 **전원 회신**(고시원 운영 도메인 · 정보구조 UX · 웹디자이너 · 백엔드 데이터 정합).
회신은 각자 최종 출력으로 받았다(SendMessage 미사용 — 전달 실패 전례를 피하려 처음부터 그렇게
지시). 배포 전 웹디자이너 패스는 별도로 한 번 더 받았다.

## 채택한 것

### 기본값은 '퇴실 다음 날', 이미 지났으면 오늘 (승인문의 '퇴실일'에서 하루 옮김)

오더는 "방이 비는 날(퇴실일)이 합리적 출발점"이었다. 도메인 패널이 그것을 반대했고 근거가
실측으로 확인돼 받아들였다.

**퇴실일 그대로면 이번 신고가 안 고쳐진다.** 퇴실 처리는 실제로 나간 날 또는 그 뒤에 누른다.
그러면 `max(퇴실일, 오늘)` 은 가장 흔한 경우(당일 처리)에 **오늘**이 되고, 지연 판정이
`w.date < today`(lib/moveCalendar:780)라 다음 날 아침 '예정일 경과'가 뜬다. 422호가 겪은 그
모양 그대로다. 실데이터로도 확인된다 — 자동 생성분 두 건(413호·422호)이 전부 퇴실일과 저장일이
같거나 하루 차이라, 종전 규칙과 `max(퇴실일, 오늘)` 의 답이 **똑같다**.

**틀리는 방향의 값이 비대칭이다.** 기본값이 하루 늦으면 그날 치우고 완료로 적으면 그만이다
(캘린더는 완료 건을 완료일에 그린다). 하루 이르면 퇴실마다 거짓 지연이 서고, 지연 표시가 매번
거짓이면 운영자는 그 색을 안 보게 된다. 그때 **진짜 늦은 청소도 함께 안 보인다.** 지표 하나를
통째로 못 쓰게 만드는 손해라 한쪽으로만 틀리게 뒀다.

**오늘로 당기는 갈래**(퇴실일이 과거)는 오더가 정해 달라고 남긴 자리다. 퇴실 다음 날을 그대로
쓰면 예정이 태어나자마자 '경과'가 된다. 아무도 약속한 적 없는 날을 어겼다고 말하는 셈이다.

미해결로 남긴 것 하나. **업체가 보통 며칠 뒤에 오는가**는 앱이 모른다. 통상 이틀이면 +2 가
맞다(지연이 예정일 다음 날부터라 +1 이면 퇴실 이틀 뒤 아침에 빨개진다). 운영자 확인 대상이다.
주말 건너뛰기는 넣지 않았다 — 고시원 청소는 요일에 안 묶이고, 규칙을 넣으면 설명 못 할
기본값이 가끔 튀어나온다.

### 비우면 '날짜 없는 예정'을 만든다 (아예 안 만들지 않는다)

청소가 필요하다는 것은 계획이 아니라 **사실**이다. 사람이 나갔으면 그 방은 더럽다. 날짜를 아직
모르는 것과 청소가 필요 없는 것은 다른 말인데, 안 만드는 쪽은 그 둘을 한 결과로 뭉갠다. 그러면
그 방이 청소가 필요하다는 사실이 앱 어디에도 안 남고 운영자 기억뿐이라, **안 치운 방에 사람을
들이는** 사고로 간다.

유령이 쌓일 위험은 이미 봉해져 있다(코드 확인). 날짜 없는 건도 호실 카드 '청소 필요' 배지와
'청소' 탭 접미 N 과 '청소 필요 N실' 에 그대로 세어지고(RoomManageClient), 목록 정렬은 `?? '9999'`
로 뒤로 밀 뿐 안 지운다. 같은 방에 열린 퇴실 청소가 있으면 둘째를 안 만든다. 안 보이는 곳은
**입퇴실 캘린더 트랙 하나**뿐이고 그것은 설계다(`fetchMoveWorks` 가 날짜 없는 건을 거른다).

정말 청소가 필요 없는 경우의 답은 이 빈칸이 아니라 이미 있는 **'안 하기로'(SKIPPED)** 다.
빈 날짜는 "아직 안 정함", '안 함'은 "안 한다". 이 둘이 갈려 있어야 목록이 읽힌다.

### 되돌리기는 손으로 고친 날짜도 걷는다 (지금 동작 유지)

걷는 이유는 '자동 생성분이라서'가 아니라 **전제가 철회돼서**다. 그 청소는 계약이 끝났기 때문에
존재한다. 퇴실을 되돌리면 그 방에는 사람이 살고, 그러면 그 CHECKOUT 청소는 누가 날짜를 쳤든
거짓이다. 판정 기준은 저작자가 아니라 전제여야 한다.

'고쳤으면 남긴다'는 절충안은 도메인·백엔드 패널이 같이 반대했다. `updatedAt` 을 보면 담당자나
메모만 고쳐도 움직여서, 규칙이 "날짜를 고쳤는가"가 아니라 "아무거나 건드렸는가"가 된다. 같은
버튼이 보이지 않는 이력에 따라 다르게 동작하는 쪽이 훨씬 나쁘다.

`clearAutoCheckoutCleaning` 의 where 조건 넷은 한 글자도 안 건드렸고, 소스 가드 축 3 이 그것을
지킨다. 예정일을 넣는 조건으로 삼지 않은 것도 의도다 — 넣는 순간 날짜를 바꾼 건이 안 걷혀서
거주 중인 방에 '청소 필요'가 남는다.

### 클래스 봉합은 '필수 인자'로

`ensureCheckoutCleaning` 의 넷째 인자를 선택으로 두지 않았다. 새 퇴실 경로가 이 함수를 부르면
컴파일러가 날짜를 어디서 가져올지 반드시 적게 만든다. 값을 안 정했다는 뜻은 `cleaningYmd:
undefined` 로 손수 적어야 하고, 그건 잊은 것이 아니라 고른 것이다.

`checkoutTenant` 쪽은 인자를 객체로 바꾸지 **않았다**. 그 함수는 `checkoutWithDepositRefund` 를
경유하고 거기서 보증금 환불 기록 날짜가 `params.moveOutDate` 를 딛는다. 인자 순서를 통째로
리팩터링하면 필요 이상으로 결제 경로에 닿는다(백엔드 패널 지적). 청소 인자만 뒤에 얹었다.

`applyStatusTransition` 의 `cleaningDate` 도 선택이다. 그 함수는 전이 8종을 전부 처리하고
호출부가 한 코드로 모든 전이를 넘긴다. 퇴실 전용 값을 전 전이의 필수로 만들면 예약 확정·투어
전환까지 더미를 채우게 되고 그 순간 규칙이 죽는다.

### 조용하던 세 갈래에 말을 붙였다

`ensureCheckoutCleaning` 은 종전에 세 경우에 아무것도 안 하고 조용히 끝났다. 방이 없을 때,
이미 열린 퇴실 청소가 있을 때, 예외가 났을 때(catch 가 통째로 삼켰다).

지금까지는 그래도 됐다. 자동 생성은 뒤에서 도는 부수효과였고 운영자는 그것이 생겼는지
약속받은 적이 없다. **화면에 '청소 예정일' 칸을 세우는 순간 그건 약속이 된다.** 고른 날짜가
어디에도 안 남는데 아무 말도 안 나가면, 이 저장소가 이미 결함으로 판정한 그 클래스다
(TenantStatusTransitions 주석 "화면과 서버가 다른 말을 했다").

퇴실을 막지 않는 원칙은 그대로다. `ok: false` 로는 절대 안 내려가고 `notice` 는 순수 부가
정보다. 상세 경로는 이미 있던 `res.notice` 토스트를 타고, 홈 경로는 그 파일이 이미 쓰는
`pushToast` 정본을 쓴다(새 문법이 아니다).

### 열린 예정의 날짜는 안 덮는다 (패널 2인과 갈린 자리)

UX·웹디자이너 패널은 **열린 예정이 있으면 그 행의 날짜를 갱신하라**고 했다. 방은 하나, 퇴실
청소도 하나, 지금 운영자가 말하는 답도 하나라는 논리다. 받아들이지 않았다.

그 행은 운영자가 `rescheduleCleaning` 으로 손수 정한 날일 수 있다(업체를 그날로 잡아 뒀을 수
있다). 퇴실 저장이 그것을 말없이 덮으면 **무를 길이 없는 덮어쓰기**가 된다. 사용자 원칙이
"적용하는 모든 기능엔 적용취소가 항상 있어야 한다"이고, 오더도 "기존 데이터 정정은 하지 마라"
였다. 무엇보다 지금 문제가 '앱이 혼자 날짜를 정한다'인데 그 답으로 날짜를 하나 더 혼자 정할
수는 없다. 백엔드 패널이 같은 결론이었다.

대신 알린다. 갱신을 원하면 운영자 승인 항목이다.

### 세그먼트를 안 세웠다 (UX 패널과 갈린 자리)

UX 패널은 `SegmentedControl` 로 `[날짜 지정 | 미정]` 을 세우라고 했고 §12("상호배타는 체크박스
금지, 세그먼트")를 인용했다. 배포 전 관점의 웹디자이너 패널이 반대했고 그쪽을 택했다.

- 정본 `SegmentedControl` 은 실제로 **30px**(`px-2.5 py-1 text-xs` + 트랙 패딩)이고 활성 표시에
  `--tc` 보더가 없다. §12 가 묘사한 세그먼트(36px, 선택 --cream + 보더 --tc)와 이미 어긋난다.
  한 벌 더 쓰면 그 부채를 복제한다.
- `role="tablist"` / `role="tab"` 이라 폼 필드에 안 맞고(§25 는 필터·뷰 전환용으로 못박는다),
  화살표 키 핸들러가 없어 tablist 가 약속하는 동작도 안 지킨다. 30px 은 §09 44px 에도 미달이다.
- 이 모달은 소프트 키보드가 열리면 본문 가시 창이 약 280px 다(Modal 주석의 실측 547 에서 계산).
  컨트롤 한 줄이 그 창의 13% 고, 같은 창 때문에 이미 신고가 셋 있었다.
- 홈 모달의 환불 선택은 **raw button 그리드**라, 세그먼트를 새로 들이면 그 모달 안에 상호배타
  컨트롤 두 개가 서로 다른 생김새로 나란히 선다. 세그먼트를 빼면 갈릴 것 자체가 없다.

비우는 길은 이미 달력 안에 있다(값이 있을 때 뜨는 '초기화'). 형제 정본 `CleaningPlanForm` 의
예정일도 라벨 + DatePicker 한 줄이고, 이제 청소 예정을 만드는 자리 넷이 같은 한 줄을 쓴다.

### `emptyLabel` 은 프롭을 더했지 동작을 안 바꿨다

빈 값은 두 가지 뜻을 가진다. '아직 안 골랐다'와 '없음으로 골랐다'다. `DatePicker` 는 종전에 둘을
구분하지 않고 placeholder 를 `opacity: .45` 로 그렸는데, **실측 대비가 라이트 2.48 · 다크 3.83**
이다(§28 본문 4.5:1 미달). 지금까지 문제가 안 된 것은 이 모달의 날짜 칸이 늘 값이 차 있어
placeholder 가 뜰 일이 없었기 때문이다. '미정'은 설계상 비우는 상태라 그 색으로 그리면 이 칸의
가장 중요한 상태가 앱에서 가장 안 읽히는 글자가 된다.

`emptyLabel` 을 안 주면 종전과 **완전히 같은 경로**라 기존 호출부는 한 곳도 안 바뀐다.
clearable(지우기 X 버튼) 안은 반대로 트리거 레이아웃을 모든 호출부에서 바꾸므로 배제했다.

### 따라 움직이는 규칙은 효과 없이

기본값이 퇴실일에서 파생되는데 퇴실일은 같은 폼에서 바뀐다. `picked === null` 이 곧 '아직 안
건드림'이라 값이 그때그때 퇴실일에서 나온다. 한 번 건드린 뒤에는 안 따라간다 — 형제 판단
("이미 적어 둔 이름은 덮지 않는다", CleaningRowBody)과 같은 결이다. 비운 것도 건드린 것이라
미정으로 두겠다는 뜻이 퇴실일 변경에 지워지지 않는다.

`useEffect` 로 동기화하지 않은 것은 퇴실일이 바뀔 때마다 setState 가 한 번 더 돌아 렌더가
겹치기 때문이다(eslint 가 그 모양을 이미 오류로 잡는다).

## 미채택 이견

- **도메인 패널의 "업체 주기가 이틀이면 +2"** — 앱이 그 주기를 모른다. 운영자 확인 항목으로 남김.
- **UX·디자이너의 "열린 예정 날짜 갱신"** — 위 참조. 승인 항목으로 올림.
- **UX 패널의 세그먼트** — 위 참조.
- **UX 패널의 캘린더 요약 줄 '날짜 미정 N건'** — 날짜 없는 건은 SQL 단계에서 안 실려 오므로 count
  조회가 한 번 더 필요하다. 신규 기능이라 범위 밖. UX 패널 스스로 위험을 '작다'고 평가했고,
  실제로 안 보이는 곳은 캘린더 하나뿐이다.
- **UX 패널의 "홈 모달 환불 세그먼트 교체"** — 돈 컨트롤이라 승인 대상(패널도 같은 단서를 달았다).
- **디자이너 패널의 "'초기화' 를 '미정으로' 로 개명"** — 그 버튼은 전역 공용 DatePicker 의 것이라
  모든 호출부의 낱말이 함께 바뀐다. 이 칸 하나 때문에 앱 전체 어휘를 바꿀 수 없다.
- **백엔드 패널의 "CHECKED_OUT 소스 감지망 신설"** — 옳은 지적이지만 그 그물이 잡을 대상
  (updateTenant·batchUpdateTenants)은 **선재 결함**이고, 그것을 고치는 것이 곧 규칙 변경이다.
  적립만 하고 승인 뒤에 그물과 함께 놓는 것이 순서다.

## 배포 전 디자이너 패스 (별도 회수, 차단 1건)

**차단은 새 칸이 아니라 그 칸이 기대는 정본 `DatePicker` 안에 있었다.** 달력의 '오늘' 버튼만
`min`·`maxDate` 를 안 봤다. 날짜 격자는 경계를 지키는데 그 버튼은 안 지켜서, 퇴실일이 미래인
정상 다수 케이스에서 회색으로 잠긴 날을 버튼 한 번으로 값에 넣을 수 있었다. 하필 그 결과가
이 기능이 없애려던 상태('태어나자마자 예정일 경과')다. 경계를 건 호출부가 열 곳이라 새 칸을
고치지 않고 **정본 한 곳**을 막았다. 잠긴 날은 눌리지 않게만 하고 감추지 않는다 — 사라지면
오른쪽 '초기화'가 자리를 옮겨 다녀 손이 헛짚는다.

**문구 하나를 고쳤다.** 보조줄이 "달력에서 비우면"이라 했는데 그 버튼의 실제 이름은 '초기화'다.
세그먼트를 안 세운 근거가 "비우는 길은 이미 달력 안에 있다"에 통째로 걸려 있으므로, 그 길을
가리키는 유일한 문장이 버튼 이름을 정확히 불러야 논리가 닫힌다.

**남아 있던 침묵 하나를 메웠다.** 청소일을 먼저 고른 뒤 퇴실일을 더 뒤로 옮기면 청소 예정일이
퇴실일보다 이른 채 남는다(격자가 잠겨 있어 닿는 길이 그 하나뿐이다). 값을 고쳐 주지는 않고 한
줄로 말한다(§27.2 검증은 인라인) — 운영자가 넣은 날짜를 화면이 말없이 바꾸면 이 기능이
없애려던 그 일을 다시 하는 것이다. 서버에 하한을 거는 것은 규칙 변경이라 승인 대상으로 남겼다.

**그 줄에서 결함을 하나 더 잡았다.** §12 가 오류 메시지 색으로 적은 `--tc-d` 는 다크에서
재정의되지 않아 **대비가 1.98:1** 이다(실측). 같은 모달의 형제 인라인 오류가 이미 `--danger-fg`
를 같은 크기로 쓰고 있어 그쪽으로 맞췄다(라이트 6.14 · 다크 7.05). 가이드 §12 의 문장이 라이트
전용이라는 뜻이라 그 자체가 별건 기록 대상이다.

**측정 하네스의 오류도 이때 드러났다.** 같은 마크업이 조합마다 다른 대비를 내고 있었는데,
`transition-colors` 가 도는 도중에 색을 재고 있었다. 전환을 끄고 다시 쟀다. 바로잡은 값은
'미정' 라이트 10.71 · 다크 17.35 다(종전 보고의 다크 10.4 대는 무효).

**비차단으로 받은 지적**(고치지 않음, 근거): 새 칸의 `<label>` 에 `htmlFor` 가 없어 소리로는
날짜 버튼 둘이 이름 없이 나란히 선다 — `DatePicker` 에 `aria-label` 프롭이 없어 고치려면 60개
호출부에 걸리는 변경이다. 저장 중 `disabled` 미적용 — 이 모달은 전체가 이미 그 상태라 이 칸만
잠그면 오히려 갈린다. 대시보드는 `alert.moveOutHasRoom`, 상세는 `lease.roomId` 로 게이트 소스가
다름 — 판정식은 같고 알림 생성 경로가 하나뿐이라 지금은 구멍이 없다.

## 범위 밖 발견 (수정 안 함, knowledge/open-issues 적립)

1. `checkoutTenant` 에는 `applyStatusTransition` 이 가진 `roomStillOccupied` 가드가 없다. 방 하나에
   계약 둘인 구성에서 홈으로 퇴실하면 사람이 사는 방이 공실로 바뀌고 청소도 붙는다.
2. `updateTenant`·`batchUpdateTenants` 도 `CHECKED_OUT` 을 쓰는데 청소를 아예 안 만든다. 셋째·넷째
   퇴실 경로가 이미 존재한다. `app/api/import/route.ts` 도 같은 상태(의도적 제외인지 불명).
3. 손으로 만든 청소가 완료 시 `leaseTermId` 를 얻고(`completeCleaning` 의 청소비 귀속) 되돌리면
   그 칸이 남아, 자동 생성분으로 오인돼 걷힐 수 있다. 근본 봉합은 생성 출처 칸이라 스키마 변경.
4. 공실 집계 제외 방(창고·사무실)의 퇴실도 청소를 만든다. 캘린더는 그 방에 행을 안 세우므로
   안 그려지는데 '청소 필요 N실'에는 세어져 유령이 된다.
5. `package.json` verify:fast 문자열에 `&&npx` 로 공백 하나가 빠진 자리가 있다(동작은 함).
6. 청소 예정일을 실제로 아는 시점은 퇴실 통보를 받는 때(CHECKOUT_PENDING)지 확정 순간이 아니다.
   자연스러운 수집 자리가 한 단계 앞일 수 있다(도메인 패널, 승인 범위 밖).

## 기존 데이터

정정 대상 **0건**이다. 살아 있는 PLANNED 4건(514호 8/21 · 409호 8/24 · 404호 8/31 · 503호 9/2)은
전부 `leaseTermId` 가 없어 운영자가 손으로 등록한 것이다. 자동 생성분은 전체 21행 중 둘
(413호 · 422호)뿐이고 **둘 다 이미 소프트삭제**돼 있다. Work_log 2026-08-20 (7) 이 적은 'PLANNED
5건'은 그 시점 기록이고, 422호는 그 뒤 지워졌다. DB 에 아무것도 안 썼다.
