# 수납 정보 탭 이용료 정산 카드 맥락 (2026-09-02)

## 왜 새 카드인가
운영자 요청은 "퇴실 예정·완료면 환불과 보증금 반환이 자동으로 떠서 예상액을 보여주고, 다르면 여기서 바로 고치기". 보증금은 DepositStatusPanel 이 이미 그 자리이고, 이용료 환불은 확정 뒤 볼 곳이 입주자 정보 탭의 적용취소 한 줄뿐이었다. 그마저 getTenantDetail 이 CHECKED_OUT 계약을 안 실어 퇴실 완료자에게는 안 그려졌다. 그래서 보증금 카드 바로 아래에 같은 문법의 이용료 정산 카드를 세운다(설계안 A, 패널 합의).

## 확정 후 수정 = 적용취소 + 재확정 (항목 1)
finalizeRentRefund 는 멱등 가드(메모 접두어)로 두 번 못 부른다. 원자적 reviseRentRefund 를 새로 세우면 스냅샷·홈택스 안내 문법이 두 벌이 된다. 카드가 undoRentRefund 뒤 finalizeRentRefund 를 부르고, 둘째가 실패하면 카드가 '환불 미처리'로 서서 다시 확정할 입구가 남는다(사람이 복구할 수 있는 중간 상태).

## 과거 회계월 보호는 재확정에도 (항목 2)
finalizeRentRefund 의 checkSettlementMonth 가 그대로 걸린다. 예외를 두면 마감 뒤 매출이 조용히 바뀐다.

## 입주자 정보 탭의 두 행은 지운다 (항목 3)
같은 되돌리기가 두 화면에 서면 문구·확인창이 갈린다. 카드가 정본이고 상시 진입점이다(§16). 호실 일정·호실 사용 중 행은 성격이 달라 남긴다.

## 환불 record 잠금 (항목 4)
확정이 만든 record(memo '[중도퇴실 환불]')를 수납 목록에서 직접 고치면 스냅샷과 어긋나 적용취소가 틀어진다. UI 는 버튼을 숨기고, 서버 updatePayment·deletePayment 도 거부한다(다른 화면이 같은 액션을 쓰므로 서버가 클래스 봉합). 현금영수증 토글은 별 액션이라 그대로.

## 퇴실 예정 카드에 편집 칸 없음 (항목 5)
예정 단계의 조정은 위젯(CheckoutProrationWidget)이 정본이라 카드는 예상액만 보이고 '정산 조정'으로 위젯을 연다. 두 편집 칸이 같은 값을 다르게 저장하는 일을 막는다.

## 옛 퇴실자 '환불 기록' (항목 6)
스냅샷 없이 checkoutProratedMonth/Amount 만 있고 실수납이 청구보다 많은 계약(getPendingRentRefundNotice). 카드가 '환불 미처리'로 서고 finalizeRentRefund 한 번으로 기록한다. 적용취소 뒤 재확정이 실패한 중간 상태도 같은 모양이라 입구가 하나다.

## 사유 한 줄 (항목 7)
수동 환불액은 계산값과 다른 이유가 있어서다(413호: 실제 입실일 기준 임의 계산). memo 꼬리와 스냅샷 reason 에 남기고, 감사 규칙 6 은 reason 있는 스냅샷을 건너뛴다(운영자가 알고 고른 금액).

## 감사 규칙 8
스냅샷 있는 계약의 그 달 살아 있는 이용료 record 합이 prepaid − refunded 와 다르면 위반. 잠금을 뚫고(옛 화면·스크립트) 고친 흔적을 잡는다. 첫 실행에 기존 불일치가 나오면 정리는 운영자 판단.

## 스타일 상수 추출
DepositStatusPanel 의 inputBase/inputCls/inputErrCls/labelCls/formBoxCls 를 panelFormStyles.ts 로 옮겨 두 카드가 같은 문자열을 쓴다. 문자열은 한 글자도 안 바꾼다(형제 대조 기준).

## 정산 조정 버튼의 재클릭
위젯 autoOpen 은 ref 로 1회만 열린다. 카드의 '정산 조정'은 카운터를 key 로 넘겨 위젯을 다시 세워 매번 열리게 한다. full 모드 전환 뒤 위젯이 화면 밖이라 래퍼 ref 로 scrollIntoView(block nearest).

## 재확정 전 과거 회계월 예행
재확정은 적용취소 뒤 확정 두 호출인데 확정만 checkSettlementMonth 에 걸린다(적용취소는 안 걸린다). 먼저 되돌려 놓고 확정이 막히면 환불 기록만 사라진 채 남는다. 카드가 같은 판정(lib/accountingGuard, 순수 함수)을 클라에서 미리 물어 그 길을 막는다.

## 홈택스 안내는 확정 쪽만
적용취소의 '재발행' 안내와 확정의 '취소·재발행' 안내를 잇달아 띄우면 서로 어긋나 보인다. 재확정은 확정 쪽 안내만 띄우고, 둘째 호출이 막혔을 때만 적용취소 쪽 안내를 대신 띄운다. 그 실패는 토스트가 아니라 카드 안 경고(warning-fg)로 남긴다 — 사람이 고칠 때까지 보여야 하는 상태라서다(디자이너 P2-4).

## prevProration 이 null 인 계약의 적용취소
506·519호처럼 퇴실 정산 위젯을 거치지 않고 확정된 계약은 스냅샷의 prevProration 이 null 이라 적용취소 뒤 checkoutProratedMonth 가 비고 카드가 사라진다(재확정 입구 없음). 506호는 그것이 의도한 결과(실제 환불 없음)라 승인 범위대로 둔다. 재확정이 필요한 계약이 나오면 퇴실 정산에서 청구를 먼저 확정하는 길을 따로 세운다(백로그).

## '환불 미처리' 0원 출구는 미구현
"돌려주지 않았다"를 기록하려면 청구 확정을 받은 돈으로 맞추는 서버 액션이 필요한데 finalizeRentRefund 는 환불 0 을 거부한다. 결제 로직 변경이라 운영자 결정으로 넘긴다(디자이너 P2-5).

## 디자이너 패스 반영(형제 동반)
청구 확정 파생값은 퇴실 정산 위젯의 자동 합산 읽기전용 문법(bg sand-s · 투명 보더 · '자동 계산' 캡션)으로, 금액 줄 break-keep, 금액 input 에 num, label htmlFor/id + aria-invalid/aria-describedby, 적용취소 svg 의 mr-1.5 제거(Btn 이 gap-1.5 를 이미 준다). 같은 클래스인 DepositStatusPanel 의 금액 줄·svg·금액 input 3곳도 함께 고쳤다.
