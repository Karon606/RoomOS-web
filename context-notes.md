# 서류 작성 문 · 발급 이력 · 몰취 날짜 축 맥락 (2026-09-03)

## A 왜 버튼이 아니라 링크인가
행 주석이 "행마다 버튼이 하나라 한 줄을 유지한다(실측 잘림 0)"고 못박고 있다. 320px 에서
체크박스 22px + 제목 + 버튼 둘이면 제목이 먼저 잘린다. 같은 시트의 계약서 행이 '판본 바꾸기'를
보조줄 밑줄 링크로 세우는 전례가 있어 그 문법을 쓴다. 새 문법이 아니다.

## A 왜 납부 게이트인가
운영자 말 그대로다. 다만 **발급 능력이 사라지는 것이 아니다.** 기록 없는 달의 수동 발급은 발급
화면의 월 스테퍼와 전역 발급 이력의 '다시 작성'이 이미 감당한다. 이 문은 매월 반복되는 정상
흐름의 지름길이다. 기존 판정("기록 없으면 경고하되 발급은 막지 않음")과 충돌하지 않는다.
조건부로 나타나는 문은 부재 이유를 말해야 한다 — 미납이면 안내 한 줄이 그 자리를 지킨다.

## A 왜 불리언인가
`rentPaidLeaseIds` 로 사실 하나만 넘긴다. 금액을 넘기면 시트가 언젠가 돈을 그리게 되고, 그 돈은
발급 화면의 정본 계산(billForLeaseMonth·청구 락)과 갈라질 길이다. 시트의 용건은 "문을 열 수
있는가" 하나다. 실입금 술어는 공유 헬퍼로 뽑아 세 자리가 같은 것을 import 한다 — 사본이
검증하는 것은 사본 자신이다.

## B 선납 구멍
`RentReceiptFile` 에 귀속월이 없어 stale 판정이 발급일 기준이다. 8월에 9월분을 선납받아 확인서를
미리 발급하면 9월에 그 종이가 stale 로 뜨고 9월 납부 기록도 있어 문이 열린다. 같은 달 확인서가
두 장 나간다. 운영자 승인으로 귀속월 칼럼을 더해 근본을 막는다. 기존 행은 NULL 이라 발급일
폴백으로 종전과 같이 동작한다.

## C 넷째 화면은 중복이다
지금 없는 능력은 정확히 하나, "이 사람의 과거 발급본 열람"뿐이다. 보내기는 서류 시트, 계약서
관리는 계약 파일 패널, 삭제·다시 작성은 전역 발급 이력이 갖고 있다. 그래서 **관리 기능 없는
열람 전용** 접이식 목록이다. 파괴 액션을 넷째 자리에 복제하면 확인 문구와 적용취소 동선이
갈라진다. 계약서는 뺀다(운영자 결정) — 바로 위 칸과 같은 파일이 두 번 보인다.

접힘 정본은 PaymentHistoryAll 이다. 카드 + 풀폭 헤더 버튼 + 셰브론 + **첫 펼침에 조회**.
모달이 열릴 때마다 안 쓸 수도 있는 조회를 얹지 않는다.

## E 몰취 날짜 축
노트의 '퇴실 정산일' 판정은 유지하되 정확히 읽는다. 정산일은 **반환하지 않기로 확정한 날**이지
앱에 타이핑한 날이 아니다. 지금 두 폼의 기본값 '오늘'은 그 어느 쪽도 아닌 클릭한 날이고,
현금영수증 발행일 사건과 같은 클래스다("클릭한 순간이지 발행한 날이 아니다").
이 사업장에서 정산은 퇴실 처리의 일부라 기본값이 퇴실일이면 두 판정이 충돌하지 않는다.
칸은 남긴다 — 진짜로 늦게 확정한 정산을 사실대로 적을 길이 있어야 한다.

곁다리 버그. 수정 폼의 전액 미반환 확인창이 귀속월을 '오늘의 달'로 계산하는데 저장은 고른
반환일이다. 두 값이 갈리면 확인창이 틀린 달을 약속한다. 형제(상태 전환)는 맞게 돼 있다.

실제 송금일은 어느 필드도 안 담는다. 지금 보증금 지출의 계좌 대사 기능이 없어 실익이 없고,
백로그의 CODEF 계좌연동이 오면 '지급일' 축을 따로 여는 것으로 기록만 남긴다.

---

## A-4 사진 인식 서버 액션 한도 (2026-09-03)

### 진짜 문은 10MB 본문이 아니라 1,000,000 슬롯이었다
어제(2026-09-03 오전) 축소 정본 lib/ocrImage 를 넣으며 원인을 "서버 액션 본문 상한 10MB"로
적었는데 **틀렸다.** 그건 바깥 문이고 실제로 먼저 걸리는 문은 React 직렬화기(decodeReply)의
슬롯 한도 1,000,000 이다. 문자열은 1자가 1슬롯이라 base64 인자는 1,000,000 자,
원본으로 약 730KB 에서 터진다.

실측 방법을 남긴다(다음에 또 재야 할 때 쓸 것).
node --conditions=react-server 로 next/dist/compiled/react-server-dom-turbopack-experimental 의
client(encodeReply)·server(decodeReply)를 직접 require 해 이분 탐색했다.
- 문자열 999,023자 통과 / 1,000,000자 실패
- 배열 원소 1,200,000개도 실패(문자열만의 문제가 아니라 슬롯 총량)
- FormData 에 실은 File 은 0.5MB·2MB·6MB 전부 통과

축소 배포가 16:03 KST 인데 오류 마지막이 19:02 KST 였다. 이 시각 대조가 "축소로는 안 끝났다"를
말해 줬다. 증상이 사라졌는지 시각으로 확인하는 습관이 값을 했다.

### 축 A(재압축)를 버리고 축 B(FormData)를 택한 이유
패널 판정이다. 재압축으로 예산에 맞추는 안은 세 가지로 반려됐다.
1. 화질을 깎는 조건이 발동하는 사진이 바로 크고 세밀한 신분증인데, 거기서 등록번호와 로마자
   성명을 읽는 것이 이 기능의 존재 이유다. 목적과 수단이 충돌한다.
2. HEIC 폴백을 원리상 못 고친다. 디코드가 안 돼서 존재하는 길이라 재인코딩 루프가 못 돈다.
   문자열을 유지하면 폴백 허용선을 6MB 에서 700KB 로 깎아야 하는데 기능 후퇴다.
3. 봉합을 정적으로 검증할 수 없다. "FormData 만 받는다"는 모양으로 보이지만 "모든 생산자가
   예산 루프를 통과했다"는 데이터 흐름 속성이라 정규식 그물로 못 잡는다.

### 위험한 건 신분증이 아니라 계약서였다
브라우저 전문가 지적. 신분증 사진은 프레임 대부분이 평탄한 배경이라 거의 0비트로 압축되는데,
계약서는 글자가 프레임을 다 덮고 폰으로 찍은 "흰 종이"가 실제로는 센서 노이즈와 그림자
그라데이션이라 비트를 먹는다. 2048px q0.9 에서 신분증 400KB~1.1MB, 계약서 500KB~1.4MB.
신고는 신분증으로 들어왔지만 상습범은 계약서·도면이었다.

### 영어 유출은 판정을 한글 음절로 한다
`(err as Error).message || '한국어 폴백'` 패턴에서 프레임워크 영어 메시지가 truthy 라
**항상 한국어 폴백을 이긴다.** 이게 운영자 스크린샷의 정체다.
전용 에러 타입(instanceof) 안은 서버 액션 경계를 넘으면 평범한 Error 로 강등돼 깨진다.
한글 음절 검사 /[가-힣]/ 는 경계를 넘어도 동작하고, 서버가 res.error 에 실어 보내는
영어(fetch failed 등)까지 표시 지점 한 곳에서 덮인다.

### 패널이 찾은 잠복 결함
신고 안 된 것 다섯이 나왔다. 특히 ReceiptScanModal 의 warp 실패 폴백은 원본 해상도 전체를
q0.85 로 인코딩해 넘기는 현역 폭탄이었고(12MP 면 base64 300~500만 자), 서로 다른 전문가
셋이 독립적으로 같은 자리를 짚었다.
app/sign/[token]/actions.ts 의 서명 상한 1,400,000 은 프레임워크 문(1,000,000)보다 높아서
1.0M~1.4M 구간에서는 사람 말 오류가 아니라 프레임워크 오류가 먼저 났다. 상한을 문 아래로
두지 않으면 사람 말 가드가 무의미하다는 교훈이다.

---

## 발급본 표기가 박제되지 않는다 (2026-09-03 밤, 운영자 지적)

운영자 원문. "이미 영어이름으로 서명도하고 발급까지 완료한 계약서인데 왜 한글로 이름이 바뀌어
있는거야? ... 발행한 계약서는 손대면 안되지."

**마지막 문장이 원칙 선언이다.** 발급이 끝난 서류의 표시는 발급 시점 상태로 고정되어야 한다.

지금 구조는 그렇지 않다. issuedSnapshot 이 표기를 기록하지 않아서(facts 에 tenant.name 원본
한글만 있음), 태그가 null 인 발급본은 렌더할 때마다 resolveDocNameStyle 이 **지금 상태**로
재해석한다. 그래서 사람 단위 설정을 바꾸면 이미 서명받은 계약서 표시가 통째로 바뀐다.

운영자가 "419호 엘레나, 411호 쩐 마이린은 영어로 되어있고"라고 한 것이 이 구조를 그대로 드러낸다.
그 셋이 영문인 것은 **박제돼서가 아니라 지금 우연히 그렇게 재해석되기 때문**이다.

### 내가 하마터면 저지를 뻔한 것
어제 A-2 백필로 "태그가 어긋난 7건을 고치자"고 제안했다. 운영자 원칙에 비추면 그중 6건은
발급 시점 표기를 증언할 데이터가 없는 상태에서 추측으로 과거 기록을 바꾸는 일이었다.
증거가 있는 것은 쩐 티 투 창 1건뿐이다(운영자가 종이를 직접 보고 영문이라고 확인).
**증거 없는 정정은 지금과 반대 방향의 오류를 만든다.** 백필을 멈추고 박제 구조를 먼저 세운다.

### 조사에서 쓸모 있었던 것
- issuedSnapshot 을 실제로 열어 키를 나열해 본 것. "표기가 기록되는가"를 추측하지 않고 확인했다.
- 발급본과 스캔 업로드본을 가른 것. 59건 중 우리가 발급한 것은 16건뿐이고 43건은 운영자가
  종이를 찍어 올린 것이다. 스캔본에 태그를 붙이는 것은 의미가 다르다.
- 커밋 시각과 서명 시각을 대조한 것. 쩐 티 투 창 서명 15:55, 표기 버그 수정 배포 15:49 로
  아슬아슬하게 걸쳐 있어 "고쳤는데도 남은 경로가 있는가"라는 질문이 남았다.

## 동의서가 서명 없이 제출된다 (2026-09-03 밤, 1순위)

운영자 확인. "발급한 서류 확인하니 쩐티투창의 임의처분동의서에 서명이 안되어 있어.
서명없이 제출된게 문제인듯."

계약서 서명은 42,898자로 남았는데 동의서 서명만 0자다. 크기 한도 문제가 아니고(전부 3만~9만자),
영업장 동의서 기능도 켜져 있다. 오늘 내가 내린 서명 상한(900,000)은 저녁 배포라 이 서명보다
나중이니 원인이 아니다.

**서명이 빈 채로 제출과 발급까지 통과했다는 것이 문제의 핵이다.** 게이트가 없다.
법적 서류라 loop.md §4 대상이고, 운영자가 1순위로 지정했다.

## 오늘의 반복 패턴
"실패가 침묵한다"가 하루에 네 번 나왔다. 사진 인식의 프레임워크 오류, catch 없는 unhandled
rejection, 정산 카드의 fetch 실패, 그리고 아마 동의서 서명까지. 한 클래스로 묶어 볼 값어치가 있다.

## 참고용 번역본 1단계 — 판정 정본과 박제까지 (2026-09-08)

checklist 12번의 1단계 시공. 화면에 번역본을 그리는 일(2단계)은 손대지 않았다.

### 왜 열쇠가 한국어 문장 자체인가
항목 번호나 인덱스로 짝을 맞추면 운영자가 조항을 하나 끼워 넣는 순간 그 아래 번역이 통째로
밀린다. 문장을 열쇠로 두면 순서가 바뀌어도 안 흔들리고, 원문을 고치면 그 항목만 저절로
'번역 없음'이 된다. 대가는 고아 번역이 생긴다는 것이고, 그래서 세되 **지우지 않는다** —
조항을 되돌리면 손번역이 살아 있어야 한다. 병합 함수에 삭제 경로 자체를 두지 않는 것으로
구조가 그것을 진다(mergeSignDocuments 와 같은 수법).

### 빈 항목을 감추지 않는 이유
번역이 없는 문장은 한국어 원문을 그 자리에 그대로 남긴다. 감추면 "3조 2항"이 번역본에서
다른 줄을 가리켜 조항 번호 대응이 깨진다. 원문으로 남은 개수는 fallbackCount 로 세어
박제와 편집기가 같은 숫자를 본다.

### 박제는 언어 하나의 결과물이다
사전 전체가 아니라 그 링크의 언어 하나를 해석한 완료본을 templateSnapshot.translation 에
담는다. 전 언어를 담으면 스냅샷이 8배로 무거워지는데 입주자가 안 본 언어는 증거가 아니다.
한국어 링크에는 아예 안 담긴다(asTranslationLang 이 ko 를 안 통과시킨다).

### 조사 중 잡은 잠복 결함 — 발급 상세 시트
printedFacts 에 축을 하나 더하면 IssuedContractSheet 가 **발급본 전건에** 줄을 하나 더
그린다(그 표가 PRINTED_FACT_KEYS 를 통째로 돌기 때문). 번역본을 안 쓰는 발급본 전부에
'참고용 번역본 · 기록 없음' 빈 줄이 생길 뻔했다. 추가 호실 특약과 같은 규칙(실린 발급본에만
줄을 세운다)으로 막고, 그물 ⓖ 로 못박았다. **축을 늘릴 때 이 시트를 같이 보는 것이 규칙이다.**

### 2단계 — 번역본을 ContractData 에 담되 **없으면 칸 자체를 안 만든다**
형제 절 셋(추가 호실·요금·거주 호실 일정)은 `ContractData` 에 `null` 을 담는다. 번역본만 옵셔널
키로 두고 조건부 스프레드로 담는 이유가 있다. 이 값이 링크 발급에서 `withoutPlainPii(snapshot)`
스프레드를 타고 그대로 `templateSnapshot` 으로 흘러가기 때문이다. `null` 을 담으면 번역본을 안
쓰는 영업장의 링크 스냅샷에 `translation: null` 이 박혀, 1단계가 세운 조건부 스프레드 무회귀
보장이 조용히 깨진다(그물 ⓑ 의 정규식은 그대로 통과하므로 더 위험하다). 그물 ⓛ 로 못박았다.

### 값의 원천은 박제뿐이다 — 화면이 사전을 다시 해석하지 않는다
`buildContractData` 는 `resolveSignedBody(...).translation`(= 서명 박제본이 든 것)만 읽는다.
서명 전 계약에는 링크 언어가 없어 번역본도 없다. 원격 화면은 링크 스냅샷을 통째로
`ContractData` 로 읽으므로 그 자리에서 채워진다. 지금 사전을 다시 조회하면 입주자가 읽은 문안과
화면이 갈리고, 그 순간 박제는 증거이기를 그만둔다. `asResolvedContractTranslation` 이 사전도
템플릿도 **인자로 안 받는다**는 사실 자체가 재해석을 구조적으로 막는다.

### 원문 폴백 표식의 근거 — 문자열 동일성
카드는 '번역문 === 그 자리의 한국어 원문' 일 때 회색 `원문` 표식을 단다. 해석이 없는 줄에
원문을 **그대로** 두므로 두 문자열이 정확히 같고, 같은 줄의 수가 `fallbackCount` 와 일치한다
(진리표로 못박음). 원문(`facts.template` · `data.template`)을 못 구하면 표식만 없고 줄은 그대로다 —
감추면 조항 번호 대응이 깨진다.

### 발급 박제에 번역 축을 채운 것은 1단계가 남긴 구멍이었다
1단계가 `printedFacts` 에 축을, `IssuedContractSheet` 에 줄을 세웠는데 **발급 라우트가 그 축을
안 채우고 있었다**(`printedFacts({...})` 인자에 translation 이 없었다). 그래서 그 줄은 어떤
발급본에서도 뜰 수 없었다. 2단계 ⑤(전문 열람)가 성립하려면 채워야 해서 `printData.translation`
을 함께 넘겼다. 무회귀는 그대로다 — 축 규칙이 '없으면 undefined' 라 번역본 없는 발급본의 박제는
바이트가 같고, `issuedSnapshot.facts` 를 지금 값과 견주는 소비처는 없다(읽는 곳은 표시와
`issuedPrintedName` 뿐).

### 우선 조항은 절 배열 **맨 뒤**다
화면·인쇄 둘 다 `appendSubLeaseAddendum(..., contractTranslationAddendum(x))` 로 마지막 인자다.
앞에 끼우면 형제 절의 번호가 밀린다. 번역본이 없으면 판정이 null 이라 `appendSubLeaseAddendum`
이 **받은 배열을 그대로**(같은 객체) 돌려주고, 그래서 그 계약서의 렌더가 문자 단위로 안 바뀐다.
실측으로도 확인했다 — 번역본 없는 8가지 입력 형태에서 인쇄 HTML 이 HEAD 와 바이트 동일.

### 피커 캡션은 세 상태다
운영자 오더의 예시는 두 개(`번역 12/26` · `번역본 없음`)였는데 실제로는 셋이다. 다 번역해 두고
공개만 안 켠 언어에 '없음'이라 적으면 화면이 운영자의 데이터에 대해 거짓을 말한다. 그래서
`비공개 · 번역 n/총` 을 갈라 세웠다. **운영 스위치가 꺼진 영업장에는 캡션을 아예 안 만든다** —
안 쓰는 기능을 일곱 줄로 광고하게 된다.

## 표시값 목록 — 결정과 근거 (2026-09-09)

### 자동값을 ContractData 에 실었지만 링크 스냅샷에서는 벗긴다
설계는 `lib/contractData.ts` 의 `hasFieldOverrides` 자리에 자동값과 고쳐진 키 목록을 더하라고
했다. 그런데 그 반환 객체는 `contractShare` 가 **통째로** `templateSnapshot` 에 저장한다(칸
화이트리스트가 없다). 그대로 두면 두 가지가 깨진다. ① 표시값을 고친 계약의 링크 스냅샷 바이트가
달라진다 — 이 저장소가 `translation` 을 조건부 스프레드로 만들면서까지 지켜 온 자리다.
② **운영자가 오버라이드로 덮어 감춘 자동값이 입주자가 여는 24시간 공개 JSON 에 실린다.** 관
제출용으로 다르게 적은 금액이 있으면 그 원값이 그대로 나간다. 그래서 설계대로 칸은 더하되
저장 직전에 `withoutScreenOnly` 로 벗겼다. 결과적으로 링크 스냅샷은 **모든 계약에서** 종전과
바이트가 같다. 두 칸을 옵셔널로 둔 것도 이 때문이다 — 스냅샷에서 읽는 `/sign` 화면에는 정말로 없다.

### nameStyle 만 '저장돼 있다 ≠ 고쳐졌다'
다른 여덟 칸은 `normalizeContractFieldOverrides` 가 자동값과 같아진 키를 저장에서 걷어내므로
저장 = 수정이다. nameStyle 은 그 가지치기의 **의도된 예외**라(외국인에게 '한글'을 고르면 지워지는
사고를 막으려고 2026-09-04 에 넣은 분기) 자동값과 같은 값도 남는다. 그것을 그냥 세면 지금 값과
자동값이 글자까지 같은 행이 서고 N 이 부풀어, 운영자는 없는 칸을 찾게 된다. 그래서 이 칸만
'종이의 표기를 실제로 바꾸고 있는가'로 센다. 판정은 `overriddenContractFieldKeys` 한 곳이고,
배지의 N 과 모달의 행 수가 같은 배열의 length 를 본다.

### 표기 해석 두 벌은 일부러 손으로 적었다
'지금 값'과 '자동값'은 `saved` 하나만 다른 같은 해석이라 공통 객체로 묶고 싶었지만, 묶으면
`scripts/check-doc-name-axis.mjs` ⓒ 가 그 호출의 축을 **못 본다**. 그 그물은 객체 리터럴 안의
`tenant:` 를 눈으로 찾으므로, 변수로 감추면 축이 빠져도 조용히 통과한다(실제로 묶었더니 한 호출이
그물의 시야에서 사라졌다). 축이 갈리는 것을 막는 일은 묶기가 아니라 그 그물이 맡는다. 대신 두
벌이 같은지는 `test-field-override-list` 가 지킨다 — 역주입에서 두 그물이 함께 발화하는 것을 확인했다.

### 되돌리기는 commitField 를 안 지난다
설계가 지목한 정본 경로는 **서버 액션**(`saveContractFieldOverride` 에 그 키를 null 로)이고 그것을
그대로 쓴다. 다만 `commitField` 를 통과시키지는 않았다 — 그 함수의 납부일 분기(원천이 빈 계약)는
빈 값을 받으면 조용히 되돌아가서, 그 길로 보내면 납부일만 되돌리기가 아무 일도 안 하고 화면은
이유를 말하지 못한다. 폼 값도 손으로 안 맞춘다. `useEffect(..., [data.lease])` 가 서버 값으로
전 칸을 다시 세우므로, 여기서 쓰면 아직 옛 병합값인 props 를 도로 적는다.

### 확인서에는 nameStyle 예외가 필요 없다
`normalizeResidenceCertOverrides` 는 nameStyle 도 가지치기 대상이라 자동값과 같으면 저장에서
빠진다. 대신 그쪽의 '자동값'은 `autoFields.nameStyle`(늘 '한글')이 아니라 저장값을 뺀 해석이어야
한다 — 외국인의 자동값은 '영문'이고, 그걸 '한글'이라 적으면 되돌린 뒤 화면이 목록과 다른 값을
보인다. `docNameStyleOf(data, false)` 가 그 답이다.

### eslint 기준선이 이미 밀려 있었다
오더의 기준선은 481(상한 497)인데 HEAD 자체가 **498**이다(별도 worktree 로 HEAD 를 그대로 측정).
이번 작업의 델타는 0 — 새 파일 둘은 경고 0건이고, 고친 파일에서도 늘지 않았다.

### 권고4 는 '라벨 고정 + aria-busy' 를 골랐다
디자이너가 준 두 갈래는 라벨 고정 + `aria-busy` 와 최소 폭 부여였다. 앞쪽을 골랐다. 이 버튼은
btn-subtle sm(11px)이라 `되돌리는 중…` 을 담을 최소 폭을 주면 `적용취소` 만 있을 때도 그 폭이
남아, 폭이 넓은 버튼이 행마다 서고 max-w-xs 모달에서 칸 이름을 밀어낸다. 진행은 두 가지가 이미
말한다 — 전 행이 함께 잠기고(권고4 본체), 앱 전역 저장 표시가 `trackSave` 로 돌아간다.
`aria-busy` 는 그 둘이 안 닿는 보조기술 몫이다.

### 확인서 버튼은 치수를 스스로 안 정한다
`.rc-btn-info` 에 남은 것은 `color` 와 `border-color` 뿐이다. 계약서 툴바는 손수 만든 28px 버튼들의
줄이라 형제(`.toolbar-btn-warn`)와 같은 치수를 스스로 지고, 확인서 툴바는 형제가 전부 `Btn md` 라
`btnClass` 를 탄다. 같은 뜻의 버튼이지만 **각 줄의 형제를 따르는 것**이 맞다. 그물도 그렇게 갈라
본다 — 계약서는 형제 규칙과 치수가 같은지, 확인서는 치수를 아예 안 적는지.

## 퇴실·투어 완료·입실 처리 적용취소 (2026-09-10)

### 목적지를 화면에 안 내린 이유
확인창이 "413호가 다시 퇴실 예정이 되고" 라고 말하려면 화면이 라벨을 알아야 한다. 그런데 목적지
enum 을 내려 주면 화면이 그것을 그대로 서버에 되돌려 보내게 되고, 그 순간 판정이 두 벌이 된다
(자정을 넘기거나 옆 카드에서 반환을 되돌리면 답이 바뀐다). 그래서 `getCheckoutRevertInfo` 는
**라벨 문자열과 차단 문장만** 내린다. `applyStatusTransition` 은 CHECKED_OUT 출발이면 화면이 실어
보낸 `toStatus` 를 술어 결과로 **덮어쓴다** — 화면이 보내는 `'ACTIVE'` 는 '거주계로 돌아간다'는
뜻일 뿐이다. 감지망 축 ⓡ 이 그 덮어쓰기와, 화면에 `CHECKOUT_PENDING` 이 안 적히는 것을 함께 본다.

### 경계(예정일이 오늘)는 거주중이다
설계 문장은 "오늘 이후면 퇴실 예정, 없거나 지났으면 거주중" 이라 오늘이 두 갈래 사이에 낀다.
거주중으로 골랐다. 예정으로 돌려 두면 오늘 안에 다시 나가야 하는 상태로 서 있게 되고, 거주중
갈래는 퇴실 예정일까지 걷으므로 크론이 다시 안 집는다. 진리표가 이 경계를 한 줄로 못박는다.

### 보증금 반환을 먼저 보는 이유
둘 다 걸린 계약은 어차피 둘 다 풀어야 한다. 첫 문장이 **바로 위 카드**를 가리키는 편이 다음 손이
짧다 — 진입점이 수납 정보의 '퇴실 정산' 우산 아래라 보증금 카드가 두 칸 위에 있다.

### 이용료 환불 차단은 새 문장을 안 만들었다
`RENT_REFUND_LOCKED` 그대로다. 같은 막힘이 화면마다 달리 불리면 안 된다는 것이 그 상수의 존재
이유고, 여기서 "퇴실을 되돌리려면…" 이라고 다시 쓰면 다섯 자리가 여섯 자리가 된다.

### 되돌림 행에 퇴실 사유를 싣는다
`inheritableCheckoutReason` 은 최신부터 거슬러 오다 CHECKED_OUT 행을 만나면 **거기서 null 로
멈춘다**(예정 구간 밖이라는 판정이 맞다). 그래서 되돌림 행이 표식('퇴실 적용취소')만 들면 그
사람의 다음 퇴실이 사유를 못 이어받는다. 퇴실 예정으로 돌아갈 때는 퇴실 완료 행의 사유를 그대로
싣고, 거주중으로 돌아갈 때는 예정 자체가 접힌 것이라 표식만 남긴다. 선례는 `undoAutoCheckout` 의
'자동 전환 되돌림' 행이다. `changedAt` 은 손대지 않는다 — 그 값이 자동 퇴실 적용취소의 가드다.

### 거주중 복귀 정리를 넓히지 않으면 하루짜리가 된다
`applyStatusTransition` 의 정리 블록(퇴실 예정일·일할·스냅샷 비우기)은 CHECKOUT_PENDING 출발만
탔다. CHECKED_OUT 출발을 안 태우면 퇴실 예정일이 남고, 크론(`cron/push-alerts`)이 다음 날 아침
그 계약을 다시 퇴실 예정으로 집는다. 되돌린 것이 하루만 사는 셈이다.

### 수정 폼 뒷문은 **차단만** 걸었다
설계의 "같은 술어를 건다"를 게이트로 읽었다(역주입 목록의 '뒷문 게이트 제거'가 그 뜻이다).
목적지까지 덮어쓰지는 않았다 — 그 폼은 상태 셀렉트로 사람이 고르는 자리라, 고른 값을 말없이
다른 값으로 바꾸면 저장 결과가 화면과 달라진다. 크론이 어차피 같은 답으로 수렴시킨다.

### `roomNoWithI` 를 lib/roomNo 에 더한 이유
확인창 주어가 "{호실}이 다시…" 다. 호실번호는 숫자만이 아니다 — 등록 폼이 'A동-3'·'옥탑방'을
권하고 제기역점에는 이미 '사무실'이 있다. 그냥 이어 붙이면 '사무실가'가 나온다. 같은 이유로
이미 `roomNoWithRo`(로/으로)가 그 파일에 있으므로 형제로 뒀다 — 14번째 손사본을 만들지 않는 것이
그 파일의 목적이다.

### verify:fast 배선이 남았다
`scripts/test-checkout-revert.ts` 는 `package.json` 수정 금지라 체인에 못 엮었다. 수동으로는 34건
전부 통과한다. `test-field-override-list.ts` 가 앞서 같은 자리에 걸렸다가 상위에서 이어 붙었다.

### 점유 정본은 `OCCUPYING_STATUSES` + `primaryRoomLease` 를 골랐다
후보 셋 중 `roomAvailability` 는 안 썼다. 그것은 '언제 비는가'를 답하는 함수라 점유 계약이 없어도
창고·사무실이면 `null` 을 돌려준다(`isVacancyExcluded`). `!== 'now'` 로 점유를 읽으면 **빈 창고가
점유로 잡혀** 그 방의 퇴실 되돌리기가 통째로 막힌다. `roomLeaseRowOrder` 도 안 썼다. 그 층에는
비거주가 들어 있는데 비거주는 방을 잡지 않고(`roomVacantForStatus` 가 vacant 로 본다) 차단 문장도
'거주·예약'이라 말한다. 남은 것이 `OCCUPYING_STATUSES`(RESERVED·ACTIVE·CHECKOUT_PENDING)로 거른
집합에 `primaryRoomLease` 를 묻는 조합이고, 이것이 문장과 정확히 같은 뜻이다. `primaryRoomLease`
문서가 "넘기는 집합이 곧 그 화면의 정의"라고 못박아 둔 그 용법이다.

### 호실 계약 목록은 거르지 않고 통째로 넘긴다
서버 조회(`roomLeasesFor`)가 상태를 미리 거르면 '어떤 상태가 방을 잡는가' 규칙이 조회부에 한 벌 더
생긴다. 정본이 바뀌는 날 한쪽만 따라간다. 그래서 조회는 그 방의 계약 전부를 주고 거르는 일은 술어가
한다. 진리표가 상태별로 축을 세울 수 있는 것도 그래서다.

### 그물이 빈 배열 우회를 못 보던 자리
`roomLeases:` 라는 인자 이름만 보면 `roomLeases: []` 로 조용히 끄는 길이 남는다. 역주입에서 실제로
안 걸렸고, `roomLeases: await roomLeasesFor(` 까지 보도록 조였다. 같은 이유로 사유 승계 검사도
1차에서 한 번 느슨했었다(변수명만 남아도 통과).

### 확인창 금지어 검사가 내 주석을 먼저 잡았다
'예약가' 금지 검사가 그 단어를 설명하는 코드 주석에 먼저 걸렸다. 그물이 파일 전체를 보기 때문인데,
검사를 문자열 리터럴로 좁히는 대신 주석 쪽 표현을 바꿨다 — 금지어를 설명하려고 금지어를 적는 것보다
'화면 이름은 호실 관리의 예약 이용료다'라고 적는 편이 다음 사람에게도 낫다.

### PaymentBody 를 한 번 잃었다 (작업 사고)
역주입 원복에 `git checkout --` 을 썼는데 그 파일의 변경은 아직 커밋 전이라 HEAD 로 되돌아가며
진입점 마운트가 통째로 사라졌다. 그물 축 ⓡ 이 즉시 잡아 백업본으로 복구했다(HEAD 와의 차이가
정확히 내 변경 하나뿐임을 diff 로 확인). 역주입 원복은 백업 파일에서만 한다.

## 점검 전파 (2026-09-11 김치)

### 설계 F 의 '수정 전 값' 가정이 문장 그대로는 기대 숫자를 못 낸다
설계는 "9/11 의 현재 행을 수정 전 9/10 값의 사본으로 간주" 하라 했는데, 그 문장을 값까지 사본으로
읽으면 9/11 의 모든 행이 휴리스틱상 이월로 판정되어 5층 상단이 1.5 에서 **3 으로 딸려 올라가고**
총량이 20 이 된다. 설계가 못박은 기대치(5층 상단 1.5 · 총 18.5)와 어긋난다. 그날 5층 상단은
운영자가 실제로 센 값이라 움직이면 안 되는 자리다(메모도 '위치별 점검 (5층 김치냉장고 상단)').

그래서 사본을 **위치 집합**으로 읽었다. 수정 전 그 점검이 갖고 있던 자리 목록 = 뒤 점검이 지금 가진
자리 목록이고, 그 자리들의 값은 수정 후 값 그대로(수정이 안 건드린 자리), 집합 밖으로 새로 생긴
행들의 합은 허브가 갖고 있었다(총량 보존 · 허브 칸은 파생값이라 차액을 흡수하는 자리).
9/10 에 대입하면 수정 전 = 5층 상단 3 · 5층 하단 17 이고, 이는 저장된 수정 후 값
(5층 상단 3 그대로 · 하단 7 = 17 − (4+6))과 정확히 맞물린다. 예행 출력이 설계의 기대 숫자를
그대로 낸다. 가정이므로 스크립트가 매 실행마다 세 줄로 찍는다.

### 9/11 의 두 행은 서로 다른 저장에서 왔다
memo 는 '5층 김치냉장고 상단' 인데 마커는 5층 하단에 붙어 있어 한동안 앞뒤가 안 맞았다. 6시간 머지
때문이다 — 상단 점검이 점검을 만들고(memo 고정), 이어진 허브 자기 점검이 같은 점검에 머지되면서
하단 행을 17 로 덮고 마커 +17 을 남겼다. memo 는 생성 시점의 것이라 나중 머지를 말해 주지 않는다.

### 이월 코드의 0 클램프는 휴리스틱과 비대칭이다
`additionsSinceCheckByLocation` 반영은 `Math.max(0, …)` 로 클램프하는데 판정식은 클램프하지 않는다.
클램프된 행은 휴리스틱이 어긋나 실측으로 보이고 그 위치에서 전파가 멈춘다 — 보수 방향이라 그대로
뒀다. 대조 그물도 같은 방향만 눈감는다(기대가 음수인데 저장이 0 이면 위반 아님).

### 표식을 값 변경과 같은 자리에서 찍기로 한 이유
처음엔 새로 만든 행에만 `carried` 를 찍었다. 그랬더니 전파가 **덮어쓴** 행(9/11 5층 하단 17→7)과
실측으로 보고 **멈춘** 행(5층 상단 1.5)이 구식(null)으로 남아, 다음 수정 때 같은 판정을 휴리스틱으로
다시 추측하게 됐다. 판정은 이미 계획이 한 것이므로 그 결과를 그대로 박는 편이 맞다.
- 그래서 `ShiftRow.locs[].carried` 를 계획이 채우고 `applyShiftRows(markCarried)` 가 세 갈래
  (생성·덮어쓰기·정지) 전부에 쓴다. 입수·구매 조정은 판정을 하지 않으므로 그 칸이 비어 있고,
  적용부는 `carried !== undefined` 일 때만 쓴다 — 종전 동작 무변동.
- **값이 안 바뀐 위치도 판정 대상(live)이다.** 안 그러면 백필을 적용한 뒤 다시 돌려도 before == after
  라 아무것도 안 나와서 표식이 영원히 안 찍힌다. 값이 같고 표식도 같으면 그때 행이 안 나온다.
- 확인창은 값이 바뀌는 행만 나열한다. 표식 행까지 나열하면 '17kg → 17kg' 이 뜬다.

### 조사 대신 화살표 (§29)
`17kg 에서 7kg 으로` 는 단위가 없는 품목에서 `4개으로` 가 된다. §29 가 화살표를 '값의 전환 표시'
하나에만 허용하므로 `17kg → 7kg` 으로 통일했다. 같은 결함이 공용 정본 `lib/stockShiftAsk` 두 자리
(askShiftRows·askShiftRowsRequired)에도 있어 함께 고쳤다 — 지출 수정·삭제·수령 취소 물음이 그 문장을
쓴다. 문장을 보고 있던 배선·진리표는 없었다.

## 보관 위치 계층화 (2026-09-14)

### 왜 자기참조 parentId 인가
경로 문자열은 이름 하나 바꾸면 자손 전부를 다시 써야 하고 FK 가 없어 고아 경로가 난다. 별도 그룹
테이블은 "4층 주방엔 냉장고 말고도 물건이 있다"는 운영자 예시와 스키마가 충돌한다(그룹은 재고를
못 가진다). 자기참조만이 허브·잔량·숨김·박제·전파 다섯 기제에 무접촉이다 — 전부 `storageLocationId`
집합 위에서 돌고 그 집합은 트리와 무관하게 평면이다. 트리는 **표시·그룹핑 층**일 뿐이다.

### 왜 모든 노드가 재고를 갖나
잎만 허용하면 김치냉장고를 4층 주방 아래로 넣는 순간 4층 주방의 라면·세제 9품목을 새 잎으로 옮기는
이관 점검이 필요하다. 데이터 땜빵이다. 검증할 불변식이 "재고는 잎에만"이 아니라 순환 없음·깊이
상한·형제 이름 유일 셋으로 준다.

### 왜 공백 결합인가
`4층 김치냉장고` 아래 `상단` 의 전체 경로가 `4층 김치냉장고 상단` — 오늘 평면 이름과 글자가 같다.
트리를 안 만든 영업장은 픽셀 하나 안 바뀌고, 만든 영업장도 보던 문자열 그대로다. ` > ` 는 칩
10.5px 에서 잡음이고 §29 가 그 기호를 설정 경로에 둔다. 구조는 트리 화면이 들여쓰기로 말한다.

### 왜 체인 저장인가 (원자 저장은 후속)
서버 무변경으로 같은 날 연속 점검 병합 정본을 그대로 탄다. 마커 보존과 허브 게이트를 그대로 받는다.
비원자성(상단 저장 후 하단 실패)은 실패 행을 붉게 남기는 것으로 받고, 실측되면 원자 저장으로 올린다.
그때는 `detectHubShort` 를 합산형으로 확장해야 해서 쌀 사건 봉합 자리를 건드린다.

### 허브 실측이 같은 저장에 있으면 게이트를 건너뛰는 이유
지금 세는 허브 값이 곧 정답이라 부족 판정이 뜻이 없다. HubShortDialog 의 2번 선택지("허브 실측")를
앞당겨 적용한 것이다. 허브 실측이 없으면 게이트는 오늘과 똑같이 선다.

### 접힘 비영속·기본 펼침
9/11 사고가 "칸을 깜빡한" 사고다. 접힌 채로 다음 날 열리면 같은 사고가 난다. 접힌 그룹에 입력값이
있으면 헤더에 `입력 N` 캡션.

### 드래그는 형제 안 순서만
부모 바꾸기는 `옮기기` Modal. 모바일 402px 에서 드래그로 부모를 바꾸면 오조작이 잦다.

### 패널이 확신 없다고 적은 것
- 체인 저장 비원자성(위).
- HUB_SHORT 뒤 이동 재시도가 base 에 마커를 안 싣는 기존 결함(`actions.ts:873` 주석). 트리 패널이
  한 품목 다중 위치 저장을 흔하게 만들어 더 자주 드러날 수 있다. 후속 그물 후보.
- 루트 부분 유니크 인덱스가 Prisma 스키마 밖에 산다. `db push` 를 돌리면 사라진다. 서버 검사가 1차.
- iOS 커스텀 드롭다운이 위치 20개 넘는 영업장에서 내부 스크롤 필요 → §30 오버레이 규칙 대상 가능.

### 3·4·8단계 시공에서 정한 것 (2026-09-14)

**pathName 은 name 을 바꾼 게 아니라 더한 칸이다.** `StorageLocationItem` 에 `pathName` 을 새로
얹고 `name` 은 트리 편집용 원본 한 칸으로 남겼다. 표시 자리는 전부 pathName 을 읽는다. 그래서
개요 JSON 바이트 대조의 차이가 **추가된 43줄뿐**이고(지운 줄·바뀐 줄 0), 그 43개 값이 전부
같은 행의 name 과 글자가 같다. name 을 pathName 으로 덮었다면 바이트는 같았겠지만 5·6단계
트리 편집기가 읽을 원본 이름이 사라진다.

**허브 폴백은 sortOrder 정렬 사본으로 넘긴다.** `locations` 의 정렬을 DFS 로 바꿨는데,
`resolveHubSync` 의 3순위는 "sortOrder 첫 열린 링크"이고 그 쌍둥이 정의가
`ledgerShift.resolveItemHubLocationId` 에 또 있다. 둘이 갈리면 위치 미지정 입수의 귀속처가
화면과 서버에서 달라진다. 그래서 표시 순서만 DFS 로 바꾸고 판정에는 sortOrder 사본을 준다.

**overview 의 배치 조회는 허브 한 줄 → 영업장 위치 전체.** 표기 경로는 조상까지 있어야 만드는데
조상이 그 품목에 링크돼 있으리란 보장이 없다. findFirst 를 findMany 로 넓혔을 뿐이라 쿼리 수는 그대로다.

**비품 배정 이력 되돌리기는 pathName 우선, 이름은 유일할 때만 폴백.** 이력 라벨은 사람이 읽는
문자열인데 `resolvePlace` 가 그걸로 위치를 되찾는다. 형제 중복 이름(`상단` 이 냉장고마다 하나)이
생기는 순간 평면 name 매칭은 엉뚱한 위치로 비품을 보낸다 — 표시 오염이 아니라 데이터 오염이다.
트리 전에 쌓인 옛 이력은 라벨이 평면 이름이라 pathName 으로 못 찾으므로, **이름이 유일할 때만**
받는 폴백을 뒀다. 둘 이상이면 찍지 않고 실패시킨다.

**삭제는 force 로도 하위를 거부한다.** `parentId` 가 `onDelete: SetNull` 이라 부모를 지우면 자식이
조용히 루트로 튀어 오르고, 그 순간 자식의 pathName 이 통째로 바뀌어 화면에서 딴 위치가 된다.
기존 2단계 영향 고지(기록 있음 → 강제 삭제)는 하위가 없을 때만 그대로 선다.

**이동 undo 에 siblingIds 를 실었다.** 옮기면 새 형제들 맨 뒤에 붙으므로, 부모만 되돌리면 옛
자리로 안 돌아간다. `move → rename → reorder` 세 번을 **같은 액션들로** 부르면 완전히 원복된다 —
되돌리기 전용 액션은 만들지 않았다.

**첫 위치의 sortOrder 가 1 에서 0 으로.** 종전 create 는 `(max ?? 0) + 1` 이라 첫 행이 1 이었는데
reorder 는 0-based 로 쓴다. 형제 안 최댓값 기준(`max(-1) + 1`)으로 바꿔 두 경로가 같은 축이 됐다.

**위치 관리 모달만 name 축 예외.** 거기서 고치는 대상이 name(한 칸) 자체이고 구조는 트리 화면이
들여쓰기로 말한다. 8단계 그물도 그 블록만 검사에서 뺐다 — 5단계 시공자가 여기를 트리로 바꾼다.

**서버 액션 진리표 대신 소스 가드.** 이동·생성·삭제·순서의 거부는 DB 가 있어야 도는 서버 액션이라
순수 진리표로 못 덮는다. 판정 함수 자체는 `test-location-tree` 가 지키므로,
`check-location-actions-wiring.mjs` 는 **액션이 그 판정을 실제로 부르는가** 다섯 축만 본다.
판정이 멀쩡해도 호출이 빠지면 순환·깊이 초과·형제 중복이 그대로 저장되고, 값 대조로는 안 잡힌다.

### 5·6·7단계 시공에서 정한 것 (2026-09-14)

**트리 행만 name 축이다 — 그리고 그 예외의 전제를 그물이 검사한다.** 위치 관리 모달 행·점검 패널
그룹 헤더·위치 드롭다운 행은 그 노드 한 칸의 `name` 을 찍는다. 근거는 하나뿐이다 — 조상 경로를
들여쓰기가 말한다. 그래서 `check-location-name-axis` 에 4'항을 더해 **세 자리가 실제로
`LOC_INDENT_PX` 로 들여쓰는가**와 **점검 패널의 그룹 헤더와 품목 행이 같은 자리에 서는가**를 본다.
들여쓰기가 빠지면 `상단` 이 어느 냉장고인지 화면이 말을 못 하고, 그 순간 name 축이 근거를 잃는다.
예외를 허용하는 그물은 예외의 전제를 함께 지켜야 한다.

**금지 식별자에 `selectedLoc` 과 옵셔널 체이닝을 더했다.** 점검 memo `위치별 점검 (X)` 는 화면
글자가 아니라 **저장 문자열이자 백필 매칭 키**(`backfill-check-carried-flip` 이 X 로 실측 위치를
되찾는다)인데 `selectedLoc?.name` 이라 그물 밖이었다. 목록에 넣으면서 `?.` 도 함께 보게 했다 —
한 글자만 비켜 가면 조용히 뚫리는 그물이었다(역주입으로 실증).

**옮기기 적용취소는 rename 이 먼저다.** 설계가 준 `move → rename → reorder` 는 앞부분 떼기로
`4층 김치냉장고 상단` 이 `상단` 이 된 노드에서 깨진다 — 먼저 옛 부모로 되돌리면 옛 형제 집합의
`상단` 과 이름이 겹쳐 서버가 거부하고 원복이 통째로 멈춘다. 이름을 먼저 되돌려 놓으면 그 충돌이
서지 않는다. 세 걸음이라 중간에 멈출 수 있으므로 **어디까지 되돌렸는지를 인라인에 남긴다** —
토스트는 사라지고, 절반만 돌아간 상태를 화면이 말하지 않으면 무엇을 손으로 고쳐야 하는지 모른다.

**순서 저장은 형제 집합 단위다.** 종전 모달은 루트 전체 DFS 목록을 `reorderStorageLocations(null, …)`
로 넘겼다. 자식이 하나라도 생기면 그 목록에 자식이 섞여 루트 전체성 검사(`total !== ids.length`)에
걸리고, 화면에는 "위치 목록이 최신이 아닙니다" 만 뜬다. ▲▼도 드래그도 **형제 배열**에서 자리를
센다. 목록이 DFS 라 화면상 이웃 행이 형제가 아니고, 드래그가 옮기는 것은 행 하나가 아니라 그
**서브트리 한 덩어리**다(`locOwnerAt`·`locReflow`). 다른 가지 위에 놓으면 아무 일도 안 한다 —
부모를 바꾸는 것은 드래그의 일이 아니다(402px 오조작).

**체인은 순차여야 한다 — 병렬은 '성공하면서 틀리는' 결함이다.** 한 저장이 (품목, 위치) 여러 쌍을
담으므로, 같은 품목의 두 번째 칸부터는 **이 저장에서 만든 점검 id**(`chainIdsRef`)에 얹어야 한다.
병렬로 쏘면 칸마다 새 점검이 서고 그 각각이 직전 점검을 base 로 잡아 앞 칸의 실측이 사라진다.
화면에는 '저장됨' 만 뜨고 총량도 그럴듯하다. 그래서 `check-draft-lifecycle` 에 ⑥항(순차·허브
마지막·이어 붙이기)을 신설했다 — 이 셋은 값 대조로는 영원히 안 잡힌다.

**HUB_SHORT 도 멈춤이지 건너뜀이 아니다.** 종전(병렬)은 부족 건들을 모아 큐로 넘기고 나머지는
이미 저장된 뒤였다. 체인에서는 그 자리에서 멈추고 `restUnitsRef` 에 나머지를 보관한 뒤, 팝업이
그 한 건을 처리하면 이어 달린다. 나머지를 버리면 팝업을 처리한 뒤 아래 칸의 실측이 조용히 사라진다.
중단(`보충으로 돌아가기`)이면 남은 행을 저장하지 않고 **어디까지 갔는지**를 진행 박스에 남긴다.

**허브 실측이 같은 저장에 있을 때만 클램프를 연다.** `allowHubClamp` 를 조건 없이 켜면 쌀 사건의
조용한 0 클램프가 그대로 되살아난다. 판정은 '그 품목의 **차감 대상 허브**(`locations.find(isHub)`)를
이번 저장에서 실측하는가' 다 — 허브가 둘인 영업장에서 `isHubUnit` 만 보면 다른 허브를 재고 있어도
게이트가 열린다. 그물이 그 조건식을 그대로 본다.

**목업과 어긋나게 둔 자리 셋(의도).**
(1) 참고줄은 목업의 `직전 잔량` 이 아니라 코드의 **`저장된 잔량`** 을 지켰다 — 같은 날(9/14)
운영자 신고로 "입력칸에는 임시저장본이 들어 있을 수 있어 어느 숫자가 저장본인지 행마다 말해야
한다"고 바꾼 문구다. 되돌리면 회귀다(`check-draft-lifecycle` 이 그 문구를 지킨다).
(2) 허브 배지 라벨은 목업의 `창고` 가 아니라 **`기본 창고`** 로 통일했다. 같은 개념을 두 화면에서
두 단어로 부르면 §29 어휘 축이 갈린다(이 파일의 기존 문구도 전부 '기본 창고').
(3) 그 밖(들여쓰기 12px·그룹 헤더 문법·행 요소 순서·`3건 저장` 라벨·드롭다운 44px 행과 품목 수)은
목업 그대로다.

**`saveProgress` 는 비원자 저장의 값이다.** 체인은 원자가 아니므로 "몇 건 중 몇 건까지, 어디서
멈췄나"가 실패 문구보다 먼저 온다. 자리는 실패 목록과 같은 **스크롤 밖 고정 영역**이다 — 행이
열이면 스크롤 안의 박스는 뷰포트 밖으로 밀려 "눌렀는데 아무 말이 없다"가 다시 난다(§27.2 채널 하나).

**행 액션은 팝오버 메뉴다.** 402px 에서 한 행에 여섯 액션(하위 추가·이름 바꾸기·옮기기·위로·
아래로·기본 창고·삭제)을 늘어놓을 자리가 없다. 패널은 `usePopoverAnchor` + portal 로 화면 기준에
띄운다 — 모달 본문이 세로 스크롤 컨테이너라 형제로 두면 아래쪽 행에서 바닥에 잘린다(CountrySelect
가 먼저 이 방식으로 푼 자리다). 메뉴 행 문법은 DisplayFieldsMenu 정본(44px·r-lg·hover --cream-soft).

**드리프트 그물이 값이 아니라 모양을 본다.** 순환·깊이 초과·형제 중복이 저장되면 화면은
`buildTree` 가 고리를 끊어 루트로 올려 **정상처럼 보여 준다**(한 행도 안 잃기 위한 설계다).
수량은 내내 맞으므로 어떤 대조 검사도 이것을 잡지 못한다. `check-location-tree-drift` 는 위반 행의
id 와 **표기 경로**를 함께 찍는다 — id 만으로는 어느 칸인지 사람이 못 찾는다.

### 독립 검수 반영 — 서버 몫 (2026-09-14)

**형제 이름 유일만으로는 같은 칩이 둘 뜬다.** 형제 검사는 같은 부모 안만 본다. 오늘 DB 의 루트
`4층 김치냉장고 상단` 과, 트리로 만든 `4층 김치냉장고` 아래 `상단` 은 부모가 달라 둘 다 통과하면서
전체 이름이 글자까지 같아진다 — 이관 과도기에 **실제로 생기는 모양**이다(실DB 14행이 정확히 그
평면 이름들이다). 그러면 화면에 같은 칩이 둘 뜨고 비품 배정 이력 역조회(`byPathName`)가 중복이라
아무것도 못 찍어 되돌리기가 멈춘다. 그래서 넷째 불변식 `conflictingPathName` 을 뒀다. 판정은
**바뀌는 쪽(자기 + 자손 전부)의 새 경로**를 나머지 전부와 대조하는 것이다 — 옮기면 자손 경로가
통째로 바뀌므로 옮긴 노드만 보면 반쪽이다. 쿼리는 0 이다(쓰기 셋이 이미 `rows` 를 쥐고 있다).

**정상 이관 경로는 막지 않는다.** 평면 루트를 새 부모 아래로 **떼기로 옮기는 것**은 그 노드 자신이
바뀌는 쪽이라 비교군에서 빠져 통과한다. 막히는 것은 평면 루트를 그대로 둔 채 같은 전체 이름을
**새로 만드는** 경우뿐이다. 실DB 행으로 네 단계를 전부 확인했다.

**허브 3순위는 화면 순서(DFS)여야 뜻이 선다.** sortOrder 가 형제 사이 순서가 되면서 다른 부모의
값끼리 비교하는 '첫 열린 링크' 는 뜻을 잃었다 — 루트 `4층 주방`(1)과 그 자식 `선반`(0)에만 링크된
품목은 미지정 입수가 `선반` 으로 가는데 카드는 `4층 주방` 을 먼저 보인다. **쌍둥이 두 정의를 같은
날 함께** 바꿨다. 화면 쪽은 sortOrder 사본을 없애 DFS 순 `locations` 를 그대로 넘기고, 서버 쪽은
기본 허브 findFirst 와 첫 링크 findFirst 두 쿼리를 영업장 위치 **한 번 읽기**로 합쳤다(쿼리 −1).

**그 변경은 데이터 대조로 증명이 안 된다.** 오늘 데이터는 전부 루트라 DFS 순 == sortOrder 순이다.
전 품목 허브 판정을 직렬화해 전후 바이트 동일을 확인했지만, 그것은 '안 깨졌다' 는 증거일 뿐
'갈리지 않는다' 는 증거가 아니다. 트리를 만든 영업장이 생기는 날 처음 드러나므로
`check-stock-ledger-parity` 에 **소스 가드**를 뒀다 — 두 정의를 함께 보고 한쪽만 되돌리면 빨강이다.

**첫 링크를 DB 에서 다시 조회하지 않는다.** 종전 3순위는 `linkedIds` 를 받고도 링크를 다시 읽었다.
`closeItemLocation` 은 숨기는 위치를 뺀 집합(`remainOpen`)을 넘기는데, 그 재조회는 아직
`closedAt` 이 안 찍힌 **그 위치를 새 허브로 도로 고를 수 있었다.** 이제 넘어온 집합 안에서만 고른다
— 함수의 계약("반드시 링크된 위치 중에서")과 같은 말이다.

**색인을 lib 로 내린 이유는 테스트가 붙을 자리가 없었기 때문이다.** `indexLocations` 가 prisma 를
문 파일에 있어 어떤 진리표도 그것을 보지 않았다. 그 사이 `pathName: id => byId.get(id)?.row.name`
한 줄이면 **감지망 전체가 초록인 채** 열세 자리가 평면 이름으로 돌아간다(역주입으로 실증 — 화면 축
그물은 OK 를 찍고 새 진리표만 4건 빨강). 예외를 허용하는 그물은 예외의 전제를 함께 지켜야 하고,
축을 보는 그물은 **축을 만드는 자리**도 함께 봐야 한다.

## 재고 기본 보기를 위치별로 (2026-09-15)

**저장 키를 `stayeum-inventory-view2` 로 승격했다.** 옛 키(`stayeum-inventory-view`)에 남은 값은
사용자의 선택이 아니라 **부작용**이다. 인라인 위치별 패널의 `onClose` 가 `changeView('item')` 이라
체인 저장에 성공할 때마다 아이템별로 튕겼고, `changeView` 가 그 길로 localStorage 에 `'item'` 을
덮어썼다. 즉 위치별로 일을 많이 한 기기일수록 `'item'` 이 확실하게 박혀 있다. 그 값을 그대로 읽으면
기본을 위치별로 바꾼 오늘 변경이 **기존 기기에서는 하나도 안 보인다.** 마이그레이션 코드를 쓰는
대신 키를 바꿔 버렸다 — 옛 값에서 건질 신호가 없으므로 옮길 것도 없다.

**복원 축도 뒤집었다.** 종전은 `=== 'location'` 이면 위치별로 갔다(기본이 아이템별이었으므로).
이제는 `=== 'item'` 일 때만 아이템별로 간다. 저장값이 없는 기기·새 프로필은 위치별로 연다.

**초기값을 URL 로 가른다.** 서버 렌더에 `'item'` 을 고정하고 마운트 후 바꾸면 기본 보기가 매번
한 프레임 아이템별로 번쩍인다. 그래서 초기값 식 자체를 `deepLinked ? 'item' : 'location'` 으로 두고,
서버·클라가 **같은 URL** 을 보므로 첫 렌더가 갈리지 않게 했다(하이드레이션 #418 회피). 딥링크 판정은
`?focus=`(종 알림 `#inventory-pending`·`#inventory-lowstock` — 그 섹션들은 아이템별 분기 안에만 있다)
와 `?q=`(통합검색 시딩) 두 축이다. localStorage 를 읽는 복원은 여전히 마운트 후 1회다.

**`deepLinked` 를 ref 로 굳힌 이유.** `useFocusSection` 이 `?focus=` 를 **즉시 소진**한다(안 지우면
같은 섹션의 다음 알림이 URL 동일로 무반응이 된다). 그래서 다음 렌더의 `deepLinked` 는 false 다.
복원 effect 가 그 값을 직접 보면 마운트 1회 의미가 깨지므로 마운트 시점 값을 ref 로 들고 본다.

**인라인 패널은 저장 후 닫지 않는다.** 기본 보기가 된 이상 닫을 뒤가 없다. 대신 성공 토스트
`N건 저장됨` 을 띄우고 입력을 **반드시** 비운다. 안 비우면 dirty 가 남아 `N건 저장` 이 다시 켜지고,
값을 조금만 고쳐도 두 번째 저장이 선다(`createStockCheck` 멱등창 20초, `updateStockCheck` 는 같은
patch 만 무시한다). `onClose` 호출 자리는 `doSave`·`onHubShortResolved` 각각 **한 곳**으로 유지했다
— `check-draft-lifecycle` ① 이 그 수를 세고, 자리가 둘이면 한쪽이 '실패 0' 조건을 비껴간다.
인라인에서는 prop 자체를 안 넘기고 타입을 optional(기본값 no-op)로 뒀다.

**스켈레톤을 패널 골격으로.** `loading.tsx` 본문의 카드 5장은 아이템별 모양이라 로딩에서 로디드로 갈 때
목록이 패널로 바뀌는 점프가 된다. 아이템별은 딥링크로만 첫 화면이 되는 소수 경로라, §21 결정표 3
(라우트 전환 = 본문 스켈레톤만)에서 다수 쪽 골격을 그리는 것이 맞다.


## 점검 위치 전체 (2026-09-15)

**'전체' 는 새 기능이 아니라 범위 하나다.** 목록(`getStorageLocations`)이 이미 DFS 순이라 숲 전체가
곧 트리 순서고, 그룹·행·`buildUnits`·체인은 전부 (품목, 위치) 쌍 위에서 돈다. 그래서 `scope` 만
`locs` 로 넓히면 나머지가 그대로 성립한다 — 허브 마지막 정렬, `allowHubClamp`(품목 단위), 멈춘 자리,
HUB_SHORT 이어 달리기는 한 줄도 안 건드렸다. 새 저장 경로를 만들지 않은 것이 이 시공의 요점이다.

**센티널을 쓴 이유.** '전체'를 위치 id 자리에 앉히려면 비-id 값이 필요하다. 같은 파일 카테고리
세그먼트가 이미 `'__all__'` 을 쓰고 있어 그 문법을 그대로 가져왔다(`ALL_LOCATIONS`). 별도 boolean
상태를 두면 `locId` 와 두 축이 되어 `!locId` 로 막아 둔 자리(임시저장·저장 버튼 disabled)가 전부
갈린다.

**memo 는 괄호 안에 넣지 않았다.** `위치별 점검 (X)` 의 X 는 화면 글자가 아니라 **백필 매칭 키**다
(`backfill-check-carried-flip` 이 `^위치별 점검 \((.+)\)$` 로 실측 위치를 되찾는다). `(전체)` 로
적으면 누군가 '전체' 라는 이름의 위치를 만드는 순간 그 칸의 점검으로 되찾아 간다. 저장 문자열은
지나간 기록이라 되돌릴 수도 없다. 그래서 접두 패턴 **밖**의 다른 문자열 `전체 위치 점검` 이다.

**들여쓰기 기준식이 조용히 무너지는 자리였다.** 기준은 `selectedLoc.depth` 인데 '전체'에는 고른
노드가 없다. `?? node.depth` 폴백에 그대로 걸리면 모든 행이 `depth - depth = 0` 단이 되어 트리가
평면으로 깔린다. 값은 하나도 안 틀리고 화면만 무너지는 종류라 대조 검사로는 안 잡힌다. 더 나쁜 것은
**트리 행이 `name`(한 칸 이름)을 찍어도 되는 근거가 들여쓰기 하나**라는 점이다 — 들여쓰기가 사라지면
`상단` 이 어느 냉장고인지 화면이 말을 못 한다(9/11 김치 사고의 모양). 그래서 기준을 루트 깊이 1 로
박고, 그 식 자체를 `check-location-name-axis` 4' 단언으로 세웠다.

**드래프트 읽기를 한 호출로 합쳤다.** `getLocationDrafts(id)` 는 위치 하나에 쿼리 2개고, 패널은
범위의 위치 수만큼 그것을 불렀다. 서브트리 두세 칸일 때는 안 보이던 비용이 '전체'(18칸)에서 36
왕복이 된다. `getLocationDraftsFor(ids)` 는 `locationId in ids` 한 번 + `locationId null` 한 번,
병합 규칙(savedAt 최신 우선)은 종전과 같다. `getLocationDrafts` 는 지우지 않았다 — 임시저장 비우기의
적용취소 스냅샷이 여전히 부르고, 그쪽은 쌍 단위라 축이 다르다.

**진행 숫자를 세운 이유.** 저장은 여전히 쌍 단위 순차다(병렬로 쏘면 같은 품목의 칸마다 새 점검이
서서 앞 칸 실측이 사라진다 — 체인 저장 1항). 45쌍이면 저장 45 + 드래프트 삭제 45 = 서버 왕복 90회가
직렬로 흐른다. 그동안 화면이 `저장 중…` 한 마디뿐이면 멈춘 것과 구별이 안 되고, 운영자는 두 번째
누르기를 시도하게 된다(`savingRef` 가 막긴 하지만 화면이 거짓말을 하는 것은 그대로다). 그래서
기존 `saveProgress` 를 재사용해 **멈춘 자리(`stoppedAt`)가 빈 문자열이면 진행 중**으로 읽고 중립
톤(`--cream-2`)으로 세었다. 상태를 하나 더 만들지 않은 것은 같은 사실을 말하는 자리가 둘이 되면
언젠가 갈리기 때문이다. 실패하면 같은 자리를 종전 경고 문구가 덮는다.

**이중 차감 물음 목록을 5건에서 잘랐다.** 그 목록은 버튼 줄 위 **스크롤 밖 고정 영역**에 산다(§14
영향 목록, 실패 목록과 같은 자리). 서브트리 서너 칸에서는 길어야 두세 줄이던 것이 '전체'에서는
열 줄도 나올 수 있고, 그러면 목록이 확인 버튼 두 개를 화면 밖으로 밀어낸다 — 묻는 것이 아니라
막는 것이 된다.

**'전체' 카운트는 품목의 중복 없는 수다.** 한 품목이 여러 칸에 서므로 칸 합계(쌍)를 적으면 28품목이
45로 읽힌다. 선택기의 다른 행들이 전부 '그 칸의 품목 수' 를 말하는데 맨 윗 행만 쌍을 말하면 같은
열의 숫자가 두 뜻이 된다.


## 패널 안 옮기기 (2026-09-15)

**깨지는 경우는 하나뿐이다.** 점검 시점 정렬은 옮김이 점검 전이든 후든 정합이다. 남은 구멍은
'옮기기 전에 센 값을 옮긴 뒤에 저장' 하나고, 그래서 봉합도 하나다 — **옮긴 순간 그 두 쌍
(품목×출발지, 품목×도착지)의 미저장 입력은 무효**. 서버 규칙을 손대지 않고 화면에서 막은 이유가
이것이다. 저장 경로에 조건을 하나 더 얹으면 체인·허브 게이트·머지 판정이 전부 그 조건을 통과해야
하는데, 무효로 만들어야 할 값은 애초에 서버에 도달하지 않는 편이 낫다.

**비우는 대상이 둘이다.** 메모리 입력값(`beforeQtys`·`afterQtys`)만 비우면 다음 진입에서 서버
임시저장이 그 값을 되살린다 — 9/14 신고("실저장을 눌렀는데 임시저장이 남아 있다")와 정확히 같은
클래스다. 그래서 `deleteStockCheckDraft` 를 두 쌍 모두에 부르고 **반환값을 읽는다**. 도착지가 지금
범위 밖이어도 키가 (품목, 위치) 쌍이라 그대로 지워진다.

**되돌릴 값은 서버에서 읽는다(정정, 2026-09-15 장부 검수).** 첫 판은 스냅샷의 `draft` 를 메모리
`rowDrafts` 에서 잡았다. 그런데 `rowDrafts` 는 **지금 범위의 쌍만** 담고 지우기는 (품목, 위치) 키로
무조건 간다 — 4층 범위에서 5층으로 옮기면 (품목, 5층) 드래프트는 지워지고 되돌릴 값이 화면에
없었다. 아이템별(`null`) 드래프트의 그 위치 값도 `deleteStockCheckDraft` 가 함께 벗긴다. 그래서
지우기 **전에** `getLocationDraftsFor([from, to])` 로 두 쌍을 읽어 스냅샷에 담고 거기서 되쓴다
(`handleClearLocDrafts` 가 이미 쓰는 문법 그대로다).

**비웠으면 되돌릴 길이 있어야 한다.** 순서는 `deleteStockCheck(이동 점검, 기대값)` → **빈 칸만**
입력 문자열 복원 → **있었던** 드래프트만 재저장이다. 없던 행까지 되쓰면 없던 임시저장이 생기고,
6초 사이에 새로 적은 값을 덮으면 방금 센 숫자가 옮기기 전 값으로 되돌아간다. 늦게 눌려 위치를
옮긴 뒤면 패널 상태는 안 건드린다(`locIdRef` 갈림). 두 쌍을 비운 뒤 남은 드래프트가 없으면 발끝
§12 칩도 내린다 — `settleChain` 의 전수 대조와 같은 축이고, 안 내리면 임시저장이 하나도 없는데
칩이 '임시저장 후 수정됨' 으로 남는다.

**적용취소가 남의 실측을 지우던 자리(정정).** 이동 점검 T 는 같은 날·6시간 안이면 뒤따르는 위치
점검의 머지 대상이 된다(`lastCheckId` 가 T 라 `updateStockCheck` 로 T 에 얹히고 `createdAt` 이
갱신된다). 그 상태에서 토스트의 적용취소가 `deleteStockCheck(T)` 를 무조건 부르면 방금 저장한
실측까지 사라진다. 그래서 `transferLocationStock` 이 `createdAtMs`·`rowCount`·`markerSum` 을 함께
돌려주고, 적용취소가 그 셋을 `expect` 로 되넘긴다. 서버는 지우기 전에 T 를 다시 읽어 셋이 같을
때만 지우고, 아니면 `'이 이동 위에 점검이 얹혀 되돌릴 수 없습니다.'` 로 끝낸다. **이동 점검을
머지 대상에서 빼는 안(b)은 운영자 결정 대상이라 손대지 않았다**(후속 — `knowledge/open-issues.md`).

**같은 판정에 `carried` 를 넣었다.** `updateStockCheck` 의 멱등 가드가 잔량·마커만 비교하면, 이동
점검 위에 얹히는 실측이 마침 `장부 − N` 그대로일 때(흔하다) no-op 으로 빠져 그 행에 실측 표식이
영영 안 박힌다. 표식이 없으면 전파가 휴리스틱으로 후퇴하고 그 행은 파생으로 덮여도 되는 행이 된다.

**완료 통지의 주인을 하나로 정했다.** `TransferStockModal` 은 원래 제 손으로 성공 토스트와 적용취소를
띄운다. 패널 진입에서 그대로 두면 모달이 "이동 완료", 패널이 "두 칸 입력 비움" 을 따로 말해 같은
사건에 토스트가 둘 선다(§27.2). **패널의 두 진입**(행 `다른 곳으로` · 헤더 `위치 이동`)에서 모달의
토스트를 접고(`silent`) 패널이 한 문장으로 말한다. 첫 판은 행 진입(`lockItem`)만 접었는데, 그러면
같은 구멍(두 쌍 미무효화)이 헤더 버튼에 그대로 남는다 — 두 진입을 같은 `handleTransferDone` 에
태우면서 축을 `lockItem`(품목 고정)과 `silent`(통지 주인)로 갈랐다. 아이템별 폼 등 다른 진입은
종전대로 모달이 마감한다. 접미 `· 두 칸 입력 비움` 은 실제로 비운 것이 있을 때만 붙인다.

**출발지 실측은 선택이고 기본은 빈칸이다.** placeholder 에 0 을 보이면 '남은 게 없다'는 실측으로
읽힌다. 안 적으면 종전과 완전히 같고(`장부 − N`), 적으면 그 값이 절대값이 된다. 차이는 삼키지 않고
미리보기 박스가 말한다 — 적으면 `소모로 기록`, 많으면 `입수 기록 누락일 수 있습니다`. 조용한 클램프가
쌀 사건의 원형이라 여기서도 숫자를 감추지 않는다.

**도착지 행의 `carried` 는 null 이다.** 이동 행은 이월도 실측도 아닌 '이월 + 델타' 다. `true` 면
`planCheckPropagation` 이 파생식으로 덮어써 옮긴 델타를 지우고, `false` 면 세지도 않은 값이 실측으로
선언돼 그 위치의 전파가 영구히 멈춘다. 값은 내내 맞고 표식만 틀리는 종류라 데이터 대조로는 안
잡힌다 — 그래서 `check-stock-ledger-parity` 소스 가드에 넣었다.

**정정 — null 은 '문을 열어 두는' 것이 아니다(2026-09-15 장부 검수).** 첫 판의 설명은 표식을 안
찍으면 판단이 유보된다는 뜻으로 읽혔는데, 실제로는 유보가 없다. `lib/stockLedger` 의 휴리스틱
(314~325)은 저장값이 파생식(직전 + 입수 − 차감)과 같을 때만 이월로 보는데 이동 행은 옮긴 N 만큼
달라 **항상 실측 판정**이 나고, 그 행은 **첫 전파에서 `carried: false` 로 확정**된다. 그래서 지금
규칙이 맞다는 근거는 '나중에 정한다' 가 아니라 '확정되는 값이 실측으로도 맞다' 다. 델타 행을
planner 가 '옮김' 으로 알아보는 표현은 아직 없다(후속 — `knowledge/open-issues.md`).

**조립부를 `lib/transferCheck.ts` 로 뺐다.** `transferCheckCreateData` 는 순수 조립인데 `actions.ts`
안에 있어 진리표를 직접 돌릴 수가 없었다. 밖으로 빼고(`actions` 는 import 만) 세 경우의 표식을
`scripts/test-transfer-check.ts`(verify:fast, `test-stock-check-merge` 바로 뒤)가 박제한다. 진리표는
조립부만 보므로 `measuredFromId = data.toLocationId` 같은 한 줄 우회는 초록인 채로 지나간다 —
그 축은 parity 소스 가드에 '대입 우변이 `data.fromLocationId` 뿐' 을 더해 막았다.

**토스트 문구에 조사를 안 썼다.** 설계 문안은 `{from}에서 {to}로` 였는데 이름 끝 받침에 따라
'5층 주방로' 가 된다. 같은 이유로 이 파일에는 그 보간 형태를 통째로 막는 그물이 이미 서 있었고
(`check-stock-ledger-parity` §29 절), 형제 토스트(헤더 `위치 이동`)가 쓰는 전환 표기가 정본이라
`A → B N단위 옮김 완료 · 두 칸 입력 비움` 으로 맞췄다. 그물을 넓히는 대신 형제를 따랐다.

**기각한 (b)안 — 행에서 바로 수량만 입력해 옮기기.** 모달 없이 행에 '옮길 양' 칸을 하나 더 두는
안이다. 도착지를 고를 자리가 없어 결국 칸이 둘(도착지 선택 + 수량)이 되고, 그러면 한 행에 입력칸이
넷(현재 잔량·채운 후·도착지·수량)이 서서 402px 에서 무엇이 점검이고 무엇이 이동인지 구분이 죽는다.
같은 일에 입구를 둘 만드는 것이기도 하다 — 모달 하나를 프리셀렉트로 여는 (c)안이 정본 재사용이다.

**미룬 (d)안 — 옮기기 이력을 행에 접어 보이기.** 그 칸에서 오늘 나간·들어온 이동을 참고줄 옆에
칩으로 다는 안. 값이 있지만 참고줄이 이미 세 조각(저장된 잔량·저장된 옮김·창고 남음)이고, 네 번째를
얹으면 402px 에서 줄이 접힌다. 타임라인이 이미 그 사실을 보이므로 급하지 않다.

**완화 미채택 — '옮긴 양 > 장부 출발지' 거부.** 출발지 실측을 받기 시작했으니 "장부가 실물보다
적어서 못 옮기는" 경우를 실측으로 풀어 줄 수 있지 않느냐는 안이 있었다. 운영자 결정은 **첫 판은
그대로 둔다** 이다. 거부를 열면 장부에 없는 수량이 이동으로 생성되는 길이 열리고, 그것은 이동이
아니라 입수다. 그 경우의 정본 경로는 출발지 실측을 **먼저 저장**한 뒤 옮기는 것이다.

**임시저장 정리에 실패해도 적용취소는 남긴다.** 형제 `handleClearLocDrafts` 는 부분 실패면 에러를
띄우고 끝낸다 — 거기서는 실패한 것이 '지우기' 자체라 되돌릴 것이 없기 때문이다. 여기는 다르다.
옮기기는 **이미 적용됐고** 정리는 뒷정리다. 뒷정리가 어긋났다고 되돌릴 길까지 거두면 원칙이 뒤집힌다
(§16 — 적용하는 모든 기능엔 적용취소가 항상 있다). 그래서 토스트는 하나 그대로 두고, 못 비운 사실을
§15 장문형 부가 줄로 덧붙인다. 채널을 늘리지 않으면서 사실도 감추지 않는 자리다.


## 위치 트리 드래그 (2026-09-16)

**`moveStorageLocation` 을 안 고치고 액션을 새로 뒀다.** 드롭 한 번은 '부모가 바뀌고 자리도
정해진다' 는 한 동작인데 move + reorder 2연타는 앞이 성공하고 뒤가 실패할 때 **부모만 바뀌고 자리는
맨 뒤인 반쪽**을 남긴다. 화면은 이미 놓은 자리를 보여 주니 그 어긋남은 아무도 모른다. 기존 액션을
넓히는 대신 `placeStorageLocation` 을 새로 둔 이유는 옮기기 모달 경로의 무회귀다 — 거부 규칙 다섯은
**글자까지 같은 호출**로 베끼고(사본이 아니라 같은 정본 함수 호출), 갈라지지 않게 그물 ⓖ 가 그
문자열들을 직접 본다.

**두 입구의 이름 제안이 한 자리에서 갈린다 — 알고 남긴 것이다.** 드래그는 `placeStorageLocation`
을 타고 이름 제안이 `preserveName(옛 pathName, 새 부모 pathName)` 이라 **얕아지는 방향에서 이름이
늘어난다**(`상단` → `김치냉장고 상단`). 옮기기 모달은 `moveStorageLocation`(무변경)을 타고 그 서버는
현재 `name` 에서 앞부분을 뗀다 — 늘리는 경우가 없다. 운영자 결정은 "옮기기 모달도 같은 규칙" 이었고
"기존 `moveStorageLocation` 은 손대지 않는다" 도 같은 결정문에 있다. 단계 6이 모달 쪽 변경 범위를
**기본값·라벨**로 적고 있어 그쪽을 따랐다. 결과로 남는 차이는 '나중에 고칠 빚' 이므로 여기 적어
둔다 — 완전히 한 벌로 만들려면 모달도 place 를 타야 하고, 그것은 운영자 결정이 필요하다.
미리보기는 각자 제 서버를 따르므로 **화면이 거짓말하는 자리는 없다**(그게 최소 안전선이었다).

**드래그 중 체크박스는 읽는 자리다.** 손잡이에 pointer capture 가 걸려 있어 드래그 중에는 다른
요소를 누를 수 없다. 고스트 아래 한 줄은 그 자리의 **기본값**과 §29 미리보기를 보여 주고, 값을
그때그때 바꾸는 것은 `관리 > 옮기기` 의 체크박스가 맡는다. 눌리지 않는 컨트롤을 눌리는 것처럼
그리면 그게 더 나쁜 거짓말이라, 캡션이 아니라 체크박스 모양을 유지하되 동작을 약속하지 않았다.
(대안 — 드래그 중 키보드 수정자 토글 — 은 터치에서 안 서므로 접었다.)

**낙관 반영을 걷어냈다.** 종전 엔진은 포인터 이동마다 `setLocs(locReflow(...))` 로 목록을 다시
엮었다. 부모까지 넘나들게 되면 후보 자리가 3배가 되어 그 방식은 한 프레임에 여러 번 트리를 재구성
한다 — 402px 에서 행이 눈에 띄게 튄다. 지금은 목록이 드롭 전까지 한 칸도 안 움직이고(자리표가 블록
높이를 그대로 채운다) 움직이는 것은 ref 로 transform 한 줄을 쓰는 고스트뿐이다. state 로 올라가는
것은 표시선 자리가 바뀔 때뿐이고, 그 동치 비교(`locDropEq`)가 없으면 포인터 이동마다 리렌더가 돈다.

**`locReflow`·`locOwnerAt` 을 지웠다.** 둘 다 '형제 안에서만' 모델의 부품이고 새 모델에 호출처가
없다. 남기면 eslint 미사용 경고가 붙어 496 기준선이 흔들린다(내 변경이 만든 고아라 함께 걷는 것이
맞다 — 원래 있던 죽은 코드를 건드린 것이 아니다).

**아래 경계에 '한 단 올리기' 를 안 얹었다.** Finder 는 들여쓰기 방향 드래그로 단을 올리지만, 그러면
같은 12px 이 '뒤 형제' 와 '한 단 올리기' 두 뜻이 되어 손이 어느 쪽을 집었는지 못 센다. 규칙을
'아래 경계는 언제나 그 행의 뒤 형제' 하나로 고정하고, 한 단 올리려면 **다음 얕은 행의 위 경계**로
가게 했다(트리 순서상 그 자리가 반드시 존재한다).

**깊이 상한에 걸린 행은 몸통을 죽인다.** 그 행 아래로는 못 들어가므로 36px 을 '눌러도 아무 일 없는
구간' 으로 두면 그 행에서만 드래그가 먹통처럼 느껴진다. 슬롯 전체를 형제 구간으로 돌려(위 절반 앞,
아래 절반 뒤) 손이 늘 무언가를 집게 했다(구간 산수는 슬롯 60px = 행 54 + 간격 6 — 실측 전 50px 로
적었던 것을 디자이너 검수에서 바로잡았다. 왜 죽었는지는 그 구간에서 이유 줄이 말한다).

**막힘 토스트는 이름 충돌에만 붙였다.** 순환·깊이는 표시선이 점선이 되고 하단 이유 줄이 이미
말한다. 거기에 토스트를 또 띄우면 같은 사실을 두 채널로 말하는 것이고(§27.2), 이름 충돌만 다른
이유는 **다음 행동이 있기 때문**이다 — 이름을 바꾸면 그 자리가 열린다. 그래서 그 하나에만
`이름 바꾸기` 액션을 달았다.

**토스트 제목에 조사를 손으로 안 붙였다.** 설계 문안은 `'415호 창고' 를 최상위 맨 위로 옮겼습니다`
였는데 이름 끝 받침에 따라 `'상단' 를` 가 된다. 이 파일에 이미 선 규칙(이름은 따옴표 안, 조사는
뒤따르는 명사가 받는다)대로 `'415호 창고' **위치를** 최상위 맨 위로 옮겼습니다` 로 한 단어를 끼웠다.
반대로 이유 문구 다섯은 설계가 준 글자 그대로 뒀다 — 다섯째가 서버 문구의 앞 절과 같아야 하고
넷째도 서버의 형제 중복 문구와 같은 꼴이라, 여기서만 바꾸면 화면과 서버가 다른 말을 한다.

### 독립 검수 반영 (2026-09-16, 코드·디자인 두 패널)

**적용취소를 세 걸음에서 한 걸음으로 바꿨다 — 그러지 않고는 고칠 수가 없었다.** 검수가 짚은 것은
`preserveName` 이 이름을 **늘리는** 방향에서 3연타의 1단계(rename)가 반드시 막힌다는 것이다.
그 층에 `상단` 이 이미 있어서 이름을 `김치냉장고 상단` 으로 늘린 것인데, 되돌리기가 **그 층에 선
채로** 짧은 옛 이름을 먼저 세운다. 순서를 뒤집으면 깊어지는 방향이 같은 이유로 깨진다 —
중간 상태가 규칙을 위반하는 것이지 순서가 나쁜 것이 아니라서, 순서로는 못 푼다. 그래서
`restoreStorageLocation(undo)` 를 새로 두고 **최종 상태에 대해서만** 규칙을 보게 했다. 되돌리기
전용 액션을 안 두는 것이 이 파일의 규칙(§16)인데 여기만 예외인 이유가 그것이다 — '이전 값으로 같은
액션을 다시 부른다' 가 성립하려면 그 액션이 중간 상태도 허용해야 하는데, 트리 규칙은 허용하지
않는다. 부수 효과로 반쪽 원복('어디까지 되돌렸는지' 토스트)이 통째로 사라졌다.

**`moveStorageLocation` 을 결국 손댔다.** 1e650de8 의 메모는 "모달은 종전 규칙, 두 입구의 제안이
갈리는 것은 알고 남긴 빚" 이었다. 검수가 그 빚을 지금 갚으라고 했고(운영자 결정문의 "옮기기 모달도
같은 규칙"), 최소 변경은 `locIndex` 를 한 번 만들어 `preserveName(옛 pathName, 새 부모 pathName)`
을 쓰는 것뿐이었다. 회귀 범위가 좁은 근거: 떼는 방향에서는 오늘 루트 노드가 `name == pathName`
이라 값이 한 글자도 안 바뀌고(진리표), 달라지는 것은 **얕아지는 방향과 최상위 목적지**뿐이다.
**차단 1 을 먼저 고치고 이걸 했다** — 순서를 바꾸면 모달까지 이름이 늘어난 채로 3연타 적용취소를
타서, 드래그에만 있던 실패가 모달에도 들어온다.

**모달 목적지 잠금을 자리 문제로만 좁혔다.** 이름이 겹친다는 이유로 행을 `disabled` 하면, 그 이름을
만드는 체크박스를 끌 수 있는데도 그 자리를 영영 못 고른다. 잠금은 순환·깊이·지금 있는 자리(체크로
못 바꾸는 것)만 걸고, 이름 충돌은 행에 이유 캡션을 남기되 고를 수 있게 뒀다 — 고른 뒤에도 겹치면
`chosenReason` 이 버튼을 막는다. 잠금과 캡션을 한 값(`reason != null`)으로 묶고 있던 것을
`lockedFor`(=`reason.clash == null`)로 갈랐다.

**미리보기 조건을 이름이 아니라 pathName 으로 옮겼다.** 옆 가지로 옮기는 드롭(`4층 주방 김치냉장고
상단` → `5층 주방 김치냉장고` 아래)은 제안이 `null` 이라 이름이 안 바뀌는데 전체 이름은 통째로
바뀐다. 이름을 기준으로 두면 **가장 많이 바뀌는 자리에서 화면이 침묵**한다. `LocDrop` 에
`nextPath` 를 한 칸 더해 그것이 바뀔 때만 뜨게 했다. 라벨은 세 갈래다 — 줄면 떼기, 늘면 붙이기,
그대로면 `전체 이름 바뀜`(이름이 안 바뀌는데 '떼기' 라고 쓰면 그 줄이 거짓말이다).

**드래그 중에도 눌리지 않는 줄이라 체크박스 모양을 걷었다.** 1e650de8 은 "눌리지 않는 것을 눌리는
것처럼 그리지 말자" 면서 체크박스 모양은 유지했는데, 디자이너 지적대로 **켜진 체크박스 모양 자체가
약속**이라 그 판단이 반쯤만 지켜진 것이었다. 글리프를 걷고 글자만 남겼고, 자리도 고스트 아래에서
위로 올렸다(아래는 잡은 엄지 바로 밑이라 읽을 수가 없다).

**높이 고정이 곧 좌표 안정이다.** 이유 줄을 `drag != null` 에만 마운트한 것이 단순한 레이아웃 흔들림이
아니었다 — 오버레이가 `items-center` 라 footer 가 23px 자라는 순간 패널이 통째로 재정렬되고, 행이
11~12px 위로 튄다. rect·grabDy·y0 는 그 **전에** 잰 값이라 고스트가 자리표보다 아래에 뜬 채 따라
온다. 그래서 이유 줄은 목록이 있으면 늘 서 있고(빈 줄), 캡션 상자는 `min-h-[2lh]` 로 두 줄을 잡아
`저장 중…` 한 줄로 줄어도 목록이 안 움직인다. 재는 좌표를 보정하는 대신 **재는 동안 안 움직이게**
한 것이다.

**settle 타이머와 저장 중 손잡이.** 150ms 안에 다시 잡으면 옛 타이머가 새 드래그를 지우고, 저장
응답 전에 다시 놓으면 옛 목록 위에서 잰 자리로 또 쓴다. 둘 다 '겹쳐 들어오는 입력' 한 클래스라
타이머 id 를 쥐어 pointerdown 에서 끄고, `placing || pending` 이면 손잡이를 아예 안 받는다.


## 서류 시트 실거주·사업자등록증 (2026-09-16)

**실거주 확인서의 문을 '조건부'로 못 만든다.** 납부 확인서의 문은 귀속월이라는 축이 있어 "낡았다"를
말할 수 있다. 실거주에는 그 축이 없다 — 어제 뽑은 종이도 오늘 이사를 갔으면 거짓이고, 석 달 전
종이도 여태 살고 있으면 참이다. 그래서 낡음 배지를 세우는 안(스키마 한 칸 추가)을 버리고 **상시
보조줄 링크** 하나로 갔다. 판정할 수 없는 것에 판정 표시를 다는 것이 더 나쁜 거짓말이다.

**`canWriteNew` 의 세 값이 각각 다른 말을 한다.** `true`='지금 열려 있다', `false`='문은 있는 서류인데
지금 닫혀 있다(그래서 화면이 이유를 말한다)', `undefined`='이 행에는 문 자체가 없다'. 실거주는
`false` 로 서는 갈래가 없다 — 미발급이면 행에 이미 [작성]이 서므로 문이 또 있을 자리가 아니다.

**중립 그룹에는 문을 안 뒀다.** 어느 계약의 종이인지 앱이 말할 수 없으면 어느 계약으로 다시 쓸지도
말할 수 없다. 거기에 문을 두면 작성 화면이 `leaseTermId` 없이 열리고 도착 화면의 추론이 늘 거주
계약을 고른다 — 서류 지목(`lib/documentLease`)이 이미 정리한 클래스와 같은 뿌리다.

**라벨을 정본으로 올린 이유는 문장이 서류마다 다르기 때문이다.** 같은 문장이면 상수로 뺄 이유가
약했을 텐데, 납부는 '이번 달 확인서 작성'이고 실거주는 '다시 작성'이다. 화면이 `row.docType` 으로
분기해 리터럴을 고르는 판이 되면, 문이 셋째 서류로 늘어나는 날 그 분기만 안 고쳐진다. 더 나쁜 것은
실거주 링크에 '이번 달'이 붙는 실수인데 그것은 **그 자리에서 곧바로 거짓**이라 눈으로만 잡힌다.

**등록증은 계약 축에 안 눕힌다.** 시트의 대전제는 "행은 계약 축"인데 등록증에는 그 물음이 성립하지
않는다. 계약마다 행을 세우면 방을 둘 쓰는 사람의 시트에 같은 파일이 두 번 서고, 운영자가 둘 다
체크하면 같은 첨부가 두 개 나간다. 그래서 `kind: 'property'` 라는 셋째 그룹을 만들었다. 그룹을
안 만들고 중립 그룹('그 밖의 보관본')에 넣는 안도 있었는데, 그 그룹의 뜻은 **"어느 계약인지 말할 수
없는 것"** 이라 "계약과 무관한 것"과 다르다. 두 뜻을 한 통에 담으면 그 통의 이름이 거짓이 된다.

**그룹 머리 규칙은 안 건드렸다.** `groups.length > 1` 이면 머리가 선다는 종전 규칙 그대로다. 등록증이
있는 영업장에서는 계약이 하나인 사람도 머리가 서게 되는데, 그것이 옳다 — 실제로 가를 것이 둘이다.
'그 밖의 보관본'이 있는 사람에게 이미 같은 일이 일어나고 있었고(선례), 같은 문법을 따르는 편이
"계약 하나면 머리 없음" 을 등록증 그룹만 예외로 두는 것보다 예측 가능하다.

**발급일 칸을 비운 것은 값이 없어서가 아니라 값의 축이 달라서다.** 업로드 시각은 우리 DB 에 있다.
그러나 그 자리에 넣으면 종이의 날짜인 척하는 값이 된다 — 등록증에 찍힌 날은 발급기관의 날짜이고
우리는 그것을 읽지 않는다. 대신 `{영업장명} · {형식}` 을 적었다. 형식을 적은 이유는 사진으로 올린
등록증과 PDF 로 올린 등록증이 사진 모드에서 다르게 동작하기 때문이다(장수).

**소유 검증 가지를 양쪽에 함께 넣었다.** `/api/doc-file` 만 늘리면 파일은 내려오는데 '보기'가 404 고,
`/doc/[fileId]` 만 늘리면 껍데기 화면이 뜨고 iframe 이 빈다. 두 파일의 주석이 서로를 가리킨다.
**권한 면이 넓어진 것이 아니다** — where 에 `id: propertyId` 가 함께 걸려 있어 늘어난 것은 '내
영업장 내 파일' 한 칸뿐이고, `/api/biz-cert` 가 세운 "임의 Drive ID 를 끼울 자리를 없앤다"는 원
규약도 그대로다.

**전화번호 정본 — 실데이터가 규칙 변경을 정당화했다.** 드라이런에서 서류 축(옛 `primary ?? 비비상`)은
127명 전건 동일이었고, **문자 축(옛 `첫 PHONE`)에서만 6명이 갈렸다.** 갈린 이유가 전부 실제 결함이다.
넷은 옛 규칙이 틀린 번호를 고르고 있었고(본국 번호 둘 — 문자가 아예 안 감, 비상 연락처 둘 — 아버지·
친구에게 감), 둘은 **연락처가 비상 하나뿐인 입주자**라 새 규칙에서 번호가 없어진다. 뒤의 둘은 기능이
줄어드는 쪽이라 운영자 판단이 필요하다 — 규칙("비상은 어떤 경우에도 안 잡는다")을 고치는 대신
그 두 사람의 연락처를 본인 번호로 정리하는 것이 정본 쪽 해법이다.

**계약서 경로 셋은 손대지 않았다**(운영자 결정 B). `lib/contractData.ts`·`contractShare.ts` 둘·
`app/api/contract/generate/route.ts`. 그 값은 종이에 찍히고 `printedFacts` 통비교에 들어가 있어,
바꾸면 기존 서명 링크가 전건 드리프트로 설 수 있다. 드라이런이 "그 축은 0명" 이라 답했으므로 2차에서
옮길 때 종이가 바뀌는 사람은 (지금 데이터 기준) 없다. `check-tenant-phone-axis` 의 ALLOW 에 파일별
**개수까지** 적어 둔 이유가 그것이다 — 파일 단위로만 눈감으면 그 안에서 손규칙이 하나 더 늘어도
조용히 지나가고, 2차에서 옮긴 뒤 ALLOW 를 안 줄이면 명단이 썩는다(줄어도 붉게 선다).

**공유 사후 토스트를 걷었다**(운영자 지적, UX 문구 패널 판정). 공유 시트에서 [프린트]를 골랐는데
"메시지 칸에 붙여넣기 하세요" 가 떴다. 시트가 닫힌 뒤에는 무엇을 골랐는지 알 길이 없으므로 **사후에
말할 수 없는 것**이다. 누르기 전 인라인 캡션으로 옮기고, 훅은 `shared` 인데 복사가 **막혔을 때만**
한 줄 말한다 — 캡션이 한 약속이 거짓이 되는 자리라서다. 취소는 침묵(§27.5). 형제 3화면은
`shareText` 를 안 넘겨 `copied` 가 늘 false 라 픽셀·동작이 안 바뀐다.

**상담 도구의 등록증 파일명을 정본으로 붙였다 — 그물이 아니라 손으로.** `check-doc-file-label` 의
DOC_WORDS 에 '사업자등록증' 을 더해도 `ConsultToolsModal` 은 안 걸린다. 그 그물은 `fileName` 이
적힌 줄만 보는데 거기서는 변수 이름이 `base` 다. 그물을 넓히는 대신(오탐이 늘고 축이 흐려진다)
그 한 줄을 `docFileLabel('bizcert', 'ko')` 로 바꾸고, 사각이라는 사실을 감지망 절에 적었다.

**환경설정 문구에서 '시트'를 안 썼다.** 설계 문안은 "입주자 서류 시트에서도" 였는데 '시트'는
[[doc-vocabulary]] 가 라벨 금지어로 못박은 개발 용어다. 화면 제목 그대로 **'입주자 서류'** 로 적었다.

### 검수 반영에서 갈린 판단 (2026-09-16)

**그룹 key 충돌이 제일 값진 지적이었다.** `key={g.leaseTermId ?? 'other'}` 는 중립 그룹 하나뿐일
때는 옳았는데, 영업장 서류 그룹이 들어오면서 null 이 둘이 됐다. React 가 형제 둘을 같은 key 로
보면 재렌더마다 리마운트하고 그 안의 체크 상태·포커스가 튄다. **null 을 키로 쓰는 코드는 그
null 이 하나뿐이라는 가정 위에 서 있다** — 가정이 깨지는 날 화면이 조용히 이상해진다.

**EmptyState 판정을 '계약 그룹' 이 아니라 '이 사람의 그룹' 으로 잡았다.** 지적문은 `some(g =>
g.kind === 'lease')` 류를 제안했는데 그대로 쓰면 **떠도는 보관본만 있는 입주자**(계약 0 + 중립 1)
에게 "보낼 서류가 없습니다"가 새로 뜬다. 그건 거짓이다(보낼 것이 있다). 그래서 축을
`some(g => g.kind !== 'property')` 로 잡았다 — 영업장 서류만 빼는 것이 지적의 실제 내용이다.

**미납·공지 문자를 2차에서 1차로 끌어올렸다.** 처음에 미룬 근거는 "옮기는 자리를 최소로" 였는데,
드라이런이 찾은 결함 넷(본국 번호 둘·비상 연락처 둘)이 **바로 그 두 화면에서 그대로 살아 있다는**
지적이 맞다. 종이도 printedFacts 도 안 걸리는 순수 문자 수신 축이라 위험이 계약서 경로와 다르다.
계약서 경로 셋은 여전히 무접촉이다.

**그물 ⓒ 를 넓히니 호실 면 9줄이 새로 잡혔다.** 방 카드·배치 로그의 '연락처' 표시인데, 옮기려면
그 면 전체(조회 3 + 표시 6)를 한 번에 봐야 해서 이번엔 ALLOW 에 개수와 함께 적었다.
`TenantClient:5272`(`contactType === 'PHONE' || === 'LANDLINE'`)는 **번호 고르기가 아니라 입력
하이픈 분기**라 오탐에 가깝다. 꼴이 같아 정규식으로는 못 가르므로 근거를 적어 두는 쪽을 골랐다 —
정규식에 예외를 새기면 그 예외가 다음 우회로가 된다.

**그물 ⓑ 는 '값 자리에 닿는가' 로 세웠다.** 이름이 `이름:` 꼴로만 나오면 속성 **키**라 소비가
아니다. 검수가 설계한 우회(`const tenantPhone = pickTenantPhone(...)` 는 두고 `tenantPhone:
손규칙`)가 정확히 그 꼴이고, 역주입으로 두 축이 동시에 빨강인 것을 확인했다.

**본국 폴백 안에서도 주 연락처를 먼저 본다.** ①과 ②의 관계를 ③과 ④에 그대로 옮긴 것이다 —
운영자가 손으로 고른 것이 만든 순서보다 세다. 폴백 안에서만 순서가 갈리므로 국내 번호가 있는
사람에게는 영향이 없고, 실측으로도 달라지는 사람이 0명이다.

**createdAt 동률의 2차 키를 `id` 로 뒀다.** 입주자 등록 폼이 본인·비상·본국을 한 트랜잭션에
넣으면 `now()` 가 같은 값으로 박힌다. 2차 키가 없으면 DB 가 돌려주는 순서에 답이 끌려가고, 그건
화면마다 다른 번호가 나올 수 있다는 뜻이다. 모양에 칸이 하나 늘어 호출부 select 도 함께 넓혔다.

**시트 기기 경로 파일명은 안 고쳤다.** 메일은 표기를 따르는데 공유·저장은 한글 고정이라 같은
서류가 경로마다 다른 이름으로 나간다 — **이번 작업 이전부터 있던 상태**라 별건으로
`knowledge/open-issues.md` 에 적었다(고칠 자리와 급소, 그 그물의 사각까지 함께).

## 아이템별 점검 폼을 위치 패널과 한 벌로 (2026-09-15)

**허브 칸을 `beforeQtys` 로 옮긴 것이 이 작업의 심장이다.** 2026-09-11 김치 사고의 봉합은 두 겹
(화면 + 서버)이었는데 화면 쪽이 위치 패널에만 적용돼 있었다. 아이템별 폼은 허브를 `afterQtys` 에
묶은 채였고, `calcLocMove` 가 후 − 전(빈칸=0)을 옮김량으로 셈하는 규칙은 두 화면이 공유한다.
서버의 `isHubChecked` 게이트가 마커를 막아 주긴 했지만, 그건 **두 번째 겹**이다. 첫 겹이 뚫린 채로
두는 것은 다음 경로가 열릴 때까지의 시한부다.

**허브 프리필(`자동 차감 후`)을 걷은 이유는 표식 때문이다.** 파생값이 칸에 들어 있고 그 칸이 절대값
경로로 나가면 서버는 폼이 보낸 위치수량을 전부 실측 선언(`carried: false`)으로 읽는다. 실측 행은
전파가 멈추는 자리라(이월 행은 파생 박제, 실측 행만 절대값), 아무도 세지 않은 숫자가 그 위치의
전파를 영구히 멈춘다. **값은 안 바뀌므로 데이터 대조도 감지망도 침묵한다** — 이 결함의 종류가
그렇다. 그래서 봉합이 '칸을 비우는 것' 이 아니라 '저장 경로를 바꾸는 것' 까지 가야 했다.

**저장 경로는 운영자가 (c) 서버 원자 저장을 골랐다.** 클라가 허브를 미리 깎아 보내는 경로 B 를
남겨 두면 같은 사건을 두 화면이 다른 규칙으로 장부에 적는다. `applyLocationChecks` 는 새 규칙이
아니라 **패널이 왕복으로 하던 일을 서버 안에서 그대로 반복**하는 것이다 — 패치마다 `detectHubShort`
로 게이트를 지나고 `applyLocationCheck` 으로 접는다. 표식이 저절로 맞는 것도 그 덕이다. 첫 패치의
base 는 직전 점검이라 표식이 없어 비점검 행이 전부 이월로 찍히고, 두 번째부터는 base 가 '이 점검의
현재 상태' 라 앞 패치의 실측을 승계한다 — `updateStockCheck` 의 base 와 같은 자리가 된다.

**원자성은 패널과 갈린다. 의도한 갈림이다.** 패널의 체인 저장은 (품목, 위치) 쌍이 수십이라 전부
아니면 전무로 하면 한 칸 실패에 스무 칸의 실측이 버려진다 — 그래서 멈춘 자리를 남기는 쪽을 택했다.
여기는 **한 품목**이고 패치 서넛이라, 절반만 반영된 장부가 다음 점검의 base 가 되는 쪽이 훨씬 나쁘다.
HUB_SHORT 에 `patchIndex` 를 실어 돌려주되 한 건도 저장하지 않는다.

**`const base` 를 한 줄로 유지하려고 두 갈래를 위에서 합쳤다.** `check-stock-ledger-parity` 의
비대칭 가드가 `create.match(/const base[^\n]*locationBreakdown[^\n]*/)` 로 **첫 줄 하나만** 집는다.
`locationPatches` 용 base 를 따로 세우면 가드가 그 줄을 안 보고, 거기에 `carried` 를 실어도 초록으로
지나간다. 그래서 `const patches = locationPatches ?? [locationPatch] ?? null` 로 정규화해 기존 블록을
그대로 재사용했다. 가드를 고치는 대신 코드를 가드가 겨누는 모양으로 둔 것이다.

**성공 토스트에서 HubShort 갈래만 예외로 뒀다.** 설계문은 세 갈래 모두 `재고 점검 저장됨` 이라
적었는데, 그 갈래의 토스트 주인은 `HubShortDialog` 다(위치 패널과 공유하는 컴포넌트). 거기서 이미
`옮기고 채움 완료 [적용취소]` / `부족한 채로 저장했습니다 [적용취소]` 를 내고 있어, CheckForm 이
또 내면 같은 저장이 두 번 통지된다(§27.2, `check-draft-lifecycle` 이 형제 자리에서 이미 세고 있는
규칙이다). 설계 의도(운영자가 저장을 되돌릴 자리를 언제나 갖는다)는 이미 충족돼 있어 그대로 뒀다.
**실제 공백은 '위치 없는 품목' 갈래**였다 — 거기만 성공 토스트가 아예 없었고, 그래서 그 저장만
되돌릴 수 없었다. 그 자리를 메웠다.

**`다음 품목` 을 토스트에서 화면으로 옮긴 이유.** §15 액션 토스트는 6초다. 연속 점검은 한 품목에
30초도 걸리는 일이라, 토스트로 다음을 권하면 세는 동안 사라진다. 보기 화면 맨 위 영속 줄로 세우고
모달이 닫힐 때까지 남긴다. **상태는 상세 모달이 아니라 호출부가 든다** — 그 함수는 `if (!row)
return null` 뒤에 훅을 세우는 자리라(기존 결함), 훅을 하나 더 얹으면 `react-hooks/rules-of-hooks`
경고가 한 건 는다. 델타 0 규칙을 지키려면 상태가 밖에 있어야 했다.

**상세 모달 골격은 '열릴 모드' 를 본뜬다.** `mode` 가 아니라 `initialMode` 로 고른다 — 데이터가
오기 전이라 `mode` 는 아직 `initialMode` 와 같고, '다음 품목' 으로 이어 열면 점검 폼부터 뜬다.
그때 보기 골격을 그리면 로디드 순간 통째로 갈아엎히는 점프가 된다.

**6단계 감사는 예행 결과가 0건이다.** 표식(`carried`) 자체가 2026-09-11에 생겼고, 그 뒤 재고 화면의
기본 보기는 위치 패널이었다(2026-09-15 결정). 아이템별 폼이 허브 파생값을 실측으로 박을 수 있던
창이 나흘뿐이었고 그 사이 DB 전체의 `carried=false` 행이 40개다. 스크립트는 남겨 둔다 — 적용
(`--apply`)과 되돌리기(`--revert`)를 갖췄고, 예행이 0 이라는 사실 자체가 나중에 다시 확인할 값이다.

## 드래그 재검수 5건 — 왜 그렇게 고쳤나 (2026-09-16)

**R1 삼항이 죽어 있었다.** `drop.nextName === drag.head[0].name ? '전체 이름 바뀜' : 앞부분 떼기/붙이기`
인데, 이 줄이 뜨는 조건이 `drop.nextPath !== drag.head[0].pathName` 이다. `preserveName` 은 pathName 을
보존하는 규칙이라 **이름이 바뀌면 pathName 은 그대로**이고(그래서 이름을 바꾼 것이다), 그런 드롭은
미리보기 자체가 안 뜬다. 실제로 뜨는 경우는 접두 불일치(제안 없음)와 최상위로 빼기 — 둘 다
`nextName === name` 이다. 즉 else 가지에 발이 닿을 수 없다. 상수로 고정했다.

**R2 는 두 상자를 하나로 합친 것이다.** 목록 위 캡션이 `placing` 으로 두 줄↔한 줄을 오가서 목록이
16px 튀었고, 그걸 `min-h` 로 잡아 놓았다. footer 에는 드래그 판정을 위한 **전용 빈 줄**이 늘 서 있었다
(드래그 중에만 마운트하면 잡는 순간 footer 가 자라 패널이 재정렬된다). 진행·이유·안내를 **기본 창고
캡션 상자 한 곳**으로 모으면 둘 다 사라진다. 다만 유효한 드롭에서는 할 말이 없으므로 그때는 평시
문장이 그대로 선다(`reason ?? note ?? 기본 창고 문장`) — 빈 문자열로 떨어뜨리면 상자가 3줄에서 0줄로
주저앉아 잡는 순간 패널이 재정렬되고, 그건 원래 막으려던 그 사고다.

**R3 `min-h-[2lh]`.** `lh` 단위는 Firefox 111~119 에서 무시되고 이 저장소에 선례가 0이다. R2 로 그
자리가 통째로 없어져 대체 클래스도 필요 없었다(저장소 전체 `lh` 사용 0 확인).

**R4 note 의 범위.** 깊이 상한 행은 슬롯 전부가 형제 구간이지만, 위·아래 12px 은 **다른 행에서와
똑같이** 앞/뒤 형제로 서는 유효한 자리다. 거기서 '4단계를 넘어 아래로 넣을 수 없습니다' 를 띄우면
눈앞에 유효한 실선이 서 있는데 화면은 안 된다고 말한다. note 가 메워야 하는 공백은 몸통 자리
하나뿐이다 — 캡션이 '행 가운데에 놓으면 그 위치 아래로' 라고 말하는데 그 행에서만 안 되는 것.

**R5 고스트 높이.** `min-h-[54px]` 는 그때 행 높이를 손으로 옮겨 적은 값이다. 행에 배지가 한 줄
늘거나 글꼴이 바뀌면 고스트만 그대로 남아 들린 것과 자리표 구멍이 어긋난다. 드래그 시작에 이미
`rects[first]` 를 재고 있으므로 그 높이를 `LocDragView.rowHeight` 로 실어 쓴다. 자리표(`height`)가
실측인데 고스트만 상수였던 비대칭도 함께 사라졌다.

## 두 독립 검수 반영 — 갈린 판단 셋 (2026-09-16)

**D1 을 px 으로 안 박았다.** 검수는 `min-h-[Npx]`(줄높이 × 줄 수) 또는 종전 구조 복귀를 제시했는데,
둘 다 약점이 있다. px 은 **폭마다 줄 수가 달라** 402px 에서 맞춰도 320px 에서 다시 어긋나고, 종전
구조 복귀는 앞선 재검수가 없애라고 한 23px 빈 띠를 되살린다. 그래서 평시 문장을 `invisible` 로
자리만 남기고 그 위에 겹쳐 쓴다 — 높이의 기준이 **그 문장 자신**이라 어느 폭·어느 글꼴에서도 세
상태가 같다. 숫자를 안 박았으니 문장이 길어져도 안 깨진다.

**D7(비허브 행 `다른 곳으로`)은 뺐다.** 검수도 "부담이 크면 빼고 이유를 적으라" 고 열어 둔 항목이다.
뺀 이유는 분량이 아니라 **드래프트 모델이 달라 '패널의 handleTransferDone 문법 그대로' 가 성립하지
않기** 때문이다. 패널은 (품목, 위치) 쌍마다 독립 드래프트라 그 두 쌍만 지우면 끝나지만, 아이템별
폼의 정본은 **맵을 통째로 든 아이템 드래프트 한 장**이다. 두 칸을 무효로 하려면 그 한 장을 키만
빼서 다시 저장해야 하고, 되돌리려면 그 이전 맵을 스냅샷으로 들어야 한다 — 그물 ⑨·③ 이 지키는
바로 그 수명주기를 새 경로로 한 벌 더 만드는 일이다. 절반만 맞으면 "옮기기 전에 센 값이 다음
진입에서 되살아난다" 가 되고, 그건 ⑧ 이 막으려던 사고와 같은 종류다. 지금도 같은 화면 한 탭
옆(상세 보기 발끝 `위치 이동`)에 입구가 있으므로 기능 공백이 아니라 **동선 차이**다. 별건으로
남긴다.

**L9(`sameDayNotice` 술어)는 안 바꿨다.** 검수는 `patches` 로 통일하라고 했지만, 그러면 아이템별
폼이 같은 날 중복 앵커 안내를 **잃는다** — 백로그 3번이 원래 겨눈 자리가 거기다(한 품목에 한 번
저장하는 자리라 같은 날 두 번이 그대로 신호다). 위치 패널은 한 칸씩 `locationPatch` 로 오고 같은 날
연속 저장이 설계된 흐름이라 칸마다 뜨면 소음이 된다. 그래서 축은 그대로 두되 `isPanelChainSave`
라는 이름과 주석을 붙였다 — 검수가 이것을 오타로 읽은 것 자체가 결함이었고, 고칠 것은 술어가
아니라 **읽히지 않던 의도**였다.

**L2 에서 `deduped` 를 새로 만들었다.** 멱등창이 삼킨 제출에 적용취소를 붙이면 그 버튼은 20초 전
다른 저장을 지운다 — `DeleteCheckExpect` 를 붙이는 길도 있지만, 그건 '지워도 되는가' 를 묻는 장치지
'이것이 내 저장인가' 를 묻는 장치가 아니다. 애초에 **내 저장이 아니면 되돌릴 것이 없다**. 그래서
표식을 실어 보내고 호출부가 액션을 안 붙인다. 대신 detail 로 "기존 기록을 그대로 두었습니다" 라고
말한다 — 침묵하면 운영자는 방금 고친 날짜가 저장된 줄 안다.

**L5 재예행도 0건이고, 이번엔 그 0을 확인했다.** 허브 후보를 '오늘의 허브 + 이 품목에 링크된 영업장
기본 창고' 로 넓히고 그 점검에 행이 있는 것을 고르게 고쳤다. 그래도 0인 이유를 실측으로 확인했다 —
DB 전체 `carried=false` 행 40개 중 폼 저장(패널·이동·수령 아님) 점검은 **2건**이고 둘 다 위치 한 행·
옮김 합 0이다. 옮김이 없으면 '파생 허브값' 이라는 개념 자체가 없다. 표식이 9/11 에 생겼고 그 뒤
기본 보기가 위치 패널이었다는 앞선 추론과 실측이 맞는다.

## 우회 봉합을 정규식이 아니라 구조로 (2026-09-16 재검수)

재검수가 설계한 변형이 앞의 봉합을 그냥 지나갔다 — 프리필을 조립부가 아니라 **드래프트 복원
이펙트**로 옮기면 세 축이 전부 초록이고, 부수로 `hubMeasured` 가 항상 참이 되어 허브 부족 게이트가
통째로 꺼진다. 정규식을 하나 더 늘리는 길은 다음 변형에 또 진다 — 프리필을 넣을 수 있는 자리는
계속 생기기 때문이다.

그래서 **'적었다' 를 값에서 떼어냈다.** `entered` 는 행 id 집합이고, 채우는 자리는 입력칸의
onChange(`markEntered`)와 드래프트 복원 둘뿐이다. 복원조차 **병합 결과가 아니라 서버 문서의 키**
(`draftedIds` — `main.beforeQtys`/`main.afterQtys` 의 키와 위치별 드래프트의 `locationId`)에서만
가져오고, 값은 그 뒤에 따로 병합한다. 그래서 병합 뒤 어느 자리에 프리필을 끼워 넣어도 집합은
안 늘고, `buildLocationPatches`·`hubMeasured`·`enteredAny` 셋이 전부 그 집합을 술어로 쓰므로
패치 수도 게이트도 안 움직인다. 역주입으로 확인했다 — 복원 이펙트 프리필만 넣은 변형은 **무해해져**
그물이 초록이고(값만 화면에 차고 패치는 그대로), 그것을 해롭게 만들려면 `entered` 를 병합 결과에서
채우는 **두 번째 편집**이 필요한데 그 순간 빨강이 된다.

'문자열이 비었는가' 는 지우지 않고 **좁히기**로 남겼다 — 적었다가 지운 칸은 '안 셌다' 로 봐야
직전값이 이월된다. 술어의 주인은 집합이고 값은 그 뒤다.

**B(죽은 분기)를 지우지 않은 이유.** 허브 패치가 실리는 조건(`hubMeasured` 참)과 HUB_SHORT 가 서는
조건(`allowHubClamp` falsy = `hubMeasured` 거짓)이 지금은 배타라, 이동 뒤 재저장에서 허브 패치를
빼는 가지에 발이 안 닿는다. 하지만 그 배타는 **오늘의 술어가 만든 우연**이다. 허브 실측이 있어도
게이트를 켜기로 바꾸는 날 이 가지가 없으면 옮겨 온 양이 조용히 증발한다(재현 B 가 그것이었다).
그래서 코드는 두고 그물을 '분기가 있는가' 에서 **'두 조건이 배타인가'** 로 옮겼다 — 배타가 깨지는
순간이 그 가지가 살아나야 하는 순간이고, 그때 그물이 먼저 말한다.

**C 는 겹쳐 쓰기의 짝이다.** 높이를 평시 문장에 맡긴 대가로, 그보다 긴 문구가 오면 상자를 넘는다.
`전체 이름이 '…' 인 위치가 이미 있습니다` 는 사용자가 지은 이름이라 길이에 상한이 없다. 두 겹
`overflow-hidden` 으로 자른다 — 잘리는 꼬리는 이름의 뒷부분이고 판정("같은 이름이 있다")은 첫 줄에서
읽히므로, 자르는 쪽이 버튼 위에 글자가 겹치는 쪽보다 낫다.

**D 는 패널 쪽이 옳았다.** 폼의 `!hubMeasured` 는 '창고 기록이 없는 품목' 에서 `차감 후 0` 을 띄운다 —
직전 잔량을 모르는데 0 을 만들어 낸 것이다. 패널의 `prev != null` 은 모르면 침묵한다. 같은 자리의
`{창고} 남음` 이 이미 "점검 기록 없음" 이라고 말하는 규칙과도 맞는다.

## HEIC 업로드 정규화 (2026-09-17)

**서버에서 고칠 수 없다는 것이 이 설계의 출발점이다.** `node_modules/sharp` 는 next 의 선택적
전이 의존이고 그 libheif 빌드가 AVIF 만 물어 HEIC 디코드가 실패한다(실측). 순수 JS 디코더는
wasm 8.78MB 에 업로드 구조 변경까지 딸려 와 기각했다. 브라우저는 이미 HEIC 를 안다 — 사파리는
네이티브로, 그 밖은 `createImageBitmap` 이 답한다. 그래서 **올리기 전에 브라우저에서 바꾼다.**

**왜 PDF 인가(등록증·스캔 계약서).** 운영자 결정이고 근거가 있다. 이 둘은 '종이' 다 — 소비처가
메일 첨부·문자·서류 묶음·보기 화면이라 전부 PDF 를 다룰 줄 안다. 반면 도장·계약서용 로고는
**그림으로 얹히는 자리**다. 거기에 PDF 를 넣을 수는 없고, JPEG 로 바꾸면 도장의 투명 배경이
흰 사각형이 되어 서명란을 덮는다. 그래서 형식이 둘로 갈린다 — 종이는 PDF, 그림은 PNG.
"무엇이 되는가" 가 형식을 정하지 "무엇이었는가" 가 정하지 않는다.

**`ocrImage` 와 딱 한 군데가 다르다.** 디코드 문법(EXIF 회전을 픽셀에 박고, 비트맵·캔버스를
즉시 반납)은 그대로 가져왔지만 **실패했을 때가 반대다.** AI 인식은 원본을 그대로 보내도
Gemini 가 HEIC 를 읽어 주니 폴백이 옳다. 여기서 원본이 통과하면 그 바이트가 **저장**되어
첨부·발급이 다시 깨진다. 그래서 던진다. 이 한 줄이 이번 작업의 본체라 진리표도 그 축이 제일
두껍고(원본 통과 금지 4축) 감지망에도 축 ⓘ 로 따로 세웠다.

**용지 비율을 사진 비율로 잡은 이유.** A4 에 억지로 끼우면 위아래 흰 띠가 생기거나 잘린다.
긴 변만 A4 긴 변(841.89pt)에 맞춰 인쇄했을 때의 물리 크기를 사람이 아는 값으로 두고, 비율은
사진 그대로 둔다. 페이지는 언제나 하나다 — 사진 한 장은 종이 한 장이다.

**최대 변이 둘인 이유(2048·1024).** 종이가 될 사진은 잔글자(사업자번호·업태)가 남아야 해서
크다(신분증 프리셋과 같은 값). 그림으로 얹힐 사진이 작은 것은 **PNG 가 사진을 못 줄이기**
때문이다 — 알파를 지키려고 PNG 로 내보내는데 2048px 사진이면 인코딩 결과가 도장·로고의 5MB
상한을 넘어 업로드가 거부된다. 이미 PNG·JPEG 인 파일은 이 경로를 아예 안 탄다(무손실 유지).

**서버 문을 좁히면서 JPEG·PNG 를 남긴 이유.** 변환은 클라이언트에서 도는데, PWA 로 굳은 낡은
화면이 남아 있을 수 있다. 그 화면이 JPEG 를 올리면 첨부·미리보기가 여전히 도니까 막을 이유가
없다. 막아야 하는 것은 **HEIC/HEIF 다** — 그 바이트가 저장되는 순간 소비처가 전부 깨진다.
그래서 화이트리스트는 '전부 거부' 가 아니라 '깨지는 것만 거부' 다.

**스캔 계약서에는 문 자체가 없었다.** 크기(25MB)만 보고 mime 은 한 번도 안 봤다. 그래서
`finalizeContractScan` 의 소유 검증도 `isOwnedByApp` 에서 `ownedDriveFileMime` 으로 바꿨다 —
같은 왕복에서 Drive 판정 mime 까지 받아 검사한다(사업자등록증이 이미 쓰던 문법). 세션의
mime 은 클라이언트가 부르는 값이라 실제 올라온 바이트와 다를 수 있다.

**발급 방어는 형제 베끼기다.** `residenceCertOverlay` 의 도장 임베드에 try/catch 가 없어, 도장이
HEIC 면 예외가 그대로 올라가 **확인서 발급 자체가 실패**했다. 형제 `rentReceiptPdf` 에는 그
catch 가 이미 있었다 — 한쪽만 안 따라간 것이다. 다만 흰 박스(인쇄된 '(인)' 덮기)를 embed
**성공 뒤로** 옮겼다. 먼저 덮고 실패하면 '(인)' 마저 지워진 빈칸이 남는다. 실패해도 종이는
나가고 사실은 서버 로그에 남는다.

**mime 판정 쌍둥이를 없앴다.** `google-drive.sniffImageMime` 이 `docMime.sniffDocMime` 과 같은
논리를 따로 적고 있었고 이미 갈려 있었다 — docMime 은 ftyp **브랜드**(heic·heix·hevc·mif1)를
보는데 그쪽은 ftyp 만 보고 **mp4(isom)까지 image/heic 이라 답했다.** 그 답이
`driveImageDataUrl` 을 지나면 동영상 바이트가 계약서 도장 자리에 실린다. 이제 위임하되
**계약은 그대로 뒀다** — '이미지가 아니면 octet-stream'. PDF 를 PDF 라고 답해 주면 부르는
자리(도장 data URI, /api/biz-cert)의 분기가 바뀐다.

**앱 로고는 안 건드렸다.** 크롭 모달이 캔버스로 다시 그려 PNG 로만 내보내므로 이미 정규화된
자리다. 감지망 축 ⓖⓗ 의 대상에서도 뺐다(그 경로의 지역 변수 이름이 `file` 이라 원본 검출
정규식과 부딪히기도 한다).

**이미 저장된 HEIC 는 못 고친다.** 변환 수단이 브라우저에만 있으니 백필 스크립트를 쓸 수가
없다. 재업로드 요청이 유일한 길이고, 그래서 6번은 '고치는 스크립트' 가 아니라 **세기만 하는
읽기 전용 예행**이다. 2026-09-16 예행 0건.

## 퇴실 낱말 zht·zh (2026-09-17)

**왜 바꿨나.** 2026-09-12 에 `退住` 를 지키며 든 근거가 "衛福部 住宿式長照機構定型化契約 이
入住/退住 를 쓴다" 였는데, 원문 PDF 실측이 `退住` 0회 · `遷出` 10회였다. 근거 문장이 사실이
아니었던 것이라 판정이 통째로 뒤집혔다. 간체 `退住` 는 그 대만 판정을 따라간 것이라 근거가
함께 사라졌다.

**간체가 `迁出` 이 아닌 이유.** 번체 결정을 글자만 바꿔 옮기면 본토에서 `迁出` 은 户口(호적)
색이 짙어 이 계약이 만들지 않는 행정행위를 가리킨다. `退宿` 은 기숙사, `退房` 은 호텔 체크아웃
(및 매매계약 해제), `退租` 는 임대차다. 남은 것이 `搬离` 이고, 본토 양로·임대차 정형계약이
둘 다 쓴다. **한 언어의 결정을 형제 언어에 글자 치환으로 옮길 수 없다**는 사례로 남긴다.

**치환 규칙은 하나였다 — 한 줄에서 그 두 글자만.** `退費`·`退款`·`退還` 같은 다른 `退` 자는
대상이 아니다. 적용 뒤 실측에서 zht `退費` 4 · `退款` 2, zh `退款` 6 이 그대로 살아 있다.
굴절 자리(`退住及退費`·`強制退住（契約終止）`·`中途退住`·`未屆滿約定期間即退住者`·`退住處置`·
`表達退住意思時`·`退住7日前`)는 전부 같은 규칙 하나로 덮였다.

**문안표 줄 수가 설계값과 달랐던 자리.** 설계는 zht 17 · zh 14 를 기대했는데 실값은 zh 도 17
이었다. 표를 늘린 것이 아니라 실값이 그랬다 — 두 언어 모두 **살아있는 13줄 + 옛 '입실료'
열쇠에 딸린 고아 4줄**이다. 설계의 14는 살아있는 줄만 센 어림이었다. 고아도 함께 옮긴 이유는,
안 옮기면 그 열쇠가 되살아나는 날 옛 낱말이 종이로 돌아오기 때문이다. 고아를 값으로 집으면
살아있는 줄과 헷갈리므로 문안표 **모든 줄에 `ko` 열쇠**를 담았다(안전장치 5).

**`none.zh` 는 이번에 처음 맞았다.** 2026-09-13 회차가 DB 만 옮기고 코드를 빠뜨려, 같은 상수
안에서 `clause.zh` 는 `退住` 인데 `none.zh` 는 `退房` 이었다. 같은 상수 안의 갈림을 본 그물이
하나도 없었다. 11단계는 요금 낱말만, 12단계는 일본어만, 13단계는 법 프레임만 본다.

**진리표 14단계를 13단계에 안 섞은 이유.** 13은 "그 낱말이 임대차·숙박업 프레임을 끌고 오는가"
이고 14는 "그 언어의 실무 문서가 실제로 그 말을 쓰는가"다. `退住` 는 어느 법 프레임도 안 끌고
오므로 13단계는 그 낱말에 영원히 침묵한다. 축이 다르면 그물도 따로 선다. 13단계에는 `退租`
(임대차)만 더했다 — 그쪽은 진짜 법 프레임 위반이다.

**급소 단언을 가운데 둘로 잡았다.** '옛 낱말 0' 하나만 두면 문장을 통째로 지워도 초록이다
(12단계가 겪은 구멍). 역주입 ⓑ(none 두 줄의 낱말 문장 삭제)가 실제로 그 둘만 빨갛게 세웠다.

**박제는 안 건드렸다.** 서명본·발급본·`signedContractSnapshot` 은 "그때 그 사람이 서명한 종이"라
지금 문안으로 덮으면 서명 증거가 사라지고 드리프트 감지가 거짓말을 한다. 적용 시점에 진행 중인
서명이 없었다(서명 전 링크 1건은 2026-09-16 14:19 KST 이미 만료). 다만 **적용 시점에 열려 있던
계약서 화면은 새로고침해야 한다** — 발급 API 가 화면이 든 번역 지문과 서버 재해석본을 견준다.

## 연락처 대체·예약 확정 게이트 (2026-09-17)

**정본 위 문을 안 고치고 밑에 단 이유.** `pickTenantPhone` 의 `!c.isEmergency` 는 "그 번호의
주인은 입주자가 아니다"라는 사실이고 그것이 종이의 규칙이다. 진리표 26건과 감지망 ⓐ 가 그
줄을 지키고 있다. 대체를 그 안에 넣으면 종이도 같이 오염되므로, 축을 이름으로 갈랐다 —
`pickTenantPhone`(종이) 대 `pickTenantPhoneWithFallback`(문자). 감지망 ⓓ 는 종이 넷에 뒤 이름이
**나타나기만 해도** 붉게 선다. 두 문이 이름으로만 갈리니 이름이 새는 것이 곧 규칙이 새는 것이다.

**타입을 바꾼 것이 이번 작업의 진짜 방어다.** 컨텍스트 넷을 `phone: string | null` 에서
`phone: PickedPhone | null` 로 바꾸자 `.value` 를 안 꺼내는 자리 열 곳이 즉시 타입 오류로
드러났다(모달 셋 · 시트 둘 · 공지 번호 조립 둘 · 행 표시 하나 …). 문자열로 두면 화면이 꼬리표
없이 그리는 것을 컴파일러가 못 잡는다. 실제로 그렇게 새는 것이 이 클래스의 재발 경로다.

**대체 갈래의 `isEmergency` 는 의미상 잉여다.** `pickTenantPhone` 이 null 을 답했다는 것이 이미
"비상 아닌 전화 연락처가 없다"는 뜻이라, 그 조건을 빼도 진리표가 안 움직인다(역주입으로 확인 —
44/0 그대로). 그래도 남겨 둔다. 읽는 사람에게 이 갈래가 비상만 본다는 것을 말해야 하고,
감지망 ⓐ 가 그 줄로 갈래 소실을 감시한다. 진리표가 못 잡는 자리라 그물이 대신 잡는 배치다.

**게이트 문 자리 — `updateTenant` 만 저장 뒤에 섰다.** 이 액션은 저장 도중(`:1283~`)에 연락처를
쓴다. 문을 앞에 두면 폼에 방금 적은 번호가 판정에 안 보여서, 번호를 채우고 확정을 켠 저장이
그 저장에서 막히고 한 번 더 눌러야 통과한다. 그래서 연락처 저장 **뒤**로 옮겼다. 그 자리는
`prisma.tenant.update` 뒤라 거부하면 사람 정보만 저장되고 계약은 안 바뀌는데, 이 액션에는
이미 같은 자리의 선례가 있고(`:1463` 단기 청구 거부) 이어서 다시 저장하면 그대로 반영된다.
등록 경로는 아직 DB 행이 없어 **폼이 방금 적은 연락처**를 그대로 판정 입력으로 넘긴다
(`contactsToCreate` 조립을 문 위로 올렸다 — 순수 계산이라 저장·중복 확인창 순서는 그대로).

**소급을 `&&` 가 아니라 인자로 옮긴 이유.** 설계는 `&& !lease.reservationConfirmedAt` 였는데,
그러면 판정의 한 조각이 호출부 셋에 흩어진다. `alreadyConfirmed` 인자로 넘기면 "이미 확정된
계약은 통과"라는 규칙이 정본 안에 남고 진리표가 그것을 잠근다(2건). 대신 감지망 ⓔ 가
호출 인자에 `!!…reservationConfirmedAt` 이 실렸는지 본다 — 상수 `false` 로 바꾸면 빨갛다.

**폼 안내가 저장값이 아니라 입력값을 보는 대신, 서버의 저장 규칙을 그대로 흉내 냈다.**
본인 칸을 비운 저장은 서버가 저장된 번호를 안 지운다(값이 있을 때만 갱신). 그래서 화면도
`contactVal.trim() || 저장된 primary` 로 조립한다. 이 한 줄이 없으면 번호를 지운 화면이
"확정 불가"라고 말하는데 서버는 통과시킨다 — 화면과 서버가 다른 답을 하는 자리가 된다.
비상 칸은 비우면 지워지고, 해외 칸은 국적이 한국이면 안 그려져 보존된다([[form-clear-vs-absent]]).

**단체 공지에서만 대체를 기본 선택에서 뺐다.** 한 사람에게 보내는 문자는 운영자가 받는 사람
줄을 보고 누르지만, 단체 공지는 스무 명이 한 줄로 나가 그 확인이 없다. 빠진 사람이 보이지
않으면 그냥 누락이 되므로, 접었다 펴는 목록(이름·호실·주인 표기)과 `문구 복사` 를 함께 뒀다.
목록은 **조건에 맞고 아직 체크 안 된** 대체 행만 센다 — 운영자가 직접 체크하면 사라진다.
패널은 고르는 화면과 쓰는 화면 양쪽에 서는데, 복사할 본문이 있는 쪽은 쓰는 화면뿐이라
`문구 복사` 버튼은 거기서만 그린다.

**설계 문구에서 바뀐 두 자리.**
- `--amber-bg` 토큰이 이 저장소에 없다. 앰버 틴트의 정본은 `--warning-bg`(§04 트라이어드)라
  그것을 썼다.
- 대체 고지 둘째 줄을 `본인 연락처가 없어 비상 연락처로 보냅니다. 받는 분은 {ownerLabel}.` 로
  적었다. 설계의 `… {ownerLabel}.` 는 `김철수(부모님).` 로 끝나 문장이 안 붙는다. `ownerLabel`
  자체는 목록에서도 쓰이므로 이름 꼴(`김철수(부모님)`)로 두고, 문장은 화면이 짓는다.

**설계에 없던 갈래 하나를 더했다.** 폼 안내 첫 줄에 `문자를 보낼 수 있는 번호가 없습니다.` —
메신저 아이디만 있거나 유선전화만 있는 사람이다. 설계의 (나) `등록된 연락처가 없어…` 를 그대로
쓰면 카카오 아이디를 등록해 둔 사람에게 "등록된 연락처가 없다"고 말하게 된다. 연락처가 0건일
때만 (나) 를 쓰고, 있는데 문자만 못 가는 경우는 새 줄로 갈랐다.

**유선전화가 확정을 통과하는 데서 두 축이 갈린다.** 확정은 `PHONE_CONTACT_KINDS`(유선 포함),
문자는 `['PHONE']`. 유선만 있는 사람은 확정이 되고 문자는 안 간다 — 폼 안내가 그 상태를
두 줄로 정확히 말한다(첫 줄 "문자를 보낼 수 있는 번호가 없습니다", 둘째 줄 "확정은 이대로 가능").

**실거주 확인서 안내 자리.** 설계는 "그 칸 근처"였는데, 그 칸은 양식 이미지 위 절대 배치라
세 문장이 칸 폭 안에 안 들어간다. 형제 안내 셋(`rc-note` — 계약 없음·30일 지난 저장값·도장
없음)이 서는 자리에 같은 문법으로 뒀다. 조건은 **지금 칸 값**이 비었을 때다(손으로 채워 넣은
발급에는 안 뜬다).

**실태(`scripts/check-tenant-own-phone.ts`, 127명).** 본인 전화 123 · 비상만 2 · 연락처 없음 2 ·
메신저만 0. 본인 번호 없는 넷은 전부 거주도 예약도 아닌 단계라 **게이트가 지금 막는 사람은
0명**이다. 즉 이 문은 앞으로 들어올 사람에게만 선다. 대체로 새로 문자가 갈 사람은 2명이고
둘 다 비상 연락처 주인 표기가 있어 화면이 이름을 말할 수 있다.
