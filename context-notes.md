# 컨텍스트 노트 — 환경설정 IA 재정리 1단계 (운영자 확정 2026-08-18)

갈리는 선택마다 근거와 되돌리는 법을 적는다.

## 배경 사실

- 홈(현황 탭) 아래에 '소개 페이지에 올릴 수 있는 방' / '소개 페이지에서 내릴 수 있는 방' 카드 두 장이
  서 있었다(DashboardClient 2872~2910). 둘 다 `Room.showOnSite` 를 켜고 끄는 같은 일을 하는데,
  홈은 '오늘의 운영 상태'를 말하는 화면이고 소개 페이지 관리는 설정의 일이다.
- 방 단위 공개 토글의 정본은 room-manage 의 `setRoomShowOnSite` 다. 사진 0장이면 켜기를 거부하고
  `/room-manage`·`/dashboard` 를 revalidate 한다. 홈 카드는 그 액션을 부르고 있었다(손사본 아님).
- 후보 산출(where)만 홈 서버(page.tsx 449·455)에 인라인으로 있었다. 웹사이트 탭이 같은 모집단을
  봐야 하므로 여기만 정본으로 뽑아낸다.

## 좁은 폭 실측 (실 dev 서버 + 헤드리스 Chrome, Pretendard 실폰트)

환경설정 탭 줄은 §25 정본(ViewTabs)이 아니라 `flex-1` 필 나열이었다 — 대시보드 주석이
'제4 변종 폐기'라 부른 바로 그 문법이다. 라벨에 `whitespace-nowrap` 이 없어 넘치는 대신 **접힌다**.

| 폭 | 현행 7탭 | 현행 문법 8탭 | ViewTabs 8탭 |
|---|---|---|---|
| 320 | 칸 36.28px · 줄 높이 66px | 칸 **31.25px · 줄 높이 86px**(4줄) | 칸 55.9~96.4px · 높이 46px · 트랙 635px 가로 스크롤 |
| 360 | 42px · 66px | 36.25px · 66px | 〃 |
| 390 | 46.28px · 66px | 40px · 66px | 〃 |

- 8탭이 되면 320px 에서 '데이터·도구'가 네 줄로 접히고 탭 줄 높이가 66 → 86px 로 부푼다.
  가로 스크롤바(document scrollWidth)는 0이지만, 31px 칸에 세로로 쌓인 한글은 읽을 수 있는 라벨이 아니다.
- 그래서 §25 정본(자연폭 + 넘치면 가로 스크롤)으로 전환했다. 호실 관리·수납 관리·홈이 이미 쓰는 문법이고,
  ViewTabs 트랙 자체가 `max-w-full overflow-x-auto` 라 어느 폭에서도 문서 가로 넘침이 구조적으로 0이다.
- **관찰(범위 밖)** — ViewTabs 문서 주석은 '2~4 권장, 5 max'다. 8탭은 그 권고를 넘는다.
  탭 수를 줄이는 것은 운영자가 2단계로 미룬 재편의 몫이라 여기서 손대지 않는다.

## 갈린 선택

### 슬러그 저장을 왜 별도 액션으로 뺐나 (회귀 1순위)

`updatePropertySettings` 는 FormData 통짜 저장이다. 슬러그 입력이 기본정보 폼에서 빠지면
`formData.get('publicSlug')` 가 null → `publicSlug || null` → **null 로 덮인다**. 기본정보에서
영업장명만 고쳐 저장한 순간 소개 페이지 주소가 조용히 지워지고, 마케팅 화면이 빈 상태로 돌아간다.

처방은 두 겹이다.
1. `updatePropertySettings` 는 `formData.has('publicSlug')` 일 때만 그 칼럼을 쓴다.
   같은 파일의 `cleaningFeeInDeposit` 이 이미 쓰던 문법이다(체크박스가 소유자에게만 렌더돼
   다른 멤버의 저장 한 번에 꺼지던 것을 `undefined` 로 막은 자리).
2. 웹사이트 탭은 전용 액션 `updatePublicSlug` 로 그 칼럼만 쓴다. `publicSlug` 는 `@unique` 라
   충돌(P2002)을 잡아 사람 말로 돌려준다 — 통짜 저장 시절엔 raw 예외가 토스트에 그대로 나왔다.

되돌리려면 두 조각을 함께 되돌린다(가드만 남기면 슬러그를 저장할 출구가 사라진다).

### 대조 테스트

`scripts/check-settings-slug-guard.mjs` — 기본정보 폼에 `name="publicSlug"` 입력이 없다는 사실과
`updatePropertySettings` 가 `formData.has('publicSlug')` 가드를 쥐고 있다는 사실을 함께 본다.
가드가 사라지면 빌드 게이트에서 잡힌다. `verify:fast` 에 등재.

### 후보를 서버 프롭으로 내리는 이유

`WebsiteTab` 은 마운트에서 서버 액션을 부르지 않는다. `settings/page.tsx` 가 목록을 실어 내리고,
토글 후에는 `router.refresh()` 로 갱신한다 — RoomManageClient 가 쓰는 'prop 직접 사용 +
revalidatePath + router.refresh' 짝 그대로다. 탭을 열 때 스켈레톤이 한 번 번쩍이지 않고,
홈이 세는 건수와 탭이 그리는 목록이 같은 한 벌에서 나온다.

### 홈의 한 줄

'이달 입퇴실 N건' 링크와 같은 문법(0.6875rem · `--tc-text` · 꼬리 ›)이다. N 은 올릴 후보 + 내릴 후보 합이고
0이면 줄 자체를 안 그린다. 홈 서버 페이로드는 목록에서 **건수 두 개**로 줄였다 — 화면이 안 그리는
썸네일·이용료를 실어 보낼 이유가 없다.

### 어휘

'공개 페이지'는 노출 문구에서 전면 소거하고 '소개 페이지'로 통일했다(운영자 확정).
코드 식별자(`publicSlug`·`showOnSite`)와 주석은 그대로 둔다 — 식별자를 바꾸면 DB 칼럼까지 끌려간다.

### 딸려 나온 것 하나 — ViewTabs 활성 탭 스크롤

8탭 트랙은 어느 폭에서도 넘친다(자연폭 합 635px). 홈에서 `?tab=website` 로 들어오면 웹사이트 탭이
스크롤 밖에 있어 '아무 탭도 안 골라진' 화면이 됐다. 트랙이 실제로 넘칠 때만 트랙의 가로 스크롤을
움직인다 — 3~4탭 페이지는 첫 줄에서 빠져나가 무동작이라 기존 화면은 변하지 않는다.
`scrollIntoView` 를 안 쓴 이유는 그쪽이 페이지 세로 위치까지 끌고 갈 수 있어서다.
별도 커밋(818c44a3)이라 이것만 되돌릴 수 있다.

## 실데이터 (읽기 전용, 2026-08-18)

더스테이원룸텔 제기역점 · 슬러그 `thestayjegi` — 반영 대기 **11건**(올릴 방 0 · 내릴 방 11).
`countSiteRoomCandidates` 와 `listSiteRoomCandidates` 의 수가 일치한다(홈 숫자 = 탭 목록 길이).
내릴 방 11개는 입주 중인데 소개 페이지에 아직 올라가 있는 방들이다(402·404·408·409·418·501·
503·504·506·508·520). 배포 후 홈에 "소개 페이지 반영 대기 11건" 한 줄이 뜬다.

## 범위 밖 관찰 (2단계로)

- **탭이 8개다.** ViewTabs 주석의 권고는 '2~4, 5 max'. 320px 에서 트랙 635px 은 두 화면 반이라
  뒤쪽 탭은 스크롤로만 닿는다. 탭 수를 줄이는 재편이 2단계의 본론이다.
- **기본정보 탭이 여전히 잡화점이다.** 로고 2종 · 인수/귀속 날짜 · 보증금·청소비 · 예약금 · 위약금 ·
  면적 · 환불 규정 · 임의처분 동의서 · 계좌 · 알림 리드타임이 한 폼에 있고, 그 안에 '계약·서류'
  소제목이 이미 서 있다. 분해선은 그 소제목이 이미 그어 놓았다.
- **통짜 FormData 저장은 그대로다.** 이번에는 슬러그 한 칸만 가드로 막았지만, 같은 함정은 기본정보에서
  필드를 하나 더 옮길 때마다 되살아난다. 2단계에서 폼을 쪼갤 거라면 저장 경로부터 필드 단위로 나누는
  편이 안전하다.
- **'전체 보기 ›' 링크들이 아직 `--coral`.** 홈 링크 줄 주석(DashboardClient 1505 근처)이 이미
  별건으로 적어 둔 것이다. 이번 한 줄은 `--tc-text` 를 썼다.
- **`check-document-override-lock` 3건**(응우옌 티 타오 아인·문정현·쩐 마이린의 표시값 '소재지'가
  자동값과 같다)은 이번 작업 이전부터 있던 데이터 결함이다. 이번 diff 는 서류 경로를 건드리지 않았고
  DB 쓰기도 없었다. 별도 처리 대상.
