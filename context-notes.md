# 컨텍스트 노트 — 입퇴실 캘린더 거주 구간 재시공 (2026-08-20)

도메인 사실은 [[knowledge/domain-room-stay]] 에 적립했다. 여기는 이번 작업의 결정과 근거다.

## 기준선 실측 (시공 전, 이 워크트리)
- 워크트리에 `.env.local` 없어 메인 체크아웃 것을 심링크. `node_modules` 없어 `npm ci`.
  `prisma/schema.prisma` 는 메인과 **바이트 동일**(md5 325c433a…) 이라 `prisma generate` 안전.
- tsc 0 · eslint 491건(291 errors · 200 warnings) · verify:fast exit 0.
- verify:db 는 `check-document-override-lock` 에서 멈춘다 — 소재지 오버라이드 3건(기지 예외).
  그 뒤 축 6개는 따로 돌려 전부 exit 0 확인. 시공 후에도 같은 자리에서 같은 이유로 멈춘다.
- 월별 eventCount: 2026-05 9 · 06 9 · **07 9** · 08 25 · 09 6.

## 조사 보고서의 사실 3건이 실 DB 와 달랐다 (패널 3인이 독립적으로 같은 정정)
- 김태란 이사일은 2026-06-15 가 아니라 **2026-07-01**(메인 세션이 이미 정정한 값).
- 그래서 "조원섭·김태란 508호 5일 겹침"은 **없다**. 오히려 6/21~6/30 열흘 공백이다.
  운영자 결정 6번(겹침 사실 확인)은 물을 것이 없어졌고, 대신 물을 것은 "그 열흘 508호가 실제로
  비어 있었나"다.
- 건수가 바뀌는 달은 2026-06 이 아니라 **2026-07**(9 에서 11).

## 설계 결정

### 막대 출처 전환
- `stays` 는 **필수 칸**이다. 선택 칸으로 두면 조회 한 곳이 빠뜨려도 타입이 안 잡고 그 순간
  이 화면은 다시 계약의 roomId 한 칸만 보는 상태로 돌아간다.
- **열린 구간의 끝은 계약이 말한다.** 구간에서 읽으면 아직 안 나간 다섯 계약(503 송호준 ·
  402 김주호 · 404 조성훈 · 513 민경진 · 522 이경호)이 전부 무기한 관통 막대가 된다. 이번 사고의
  (다)증상을 다섯 배로 늘리는 선택이었다.
- 회귀 픽스처는 계약당 구간 하나를 기본값으로 세운다. 빈 배열을 기본으로 두면 123 케이스가
  전부 **폴백 경로**를 지나 새 코드를 한 줄도 안 밟는다.
- 같은 계약의 두 조각끼리는 겹침 판정을 하지 않는다(같은 사람이다).

### 이사 라벨에 상대 호실을 넣은 이유
- 행 정렬이 호실번호 고정이라 506 과 508 사이에 507 이 낀다. 선으로 잇기·화살표는 가이드
  미등재라 금지다. 게다가 **범위가 이사일을 안 물면 한쪽 막대는 아예 안 선다.**
  어느 막대 하나만 봐도 사건을 알아야 그 사실이 화면에서 사라지지 않는다.
- 웹디자이너와 UX 가 독립적으로 같은 문안에 도달했다("7/1 508호로 이사" / "7/1 506호에서 이사").

### 감지망을 위해 조회를 분리한 것
- `lib/moveCalendarData` 신설. 그물이 **같은 조회·같은 조립**을 지나야 표시와 데이터를 맞댈 수
  있다. 액션 파일에 두면 그물은 사본을 들 수밖에 없다('use server' 는 async 만 내보낼 수 있고
  내보내는 순간 인증 없는 입구가 된다).
- 축 A 는 위반이 아니라 **모집단**이다. B·C 가 지켜보는 대상이 그것뿐이라 조용히 늘면 B·C 의 0 이
  '덮었다'인지 '안 봤다'인지 구분이 안 된다. 그래서 기준선 래칫(저장소 관례 4곳과 같은 문법).
- 축 B 에 마감 조건이 없으면 NON_RESIDENT 구간 3건이 오탐으로 섞인다(사무실 이원빈 · 415 최은옥 ·
  601 김상혁). 조사 보고서의 "미퇴실 4건 오탐"은 재현되지 않았다 — 실제 오탐원은 NON_RESIDENT 다.

### 이사일 미래 금지
- 감지망 축 ⑥ 이 미래에 시작하는 열린 구간을 사고로 부른다(KST +1일 유예). 모레 이사를 미리
  적으면 저장하는 순간 그물이 켜진다. 미래 이사를 받으려면 축 ⑥ 을 함께 고쳐야 하고 그건 별건이다.
- 부수 효과 — 미래 이사가 없으므로 `getMoveCalendarRange` 의 범위 스캔은 손댈 필요가 없다.
  이사일은 늘 입주일과 퇴실일 사이라 범위의 최소·최대를 못 넓힌다.

### holding 필터를 계약 상태로 남긴 이유 (도메인 패널이 결정을 요구한 자리)
- 도메인 전문가가 대안을 냈다 — "그 구간이 열려 있을 때만 방을 잡은 것으로 본다".
  **채택하지 않았다.** 그러면 이사로 떠난 방에서 앞사람이 아직 나가기 전에 뒷사람이 들어온
  **진짜 이중 점유**가 조용해진다. 그리고 같은 선을 정본 셋(roomAssignmentBlockReason ·
  createOverlapAck · check-room-availability-drift 축 ②)이 공유하는데 캘린더만 넓히면
  "화면은 부르는데 확인 버튼이 거절하는" 상태가 생긴다.

## 패널 회신 현황 — 4인 전원 회신(최종 출력으로 도착, SendMessage 미사용)

| 관점 | 회신 | 주요 이견 |
|---|---|---|
| 호실 배정·기간 모델 도메인 | 도착 | holding 필터 재정의를 제안(미채택, 위 근거) |
| 백엔드 정합 | 도착 | 없음. 조회 형태·축 설계·기준선 관례를 그대로 채택 |
| UX/UI | 도착 | **이사를 입퇴실 2건으로 세는 것에 반대**(아래) |
| 웹디자이너 | 도착 | 거주색 교체를 별건으로 빼자는 UX 의견과 충돌(승인 범위대로 포함) |

### 채택하지 않은 의견과 근거
- **UX: 이사를 건수에서 빼거나 1건으로 세자.** "입퇴실"이 사람이 들고 나는 사건이라 2로 세면
  월 회전율 감각이 과대 계상된다는 지적. **운영자 오더가 '포함'이라 포함으로 시공했다.**
  다만 지적 자체는 타당하고, 숫자가 아니라 문구를 바꾸는 쪽이 나을 수 있다 — 열린 질문으로 남긴다.
- **UX: 거주색 교체를 별건으로.** 색 변경과 무회귀 판정이 섞인다는 지적. 운영자 승인 범위에
  들어 있어 포함하되 **커밋을 갈라** 원인 추적이 섞이지 않게 했다.
- **UX: barLabel 을 손대지 마라.** (다)증상은 시공만으로 닫힌다는 것은 맞다. 다만 손대지 않으면
  옛 방에 '7/1 퇴실'이라 적히는데 그 사람은 건물 안에 있다. 같은 회신의 권고 A 가 어휘 신설을
  필수로 꼽았고 그쪽을 따랐다.
- **웹디자이너: 겹침 밴드를 막대 뒤로 옮기자.** 밴드가 보이는 방식이 바뀌는 변경이라 승인 범위
  밖으로 뒀다. 대비 우려는 실측으로 기각됐다 — 막대 안 글자는 `position: sticky` 라 이미 밴드
  **위**에 그려지고, 실측 4.54:1(라이트) · 6.44:1(다크) 로 AA 를 넘는다.

## 백필 필요 여부
- **불필요.** DB 쓰기 0. 커버리지 구멍 0(비예약 75건 전부 구간 보유).
- 시작일 없는 구간 6건은 계약의 `moveInDate` 도 없다 — 틀린 값이 아니라 없는 정보다.
  백필하려면 원 입주일을 운영자가 알아야 하고 그건 데이터 땜빵이 아니라 사실 확인이다.

## 배포 전 디자이너 패스에서 나온 것 (전부 반영)
- **포커스 링 소실.** `.mc-bar` 의 투명 outline 이 Tailwind 의 focus-visible 유틸을 이겼다.
  `app/globals.css` 에는 `@layer` 블록이 하나도 없어 그 파일의 규칙은 언레이어드이고, 언레이어드
  author 선언은 명시도와 무관하게 어떤 레이어보다 앞선다. **커스텀 클래스에 outline 을 얹을 때는
  같은 자리에서 focus-visible 도 함께 세워야 한다** — `.room-tile` 이 같은 문법인데 붙는 요소에
  focus 유틸이 없어 여태 안 드러났다. 다음에 이 문법을 또 쓸 때 밟을 자리다.
- **조사 '로'.** `fmtRoomNo` 는 숫자가 아니면 입력을 그대로 돌려주고, 호실 등록 폼이 'A동-3'·
  '옥탑방'을 예시로 권한다. "특정 영업장 호실은 다 숫자"는 멀티테넌트 전제에서 근거가 못 된다.
- **tnum.** 이사 문구가 막대 라벨에 호실번호를 처음 실었다. 같은 행 왼쪽 호실 열은 이미 tnum 이다.

## 게이트 결과
- tsc 0 · verify:fast exit 0 · 프로덕션 빌드 exit 0
- eslint 손댄 파일 기준선 동일(신규 0). 전체 491 유지.
- verify:db — 신규 축 통과, 체인은 기준선과 같은 자리(소재지 오버라이드 3건)에서만 멈춘다.
- 감지망 역주입 — 조립을 사고 전 상태로 되돌리자 축 B 1건 · 축 C 66건 발화, exit 1.
- 좁은 폭 실측(실제 컴포넌트 esbuild 번들 + 빌드 산출 CSS + Pretendard, 실데이터 19행 36막대):
  320/360/390 × 라이트·다크 6조합 문서 넘침 0 · 요소 넘침 0 · 막대 히트 최소 44.0px ·
  10.5px 미만 글자 0 · aria 없는 막대 0 · 범례 폭 286px(320px 뷰포트 안).
- 대비 실측(캔버스 합성): 라이트 거주 8.01 · 예약 10.13 · 겹침 밴드 위 4.54 · 범례 5.12 /
  다크 거주 7.25 · 예약 10.35 · 겹침 밴드 위 6.44 · 범례 4.91. 전부 AA 통과.
