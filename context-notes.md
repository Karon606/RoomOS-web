# 서류 성명 표기 사람 단위 기본값 맥락 (2026-09-03)

## 이미 있던 것
2026-08-29 작업으로 `lib/documentName.ts` 정본이 서 있다. 표기 셋('ko'|'en'|'native'),
사람별 후보(docNameStyles), 서류를 열 때의 기본값(resolveDocNameStyle), 계약 단위 이어받기
(LeaseTerm.lastDocNameStyle), 서류별 저장 자리 셋(ContractFile·ResidenceCertFile·RentReceiptFile
의 nameStyle). 국적 비한국이면 영문 기본도 이미 있다. **없던 것은 사람 단위 기본값 한 칸이다.**

## 운영자 결정 (전부 권고대로)
- 컨트롤은 토글이 아니라 셀렉트 넷(자동·한글·영문·현지). 이 코드베이스에 토글 정본 컴포넌트가
  없고(이진값인 흡연 여부도 SelectField), 형제 칸 '카드 표시 이름'과 같은 문법이 된다.
- 이어받기 충돌은 앞 서류 우선. "한 사람의 서류는 같은 표기로"(2026-08-29 확정)가 세다.
  대신 폼에서 저장할 때 안내 한 줄.
- 외국인 판정에 외국인등록번호 보유를 OR 로 넣는다. 오늘 데이터로는 무변화(보유 6명 전원이
  국적도 비한국, 합집합 24 = 국적 단독 24)지만 국적을 비운 채 번호만 넣는 순서가 가능하다.
- 노출은 외국인에게만. 내국인 93명 화면은 0비트 변화.
- 폼의 `!== '대한민국'` 직접 비교를 정본 판정으로 통일. 국적을 '한국'으로 적은 사람이 폼에서는
  외국인, 서류에서는 내국인으로 갈리던 자리다.

## 왜 새 칼럼인가 (displayNameStyle 확장 기각)
스키마 주석이 "서류는 이 값을 절대 읽지 않는다"고 못박고 있다. 카드와 서류가 한 칸을 읽는 순간
2026-08-11 사건(영문 계약서가 필요해 운영자가 Tenant.name 자체를 영문으로 갈아엎어 앱 전체에서
한글 이름이 사라진 일)의 혼동 자리가 재연된다. 두 축은 이름이 비슷해 더 위험하므로 주석을
양방향으로 박고 감지망으로 침범을 막는다.

## 왜 3순위인가
새 값은 강제가 아니라 **기본값 공급자**다. 국적 추정 자리를 대체하되, 서류 화면에서 손으로 고른
값(1순위)과 앞 서류(2순위)에는 진다. 기존 원칙 "손으로 고른 것이 국적 추정보다 세다"와 한 결이다.

## 소급 없음
발급본은 세 파일 모델의 nameStyle 에 박제되고 재전송 파일명도 저장값을 읽는다. 서명된 계약은
lib/contractData 의 signedAlready 분기가 저장값만 읽는다. 스위치는 앞으로 여는 서류의 초기값만
바꾼다. 배포 직후 화면·종이가 바뀌는 사람은 0명이다(기본 NULL = 오늘 규칙).

## 남은 데이터 갭
영문 이름이 없는 외국인 2명은 어떤 규칙을 넣어도 영문으로 못 나간다(docNameStyles 가 후보에
'en' 을 안 넣는다). 코드가 아니라 데이터 문제라 안내 문구로 답한다.
