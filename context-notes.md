# 재고 보정 소비 귀속 선택지 — 결정과 근거

## 왜 새 산식을 만들지 않았나

"차이를 지난 기간 소비로 계산"은 일반 점검이 이미 하는 일이다. overview.ts 의 intervalPairs 가
`isReconcile` 구간만 건너뛰므로(409-413행), 같은 저장을 isReconcile=false 로 만들면 그 차이가
자동으로 telescoping 합산에 들어가고 월별 사용량(562행)과 평균 소모율(444-445행 분모)에 함께
반영된다. 그래서 이번 작업은 스키마도 산식도 건드리지 않는 **플래그 분기 하나**다.

## 판정을 서버에 둔 이유

차이의 부호를 클라가 판정해 isReconcile 을 직접 보내면, 화면마다 규칙 사본이 생긴다.
대신 화면은 '무엇을 골랐나(attribution)'와 '무엇을 보여줬나(expectedQty)'만 보내고,
`isReconcile` 은 constants.resolveDiffAttribution 한 곳이 정한다. 회귀 테스트도 그 함수 하나에 건다.

두 인자 모두 선택이라, 이 인자를 안 보내는 호출은 한 글자도 안 바뀐 채 현행대로 저장된다.
기본값이 '현행'인 것이 무회귀의 근거다.

## 증가분을 항상 보정으로 두는 이유

늘어난 양을 '소비'라 부를 수 없다. 게다가 일반 점검으로 저장하면 그 구간 consumed 가 음수가 되어
30일 창 합산(rateOver)을 갉아먹고, 월별 사용량은 0 으로 클램프되면서(566행) 실제로는 존재하는
사용을 가린다. 그래서 모달 단위 선택이어도 증가 품목만은 선택 밖에 둔다.

## dedupSameDay 승격 함정

overview.ts 228-229 는 같은 날에 보정이 하나라도 있으면 살아남는 점검까지 보정으로 승격시킨다.
즉 '사용으로 기록'을 골라 저장해도, 그 날에 보정이 이미 있으면 결과가 통째로 보정이 된다.
타임라인 보정 폼은 이미 같은 날 점검 경고를 갖고 있었으므로 그 박스 안에 한 줄만 더했다
(사용 기록을 고른 경우에만). 새 경고 표면을 만들지 않았다.

## 문구가 선택을 따라가야 하는 이유

'보정 저장' 버튼을 눌렀는데 일반 점검이 저장되면 그 자체가 거짓말이다. 그래서 폼 머리글·저장
버튼·저장 토스트·되돌리기 토스트를 전부 실제 저장 모드에 묶었다. 판정식은 서버와 같은
`차이 < -0.001 && 선택 == usage` 다.

## 착수 전 발견한 회귀 (선행 커밋 5bbcfdf7)

전체 재고 보정 모달을 공용 Modal 로 옮긴 e24c4e03(2026-07-02)이 머리 블록을 통째로 지웠다.
DatePicker 와 '창고 → 방 보충을 모두 마쳤습니다' 게이트가 사라졌는데 `disabled={!restockDone}`
는 남았다. `setRestockDone` 호출이 파일 전체에 0회라 restockDone 이 영원히 false 였고,
그 결과 **약 7주간 이 모달은 열려도 모든 칸이 잠기고 저장이 불가능**했다. 보정 날짜도 오늘 고정.

eslint 기준선이 이를 방증한다 — 546c6927 에서 494가 아니라 496이었고, 늘어난 2건이 바로
`setDate`·`setRestockDone` 미사용 경고다. 복구하니 그 2건이 사라졌다.

이번 사양이 이 모달을 두 표면 중 하나로 지목하므로, 잠긴 채로 두면 새 선택지가 시연도 검증도
불가능하다. 삭제된 마크업을 원문 그대로 되살렸고(게이트 문구 크기만 현행 캡션 스케일로),
같은 커밋에 다른 변경을 섞지 않았다.

## 실측 (읽기 전용, DB 변경 없음)

- 좁은 폭 — 새 UI 조각(DiffAttributionChoice · 복구한 보충 게이트 · 같은 날 경고 · 점검 폼
  체크박스)을 InventoryClient.tsx 원문에서 잘라 esbuild 로 컴파일하고, 빌드 산출 CSS +
  Pretendard 를 물린 헤드리스에서 계측. 320/360/390 × 라이트·다크 6조합 전부 문서 넘침 0 ·
  요소 넘침 0. 라벨 박스 높이 47~76px(터치 44px 하한 충족).
- verify:db 재고 축 — 재고 경계 위반 0 · 원장 조정 위반 0(점검 605건 대조) · 옮김 허브 미차감 0
  (점검 144건) · 재고 귀속 위반 0(지출 488건 · 카드 33개) · 비추적 카테고리 활성 카드 0.

## 범위 밖 관찰 (이번 diff 에 손대지 않음)

- **품목 상세의 진입 버튼 라벨이 낡았다.** '보정 (차이 소모 제외)' 인데 이제 그 폼에서 소비로
  기록할 수도 있다. 승인 사양에 없어 그대로 뒀다. 문구 조정이 필요하면 별건.
- **전체 보정 모달엔 같은 날 점검 가드가 없다.** 타임라인 폼에만 dateHasCheck 확인이 있다.
  모달은 오늘 날짜로 저장하는 게 보통이라 충돌 빈도가 낮지만, 승격 함정 자체는 같다.
- **verify:db 기존 결함 3건은 이번 변경과 무관하다.**
  ① verify-recurring-estimate — 전기요금 이번 달 기록 완료인데 예약금액 1,911,220원 잔존.
  ② check-room-availability-drift — 422호 한희규(RESERVED 8/18~)와 박호정(CHECKOUT_PENDING
     ~8/19) 체류 겹침. Work_log 3433행에 운영자 확정(당일 회전 정상)과 개정 과제로 남아 있다.
  ③ check-document-override-lock — 응우옌 티 타오 아인·문정현·쩐 마이린의 '소재지' 오버라이드.
  세 스크립트 모두 이번 커밋에서 파일이 안 바뀌었고 DB 는 읽기만 했다.
- **check-spec-dims 라면 차원 2건 · check-item-category-split 수세미 경고 1건**도 기존 대기분
  (Work_log 3164행)이라 그대로 뒀다.
- **분실·폐기 3분류는 2단계다.** 이번 범위 밖으로 명시돼 있어 손대지 않았다.
