# 컨텍스트 노트 — 창고 특약(추가 호실 특약) 시공, 2026-08-19

## 무엇을 만들었나

거주용이 아닌 방(창고·사무실)을 추가 호실로 딸고 있는 계약서에만 코드가 절 하나를 붙인다.
제목은 '추가 호실 특약(보관 용도)', 번호는 렌더 시 동적(기본 5), 문안 8항은 전 영업장 공통 고정.
601호 창고를 딸고 있는 김상혁 509호 합본 계약서가 현재 유일한 대상이다.

## 왜 이 구조인가

### 특약을 template 객체에 넣지 않는다
본문 템플릿(ContractTemplate)은 박제·드리프트 비교의 축(printedFacts.template)이다. 코드가 만든
절을 그 안에 섞으면 조항을 한 글자도 안 고친 계약서가 통째로 드리프트로 뜬다. 그래서 ContractData 의
별도 칸으로 내려보내고 렌더 직전에 절 배열 뒤에 붙인다. 환불 조항·신원번호 동의문이 이미 같은 문법이다.

### 영업장 템플릿에도 넣지 않는다
템플릿에 넣으면 운영자가 지울 수 있고, 지워진 채 창고 호실만 인쇄된다. 그러면 그 방을 주거로 쓰지
말라는 근거가 종이 어디에도 없다. 영업장별 문안 편집은 별도 백로그다(설계 확정 사항).

### 조건은 두 개가 함께 참일 때
① 딸린 계약의 상태가 NON_RESIDENT ② 그 방이 Room.nonResidentVacant=false(창고·사무실처럼 점유로
세는 방). 601호가 정확히 이 조건이고, 주소지만 유지하는 비거주나 미래 거주 예정 방은 ②에서 빠진다.
판정 정본은 lib/contractData 의 contractSubLeaseAddendum 하나다 — 화면과 발급 API 가 각자 조건을
들면 화면에는 있고 종이에는 없는 절이 생긴다.

### 서명이 끝난 계약은 그때 굳은 값을 쓴다
resolveSignedBody 가 박제본(SNAPSHOT)이면 snapshot.subLeaseAddendum 을 그대로 돌려주고, 판정
헬퍼는 그 값을 그냥 통과시킨다. 지금 조건으로 다시 판정하면 나중에 창고를 딸았다는 이유만으로 이미
서명한 종이의 본문이 늘어난다(서명 시점 본문 격리를 스스로 깨는 것).
반대 방향도 막아야 해서 **박제하는 두 자리 모두에 값을 넣었다** — 원격 서명(sign actions)과 대면
서명(generate route). 안 담으면 서명 뒤 재발급에서 박제본이 특약을 모르고(null) 절이 통째로 사라진다.

### diff 0 을 구조로 보장한다
appendSubLeaseAddendum 은 addendum 이 없으면 **받은 배열을 그대로 반환**한다. 새 배열도 만들지
않으므로 splitClauseColumns 의 입력이 종전과 동일하고, 렌더 결과가 문자 단위로 같다.
사영 축도 값이 없으면 undefined 로 남아 옛 박제·옛 링크와 비교 자체가 생략된다.

## 실측 (읽기 전용, DB 변경 없음)

- 종속 계약은 전 영업장 통틀어 1건(김상혁 601호, NON_RESIDENT, nonResidentVacant=false).
  거주용 아닌 방은 6개(415·601·602·603·604·사무실)지만 딸린 계약이 걸린 것은 601호뿐이다.
- 김상혁 509 계약은 서명 박제·서명 이미지·개별 본문이 모두 없다(LIVE) — 특약이 바로 붙는다.
- 발급 대상 실계약 전건 렌더(지목 없음 + 계약별 지목 = 164 조합) 중 162건이 변경 전과 문자 동일.
  차이 2건은 김상혁 509(auto·지목)이고, 삽입분은 특약 절 하나와 2단 재분배(§3 꼬리가 왼쪽으로,
  §4 가 오른쪽 단 머리로 이동)뿐이다.
- 김상혁 합본 PDF 실측 — 특약 유무와 무관하게 100% 배율·총 3장(계약서 2 + 임의처분 동의서 1),
  축소맞춤이 0.88 하한까지 내려갔다가 원본으로 되돌리는 경로가 양쪽 동일. 즉 이 계약서는
  **특약 이전부터 이미 2장**이었고, 특약이 장수를 늘리지 않았다.
- 서명 링크 23건·발급본 박제 6건 모두 새 축이 undefined(기준선 무변동).

## 범위 밖 관찰 (이번 diff 에 손대지 않음)

- **김상혁 509 계약서는 특약 이전에도 2장이었다.** 운영자 조항이 길어져 축소 하한(88%)으로도 한 장에
  안 들어간다. 설계가 허용한 '2장'이 이미 현실이라 이번 변경으로 새로 생긴 상태가 아니다.
- **lib/contractPrintedFacts.ts 머리말의 '15축'이 실제와 어긋난다.** 추가 호실 축이 붙었을 때 이미
  16이었고 지금은 18이다(app/api/contract/generate/route.ts 주석도 같은 숫자를 옮겨 적고 있다).
  이번 작업과 무관한 주석 드리프트라 손대지 않았다.
- **verify:db 세 건은 이번 변경과 무관한 기존 데이터 결함이다.**
  ① verify-recurring-estimate — 전기요금이 이번 달 기록 완료인데 예약금액 1,911,220원이 남아있다.
  ② check-room-availability-drift — 422호 한희규(RESERVED 8/18~) 와 박호정(CHECKOUT_PENDING
     ~8/19)의 체류 구간이 겹친다.
  ③ check-document-override-lock — 응우옌 티 타오 아인·문정현·쩐 마이린의 '소재지' 오버라이드가
     자동값과 같다(직전 세션 기록에도 같은 3건이 남아 있다).
  세 스크립트 모두 이번 커밋에서 파일이 바뀌지 않았고 DB 도 읽기만 했다.
- **감지망을 새로 만들지 않았다.** 설계의 적용 지도에 scripts·package.json 이 없어 상설 축은 범위
  밖으로 봤다. '특약 조건 대 렌더 일치'는 이번 세션에 164건 전수로 확인했다(불일치 0). 상설화가
  필요하면 별건으로 판단받는 편이 맞다.
- **8항 방침은 설계대로 문안 유지다.** '본 계약 종료 시 추가 호실 계약도 함께 종료'를 종이가
  선언하지만 시스템은 여전히 경고 방식이다(부모 퇴실 시 딸린 계약 자동 종료 없음). 종이가 앞서고
  시스템이 따라가는 간극을 운영자가 허용한 상태다.
