# 겹침 판정 개정 — 시공 노트 (2026-08-19)

## 이 개정이 푸는 문제
겹침 판정이 한 벌(occupancyOverlaps, 같은 날도 겹침)뿐이라 두 가지가 섞여 있었다.
① 당일 회전(앞 계약 퇴실일 == 뒤 계약 입주일)은 고시원 일상인데 사고로 셌다.
② 하루 이상의 의도된 겹침은 화면 확인창을 지나가도 아무 데이터도 안 남아, 감지망이 매번
   같은 줄을 다시 불렀다. 확인은 사람의 머릿속에만 있었다.

## 확정 설계에서 지킨 선
- **occupancyOverlaps 의 `>=` 는 건드리지 않는다.** 소비처가 넷(가져오기·캘린더·감지망·화면)이라
  판정선 자체를 옮기면 전부가 같이 흔들린다. 회전은 **이름 있는 술어**(isSameDayTurnover)로 따로
  묻고, 소비처가 각자 "회전은 빼고 본다"를 명시한다.
- **확인은 데이터다.** LeaseOverlapAck 한 행이 '누가 언제 어느 구간을 의도된 겹침으로 확인했다'를
  진다. 구간을 스냅샷으로 박는 이유는 자동 실효다 — 날짜가 하루라도 더 밀리면 그 확인은 더 이상
  같은 사실이 아니므로 다시 발화해야 한다.
- **줄은 지우지 않는다.** 확인된 겹침도 캘린더에 남는다. 사라지면 해제할 길이 없고(적용취소 원칙),
  한 방에 두 사람이 있다는 사실 자체가 없던 일이 된다. 색만 중립으로 내린다.

## 판정 정본의 모양
- `occupancyOverlapSpan(a, b)` — 실제 겹침 구간. 열린 쪽(입주일·퇴실 예정일 없음)은 null 로 남는다.
  겹치지 않으면 null. 두 술어(회전·확인)가 전부 이 하나를 딛는다.
- `isSameDayTurnover` — 겹침 구간이 하루이고, 그 하루가 한쪽의 퇴실일이자 다른 쪽의 입주일일 때.
  같은 날 함께 시작하는 두 계약(예: 둘 다 8/18 입주)은 회전이 아니다 — 앞 계약의 입주일이
  그 하루보다 **앞서야** 회전이다.
- `findOverlapAck` — 지금 겹침 구간이 확인 구간 안에 들어 있으면 그 확인 줄. 구간이 열려 있으면
  (무기한) 절대 덮이지 않는다. 확인은 유한한 사실에만 붙는다.

## 기록 경로를 셋으로 둔 이유
운영자가 "그래도 하겠다"를 말하는 자리가 원래 셋이다(예약 확정 확인창·퇴실일 연장·캘린더).
그중 둘은 이미 확인창이 있었는데 승인이 휘발했다. 그 승인을 그대로 행으로 적는 것이 이번 변경의
전부다 — 새 확인 절차를 만들지 않았다. 캘린더 [겹침 확인] 만 새 진입점이고, 그것은 이미 벌어진
겹침(다른 경로로 생겼거나 과거에 만들어진)을 사후에 확인하는 유일한 길이라 필요하다.

## 단기 연장(extendShortStay)을 기록 경로에 넣지 않았다
설계가 지목한 것은 `allowRoomOverlap` 세 경로다. extendShortStay 의 승인 표식은 `allowOverlap` 로
이름도 흐름도 다르고, 설계에 없다. 그 경로로 생긴 겹침은 감지망이 부르고 캘린더 [겹침 확인] 이
받는다 — 구멍이 아니라 사후 확인으로 처리되는 자리다.
