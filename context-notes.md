# 컨텍스트 노트 — 오류신고 2건 시공 (2026-08-24)

8b9b6c43 현금영수증 합계 · c2ab5b83 날짜 변경 어포던스. 결정과 그 근거를 적는다.

## 실측 도구를 세우고 시작했다

이번 두 건은 "몇 줄이 되나" · "잘리나" 가 곧 판정이라 계산으로는 못 끝냈다. 빌드된 Tailwind
CSS(`.next/static/chunks/*.css`) + 로컬 `public/fonts/PretendardVariable.woff2` + 실제 Chrome
으로 조각 마크업을 렌더해 `scrollWidth > clientWidth`(잘림)와 줄 높이를 쟀다. 다크는
`html.dark` 토글이다 — `prefers-color-scheme` 로 재면 라이트 값이 나온다(`globals.css:474`).

**계산과 실측이 실제로 갈렸다.** 디자이너 패널은 캡션 줄이 세 폭 전부 이미 2줄이라 한정어를
붙여도 무변화라고 계산했는데, 오늘 실데이터(카드 380,000원 1건)로 재면 390px 은 1줄이었고
한정어를 붙이면 2줄이 된다. 즉 **줄 증가는 데이터 의존**이고 최악 +1줄이다. 패널에 실측을
되돌려 재판정을 받았고 A 유지 결론은 안 바뀌었다(그 줄의 길이는 위로 열려 있고, 대안 C 는
데이터와 무관하게 항상 +1줄이라 엄격히 더 나쁘다).

## 신고 1 — 축은 안 바꾼다. 축 이름을 바꿨다

**차액의 실체.** payDate 축 7,170,000원 17건, targetMonth 축 7,640,000원 18건. 차이
470,000원은 **7/31 에 받은 8월분 한 건**이 전부였다(회계 패널이 차집합으로 확인). 운영자가
목록을 손으로 더한 값이 곧 귀속월 축이다. 계산 결함이 아니다.

**어휘가 이번 건의 핵심 판정이었다.** 브리프는 "'납부일 기준' 계열"을 제안했는데 회계·UX 두
패널이 독립적으로 **차단**을 냈고 디자이너가 근거를 하나 더 얹었다.

- 이 앱에서 `납부일` 은 `LeaseTerm.dueDay`, 곧 **매달 며칠**이라는 약정 지급일이다.
- 그리고 그 약정 지급일이 **귀속월 축의 앵커**다(소득세법 원칙, [[cash-receipt-refund]]).
- 결정적으로, 고치려는 그 화면에 `납부일` 이라는 **표 열이 실제로 서 있고 값이 dueDay 다**
  (`RoomsClient.tsx` colVis·필터·정렬 헤더·미납 배지까지 7자리).
- 그 위에 '납부일 기준' 이라 적으면 "저 납부일 열을 기준으로 셌다" 로 읽히는데 정확히
  거짓이고, **고치러 온 오해를 문자로 굳힌다.**

`입금일` 은 `PaymentSummaryCards.tsx:50` 이 같은 payDate 축에 이미 상시 캡션으로 쓰는 말이고
발생주의 데이터 진단 화면도 '입금일 / 귀속 월' 쌍으로 부른다. 카드 승인일을 '입금일'이라
부르는 것이 엄밀히는 늘어나지만, 같은 화면이 이미 카드를 포함한 총수납을 그렇게 부르고 있어
어휘 일관성을 택했다.

**칩이 아니라 평문으로 갔다.** 대시보드 '추이' 옆 축 칩(`DashboardClient:1392`)을 재사용하는
안(E)을 검토했으나 디자이너가 기각했다. 그 칩이 칩으로 읽히는 것은 `text-sm` 제목 옆의
10.5px 이라는 **크기 단차** 덕인데, 우리 자리는 11px 캡션이라 단차가 0.5px 이고 §05 스케일에
그 사이 계단이 없다. 게다가 칩은 줄 상자를 부풀리는데 이 캡션은 2줄로 접히므로 1행과 2행의
행간이 갈린다. 형제(`만실 기준 ₩x`)가 평문이라 평문이 맞다.

**'보증금 포함'은 상시로 안 올렸다.** UX 는 올리자고 했고 회계는 반대했다. 회계 손을 들었다 —
현금영수증 합계에 보증금이 잡힌 적이 **전 기간 0건**이고(카드 합계에는 3건 실재) 이번 차액의
원인도 아니다. 상시 텍스트는 항상 참인 것만 담는다. 규칙 자체는 InfoHint 에 남겼다(경로가
열려 있어 죽은 글이 아니다).

**소스 가드는 '되숨음'을 잡는다.** 파일 전체 `includes('입금일 기준')` 는 그물이 못 된다 —
같은 낱말이 InfoHint 본문에도 있어서 **한정어가 InfoHint 안으로 되숨는 바로 그 회귀에서
통과한다.** 그래서 `<InfoHint` 앞 상시 텍스트 조각만 잘라 본다. 축이 서버에서 뒤집히는 것도
함께 잠갔다 — 캡션이 '입금일'이라 말하려면 `getMonthPaymentAggregates` 가 실제로 payDate
창이어야 한다. 역주입 4종(한정어 제거 · 어휘 회귀 · 설명 문장 소실 · 서버 축 뒤집기) 전부 발화.

## 신고 2 — (a) 였다. 껍데기로 닫힌다

운영자 원문의 "버튼이 없어서"가 (a) 입력칸처럼 안 보인다인지 (b) 달력 아이콘이 없다인지를
먼저 갈랐다. **(a) 다.** 호출부 71곳 중 달력 아이콘을 붙인 곳이 0곳이라 (b)의 비교 대상이
없고, 정본 형제 `CheckoutCleaningDateField` 는 아이콘 없이 껍데기만으로 신고가 안 왔다.
아이콘 어포던스는 정본 수정이라 범위 밖으로 명시한다.

**정본 `DatePicker` 에 기본 껍데기를 주지 않았다.** 소비처 71곳의 높이가 자리마다 다르다
(dense 26px ~ 모달 42px). 한 벌을 강제하면 §12 '한 폼 안 입력 높이 혼용 금지'를 이쪽에서
깬다. 각 자리가 **자기 폼 형제와 같은 껍데기**를 넘기는 것이 정본이고, 신설 감지망은 그
약속만 본다(배경과 보더가 있는가).

**두 건은 실측이 아니었으면 놓쳤다.**

1. **dense 행의 라벨이 이미 꺾여 있었다.** 기본 클래스의 `w-full` 이 flex-basis 를 auto 로
   남겨 트리거 base 가 행 폭 전체(254px)가 되고, shrink 가 base 비례로 분배되면서 익명 flex
   아이템인 한글 라벨('완료일')까지 깎여 음절 단위로 꺾인다(행 32px). 껍데기만 주면 테두리
   생긴 26px 칸 옆에 2줄 라벨이 서서 **지금보다 어색해진다.** `flex-1 min-w-0` 로 base 를 0
   으로 만들어 26px 한 줄로 되돌렸다. 디자이너가 "내 §12 판정이 이것 없이는 거짓이 된다"며
   범위 안으로 판정했다.
2. **재고 2열 그리드에서 껍데기가 잘림을 만든다.** 맨글자 트리거는 `text-sm` 이 없어 body
   16px 로 렌더된다. 껍데기를 주면 14px 로 **줄어드는데** 좌우 패딩이 그 이득을 다 먹어,
   360px 에서 지금 딱 맞던 `2026년 12월 30일` 이 잘린다. `px-1` 까지 깎아도 320px 은 못 산다
   (셀 106px, 14px 글자만 110px). **패딩 문제가 아니라 2열 문제**라, 세 그리드를
   `grid-cols-1 sm:grid-cols-2` 로 접었다(같은 파일 :805 가 이미 쓰는 문법). 결과 320·360·390
   전부 잘림 0.

**dense 행을 44px 로 안 올렸다.** §09 터치 타겟과 §12 높이 혼용 금지가 충돌하는데 §12 를
택했다. 그 행 입력칸 넷 중 셋이 26px 인데 날짜만 44px 로 키우면 **혼용을 내가 만든다.** 그리고
className 만으로는 진짜 44px 히트가 안 나온다 — `RowActionBtn.tsx:7` 이 적어 둔 대로 히트와
크롬을 한 요소에 겹치면 보이는 테두리까지 부푼다. 정본이 크롬을 내부 `<span>` 으로 분리하는
것이 근본이고 그건 후속이다.

## 미채택·기록만 남긴 것

- **`InfoHint` (i) 아이콘 대비 2.20:1**(WCAG 1.4.11 미달). UX 가 "접힌 설명이 안 닿은 진짜
  이유"라며 차단으로 냈다. `opacity: 0.55 → 0.8` 한 줄로 3.43:1 이 된다. **안 고쳤다** — 정본
  컴포넌트라 29곳이 동시에 진해지고, 그건 두 신고와 무관한 앱 전역 시각 변경이라 운영자
  사선과 별도 디자이너 패스가 필요하다. 이번 처방은 축을 **상시 텍스트**로 꺼내는 것이라
  (i) 없이도 신고는 닫힌다.
- **입력칸 경계 대비.** `--warm-border` 대 `--cream` 이 라이트 1.43:1, 다크 1.20:1 로 §1.4.11
  3:1 미달이다. 이번 변경이 만든 것이 아니라 앱의 모든 입력칸이 같은 조건이고, 고치려면
  토큰을 움직여야 해서 카드·디바이더까지 따라간다. 별건 등재.
- **`RoomsClient:1670` 라벨 `납부일`.** 실제 값은 payDate 라, 같은 파일 안에서 `납부일` 이
  표에서는 dueDay·모달에서는 payDate 다. 두 글자로 닫히는 자리지만 형제 수납 폼들
  (`PaymentEntryForm`·`TenantClient`)도 payDate 를 `납부일` 이라 부른다. 하나만 고치면
  케이스 수정이 되므로 안 고쳤다.
# 컨텍스트 노트 — 보증금 수납 경로 (2026-08-24)

신고 98fb6fce · 00c39371 · 9e6c7cb3. 왜 그렇게 했는지만 적는다. 무엇을 했는지는 checklist,
무엇이 참인지는 knowledge/deposit-entry-paths.

## 왜 진입로를 새로 세웠나

운영자 원문이 진단이다. "수정은 말그대로 잘못된 정보나 추가 정보가 있을 때나 사용하는거지 수납에
대한 내용을 입력할 때 쓰는 기능이 아니야." **사실을 보여주는 자리와 그 사실을 고치는 자리가 갈려
있으면 사람은 반드시 딴 길을 찾는다.** 그 딴 길이 하필 가드가 가장 얇은 경로였다.

그래서 '미수납' 배지 바로 옆에 CTA 를 세웠다. 케이스가 아니라 자리를 고친 것이다.

## 라벨을 '수납 기록'이 아니라 '받음으로 기록'으로 한 이유

디자이너는 '수납 기록'을, UX 는 '받음으로 기록'을 냈다. UX 쪽을 택했다. 근거가 전수였다 —
이 앱에서 '수납 기록'은 이미 **명사**다(‘수납 기록 삭제됨’·‘수납 기록 수정됨’·‘최근 3개월 수납
기록이 없습니다’). 같은 화면에서 명사로 쓰이는 말을 버튼에 얹으면 "수납 기록(을 본다)"으로 읽힌다.
그리고 수납관리 보증금 탭의 같은 행동이 이미 '받음으로 기록'이라 라벨을 하나로 맞출 수 있었다.

두 자리가 같은 라벨이지만 **서버 경로는 다르다**(패널 = 실입금 정본, 수납관리 행 = 소급 기록).
사람에게는 한 행동이고 앱 안에서는 두 함수라는 뜻이다. 그 경계를 코드 주석과 감지망으로 못박았다.

## 왜 저장 분기를 둘로 뒀나

3단이 요구한 것은 **고칠 수 있는 분해**다. 그런데 `saveDepositPayment` 는 구조상
`min(총액, 잔여)` 를 먼저 뗀다 — 보증금 몫을 잔여보다 **낮게** 잡는 것은 그 함수로 **표현 자체가
불가능하다.** 그래서 사람이 몫을 내렸을 때만 몫별로 각자의 정본 저장부에 보낸다.

제안을 그대로 두면 오늘과 글자 그대로 같은 한 번의 호출이다. 이것이 "산식 무접점"의 실체다 —
바꾸지 않은 것은 정말로 한 줄도 안 지났다.

**분기 판정을 UI 상태(고쳤는가)가 아니라 값으로 한 이유**는 회계 패널 지적이다. 값을 고쳤다
되돌린 사람과 아예 안 건드린 사람이 같은 숫자로 다른 저장을 하면 그게 곧 다음 사고다.

두 분기는 이용료가 앉는 규칙이 다르다(정본은 그 달 못박기, 조정은 `savePayment` 의 이월).
합치려면 초과분 못박기를 FIFO 로 바꿔야 하는데 **그건 산식 변경이라 이번 범위가 아니다.**
대신 조정 분기에서만 이월 규칙을 한 줄로 말한다.

## 이용료 몫을 편집 칸으로 열지 않은 이유

총액이 위에서 확정된 이상 자유도는 정확히 둘이다(칸 3개 − 합계 제약 1개). 셋을 다 열면
**존재하지 않는 자유도 하나**를 사람이 다루게 되고, 그 결과가 곧 '합 불일치'다. 즉 앱이 오류
상태를 스스로 만들어 놓고 막는 구조가 된다. 오더가 요구한 두 조작(승계에서 보증금 0, 이용료만
받기)은 보증금 칸 하나로 전부 된다.

## '기타' 스탬프를 안 고친 이유

오더가 그렇게 정했고, 조사 결과도 같았다. 소급 기록에서 수단을 모르는 것은 **사실**이다.
모르는데 '계좌이체'를 박으면 카드·현금 대사에 없는 유령이 생긴다. 결함은 수단이 아니라
**날짜**였다 — 버튼 누른 날이 세무 대사 축(payDate)에 입금일로 남았다.

## 인수 승계를 `carriedOver` 로 거르지 않은 이유

`carriedOver = preAcquisition && paid === 0 && depositAmount > 0` 이다. **일부를 받은 승계
계약에서 거짓이 된다**(실측 3건). 그 계약은 배지가 '부족 N'으로 뜨고, 그 옆 CTA 는 "그 부족분을
채우라"는 뜻이 된다 — 앞선 원장이 받은 돈을 이 앱이 받았다고 적게 하는 유인이다. 게다가 승계
계약에 record 가 생기면 `getDepositBasisForLease` 가 퇴실 환불 기준액을 계약액에서 실수납액으로
조용히 넘긴다. 그래서 원 사실(`preAcquisition`)을 따로 내려보내 게이트로 썼다.

## 이용료 과납 가드를 조인 이유

`alreadyRent + incomingRent > monthBill` 분기가 순수 보증금 입력(incomingRent=0)에서
"이미 과납인 달"이라는 이유로 저장을 거절했다. 가드의 목적은 주석에 적혀 있다 — **이용료 과납**을
보는 자리다. 이번 저장이 이용료 record 를 하나도 안 만드는데 막는 것은 막을 대상이 없는데 막는
것이고, 에러 문구("지금 입력한 금액까지 더하면")도 사실과 다르다.

그 달이 과납이 되는 경로는 실재한다 — 단기 입주월 흡수(설계상 그렇게 만든다) · 예약 선납 재앵커 ·
퇴실 일할 · 단기 감액 되쓰기. 어댑터가 입주월을 고정으로 넘기니 이 분기를 상시로 밟게 된다.
**신고 00c39371 "보증금 수납처리가 안되는데?" 가 닿는 자리다.** 금액은 한 톨도 안 움직인다 —
거절 조건만 좁혔다.

## 경계값을 360 이 아니라 400 으로 잡은 이유

디자이너 계산은 360 을 통과로 봤다. 그런데 헤드리스로 실측했더니 **세 폭 전부** 2열에서 날짜가
truncate 에 걸렸다('2026년 12월 30일' 필요 폭 130~134px, 360 에서 실제 115~128px). 디자이너
계산이 '2026년 8월 30일'(9자) 기준이었고 두 자리 월·일이 더 넓다. 잘리면 30일이 3일로 읽힌다 —
수납일 오독은 이 앱이 결함으로 다루는 부류라 안전한 쪽으로 갔다.

**그리고 그 처방도 절반은 틀렸다(디자이너 배포 전 패스에서 잡혔다).** 전수 확인 결과
`min-[400px]` 는 이번에 처음 들어온 값이다(빌드된 CSS 에서 본 `min-width:400px` 는 내 변경이
만든 것이었다). 그리고 **보증금 패널에서는 그 질의 자체가 성립하지 않는다** — 폭이 잠긴 모달
안이라 뷰포트가 넓어져도 칸이 안 넓어진다. 즉 넓은 기기에서만 2열이 켜져 거기서만 날짜를
잘랐을 것이다. 320·360·390 만 재서 안 걸린 이유가 그것이다(셋 다 1열 구간이라 켜지지도 않았다).
패널은 세로 스택으로 고정했고, 페이지인 수납관리 행만 질의를 남기되 경계를 440 으로 올렸다.

**측정 방법의 한계를 적어 둔다.** Chrome 헤드리스는 창 폭을 500px 아래로 못 내린다(실측
`innerWidth=500`). 그래서 ① 측정 컨테이너 폭을 목표 폭으로 고정하고 ② 목표 폭에서 실제로 걸리는
미디어 쿼리(`min-[400px]`·`sm`)를 마크업으로 확정해 넣었다. 폰트는 Pretendard 가 아니라 시스템
폴백이라 **실제보다 넓게** 잡힌다(보수적). 컴파일된 프로덕션 CSS 를 그대로 물렸다.

## 패널 미니폼에 분해를 넣지 않은 이유

회계 패널의 마지막 조언이었고 동의했다. 패널 미니폼은 "보증금만 받는 자리", 수납 폼은 "한 번에
받은 돈을 나누는 자리"다. 패널에 분해까지 넣으면 같은 결정이 두 화면에 생기고, 그러면 어느 쪽이
정본인지 다시 갈린다 — 보증금 표시에서 이미 한 번 겪은 일이다.

## 감지망에서 배운 것

규칙 3 의 소스 가드가 `setPayDateVal` 의 **대문자 P** 에서 빗나가고 있었다. 즉 **그물은 통과했는데
결함은 살아 있었다.** 그물을 새로 짤 때마다 역주입으로 종료코드 1 을 확인해야 하는 이유가 이것이다.
합 불일치 차단 가드도 첫 판은 `includes('splitBlocked')` 였는데, 이름을 `splitBlockedRenamed` 로
바꿔도 부분 문자열이라 통과했다. 선언·버튼 결선·제출 경로 셋을 따로 보게 조였다.

## 높이 티어를 하나로 못 만든 이유

`<select>` 는 같은 패딩에서도 브라우저 고유 높이 때문에 `<input>` 보다 2px 낮게 선다(실측 40 대 42).
`leading-5` 로도 안 움직인다. 42 를 강제하려면 토큰에 없는 `min-h-[42px]` 를 박아야 하는데, 그건
40·44 두 토큰 사이에 세 번째 높이를 magic number 로 들이는 일이라 더 나쁘다. 종전 4px 차(38 대 42)를
2px 로 줄이는 데서 멈추고 기록으로 남긴다. 완전 통일은 `MoneyInput` 을 44 로 올리는 정본 작업과
함께 갈 일이다.

## 확인창 둘을 없앤 판단

수납관리 보증금 탭의 '받음으로 기록'은 `choiceDialog`(금액 3지선다) + `confirmDialog`(기록 확인)
둘을 순차로 띄웠다. 여기에 입금일·결제수단을 더하면 한 동작에 질문이 셋이 된다. 그런데 **금액이
편집 가능한 칸이 되면 3지선다가 물을 것이 없어지고**, 명시적 '기록' 버튼이 달린 폼 자체가 확인이다.
확인창이 둘 사라지면서 정보는 늘었다. `loop.md` 4번(결제 로직)으로 보지 않았다 — 금액을 사람이
정하는 것은 그대로이고 산식·집계에 접점이 없다. 다이얼로그를 폼으로 바꾼 것이다.

**그래도 사실은 사실이다.** 돈을 쓰는 경로에서 확인 단계가 둘 줄었다. 설계상 정당해도 운영자가
모르면 안 되므로 보고서에 한 줄로 남긴다. 되돌리기는 토스트의 적용취소로 붙어 있다.

## 패널 회신 현황

셋 다 회신했다(수납·회계 도메인 · UX/UI · 웹디자이너). 이견은 라벨 하나였고 위에 적었다.
디자이너 회신은 계획 단계와 **배포 전 실물 패스** 두 번 받았다.

## 범위 밖 발견 — 손대지 않고 기록만

- **`/tenants` 에 수제 수납 폼이 따로 있다**(`TenantClient.tsx` 의 `handleSavePayment` 경로).
  정본 `PaymentEntryForm` 이 아니라 옛 폼이라 초과분 3지선다·청소비 모드·귀속월 선택·자릿수
  확인이 전부 없고, 이번 3단 분해도 안 닿는다. **이번 신고가 난 화면이 거기다.** 1단 CTA 는
  `DepositStatusPanel` 이라 /tenants 에서도 서지만, 3단은 /rooms 수납 모달에만 있다.
  정본 수렴은 `handleSavePayment` 와 그 상태들을 걷어내는 일이라 별건 승인 대상이다.
- `saveCleaningFeePayment` 에 잔여 가드가 없다(`min(totalPaid, 계약 청소비)` 만 본다).
  보증금 쪽에는 잔여 가드와 확인창이 있는데 청소비 쪽은 둘 다 없어 같은 청소비를 두 번 받는 길이 열려 있다.
- `saveDepositPayment`·`saveCleaningFeePayment` 에 영업장 스코프 검증이 없다(형제 `deletePayment`
  에는 있고 주석까지 붙어 있다).
- `getMonthPaymentAggregates` 가 `isDeposit` 을 안 뺀다 — 위 현금영수증 갭의 코드 근거.
- `ExtraIncome` 에 현금영수증 칸이 없어 청소비 몫의 발행 표시를 앱이 못 남긴다(스키마).
- `MoneyInput` 에 `₩` prefix 가 없고 높이가 42px 다(§12 · §09).
- `DepositSection` 서브탭이 손으로 그린 버튼이다(§25 ViewTabs 정본 이탈).
- `DepositSection` 의 `hasNoInRecord` 판정에 인수일 비교가 없다 — 승계 계약에도 '받음으로 기록'
  버튼이 선다. 2단이 그 버튼을 쓰기 쉽게 만들었으므로 위험이 커졌다. 고치려면 `DepositPerTenant`
  에 `preAcquisition` 을 실어야 하고, 어떤 행을 감출지는 운영자 판단이다.
- `TenantClient` 에 화살표(→) 노출 문자열 2건(§29).
- 보증금 record 의 현금영수증 표시를 끌 자리가 앱에 없다(패널 수정 폼에 칸이 없고, 수납 내역의
  원터치 토글은 서버가 보증금 행을 걸러 내려보내 못 만난다).

## 현금영수증 합계 축 정정 (2026-08-24, 신고 8b9b6c43 재판정)

### 첫 판정이 왜 틀렸나 — 남겨야 할 것은 이것이다

같은 날 오전에 "계산은 맞고 표시가 문제"로 판정하고 라벨에 '입금일 기준'을 붙여 배포했다.
근거는 **일반론**이었다. 홈택스는 승인일자로 집계하고 `cashReceiptIssuedAt` 은 결제 단위
스탬프이니 두 축이 같을 것이다. 그럴듯했고, 실제로 대부분의 사업장에서 맞다.

**틀린 지점은 하나다. 그 전제를 운영자에게 안 물었다.** "현금영수증은 언제 발행하세요?"
한 줄이면 끝났다. 두 축이 같다는 전제가 무너지면 판정 전체가 무너지는데, 판정을 지탱하는
유일한 가정만 검증을 건너뛴 것이다.

**두 번째 함정은 숫자가 두 축으로 설명됐다는 것이다.** 귀속월 축 합이 우연히 764만이라
"운영자는 목록을 귀속월로 더했구나"라는 이야기가 만들어졌다. 이야기가 숫자와 맞아떨어지자
다른 설명을 찾지 않았다. 실제로는 8/22 발행분 합이 같은 764만이었다. **한 숫자에 설명이
둘 이상 붙을 때는 맞는 설명을 찾은 것이 아니라 아직 못 고른 것이다.**

### 축 선택의 근거

- 현금영수증은 국세청에 **발행 시점**으로 올라간다. 앱 합계가 홈택스와 대사되려면 같은 축이어야 한다.
- 카드는 다르다. 매출전표는 결제 시점에 성립하므로 `payDate` 가 맞다. **그래서 한 줄에 축이 둘이다.**
- 같은 날 발행하면 두 축이 같은 답을 낸다. 평소엔 숫자가 안 바뀌고 지연됐을 때만 바로잡힌다.
  즉 이 변경은 정상 운영을 흔들지 않고 예외만 잡는다.

### 컷오프를 발행일에 건 이유

현금영수증은 **발행자 사업자번호**로 귀속된다. 인수 전에 받은 돈이라도 현 사업자가 발행했으면
국세청 자료에는 현 사업자 매출로 들어간다. 컷오프를 `payDate` 에 그대로 두면 그 건이 앱에서만
빠져 홈택스와 어긋난다. 실측 해당 0건이라 오늘 값은 어느 규칙이든 같지만, **값이 같을 때 규칙을
맞춰 두는 것이 값이 갈릴 때 고치는 것보다 싸다.**

`isPrevOwner: false` 는 유지했다. 그 표식은 "이 달은 양도인이 받음"이라는 뜻이고, 앱의 발행
경로(수납 폼·토글)는 그런 record 에 스탬프를 찍지 않는다. 표식과 스탬프가 함께 있는 record 는
데이터 모순이지 정상 조합이 아니다.

### 창을 둘로 나눠 두 번 조회한 이유

한 번에 `OR` 로 긁으면 어느 축으로 들어온 행인지 잃는다. 그러면 다시 세어야 하고, 중복 계상을
막는 책임이 호출부로 흩어진다. 버킷 판정이 배타(카드 우선)라 **한 record 가 두 합계에 동시에
들어가는 일이 구조로 막힌다.** 카드 record 가 두 조회에 다 걸려도 카드 루프만 세고 현금영수증
루프는 버킷이 달라 건너뛴다.

### 발행일 값 결정을 정본 하나로 모은 이유

다섯 저장 경로가 각자 `new Date()` 를 박고 있었다. 축이 발행일로 바뀌기 전에는 그 값이 아무
데도 안 쓰여 무해했지만, 이제는 **클릭한 순간과 발행한 날의 차이가 곧 세무 숫자의 오차**다.
사본이 다섯이면 한 곳만 고치고 나머지가 새는 것을 아무도 모른다. 감지망 20-b 가 호출 개수를
세는 이유가 그것이다 — 이름만 보면 한 곳 고친 것도 통과한다.

**기존 값 보존이 특히 중요하다.** 금액만 고쳐 재저장했는데 발행일이 오늘로 밀리면 그 달 합계가
소리 없이 움직인다. `updatePayment` 은 원래부터 그렇게 하고 있었고, 그 규칙을 전 경로로 넓혔다.
반대로 **운영자가 명시한 날짜는 덮어야 한다** — 안 그러면 고칠 길이 없다. 그것이 편집 경로다.

### 미래만 막는 이유

운영자 원문 — "원칙은 입금된 날짜에 발행하는게 맞아. 근데 업무특성상(누락 매출분도 있기
때문에) 날짜가 다르게 할 필요가 있거든". **발행일 != 입금일은 정상 업무다.** 경고를 띄우면
정상 업무를 매번 나무라는 셈이고, 그러면 진짜 경고도 안 읽힌다. 소급 상한도 안 둔다 — 누락분은
얼마든지 오래됐을 수 있다. 미래만 막는다. 아직 하지 않은 발행이 국세청에 있을 수는 없다.

### 원터치 토글에 날짜를 안 물은 이유

그 자리는 한 번 눌러 켜는 자리다(신고 241c02ea 로 배지에서 체크박스로 바꾼 자리). 날짜를 물으면
편의가 죽고, 대부분은 오늘이 맞다. 그래서 오늘로 켜고 **고칠 길을 기존 수정 폼에 뒀다.**
새 인터랙션을 만들면 loop.md 4번(인간 호출) 대상이 되고, 만들 이유도 없다. 대신 발행일이
입금일과 다르면 행 배지에 날짜가 함께 떠 눈에 걸리게 했다 — 같은 날이면 안 적는다. 그 경우가
대부분이고, 배지가 넷인 줄에 뜻 없는 숫자를 늘리지 않는다.

### 배포 전 디자이너 패스 — 직접 수행(패널 회신 없음)

수납·회계 도메인 · UX/UI · 웹디자이너 셋을 서브에이전트로 섭외했으나 이 세션에서 회신이 안 왔다.
**지어낸 회신은 없다.** 아래는 직접 수행한 결과다.

- 새 DatePicker 셋 전부 **자기 폼 형제 날짜 칸의 껍데기 문자열을 그대로** 넘긴다(§12 한 폼 안
  입력 높이 혼용 금지). `check-datepicker-shell` 78곳 0건.
- 라벨은 폼마다 형제와 같은 스케일·토큰이다 — 수납 폼 `text-xs text-[var(--warm-muted)]`,
  수정 폼 `text-xs font-medium text-[var(--warm-mid)]`.
- 조건부 노출(체크했을 때만)로 레이아웃 점프가 생긴다. 같은 폼 바로 아래 안내 문구들이 이미
  같은 문법이라 이질감이 없고, 안 켠 상태에서 그 칸은 뜻이 없다. `pl-6` 은 체크박스의 하위
  항목이라는 표시다(형제 필드가 아니다).
- 합계 줄은 색·스케일 그대로고 문장만 짧아졌다(한 줄 → 두 줄). 새 색·새 간격 없음.
- 헤드리스 320/360/390 × 라이트·다크 6조합 — 가로 스크롤 0 · 넘침 0 · 날짜 칸 잘림 0 ·
  합계 두 줄 모두 320px 에서 한 줄에 들어감.

### 게이트를 막고 있던 것 — `check-datepicker-shell` 오탐

상수 해석이 **한 겹뿐**이라 상수 위에 세운 상수를 못 폈다.
`const inputBase = '… bg-… border …'` · `const inputCls = \`${inputBase} border-…\`` 에서
`className={inputCls}` 를 한 겹만 펴면 배경이 없다고 신고한다. `DepositStatusPanel` 세 자리가
그 오탐이었다. **base(main 체크아웃과 파일 바이트 동일)에서도 같은 오탐이라 verify:fast 가 이미
빨간불이었다.** 범위 밖이지만 게이트를 막고 있어 고쳤다 — 이름이 새로 안 나올 때까지 편다.
역주입으로 실제 위반은 여전히 잡는 것을 확인했다.

### 범위 밖 발견(기록만)

- `getMonthPaymentAggregates` 가 `isBillingAdjust`·`isDeposit` 을 안 거른다. 조정전표 6건이 전부
  `actualAmount=0`·`payMethod=null` 이라 오늘 값에는 무영향이지만, 전표에 `payMethod` 가 붙는 날
  `cardCount` 만 부푼다.
- `check-contract-issued-snapshot` 의 축 B 기준선이 9 인데 실측 11 이라 올리라는 안내가 뜬다
  (위반은 아니다).
- `ExtraIncome`(분해 수납의 청소비 몫)에는 발행 표시 칸이 없어 그 몫의 발행을 앱이 기록 못 한다.
  화면이 이미 그 사실을 적고 있다.
