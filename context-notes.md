# 컨텍스트 노트 — 발신 주소·보낸 메일 사본 (2026-08-26)

운영자 요구 둘에서 출발했다. 보내는 주소를 바꾸고 싶다는 것과, 보낸 메일을 자기 사서함에서
확인하고 싶다는 것. 결정과 근거를 적는다.

## 먼저 사실을 정확히 전달했다

"보내는 이메일 주소를 noreply말고 다른걸로는 못바꿔?"에 "네/아니오"로 답하면 안 되는 자리였다.
도메인과 로컬파트가 서로 다른 제약을 받기 때문이다. 도메인은 SPF/DKIM/DMARC 인증이 필요해
지메일 주소로는 애초에 못 보내고(구글 도메인이라 인증 불가), 앞부분은 자유다. 이 구분을
먼저 말하지 않으면 운영자가 "지메일로 보내달라"고 요구했을 때 나중에 못 한다고 해야 한다.

회신 주소는 반대다 — 인증이 필요 없어 어떤 주소든 된다. 그리고 그 기능은 **이미 있었고**
운영자가 등록을 안 한 상태였다. DB 를 확인해 미등록 사실을 먼저 확인하고 답했다.

## 로컬파트만 저장한 이유

전체 주소를 저장하면 운영자가 `thestayjegi@gmail.com` 을 넣는 길이 열린다. 그 발송은 반송되고,
막으려면 결국 "도메인은 stayeum.com 이어야 한다"는 검증을 코드에 넣어야 한다. 그러면 도메인
지식이 상수와 데이터 두 곳에 살게 되고 도메인이 바뀌는 날 전 행을 마이그레이션해야 한다.

대신 정규화가 `@` 뒤를 잘라낸다. 전체 주소를 붙여넣어도 앞부분만 남고 화면의 접미 표시가
결과를 그대로 보여준다 — 손을 막지 않으면서 잘못된 값이 저장되지 않는다.

## 순수 모듈을 따로 세운 이유

정규화를 `lib/propertySettingsPatch`(슬러그 선례가 있는 자리)에 두려다 말았다. 이 값의
소비자가 셋이고(저장·발송·화면 표시), 패치 파일에 두면 `lib/mailSend` 가 환경설정 폼 해석
모듈을 임포트하는 역방향 의존이 생긴다. 규칙 단일성은 지키되 의존 방향을 바로 세우려고
`lib/mailFrom` 을 순수 모듈로 세우고 패치가 그것을 임포트하게 했다.

`lib/mailSend` 는 서버 전용이다(RESEND_API_KEY 를 읽는다). 화면이 임포트하면 스테이징
가드와 키 판단이 무의미한 번들로 들어간다. 그래서 화면은 서버가 내려준 `fromAddress` 를
표시만 하고, 그 값을 서버가 같은 조립 함수로 만든다 — 표시와 발송이 갈릴 수 없다.

## 폴백이 정답인 이유

발신 주소가 이상할 때 발송을 막을 수도 있었다. 그러면 잘못된 설정값 하나로 서류가 통째로
못 나간다. 발신 주소는 서류 전달이라는 목적의 부속이지 목적이 아니다. no-reply 폴백은
오늘까지의 전 발송과 같은 동작이라 회귀 위험이 0이고, 폴백 사실은 확인 화면이 보여준다.

## 허용 문자 집합이 곧 보안선이다

로컬파트가 발신 헤더 문자열에 직접 들어가므로 CR/LF 가 남으면 다른 헤더를 끼워 넣을 수 있다.
정규화가 `[a-z0-9._-]` 밖을 전부 지우므로 그 공격이 **문자 수준에서** 불가능하다. 이스케이프나
검증으로 막는 것보다 애초에 그 문자가 남지 못하게 하는 쪽이 낫다. 회귀에 주입 케이스를 넣어
못 박았고, `@` 가 먼저 잘려서 주입하려던 주소 자체가 통째로 사라지는 것도 확인했다.

## 사본 주소용 칸을 안 만든 이유

칸이 셋이 되면(보내는 주소·답장 받을 주소·사본 받을 주소) 운영자가 무엇이 무엇인지 모른다.
사본이 가야 할 곳과 답장이 와야 할 곳은 같은 사서함, 즉 "내가 확인하는 곳"이라 개념이 하나다.
답장 주소를 재사용하면 "내가 답장을 받기로 한 곳에 사본이 온다"는 한 문장으로 규칙이 끝난다.
그 값은 이미 열거 검증을 지난 것이라 자유 입력 창구가 새로 열리지도 않는다.

## BCC 형식 검사를 발송 문 안에 둔 이유

Resend 는 한 요청에 to·bcc 를 함께 싣는다. bcc 형식이 어긋날 때 요청 전체가 거절되는지
문서가 명시하지 않는데, 도박할 자리가 아니다. 사본 주소 오타 하나로 서류가 안 나가면
안 되므로 서버가 자기 형식 검사를 통과한 bcc 만 싣는다. 어긋나면 사본만 조용히 빠진다.

## 라벨을 함께 고친 이유

'보내는 주소' 옆에 기존 '메일 주소'가 서면 무엇이 무엇인지 알 수 없다. 마침 발송 확인
화면이 이미 '답장 받을 주소'라는 라벨을 쓰고 있어, 그 이름으로 맞추면 두 화면이 같은 말을
하게 된다. 캡션도 "네이버·지메일 등 어떤 주소든 됩니다"로 사실을 적었다 — 운영자가 방금
물어본 것이 정확히 그것이었다.
