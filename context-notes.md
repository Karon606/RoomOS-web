# 컨텍스트 노트 — 고정지출 주기 (2026-08-26, 신고 7e7da5c4)

"고정지출이 월간이 아니라 격월이나 분기, 반기, 연1회 등도 있을 수 있네"에서 출발.
스키마는 이미 있었고 소비처가 0이라, 이번 작업은 **판정을 한 곳에 세우고 그것을 읽는
자리를 전수로 찾는 일**이었다. 결정과 근거를 적는다.

## 판정을 서버 현황 정본에서 한 번만 한다

`isRecurringDueMonth`를 화면마다 부르는 대신, `computeRecurringExpensesWithStatus`가
한 번 계산해 `isDueThisMonth`·`nextDueMonth`를 상태에 실었다. 이 저장소는 같은 판정을
여러 자리가 각자 짜다 갈린 사고를 이미 여러 번 겪었고(대표 계약서 판정, 알림 모집단),
그 주석들이 지금도 파일 머리에 남아 있다. 같은 실수를 반복하지 않으려는 것이다.

알림 게이트는 한 발 더 들어가 `recurringDueToday` **함수 안에** 넣었다. 푸시 크론과
인앱 종이 그 함수 하나를 쓰기 때문에, 게이트를 밖에 두면 다음 소비처가 또 빠뜨린다
(신고 568633fb가 정확히 그 사고였다).

## 커밋 순서가 곧 안전장치다

CRUD(주기 입력)를 **마지막**에 뒀다. 게이트가 먼저 깔린 뒤라, interval 이 1보다 큰 데이터가
게이트 없이 생기는 창이 구조적으로 없다. 순서를 뒤집었으면 그 사이에 만든 연1회 항목이
매달 예정 합계에 잡히는 구간이 생긴다.

## 달력 달 기준으로 판정한 이유

절대 인덱스(년×12+월) 방식은 기준점에 연도가 필요한데 anchorMonth 에는 연도가 없다.
달력 달 mod 는 "해마다 같은 달들"이라는 뜻이 되고, 그게 가스 안전 검사 같은 실무의
심성 모형과도 맞는다. 화면 다섯 선택지가 전부 12의 약수라 연 경계에서도 안정적이다.

깨진 월 입력은 **도래하는 쪽으로 실패**하게 했다. 항목이 소리 없이 숨는 것이 가장 나쁜
실패라서다 — 운영자는 "이 지출이 사라졌나"를 알아챌 수 없다.

## 비도래 항목을 감추지 않은 이유

가장 쉬운 구현은 예정 목록에서 빼는 것이다. 그러면 격월 항목이 두 달에 한 번씩 화면에서
사라졌다 나타난다. 이 저장소에는 "값을 감춘 표기가 원인 추적을 막는다"는 기록이 이미
있다(2026-08-20 연도 없는 날짜 표기 사건). 그래서 예정 합계에서만 빼고, '이번 달 도래
없음' 섹션에 다음 도래 날짜와 함께 세웠다.

다음 도래 날짜는 `recurringDueDateFor` 정본을 경유한다 — 말일 클램프와 자동이체 휴일
시프트가 그대로 맞는다. 화면 밖(조회 달 밖) 날짜라 연도를 포함해 적는다(fmtDateDot).

## 기록된 지출은 도래 여부를 안 본다

조기 납부(연납 할인 등)는 실제로 나간 돈이다. 도래 달이 아니어도 기록이 있으면 그대로
합계에 든다. 게이트는 '예정'에만 건다.

## 매월로 되돌릴 때 기준 달을 지우는 이유

`normalizeCycle`이 interval 1이면 anchorMonth 를 null 로 만든다. 안 지우면 낡은 기준 달이
행에 남아 있다가 나중에 다시 격월로 켰을 때 도래 달이 운영자가 모르는 값으로 정해진다.
적용에는 적용취소가 늘 붙어야 한다는 원칙의 데이터 판이다.

## 관리 모달 라벨을 커밋 3에서 되돌렸다가 5에서 다시 넣었다

재무 관리 모달의 목록 부제도 주기 라벨로 바꾸려 했는데, 그 목록이 쓰는 타입
(RecurringExpenseRow)에 주기 필드가 커밋 5 전까지 없었다. 타입 에러가 나서 그 한 줄만
되돌리고 커밋 5에서 함께 넣었다 — 커밋 경계를 억지로 넘기지 않는 쪽을 택했다.

## 추정 산식은 손대지 않았다

변동 고정지출의 추정 사다리(직전 3개월 평균 → 작년 같은 달 → 수기 값)는 그대로다.
격월은 직전 도래가 3개월 창에 자연히 걸리고, 연1회는 작년 같은 달이 정확히 맞는 자리라
지금 바꿀 실익이 없다. 회귀(test-recurring-estimate 30건)가 무변경으로 통과하는 것이
"안 건드렸다"의 증거다.
