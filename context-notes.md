# 입퇴실 캘린더 0단계 — 결정과 근거 (2026-08-20)

운영자 실기로 원인이 확정된 상태에서 착수했다. 신고 3·4·5가 전부 '변화 없음'이었던 이유는
하나다 — 스크롤 → 월 동기화가 통째로 죽어 있어 비교 자체가 불가능했다.

## 패널 회신 현황

4인 전원 회신(정보구조 UX · 웹디자이너 · 프런트엔드 성능 · 호실 도메인). 전부 최종 출력으로
도착했다(SendMessage 미사용 — 8/19·20 전달 실패를 피하려 처음부터 그렇게 지시). 배포 전
디자이너 패스는 별도로 한 번 더 받았다.

## 채택한 것

### 키 이름 `at` (IA 권고)
`mcMonth`·`calMonth`·`viewMonth` 를 전부 기각했다 — 이름에 'month' 가 들어가면 다음 사람이
`resolveMonthParam` 에 먹이거나 `BottomNav` 옆에 한 줄 더 붙인다. `focus` 도 못 쓴다:
`lib/useFocusSection` 이 알림 딥링크로 그 키를 점유하고 **소진(삭제)** 하므로, 누군가 room-manage
에 `useFocusSection()` 을 한 줄 추가하는 날 트랙 위치가 조용히 지워진다. 전수 grep 으로 `at` 은
현재 앱이 쓰는 URL 키 어디에도 없음을 확인했다.

### `?month=` 를 지우지 않는다 (IA 권고에서 이탈)
IA 는 마운트 시 `month` 를 `at` 으로 옮겨 담고 **삭제**하라고 했다(URL 이 두 달을 들고 있는 상태를
피하려고). 채택하지 않았다 — 그 값은 홈에서 정당하게 실려 온 조회 장부 월이고, 지우면 캘린더를
거쳐 지출로 넘어갈 때 보던 달을 잃는다. 두 키가 함께 사는 것이 곧 범주 분리의 요점이다.
표시 불일치는 해석 사슬(`resolveTrackMonth = at ?? month`) 한 벌로 닫히므로 마운트 시 URL 을
고쳐 쓸 필요 자체가 없어졌다.

### 착지 억제는 시간이 아니라 좌표로
IA·성능 모두 "착지 직후 한 번은 재우라"고 했다. 타임스탬프(250ms) 대신 **착지가 만든 실제
scrollLeft 를 되읽어 저장**하고, 그 값 그대로면 URL 을 안 적는다. 시간 창은 그 안에 사용자가
손대면 한 번을 잃지만 좌표 비교는 창이 없다.

### 트랙 끝 clamp 보정 (성능 지적)
넓은 화면에서 마지막 달에 착지하면 스크롤이 clamp 되어 1/4 판정점이 직전 달을 가리킨다.
성능 패널이 산술로 예고했고, 실측에서 1280px 오른쪽 끝이 종전 `2026-09` → 교정 후 `2026-10` 으로
바뀌는 것을 확인했다.

### `memo(MoveCalendar)` + `useSearchParams` 제거 (성능 권고)
이 화면의 URL 구독자가 여섯이라 URL 이 한 번 바뀔 때마다 열일곱 행 × 수백 칸이 함께 그려졌다.
`jumpToMonth` 가 클릭 시점에 `window.location.search` 를 읽게 하니 훅이 사라지고 memo 가 실제로
산다. 이것이 가장 값싼 큰 승리라는 성능 패널 판단에 동의한다.

### 창 미끄러뜨리기는 **점프에만**, 기본 창은 불변 (도메인 반대 의견 채택)
오더는 "창을 항상 3~4개월로 유지"였다. 도메인 패널이 반대했고 그 근거를 채택했다 — 기본 창의
오른쪽 끝을 '마지막 변동이 있는 달'에 두는 규칙은 "다음에 무엇이 오는가"라는 이 화면의 존재
이유에 직접 답하는 자리다. 여기를 4개월로 깎으면 매일 아침의 기본 화면이 바뀌고, 종전에 막대로
보이던 예약이 `beyond` 한 줄로 강등된다. 문제였던 것은 기본 창이 아니라 점프였다.
회귀 축 2(무회귀 앵커)가 "focus 가 오늘 달이면 종전 산식과 완전히 같다"를 못 박는다.

### 라벨 조각은 조립이 낸다
화면이 `label` 을 `' · '` 로 다시 쪼개는 안은 이 저장소의 명시적 금기이고(파일 머리), 기계적으로도
깨진다 — 호실번호가 숫자라는 보장이 없어 `'A동-3'` 같은 값이 라벨에 섞인다. `startLabel`·
`endLabel`·`stateLabel` 세 칸을 추가하고 `label` 은 종전 문자열 그대로 뒀다(회귀 160 무변동의 근거).

### 역전 막대는 문구를 안 세운다 (도메인 발견)
`assemble` 은 역전이면 기하를 뒤집어 그리는데 `stayFrom`/`stayTo` 는 원본 그대로다. 종전에는 한
문자열이 막대 어딘가에 떠 있어 티가 안 났지만, 문구를 날짜 칸에 못 박는 순간 8/10 칸 위에
"9/20 입실"이 정확히 앉는다. 그 방을 9/20 에 광고에 올리면 실제 손실이다.

### 창이 오늘을 안 물 수 있다 — 세 얼굴을 한 커밋에
도메인이 지목한 세 가지를 창 미끄러뜨리기와 **같은 커밋**에 넣었다. 따로 내면 그 사이에
거짓말하는 배포가 남는다. ① `beyond` 의 today 가드(없으면 지난 변동이 "이후 예정"으로 적힌다)
② '다가오는 14일' 의 분기(창 밖 오늘이면 반드시 비는데 "예정 없음"이라 적었다) ③ '오늘로' 를
카드 밖으로(변동 없는 창에서는 카드가 안 서서 버튼이 함께 사라졌다).

## 채택하지 않은 것

### 탭 접미를 범위 총계로 (IA)
IA 는 접미가 스크롤을 따라 깜빡이는 것이 같은 tablist 안 형제(청소 접미는 정적)와 법이 갈리는
범주 오류라며, 접미를 범위 총계로 올리고 월 건수는 월 밴드로 이관하라고 했다. 운영자 오더가
"접미를 떼는 안은 채택하지 마라(홈 '이달 입퇴실 N건'과의 정합 때문)"로 명시적이라 따르지 않았다.
도메인 패널은 반대로 "스크롤을 따라 바뀌는 것이 옳다"고 했고(라벨은 9월인데 배지는 8월인 지금이
오히려 이상하다) 그쪽 근거를 채택했다. **월 밴드에 건수를 함께 적는 IA 의 제안은 좋은 안이지만
승인 범위 밖이라 기록만 한다.**

### 표시 판정점을 왼쪽 끝으로 (오더에서 이탈 — 운영자 확인 필요)
오더는 "표시는 왼쪽 끝 기준 + rAF 즉시, URL 쓰기는 1/4 + 180ms 유지"였다. **자를 둘로 두면 한
화면에서 두 답이 뜬다** — 셀렉터는 URL(1/4)을 읽고 트랙의 월 밴드는 왼쪽 끝을 보이므로, 그
차이가 곧 신고 4번이 물은 '판정점 어긋남'이다. 패널 셋 중 둘이 각각 반대 방향으로 통일을
권고했고(IA 는 왼쪽 끝으로, 성능은 1/4 로), 왼쪽 끝은 착지 규칙(오늘을 1/4 에)과 구조적으로
어긋난다는 성능 패널의 산술이 결정적이었다 — 오늘이 달 초순이면 착지 직후 왼쪽 끝이 전달이다.
그래서 **자는 1/4 하나로 두고 시계만 둘로 갈랐다**: 표시(‘오늘로’ 버튼)는 rAF 즉시,
URL 쓰기는 180ms 디바운스. `todayOff` 를 React state 에서 DOM 직접 쓰기로 내린 것이 오더의
"표시 값을 React state 로 들지 마라"에 해당한다. **왼쪽 끝 통일 여부는 동기화가 살아난 뒤
실기로 다시 볼 항목으로 올린다.**

### 미래 알약을 info 틴트로 강등 (디자이너)
디자이너는 상대월 알약을 지우지 말고 §11 틴트 배지 + `--info-*` 로 내리라고 했다(그래야 "몇 달
뒤인가"를 잃지 않는다, 실측 대비 라이트 7.12 · 다크 6.14). 오더가 "상대월 알약을 안 세운다"로
명시적이라 따르지 않았다. **잃는 정보는 실재하므로 운영자 판단 항목으로 올린다.**

### `place()` 4단 규칙·`ink` 배열화 (디자이너)
조각이 둘이면 바깥 라벨도 둘이 될 수 있어 `Placed.ink` 를 배열로 바꾸고 `place()` 를 다시 쓰라는
권고. 채택하지 않았다 — `place()` 를 안 건드리면 `ink`·`blocked`·`captionSpan` 이 그대로라 공백
캡션 회귀가 구조적으로 0이고(실측 캡션 20개 불변), 승인 범위는 '막대 안 라벨'이다. 바깥 라벨은
애초에 sticky 가 아니라 이번 신고의 증상도 아니다.

### 연도 10.5px (디자이너)
같은 baseline 줄에서 11px 과 10.5px 을 섞는 것이 더 나쁘다고 판단해 같은 크기에 색(`--ink-m`)과
무게로만 눌렀다. 디자이너 의도("굵기·색으로 눌러 둔다")는 그대로 달성된다.

### '이전 달 더 보기' 삭제 (IA)
IA 는 창이 미끄러지면 "더 보기"가 거짓이 되므로 컨트롤을 지우고 셀렉터 ◀ 로 일원화하라고 했다.
도메인은 반대로 그 버튼을 살리는 산식(focus 를 창의 첫 달에)을 제안했고 그쪽을 채택했다 —
한 번에 한 달씩 물러나므로 낱말이 거짓이 되지 않는다. **다만 창의 오른쪽 끝이 함께 당겨지는
것은 사실이라, 낱말을 다듬을지는 열린 질문으로 남긴다.**

## 배포 전 디자이너 패스 — 차단 2건 반영, 나머지 조건부 통과

- **접미 자릿수로 헤더가 접혔다 펴지던 것**(1px 스윕: 2자리 393 · 1자리 384 · 없음 362)은
  `ViewTabs.suffixWidest` 로 닫았다. 유령 span 을 같은 격자 칸에 겹쳐 폭을 잡는 방식이라
  ch·px 매직넘버가 없고, 남는 자리가 닫는 괄호 **뒤**로 흘러 괄호 안 빈칸이 안 생기며,
  `(0)` 을 세우지 않아 형제인 청소 접미와 규칙이 하나로 유지된다. 형제 호출부 무변경은
  되돌린 ViewTabs 와 나란히 재서 소수점까지 일치함으로 증명했다.
- **빈 상태 위 부유 알약 겹침**(세로 22px)은 EmptyState CTA 자리로 옮겨 닫았다. 이 조합은
  예외가 아니라 먼 달 점프의 주 경로다 — 커밋 5가 정확히 그 자리를 위해 버튼을 카드 밖으로 뺐는데,
  그 결과 빈 상태 위에 뜨게 된 것이 이번에 드러났다.

## 별건으로 분리한 것

- **월 셀렉터 자체 폭 불안정**(600~679px 에서 달을 넘기면 헤더 56px 점프). 상대월 배지·[오늘]
  버튼·보더 두께가 조건부라 알약 폭이 상태마다 다르다. **이 화면이 만든 클래스가 아니다** —
  같은 헤더 문법을 쓰는 형제 아홉 화면에서 ◀ 를 눌러도 오늘 같은 반사가 난다. 처방은 ViewTabs 와
  같은 유령 span 예약이지만 배지 문구 폭이 가변('지난달' 대 '11개월 전')이라 공용 컴포넌트 차원
  작업이다. 대안 둘(768 미만 두 줄 고정 · 680 전용 브레이크포인트)은 각각 실사용 폰 폭에 40px
  상시 비용, 저장소 최초의 미등재 브레이크포인트라 기각. knowledge/open-issues 에 적립.

## 디자이너가 기록만으로 넘긴 것 (다음 창)

- 막대 시작·끝 조각이 `shrink-0 whitespace-nowrap` 이라 종전 단일 span 의 `truncate` 안전망이
  사라졌다. 계산상 새 조립이 판정보다 2.6~5.2px 좁아 여유가 있고 실측 유출 0 이지만,
  호실번호가 숫자라는 보장이 없으니 다음 창에서 그물을 다시 치는 편이 낫다.
- 세 조각의 타이포 값이 11px/600/`--ink-2` 로 동급이라 넓은 막대에서 대등한 라벨 셋으로 읽힌다.
  색으로 누르는 길은 막혀 있다(밴드 위 `--ink-s` 는 라이트 3.08:1 로 AA 미달).
- 다크에서 부유 알약의 `shadow-lift`(`rgba(0,0,0,.55)`)가 카드 표면(#1A130E) 위에서 사실상
  안 보인다. 토큰 자체의 다크 거동이고 이번 diff 이전에도 같았다(종전에는 카드가 잘라 냈다).
