# Stayeum 프로젝트 작업 감독관 (Loop)

이 문서(loop.md)는 stayeum 프로젝트의 모든 개발 작업(기능 추가, 버그 수정 등)에 대한 **최종 검증 및 운영 루프 기준**이다.
너(AI)는 어떤 태스크를 수행하든 "구현 완료했습니다"라고 말하기 전에 반드시 아래의 기준들을 스스로 점검하고, 통과하지 못했다면 스스로 원인을 분석하여 수정해라. 모든 과정이 끝나면 최종 증거 보고서를 제출해라.

## 1. 필수 통과 기준 (Fail 시 완료 불가)
이 항목 중 하나라도 실패하면 작업이 끝난 것이 아니다. 하나라도 어긋나면 스스로 고치고 다시 테스트해라.
- **빌드 및 환경 검증:** Vercel 빌드 성공, TypeScript 에러 없음, Lint 통과 여부.
- **보안 및 인증:** Supabase API Key 및 환경 변수 노출 절대 불가.
- **DB 무결성:** 기존 DB 스키마 및 RLS(Row Level Security) 정책 충돌 발생 여부 확인.
- **핵심 운영 로직:** 입실자 계약 기간 처리, 결제일 기준 임대료 일할 계산 로직, 방 상태 변경 등이 에러 없이 정상 동작하는가.
- **UI/UX 정합(상용화 기준):** UI를 신설·변경했다면 ① 정본 공용 컴포넌트(Btn·Modal·SearchBar·SegmentedControl·ViewTabs·SelectionPillBar·MergeSheet·EmptyState·SpecWizard·ConfirmDialog) 재사용 확인 ② 같은 행동을 하는 형제 페이지와 동일 문법인지 전수 대조(선택=꾹/버튼+알약바, 합치기=MergeSheet, 등록=Modal+dirty, 검색=상시 SearchBar, 월전환=우측) ③ docs/brand-guide-v1.3.md 해당 § 확인. **새 인터랙션 패턴 도입은 4번(인간 호출) 대상 — 가이드 등재 없이 임의 신설 금지.** 레이아웃 변경은 모바일 폭 시각 확인.

## 2. 측정 기준 (객관적 지표)
숫자와 로그로 측정할 수 있는 아래 기준들을 확인해라.
- Supabase API 호출 에러 로그 없음.
- 불필요한 리렌더링이나 API 중복 호출로 인한 성능 저하 없음.
- 테스트 커버리지 하락 및 런타임 에러 발생 여부.

## 3. 평가 기준 (1~5점 평가 및 근거/액션 필수)
아래 항목에 대해 스스로 1~5점의 점수를 매겨라. 단, **단순히 점수만 주지 말고 반드시 구체적인 판단 근거와 추가 수정 액션(Action Item)을 명시해라**.
- **아키텍처 및 유지보수성:** 코드가 읽기 쉽고 기존 컴포넌트 구조를 잘 따르고 있는가?
- **사용자 흐름(User Flow):** 공지사항 발송, 방 상태 변경, 결제일 갱신 등의 흐름이 자연스러운가? DB 업데이트 후 화면 상태가 즉시 갱신(Refetch) 되는가?
- **작업 범위:** 할당된 태스크(Task)의 요구사항을 넘어서는 불필요한 코드 변경(오버엔지니어링)은 없는가?

## 4. 인간 호출 기준 (반드시 운영자에게 물어볼 것)
아래 항목은 AI가 임의로 판단해서 자동 수정하면 절대 안 된다. 발생 즉시 작업을 멈추고 운영자에게 질문하여 승인을 받아라.
- **DB 스키마 변경:** Supabase 테이블 구조 변경이나 컬럼 추가/삭제가 필요한 경우.
- **인증 및 권한 정책 변경:** 사용자 로그인 구조 변경이나 데이터 접근 권한(RLS) 수정이 필요한 경우.
- **결제 및 데이터 손실 위험:** 임대료 일할 계산 로직 변경, 기존 입실자 데이터 마이그레이션 및 삭제 로직.
- **기획 충돌:** 기존 요구사항(PRD)과 충돌하거나 설계 의도가 크게 달라지는 변경.

*단, 위 사항에 해당하지 않는 단순 린트/타입 오류, 명확한 버그 수정, 오타 교정 등은 스스로 판단하여 즉시 고쳐라.*

## 5. 최종 증거 보고서 제출
작업을 완료할 때, 위 1~4번을 어떻게 통과했는지 알 수 있는 객관적인 증거(테스트 통과 로그, 자체 수정 액션, 검증된 사용자 흐름 등)를 포함하여 최종 머지 준비 보고서를 작성해라.
